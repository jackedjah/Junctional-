R238 COMPARISON SHEETS

LEFT half of every sheet  = BEFORE, retained baseline A3, commit 28d1f04.
RIGHT half of every sheet = CANDIDATE R238, commit 5a9450a. NOT user-approved.

Both halves are crops of captures already produced for this pass; nothing was
re-rendered to make them. Same camera, framing, pose, lighting, material,
tier and image scale, with the idle animation frozen (reducedMotion:"reduce").
Both halves use the identical crop rectangle and the identical magnification.

  __PLAIN     pixels only, no marks of any kind.
  __LABELLED  the same sheet with the checkpoint names and, in amber, the
              per-side region where the surface actually moved (measured at
              >=6 luma). The GAP between the two amber boxes is the inner
              face, which is bit-identical between the two builds.

SHEET-outer-band-zoom is the front view magnified 2x on the changed outer
band alone, which is where the change is largest.
