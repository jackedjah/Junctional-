# GAMEPLAY RUNTIME — PHASE 2 REPORT (headless gameplay core)
2026-09-13 · Claude engineering lane · NO BLENDER · NO ASTRA ASSET MUTATION · NO VFX · NO ANIMATION · NO NETWORKING · HARD STOP

| field | value |
|---|---|
| HEADLESS MVP STATUS | **WORKING.** The approved vertical slice runs end to end headlessly: CREATE ATHLETE PROFILE → SPAWN (hover idle, flourish trigger) → FUSED MOVE (glide + travel flight under the 50 m ceiling) → UNFUSE (mahloco chain, one split commit) → SPLIT MOVE → ACCEPT MENTOR CONTRACT → START DUEL (lock, combat flight cap 8 ft, two spectators, two Call Next requests) → SELECT MOVEMENT PATTERN (touch gesture → abstract action) → EXECUTE UPRIGHT ROW → MAIN DAMAGE → 5 ECCENTRIC TICKS (reversed, darker, tail below main) → END DUEL (KO, result, Call Next offer + decline under an EXPERIMENTAL policy) → XP REWARD (duel + mentor, permanent) → REFUSE (mahloco chain, one fusion commit) → FUSED TRAVEL (dash → glide, travel ceiling restored). |
| SIMULATION RESULT | `node 15_SIMULATOR/simulate_vertical_slice.mjs --policy A` → `HEADLESS VERTICAL SLICE: COMPLETE`; telemetry: transform_count 2, attack_count 10, eccentric_attack_count 1, spectator_count 2, call_next_requests 2, time in fused / split both > 0, flight_duration > 0. Log written by `--log`. |
| TEST TOTALS | Phase 2 suite `16_TESTS/gameplay_runtime.test.mjs` **125 / 125** (profile assembly, fused / split speed config, backward multiplier, travel vs combat ceilings, resource depletion + regen, transformation permission adapter, attack phase ordering, eccentric ticks + lower damage, fused / split availability, duel lifecycle, spectator cap, FCFS Call Next, policy plug-ins A / B / C, activity join / leave, raid party max, mentor quest lifecycle, temporary buff expiration, workout progression separation, idle flourish interval, headless vertical-slice simulation). Existing suites re-run green: gameplay foundation validator 118, runtime foundation phase 3 218 / phase 4.5 92 / Python 25, mahloco 33, mrmah3d sync 29 entries in sync. |
| SYSTEMS IMPLEMENTED | gameplay profile · one canonical config consumer with unit conversions · movement capability layer · flight session (two ceilings, drain, forced descent, hover fallback) · abstract resource · attack data model + validation + movement-pattern kit · eccentric damage runtime with the six canon fields and the six canon events · first exercise-attack fixture (DEVELOPMENT_REFERENCE) · input abstraction · duel session state machine · spectator system · Call Next queue with three EXPERIMENTAL after-decline policies · activity engagement framework · climbing hooks (7 events) · raid party foundation with difficulty inputs and scaling hooks · mentor quest foundation (5 types, 4 reward hooks, 12 placeholder slots) · progression boundaries (4 permanent events) · temporary buff system (stacking + expiration) · idle flourish data + scheduler · emote registry scaffold · playtest telemetry · debug simulator · mahloco adapter. |
| OPEN POLICIES REMAINING | CALL_NEXT_POLICY (OD-02: A / B / C registered, dev default A, none canonical) · TARGETING_POLICY (OD-15: slot only) · HERO_ARENA_CAMERA_MODE (OD-01: slot only, `UNDECIDED`) · RAID_SCALING_POLICY (OD-08: LINEAR_DEV / STEPPED_DEV placeholders) · RESOURCE_TUNING (OD-06 / OD-14: dev_tuning.dev.json) · eccentric ratio (OD-18: dev tick value; runtime guard enforces tail < main) · first prototype class (OD-19: Athlete as DEVELOPMENT choice only) · split backward / split flight representation (OD-23: handled as parameters on existing mahloco states, not new states). `policies.describe()` prints every slot with its ledger id; an unselected slot refuses loudly. |
| INTEGRATION POINTS READY FOR VISUAL ASSETS | `IDLE_FLOURISH_TRIGGER` (beats + gaze targets, `clip: NOT_AUTHORED_YET`) → idle clips; mahloco `STATE_ENTER` / `DL_*` events already bound to the 29-clip contract → transformation / gait clips; `ATTACK_CONCENTRIC_BEGIN / MAIN_IMPACT / ECCENTRIC_BEGIN / TICK / END` with direction, colour shift and AOE payloads → attack animation + manifestation VFX; `FLIGHT_STATE` / `FLIGHT_CONTEXT` → propulsion VFX intensity; `DUEL_LOCKED` camera metadata (`FOLLOW_BOTH`, targets) and `SPECTATOR_JOINED` → fight camera; `CLIMB_*` → IK / attachment; `EMOTE_PLAY` → emote clips; `RESOURCE_*` → HUD. The character comes from the existing registry (Athlete_F currently `PREPARED`, eyes `LEGACY_GLOW`) — the headless slice runs on identity only and never substitutes a body. |
| FILES CREATED | 27 under `CLAUDE_GAMEPLAY_RUNTIME\`: package.json, README.md, 00_CORE (config.js, events.js, policies.js, bootstrap.js, dev_tuning.dev.json), 01_PROFILE/GameplayProfile.js, 02_MOVEMENT (MovementCapabilities.js, FlightSession.js, MahlocoAdapter.js), 03_RESOURCES/Resource.js, 04_COMBAT (AttackModel.js, data/attacks.dev.json, data/class_loadouts.dev.json), 05_ECCENTRIC/EccentricRuntime.js, 06_INPUT/InputActions.js, 07_DUEL/DuelSession.js, 08_SPECTATOR/SpectatorSystem.js, 09_ACTIVITIES/ActivitySession.js, 10_RAIDS/RaidParty.js, 11_MENTORS/MentorQuest.js, 12_PROGRESSION/Progression.js, 13_EMOTES/IdleAndEmotes.js, 14_TELEMETRY/Telemetry.js, 15_SIMULATOR (simulate_vertical_slice.mjs, last_run_telemetry.json), 16_TESTS/gameplay_runtime.test.mjs, this report. |
| FILES MODIFIED | **none** outside the new folder. mahloco, the runtime foundation, the creator runtime, the gameplay foundation canon / defaults / ledger, the manifest and every Astra folder are untouched (protected hash set: MUTATION false). |
| NEW REGRESSIONS | none (all pre-existing suites unchanged and green; the mrmah3d isolation suite's single pre-existing failure is unrelated and not re-run here). |
| BLENDER | not launched by Claude. Astra's Blender session was still running on the machine and was not touched. |

## Self-check (brief §29)
- all new tests run (125 / 125); gameplay foundation validator 118 / 118; mahloco 33 OK; runtime foundation suites green.
- no Blender launched; no Astra file mutated; no visual asset touched.
- all tuning values come from canonical config: `config.js` is the only reader of `PLAYER_MOVEMENT_DEFAULTS.json`; the owner defaults (20 / 40 / 8 / 15 / 0.5 / 50 / 8–12 / 8 ft / 30 s / 2–3 s / 20 / 4 / 8–12) are asserted from the file, and a test scans the runtime modules for hard-coded owner tunables. Undecided values (OD-06 / 14 / 18, attack damage / cost / cooldown) live only in `dev_tuning.dev.json` marked EXPERIMENTAL.
- OPEN_DECISION items remain externally configurable: policy slots with ledger ids; unselected slots refuse; the simulator accepts `--policy`.
- no copied state machine replaced mahloco: `MahlocoAdapter.js` contains no state names or table rows (test-asserted); it wraps the approved Phase 3 JS port of mahloco rev 2, and mahloco's MAHGIC guard now reads the gameplay resource through the world adapter.
- eccentric system traces to the brief: canon §8 / `ECCENTRIC_DAMAGE_SYSTEM.md` → `EccentricRuntime.js` (six fields, six events, long-range only, tail < main guard) → `ATHLETE_BILATERAL_UPRIGHT_ROW_MANIFESTATION` fixture (DEVELOPMENT_REFERENCE).

## Not done (by instruction)
No visual gameplay, VFX, animation, multiplayer, monsters, maps, final mentor NPCs, cosmetics, or character-model changes. Athlete is not declared the permanent first-playable class.

**HARD STOP — awaiting human review.**
