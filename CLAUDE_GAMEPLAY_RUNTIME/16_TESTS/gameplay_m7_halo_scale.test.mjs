/* M7 HALO scale/access addendum — focused authority check only.
   node 16_TESTS/gameplay_m7_halo_scale.test.mjs */
import { loadHeadlessData, composeHeadless } from '../00_CORE/bootstrap.js';
import { createDuelHost, hostIdentity } from '../26_LOCAL_AUTHORITY/DuelHost.js';
import { createMemoryStore } from '../26_LOCAL_AUTHORITY/DurableStore.js';
import { makeEnvelope } from '../26_LOCAL_AUTHORITY/Protocol.js';
import { HALO_LAYOUT, haloBoundaryConstraint } from '../26_LOCAL_AUTHORITY/play/haloLayout.js';

var pass = 0, fail = 0;
function ok(id, cond, detail) { if (cond) { pass++; console.log('PASS ' + id); } else { fail++; console.log('FAIL ' + id + (detail === undefined ? '' : ' — ' + JSON.stringify(detail))); } }
var data = await loadHeadlessData(), DT = data.cfg.dev('local_authority.tick_dt_s');
var store = createMemoryStore(), opened = store.open({ identity: hostIdentity(data) });
var h = createDuelHost({ data: data, composeHeadless: composeHeadless, store: store, opened: opened });
h.dev.createAccount('A', { classId: 'ATHLETE', sex: 'M' });
var token = h.dev.tokens().A, c = h.connect({ client_id: 'halo-scale', account_id: 'A', token: token }), seq = 0;
function send(action, payload) { return h.submit(makeEnvelope({ session_id: c.session_id, client_id: 'halo-scale' }, ++seq, action, payload || {})); }
function run(seconds) { for (var i = 0; i < Math.round(seconds / DT); i++) h.tick(DT); }
function snap() { return h.snapshot(c.session_id).play; }
function place(x, z) { return h.dev.play('PLACE', { account_id: 'A', x: x, z: z }); }
function crashRecover() { store.simulateCrash(); opened = store.open({ identity: hostIdentity(data) }); h = createDuelHost({ data: data, composeHeadless: composeHeadless, store: store, opened: opened }); var recovered = h.recover(); var resumed = recovered.ok && !recovered.unresolved ? h.dev.resume() : null; var reconnected = resumed && resumed.ok ? h.reconnect({ session_id: c.session_id, client_id: 'halo-scale', token: token }) : null; return { ok: recovered.ok && !recovered.unresolved && resumed && resumed.ok && reconnected && reconnected.ok, recovered: recovered, resumed: resumed, reconnected: reconnected }; }

send('ENTER_ROOM', { room: 'FIELD' }); run(0.2);
ok('1 exact one-time dimensions', HALO_LAYOUT.arrival_height_m === 240 && HALO_LAYOUT.shell_radius_m === 147.6 && HALO_LAYOUT.shell_diameter_m === 295.2 && HALO_LAYOUT.apex_height_m === 387.6 && HALO_LAYOUT.scale.arrival_height === 10 && HALO_LAYOUT.scale.dome_linear === 18, HALO_LAYOUT);
place(100, 40); run(0.1); ok('2 world ground beneath the oversized upper deck remains mainland ground', snap().spatial_domain === 'WORLD' && snap().flight.ground === 0, { domain: snap().spatial_domain, ground: snap().flight.ground });
place(HALO_LAYOUT.ground_dock.x, HALO_LAYOUT.ground_dock.z); run(0.1);
var manifest = send('EQUIP_MANIFEST', { kind: 'BARBELL', count: 1 }); run(2); var bar = snap().equipment.manifested.filter(function (e) { return e.kind === 'BARBELL'; })[0]; var pickup = bar ? send('EQUIP_PICKUP', { id: bar.id }) : null; run(0.1);
var before = snap(), upPrompt = before.prompts.some(function (p) { return p.id === 'ELEVATOR_UP'; }), downPromptBelow = before.prompts.some(function (p) { return p.id === 'ELEVATOR_DOWN'; }), pool0 = before.flight_mahgic.current;
var up = send('TRANSIT', { id: 'ELEVATOR_UP' }); var justStarted = snap(); run(6); var midBeforeRecovery = snap(), recoveredRide = crashRecover(), midAfterRecovery = snap(); run(4); var middle = snap(); run(10.2); var upper = snap();
ok('3 reachable single cabin starts a 20 s authority timeline instead of teleporting', up.accepted && up.travel_s === 20 && justStarted.spatial_domain === 'TRANSIT' && justStarted.transit && justStarted.flight.ground === 0 && middle.spatial_domain === 'TRANSIT' && middle.flight.ground > 100 && middle.flight.ground < 140, { prompt: upPrompt, down_below: downPromptBelow, up: up, start: { domain: justStarted.spatial_domain, ground: justStarted.flight.ground }, middle: { domain: middle.spatial_domain, ground: middle.flight.ground, transit: middle.transit } });
ok('4 mid-ride recovery resumes the same accepted cabin and carried barbell', recoveredRide.ok && midAfterRecovery.spatial_domain === 'TRANSIT' && Math.abs(midAfterRecovery.transit.progress - midBeforeRecovery.transit.progress) < 0.01 && midAfterRecovery.equipment.held && midAfterRecovery.equipment.held.kind === 'BARBELL' && midAfterRecovery.equipment.manifested.some(function (e) { return e.id === bar.id && e.state === 'HELD' && Math.abs(e.y - (midAfterRecovery.flight.ground + 1)) < 0.01; }), { recovery: recoveredRide, before: midBeforeRecovery.transit, after: midAfterRecovery.transit, equipment: midAfterRecovery.equipment });
ok('5 docking activates HALO only at 240 m with zero transit drain and intact gear ownership', manifest.accepted && pickup && pickup.accepted && upPrompt && !downPromptBelow && upper.spatial_domain === 'HALO' && upper.flight.ground === 240 && upper.flight.altitude === 240 && !upper.transit && upper.flight_mahgic.current === pool0 && upper.equipment.held && upper.equipment.held.kind === 'BARBELL', { domain: upper.spatial_domain, flight: upper.flight, pool0: pool0, pool1: upper.flight_mahgic.current, equipment: upper.equipment.held });
var disconnectedInside = h.disconnect(c.session_id), reconnectedInside = h.reconnect({ session_id: c.session_id, client_id: 'halo-scale', token: token }), upperRecovered = snap();
ok('6 an in-HALO reconnect restores the same safe contained state, not an exterior shortcut', disconnectedInside.ok && reconnectedInside.ok && upperRecovered.spatial_domain === 'HALO' && upperRecovered.flight.ground === 240 && upperRecovered.halo.contained && upperRecovered.halo.radial_distance_m <= upperRecovered.halo.radial_limit_m, { disconnect: disconnectedInside, reconnect: reconnectedInside, domain: upperRecovered.spatial_domain, halo: upperRecovered.halo });
place(HALO_LAYOUT.guide.x, HALO_LAYOUT.guide.z); run(0.1); var atGuide = snap(), talk = send('NPC_TALK', { npc: 'NPC_HALO_GUIDE' });
ok('7 upper realm streams its guide and supports its interaction', atGuide.npcs.some(function (n) { return n.id === 'NPC_HALO_GUIDE'; }) && atGuide.prompts.some(function (p) { return p.npc === 'NPC_HALO_GUIDE'; }) && talk.accepted, { npcs: atGuide.npcs.map(function (n) { return n.id; }), prompts: atGuide.prompts, talk: talk });
var navOutside = send('NAV', { dest_x: HALO_LAYOUT.center.x + 200, dest_z: HALO_LAYOUT.center.z });
ok('8 navigation cannot target through the dome shell', !navOutside.accepted && navOutside.reason === 'TARGET_BLOCKED', navOutside);
place(HALO_LAYOUT.center.x, HALO_LAYOUT.center.z); run(0.1); var targetForm = snap().form === 'FUSED' ? 'SPLIT' : 'FUSED', alternate = send('TRANSFORM', { to: targetForm }); run(4); var enterFlight = send('FLIGHT', { op: 'ENTER' }), ascend = send('FLIGHT', { op: 'ASCEND' }); run(30); var high = snap();
send('MOVE', { forward: 1, strafe: 0, run: true, fast: true, yaw: Math.PI / 2 }); run(8); send('MOVE', { forward: 0, strafe: 0, yaw: Math.PI / 2 }); run(0.1); var shell = snap();
ok('9 alternate form, interior flight and fast movement remain inside the curved shell', alternate.accepted && enterFlight.accepted && ascend.accepted && high.flight.altitude <= high.halo.ceiling_m + 0.01 && shell.halo.radial_distance_m <= shell.halo.radial_limit_m + 0.01 && shell.flight.altitude <= shell.halo.ceiling_m + 0.01, { form: shell.form, high: { altitude: high.flight.altitude, ceiling: high.halo.ceiling_m }, shell: shell.halo });
var sealedPickup = send('EQUIP_PICKUP', { id: 'anything' }), sealedLock = send('LOCK_TARGET', { aim_id: 'FIELD_DUMMY' });
ok('10 mainland items and ground combat targets cannot leak into HALO', !sealedPickup.accepted && sealedPickup.reason === 'HALO_SEALED_WORLD_ITEM' && !sealedLock.accepted && sealedLock.reason === 'HALO_SEALED_TARGET' && snap().nodes.length === 0 && snap().wildlife.length === 0, { pickup: sealedPickup, lock: sealedLock });
send('FLIGHT', { op: 'EXIT' }); run(30); var landedInside = snap(); place(HALO_LAYOUT.upper_dock.x, HALO_LAYOUT.upper_dock.z); run(0.1); var downReady = snap(), down = send('TRANSIT', { id: 'ELEVATOR_DOWN' }); run(20.2); var lower = snap();
ok('11 dropping lands on the sealed upper floor; only its dock exposes the same 20 s return cabin', landedInside.spatial_domain === 'HALO' && landedInside.flight.ground === 240 && landedInside.flight.altitude === 240 && downReady.prompts.some(function (p) { return p.id === 'ELEVATOR_DOWN'; }) && !downReady.prompts.some(function (p) { return p.id === 'ELEVATOR_UP'; }) && down.accepted && down.travel_s === 20 && lower.spatial_domain === 'WORLD' && lower.flight.ground === 0 && !lower.transit, { landed: { domain: landedInside.spatial_domain, flight: landedInside.flight }, prompts: downReady.prompts, down: down, lower: { domain: lower.spatial_domain, ground: lower.flight.ground } });
var outsideHeights = [250, 300, 370].map(function (alt) { return { alt: alt, hit: haloBoundaryConstraint('WORLD', alt, { x: HALO_LAYOUT.center.x + 170, z: HALO_LAYOUT.center.z }, { x: HALO_LAYOUT.center.x, z: HALO_LAYOUT.center.z }) }; });
var insideHeights = [250, 300, 370].map(function (alt) { return { alt: alt, hit: haloBoundaryConstraint('HALO', alt, { x: HALO_LAYOUT.center.x, z: HALO_LAYOUT.center.z }, { x: HALO_LAYOUT.center.x + 170, z: HALO_LAYOUT.center.z }) }; });
var underDeckCap = h.dev.play_internals().rulesField('FIELD').colliders(null).ceilingAt(HALO_LAYOUT.center.x, HALO_LAYOUT.center.z, 230);
ok('12 isolated shell fixtures deny swept outside-in and inside-out crossings at several heights; the deck underside owns the below-shell cap', outsideHeights.every(function (e) { return e.hit && e.hit.kind === 'OUTSIDE_SHELL' && e.hit.radius_m > 0; }) && insideHeights.every(function (e) { return e.hit && e.hit.kind === 'INSIDE_SHELL' && e.hit.radius_m >= 0; }) && underDeckCap !== null && underDeckCap <= 240, { outside: outsideHeights, inside: insideHeights, under_deck_ceiling_m: underDeckCap });
var live = h.dev.play_internals().actorOf('A'); live.flightRes.drain('flight_drain_per_s', 10000, 'ZERO_POOL_FIXTURE'); var zeroUpPool = snap().flight_mahgic.current; var up2 = send('TRANSIT', { id: 'ELEVATOR_UP' }); run(20.2); var upper2 = snap(); live = h.dev.play_internals().actorOf('A'); live.flightRes.drain('flight_drain_per_s', 10000, 'ZERO_POOL_FIXTURE'); place(HALO_LAYOUT.upper_dock.x, HALO_LAYOUT.upper_dock.z); var zeroDownPool = snap().flight_mahgic.current, down2 = send('TRANSIT', { id: 'ELEVATOR_DOWN' }); run(20.2); var lower2 = snap();
ok('13 a second complete round trip remains usable at zero flight MAHGIC and keeps held ownership', zeroUpPool === 0 && zeroDownPool === 0 && up2.accepted && down2.accepted && upper2.spatial_domain === 'HALO' && lower2.spatial_domain === 'WORLD' && lower2.flight.ground === 0 && lower2.equipment.held && lower2.equipment.held.kind === 'BARBELL', { zeroUpPool: zeroUpPool, zeroDownPool: zeroDownPool, up2: up2.accepted, down2: down2.accepted, lower: { domain: lower2.spatial_domain, ground: lower2.flight.ground, equipment: lower2.equipment.held } });

console.log('RESULT M7 HALO scale/access: ' + pass + ' passed, ' + fail + ' failed');
process.exit(fail ? 1 : 0);
