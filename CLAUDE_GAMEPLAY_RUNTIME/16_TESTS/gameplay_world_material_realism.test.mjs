/* M8 MATERIAL REALISM (owner 2026-09-25): every major hard-surface family is a deliberate material — world-space structural surface logic
   (lab/world/surfaceDetail.js) on floors, facades, structure, metal and stone; daylight trims read by contrast, metals are satin / brushed,
   never sky mirrors; the pattern modulates value / roughness / metalness only (the five-class colour law is untouched).
   Runs the real patch against the vendored three.js standard shader source.  node 16_TESTS/gameplay_world_material_realism.test.mjs */
import fs from 'node:fs'; import path from 'node:path'; import { fileURLToPath } from 'node:url';
import * as THREE from '../26_LOCAL_AUTHORITY/vendor/three/three.module.min.js';
import { surfaceDetail, applySurface, SURFACE, zonedPaving, createPavingZones, PAVING_TYPES, applyGeology, applyCrystal } from '../26_LOCAL_AUTHORITY/lab/world/surfaceDetail.js';
import { celestialCloudOptics } from '../26_LOCAL_AUTHORITY/lab/world/sky.js';
import { towerCluster, cloudCluster } from '../26_LOCAL_AUTHORITY/lab/world/cloudBodies.js';
var HERE = path.dirname(fileURLToPath(import.meta.url)); var LA = path.join(HERE, '..', '26_LOCAL_AUTHORITY');
var pass = 0, fail = 0; function ok(name, cond, detail) { if (cond) { pass++; console.log('PASS ' + name); } else { fail++; console.log('FAIL ' + name + (detail === undefined ? '' : ' — ' + JSON.stringify(detail).slice(0, 1600))); } }
function src(rel) { return fs.readFileSync(path.join(LA, rel), 'utf8'); }
function compiled(mat) { var sh = { vertexShader: THREE.ShaderLib.standard.vertexShader, fragmentShader: THREE.ShaderLib.physical.fragmentShader, uniforms: {} }; mat.onBeforeCompile(sh, null); return sh; }

/* 1. every injection anchor exists in this three.js build: world varyings, pattern, roughness, metalness and the chamfer normal */
var hs = compiled(applySurface(THREE, new THREE.MeshStandardMaterial(), 'PLAZA', 'HIGH')), fa = compiled(applySurface(THREE, new THREE.MeshStandardMaterial(), 'FACADE', 'HIGH'));
var anchors = { vary: /vSdW = \(modelMatrix \* sdP\)\.xyz/.test(hs.vertexShader), inst: /#ifdef USE_INSTANCING\n  sdP = instanceMatrix \* sdP/.test(hs.vertexShader), seam: /float sdSeam = /.test(hs.fragmentShader), tone: /diffuseColor\.rgb \*= sdTone/.test(hs.fragmentShader), rough: /roughnessFactor = clamp\(mix\(roughnessFactor \* sdRough/.test(hs.fragmentShader), metal: /metalnessFactor \*= 1\.0 - /.test(hs.fragmentShader), bevel: /normal = normalize\(normal \+ sdK/.test(hs.fragmentShader), band: /float sdBand = /.test(fa.fragmentShader), stagger: /sdG\.x \+= 0\.5000 \* mod\(sdRow, 2\.0\)/.test(hs.fragmentShader) };
ok('1. the surface patch lands on every anchor of the vendored standard shader (world varyings incl. instancing, seams, tone, roughness, metalness, chamfer, storey bands, running bond)', Object.keys(anchors).every(function (k) { return anchors[k]; }), anchors);

/* 2. tiers + families: LOW drops the chamfer / grain / macro but keeps per-piece tone and roughness; BRUSHED has no seams; keys are distinct */
var lo = compiled(applySurface(THREE, new THREE.MeshStandardMaterial(), 'PLAZA', 'LOW')), br = compiled(applySurface(THREE, new THREE.MeshStandardMaterial(), 'BRUSHED', 'HIGH'));
var keys = Object.keys(SURFACE).map(function (F) { return applySurface(THREE, new THREE.MeshStandardMaterial(), F, 'HIGH').customProgramCacheKey(); }).concat([applySurface(THREE, new THREE.MeshStandardMaterial(), 'PLAZA', 'LOW').customProgramCacheKey()]);
ok('2. LOW keeps tone / roughness only (no chamfer, grain or macro); BRUSHED streaks without seams; every family × tier compiles to its own program key', !/sdK/.test(lo.fragmentShader) && !/sdGrain/.test(lo.fragmentShader) && !/sdMacro/.test(lo.fragmentShader) && /sdTone/.test(lo.fragmentShader) && /sdRough/.test(lo.fragmentShader) && /sdStreak/.test(br.fragmentShader) && /sdSeam = 0\.0/.test(br.fragmentShader) && new Set(keys).size === keys.length, { keys: keys.length, unique: new Set(keys).size });

/* 3. chaining: an existing onBeforeCompile (ripples, rock, crystal emissive patches) still runs, the key keeps its prefix, double application is a no-op */
var m3 = new THREE.MeshStandardMaterial(); var ran = false; m3.onBeforeCompile = function (sh) { ran = true; sh.fragmentShader = sh.fragmentShader.replace('#include <dithering_fragment>', '#include <dithering_fragment>\n// prior-patch'); }; m3.customProgramCacheKey = function () { return 'prior'; };
applySurface(THREE, m3, 'FACADE', 'HIGH'); var k1 = m3.customProgramCacheKey(); applySurface(THREE, m3, 'STONE', 'HIGH'); var s3 = compiled(m3);
ok('3. the patch chains after an existing shader patch, keeps its cache-key prefix, and a second application is ignored', ran && /prior-patch/.test(s3.fragmentShader) && /sdSeam/.test(s3.fragmentShader) && k1.indexOf('prior|') === 0 && m3.customProgramCacheKey() === k1 && m3.userData.surfaceDetail.kind === 'PANELS', { ran: ran, key: k1 });

/* 4. the application map: plaza + district floors, civic facades / structure / chrome / stone, the HALO deck, shared world families, class houses */
var FS = src('lab/fieldScene.js'), CS = src('lab/cityScene.js'), WB = src('lab/world/worldB.js'), AR = src('lab/world/architecture.js'), TR = src('lab/world/terrain.js');
var map = { plaza: /zonedPaving\(THREE, floor\.material, pavingZones/.test(FS), district: /zonedPaving\(THREE, dFloor\.material, pavingZones/.test(FS), glazing: /applySurface\(THREE, M\.glass, 'GLAZING'/.test(CS), proxies: /applySurface\(THREE, chrome, 'BRUSHED'/.test(FS) && /applySurface\(THREE, platinum, 'STRUCTURE'/.test(FS), facade: /applySurface\(THREE, M\.platinum, 'FACADE'/.test(CS), civic: /applySurface\(THREE, M\.chrome, 'BRUSHED'/.test(CS) && /applySurface\(THREE, M\.stone, 'STONE'/.test(CS), deck: /applySurface\(THREE, mat, 'DECK'/.test(CS), world: /applySurface\(THREE, M\.platinum, 'STRUCTURE', tier\)/.test(WB) && /applySurface\(THREE, M\.chrome, 'BRUSHED', tier\)/.test(WB), houses: /applySurface\(THREE, platMat, 'STRUCTURE'/.test(AR) && /applySurface\(THREE, stoneMat, 'STONE'/.test(AR), trunk: /applySurface\(THREE, trunkMat, 'TOWER'/.test(CS), roads: /applySurface\(THREE, frameMat, 'KERB'/.test(TR) && /applySurface\(THREE, coreMat, 'PAVER'/.test(TR) && /mahworld_path_core_owned/.test(TR) };
ok('4. every major hard-surface family carries its surface logic (floors, facades, structure, chrome, stone, HALO deck + trunk, shared world families, class houses, road kerbs + causeway cores)', Object.keys(map).every(function (k) { return map[k]; }), map);

/* 5. daylight value policy + colour law: trims ≤ 0.5 emissive by day, no sky-mirror metals by day, the pattern never changes hue */
var trimDay = /trim: new THREE\.MeshStandardMaterial\(\{ color: 0xe6ecf6, emissive: 0xdfe8ff, emissiveIntensity: \(DAY \? ([0-9.]+)/.exec(CS), fsTrim = /trim\.emissiveIntensity = ([0-9.]+); \}/.exec(FS);
var chromeDay = /chrome: new THREE\.MeshStandardMaterial\(\{ color: 0xa4aeb9, roughness: \(DAY \? ([0-9.]+)/.exec(CS), wPlat = /platinum: std\(\{ color: 0x([0-9a-f]{6}), roughness: ([0-9.]+)/.exec(WB), wChrome = /chrome: std\(\{ color: 0x[0-9a-f]{6}, roughness: ([0-9.]+)/.exec(WB);
var SD = src('lab/world/surfaceDetail.js'); var mulAll = (SD.match(/diffuseColor\.rgb \*=/g) || []).length, mulOk = (SD.match(/diffuseColor\.rgb \*= sdTone \* mix\(1\.0, ' \+ f\(S\.seamDark\) \+ ', sdSeam\);/g) || []).length + (SD.match(/diffuseColor\.rgb \*= mix\(gTone, 1\.0, gSnow \* 0\.9\);/g) || []).length + ((/'float cTone = /.test(SD) && SD.match(/diffuseColor\.rgb \*= cTone;/g)) || []).length;   /* M8C crystal: cTone is a float */
var mixes = SD.match(/diffuseColor\.rgb = mix\(diffuseColor\.rgb, [^;]*;/g) || [], hueFree = mulAll >= 1 && mulAll === mulOk && mixes.length === 1 && /vec3\(0\.9, 0\.93, 0\.97\) \* diffuse, gSnow/.test(mixes[0]) && !/diffuseColor\.rgb\s*=\s*vec3/.test(SD);   /* every value change is a scalar (tone × seam, geology tone, crystal facet tone); the one blend is neutral-white snow */
ok('5. daylight trims glow ≤ 0.5, civic and world metals are satin / brushed (roughness ≥ 0.3, platinum no longer near-white), and the pattern only scales value', trimDay && +trimDay[1] <= 0.5 && fsTrim && +fsTrim[1] <= 0.5 && chromeDay && +chromeDay[1] >= 0.3 && wPlat && parseInt(wPlat[1], 16) < 0xd0d0d0 && +wPlat[2] >= 0.3 && wChrome && +wChrome[1] >= 0.3 && hueFree, { trimDay: trimDay && trimDay[1], fsTrim: fsTrim && fsTrim[1], chromeDay: chromeDay && chromeDay[1], worldPlatinum: wPlat && wPlat.slice(1), worldChrome: wChrome && wChrome[1], hueFree: hueFree });

/* 6. M8B zoned paving: one shader, nine families, threshold courses, soft ecology patches; zones come from the registry (class → family) */
var REG = JSON.parse(src('lab/assets/world/world_registry_v1.json')); var Z = createPavingZones(THREE).setFromRegistry(REG, { halo: { x: 30, z: 40, r: 18 } });
var zm = new THREE.MeshStandardMaterial(); zonedPaving(THREE, zm, Z, { tier: 'HIGH' }); var zs = compiled(zm); var zl = compiled(zonedPaving(THREE, new THREE.MeshStandardMaterial(), Z, { tier: 'LOW' }));
function typeAt(x, z) { var t = 0; for (var i = 0; i < 16; i++) { var R = Z.rects[i]; if (x > R.x && x < R.z && z > R.y && z < R.w) t = Z.rtype[i]; } for (i = 0; i < 6; i++) { var C = Z.circles[i]; if (C.z > 0 && Math.hypot(x - C.x, z - C.y) < C.z) t = C.w; } return t; }
var zoneMap = { plaza: typeAt(10, 10) === PAVING_TYPES.RINGS, halo: typeAt(30, 40) === PAVING_TYPES.FINE_RINGS, athlete: typeAt(0, 180) === PAVING_TYPES.DIAMOND, titan: typeAt(110, 110) === PAVING_TYPES.HEX, lean: typeAt(-110, 180) === PAVING_TYPES.PLANKS, visionary: typeAt(-120, 0) === PAVING_TYPES.TRI, bage: typeAt(0, 270) === PAVING_TYPES.MOSAIC, match: typeAt(120, 0) === PAVING_TYPES.SQUARE, civic: typeAt(-40, 100) === PAVING_TYPES.BOND, soft: Z.info.soft >= 5 };
var zsrc = { families: ['pBond(zp, vec2(3.2, 1.6)', 'pRings(zp - zC, 1.8', 'pHex(zp', 'pDiamond(zp', 'pTri(zp', 'pBond(zp, vec2(3.6, 0.6)', 'pBond(zp, vec2(2.4, 2.4)', 'pBond(zp, vec2(0.9, 0.9)', 'pRings(zp - zC, 1.2'].every(function (k) { return zs.fragmentShader.indexOf(k) >= 0; }), band: /float zBand = 0\.0; if \(zEdge < 0\.7000\)/.test(zs.fragmentShader), patches: /zPatch = zSoft \* smoothstep/.test(zs.fragmentShader), bevel: /normal = normalize\(normal \+ sdK/.test(zs.fragmentShader), lowLean: !/zPatch = zSoft/.test(zl.fragmentShader) && !/sdK/.test(zl.fragmentShader), shared: zm.onBeforeCompile && (function () { var sh = { vertexShader: THREE.ShaderLib.standard.vertexShader, fragmentShader: THREE.ShaderLib.physical.fragmentShader, uniforms: {} }; zm.onBeforeCompile(sh); return sh.uniforms.uZR.value === Z.rects && sh.uniforms.uZRT.value === Z.rtype; })() };
ok('6. zoned paving: nine pattern families in one shader (bond, rings, hex, diamond, triangle, planks, square, mosaic, fine rings), 0.7 m threshold courses, soft ecology patches, LOW keeps pattern + tone only, zones bound live from the registry (class → family, plaza + HALO base rings, forest softening)', Object.keys(zoneMap).every(function (k) { return zoneMap[k]; }) && Object.keys(zsrc).every(function (k) { return zsrc[k]; }), { zoneMap: zoneMap, src: zsrc, info: Z.info });

/* 7. M8B sky: long zenith → horizon falloff with dither, lunar aureole palette, horizon cloud banks outside the celestial optics, a soft Sun veil */
var ATM = src('lab/world/atmosphere.js'), SKYS = src('lab/world/sky.js'), CELS = src('lab/world/celestial.js'); var A = REG.sky.atmosphere, bank = REG.sky.layers.filter(function (L) { return L.bank; })[0];
var noBank = JSON.parse(JSON.stringify(REG.sky)); noBank.layers = noBank.layers.filter(function (L) { return !L.bank; }); var o1 = celestialCloudOptics(REG.sky, [0.6, 0.6, 0.5], 42, false, {}), o2 = celestialCloudOptics(noBank, [0.6, 0.6, 0.5], 42, false, {});
var sky = { falloff: /pow\(1\.0 - hp, 2\.2\)/.test(ATM), dither: /gl_FragCoord\.xy, vec2\(12\.9898, 78\.233\)/.test(ATM), hg: /1\.0 - g \* g\) \/ pow\(1\.0 \+ g \* g - 2\.0 \* g \* mu, 1\.5\)/.test(ATM), aureole: A.night.mie >= 0.5 && A.day.haze_k >= 0.75, bank: !!bank && !bank.occludes && bank.ring_m[0] > 700 && bank.max_top_m < 240, ring: /if \(Ly\.L\.ring_m\)/.test(SKYS) && /L\.bank\) continue;/.test(SKYS), opticsExcluded: Math.abs(o1.tau - o2.tau) < 1e-9, sunVeil: /cloudTex: softVeilTex\(512\), cloudOpacity: 0\.55/.test(CELS) && /rayOpacity: 0\.05/.test(CELS), occluders: REG.sky.layers.filter(function (L) { return L.occludes; }).length === 2 };
ok('7. sky realism: long zenith-to-horizon falloff with dither, a strong lavender lunar aureole at night, a far horizon cloud-bank ring that never enters the Sun / Moon optics (two occluding layers kept), a soft Sun veil and barely-there rays', Object.keys(sky).every(function (k) { return sky[k]; }), sky);

/* 8. M8C physical world: ONE geology pipeline (ridge inner face = collision plane, so shader only) on the ridges, the far massifs (unlit
      value) and the coast islands — beds + ledges, joints that stop at bedding planes, sparse master fractures, clefts; only SMOOTH fields
      reach the derivative bump (thin lines in the bump sparkled). Crystal family on the roof diamonds + class crystals; natural ground only
      where a zone is soft (plain hardscape never loses slabs); the shallow-water cue on the canal / pond surface. */
var geoM = new THREE.MeshStandardMaterial(), geoS = compiled(applyGeology(THREE, geoM, { snowY: 40 })), geoF = geoS.fragmentShader;
var cryS = compiled(applyCrystal(THREE, new THREE.MeshStandardMaterial({ flatShading: true }), {})).fragmentShader, ZP = src('lab/world/surfaceDetail.js');
var MAC = src('lab/world/macro.js'), FARW = src('lab/farWorld.js'), CST = src('lab/world/coast.js'), WAT = src('lab/world/water.js'), CTY = src('lab/cityScene.js'), ARC = src('lab/world/architecture.js');
var hLine = (/gH \+= ([^;]*);/.exec(geoF) || [])[1] || '', hBase = (/float gH = ([^;]*);/.exec(geoF) || [])[1] || '';
var m8c = { applied: /applyGeology\(THREE, mat, \{ snowY: R\.h_max \* 0\.6/.test(MAC) && /applyGeology\(THREE, farMat, \{ lit: false/.test(FARW) && /applyGeology\(THREE, landMat/.test(CST),
  beds: /float gLip = /.test(geoF) && /float gUnder = /.test(geoF) && /float gBed = /.test(geoF) && /float gJoint = /.test(geoF) && /float gCrack = /.test(geoF) && /float gCleft = /.test(geoF),
  aa: /clamp\(0\.035 \/ gBedW, 0\.0, 1\.0\)/.test(geoF) && /clamp\(0\.05 \/ gJw, 0\.0, 1\.0\)/.test(geoF) && /clamp\(0\.05 \/ gCw, 0\.0, 1\.0\)/.test(geoF),
  smoothBump: !!hLine && !/gJoint|gCrack|gBed/.test(hLine + hBase) && /smoothstep\(0\.0, gRw, gBot\)/.test(hBase) && /normal = geoBump\(-vViewPosition, normal, gH/.test(geoF),
  crystal: /diffuseColor\.rgb \*= cTone;/.test(cryS) && /float cFr = pow\(1\.0 - /.test(cryS) && /applyCrystal\(THREE, M\.diamond/.test(CTY) && /applyCrystal\(THREE, crystalMat/.test(ARC),
  naturalGated: /smoothstep\(0\.03, 0\.3, zNat0\)/.test(ZP),
  water: /float wShallow = 1\.0 - smoothstep\(1\.4, 6\.5, wEd\);/.test(WAT) && /metalnessFactor \*= 1\.0 - 0\.6 \* wShallow;/.test(WAT) && /\|mahworld-water-shallow2/.test(WAT) };
ok('8. M8C physical world: one geology pipeline on ridges / far massifs / islands (beds, ledges, joints, master fractures, clefts; footprint AA; only smooth fields in the bump), the crystal family, natural ground gated to soft zones, the shallow-water cue', Object.keys(m8c).every(function (k) { return m8c[k]; }), m8c);

/* 9. M8D cloud realism: the cotton-ball puffs are replaced by a shared procedural ATLAS of four noise-eroded shapes (gaussian-sum masses,
      domain warp, billow cauliflower rims, a thickness channel); every body is lit from the key's side as a whole, self-shadows through its
      thickness (tiered taps), has a flat darker base, aerial perspective and a horizon fade; a camera-relative ring of TOWERING cumulus adds
      the missing scale behind the far massifs, outside the celestial optics, clear of the Sun / Moon sectors, drawn before nearer layers. */
var CB = src('lab/world/cloudBodies.js'), SK = src('lab/world/sky.js'), TW = REG.sky.layers.filter(function (L) { return L.style === 'TOWER'; })[0];
function seededR(n) { var st = n >>> 0; return function () { st = (st * 1664525 + 1013904223) >>> 0; return st / 4294967296; }; }
var tA = towerCluster(420, 300, seededR(11)), tB = towerCluster(420, 300, seededR(11)), tBase = tA.filter(function (p) { return p.flat === 1; }), tCaps = tA.filter(function (p) { return p.height === 1; });
var noTower = JSON.parse(JSON.stringify(REG.sky)); noTower.layers = noTower.layers.filter(function (L) { return L.style !== 'TOWER'; }); var oT1 = celestialCloudOptics(REG.sky, [0.4, 0.5, -0.6], 77, false, {}), oT2 = celestialCloudOptics(noTower, [0.4, 0.5, -0.6], 77, false, {});
var clouds = { atlas: /var SHAPES = \[/.test(CB) && /Math\.exp\(-\(dx \* dx \+ dy \* dy\) \* 1\.7\)/.test(CB) && /nb = 1 - Math\.abs\(2 \* fbm\(/.test(CB) && /ATLAS\[key\] = \{ tex: t, refs: 1 \}/.test(CB) && /function releaseAtlas\(tex\)/.test(CB),
  light: /float side = dot\(normalize\(vRel \+ vec3\(0\.0, 0\.2, 0\.0\)\), Lw\)/.test(CB) && /occl = cellTex\(u \+ vLs \* 0\.06\)\.r/.test(CB) && /#if CLOUD_TAPS > 1/.test(CB) && /float baseK = 1\.0 - smoothstep\(0\.0, 0\.3, vH\)/.test(CB) && /float horK = 1\.0 - smoothstep\(uHor\.x, uHor\.y, vElev\)/.test(CB) && /taps: qt === 'LOW' \? 0 : \(qt === 'MED' \? 1 : 2\)/.test(SK),
  towers: !!TW && TW.bank === true && TW.follow === true && TW.occludes === false && TW.alt_m >= 240 && TW.ring_m[0] - TW.size_m[1] / 2 > 700 && TW.avoid_deg >= 20 && Math.abs(oT1.tau - oT2.tau) < 1e-9,
  towerShape: JSON.stringify(tA) === JSON.stringify(tB) && tBase.length >= 4 && tCaps.length >= 2 && tCaps.every(function (c) { return tBase.every(function (b) { return c.y > b.y; }); }) && tA.every(function (p) { return Math.abs(p.x) < 420 * 0.75; }),
  order: /renderOrder: L\.follow \? 1 : \(L\.bank \? 2 : 4 \+ li\)/.test(SK) && /im\.renderOrder = L\.occludes \? 4 \+ li : 2/.test(SK) && /var rcx = Ly\.L\.follow && camPos \? camPos\.x : 0/.test(SK),
  sizes: REG.sky.layers.filter(function (L) { return L.id === 'CUMULUS_MID' || L.id === 'STRATUS_LOW'; }).every(function (L) { return L.size_pow > 1; }) && /Math\.pow\(rnd\(\), L\.size_pow \|\| 1\)/.test(CB),
  pinned: cloudCluster(160, seededR(7)).length === cloudCluster(160, seededR(7)).length };
ok('9. M8D cloud realism: noise-eroded shape atlas, whole-body key lighting with thickness self-shadow (tiered), flat darker bases, aerial perspective + horizon fade, skewed sizes, a camera-relative towering-cumulus ring (bank, outside the optics, clear of the Sun / Moon, far layers first)', Object.keys(clouds).every(function (k) { return clouds[k]; }), clouds);

console.log('RESULT world material realism: ' + pass + ' passed, ' + fail + ' failed'); process.exit(fail ? 1 : 0);
