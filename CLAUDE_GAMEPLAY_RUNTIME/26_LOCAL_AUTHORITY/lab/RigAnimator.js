/* MAHWORLD PLAYABLE SAMPLE :: RIG ANIMATOR v3 — rig-motion repair (development presentation; never simulates)
   Layered procedural + authored-clip animation for the derivative class rigs (bone names in lab/ANIMATION_CHANNELS.md). Every pose is a
   function of AUTHORITATIVE host state (form, mahloco state, flight, dash, guard, cast phase + progress, hit, emote id/time, paired
   timeline) plus bounded, seeded presentation variation; nothing here decides timing, damage, reach, collision or movement.

   ONE composition per character per rendered frame, in this order:
     1 ALIVE idle (breathing, blinks, gaze, posture variation)  2 LOCOMOTION (fused whole-body lean / separated single-phase gait)
     3 FLIGHT  4 DASH  5 ATTACK (per movement pattern, host phases)  6 GUARD  7 EMOTE clips (authored, additive)  8 HIT  9 TRANSFORM  10 KO
     → 11 JOINT LIMITS (soft approach + safety clamp)  → 12 BODY CLEARANCE (elbows outside the ribcage, hands off the chest) → apply.
   Steps 11–12 run AFTER the whole stack, so locomotion, flight, dashes, attacks, guards, emotes and both transformation directions all
   inherit the same constraints and no emote can bypass what locomotion obeys.

   ANGLE CONVENTION (lab/rig_profile.json): INSIDE angles — a straight elbow is 180°. The Athlete's deepest elbow bend is ≈105° inside,
   i.e. ≈75° of FLEXION from straight = 1.31 rad, so FOREARM_x never goes below −1.31. Bone values here are radians of flexion from the
   identity-bind rest pose. Limits, lean distribution, clearance volumes and transformation beats come from the profile (opts.profile);
   rhythm/beam numbers come from lab/presentation_defaults.json (opts.defaults). Nothing numeric is scattered anywhere else.

   Gait: ONE phase, phase += 2π·(speed / distance_per_full_cycle)·dt; support events are phase crossings (left at 0, right at π) handed to
   the caller for beam pulses and audio — never timers. Stride and knee lift are fitted to the rig's MEASURED segment lengths. */
/* bones a donor upper-body layer may write; everything else (PELVIS, THIGH/KNEE/SHIN/TIP, LEG_TAIL, root) is MAH-owned */
var UPPER_MASK = {}; ['SPINE', 'SPINE2', 'CHEST', 'NECK', 'HEAD', 'CLAV_L', 'CLAV_R', 'SCAP_L', 'SCAP_R', 'UPPERARM_L', 'UPPERARM_R', 'ARMTWIST_L', 'ARMTWIST_R', 'FOREARM_L', 'FOREARM_R', 'FORETWIST_L', 'FORETWIST_R', 'HAND_L', 'HAND_R', 'FINGERS_L', 'FINGERS_R'].forEach(function (k) { UPPER_MASK[k] = 1; });
export var UPPER_LAYER_MASK = UPPER_MASK;
export function createRigAnimator(THREE, rig, opts) {
  opts = opts || {}; var D = opts.defaults || {}; var DA = D.alive || {}, DG = D.gait || {}, DF = D.flight || {}, DT = D.fused_travel || {}, DD = D.dash || {}, DB = D.beams || {}, DFC = D.face || {}, DFH = D.barrage || {};
  var AD = opts.animData || null; var CLS = (AD && AD.CLASS_STYLE && AD.CLASS_STYLE[opts.classId]) || { amp: 1, speed: 1, settle: 'quick' };
  /* ---- rig profile: the angle convention, limits, lean distribution, clearance volumes and transformation beats (fallback keeps the animator alive if play.js cannot load the file) ---- */
  var PROFILE_FALLBACK = {
    limits_rad: { FOREARM_x: [-1.31, 0.05], SHIN_x: [-0.03, 1.48], THIGH_x: [-1.31, 0.95], UPPERARM_x: [-1.75, 0.70], UPPERARM_z: [-1.75, 1.75], UPPERARM_y: [-0.6, 0.6], CLAV_z: [-0.45, 0.45], CLAV_x: [-0.35, 0.35], HAND_x: [-0.9, 0.9], FINGERS_x: [0.0, 1.45], SPINE_x: [-0.45, 0.70], SPINE_y: [-0.5, 0.5], SPINE_z: [-0.35, 0.35], CHEST_x: [-0.35, 0.60], CHEST_y: [-0.7, 0.7], CHEST_z: [-0.35, 0.35], PELVIS_x: [-0.35, 0.55], PELVIS_y: [-0.6, 0.6], PELVIS_z: [-0.3, 0.3], NECK_x: [-0.45, 0.45], NECK_y: [-0.6, 0.6], NECK_z: [-0.3, 0.3], HEAD_x: [-0.5, 0.5], HEAD_y: [-0.8, 0.8], HEAD_z: [-0.35, 0.35], TAIL1_x: [-0.5, 1.2], TAIL2_x: [-0.5, 1.0], TAIL3_x: [-0.4, 0.8], TIP_x: [-0.5, 0.6], BROW_x: [-0.12, 0.12], CHEEK_x: [-0.12, 0.12], JAW_x: [0.0, 0.16] },
    travel_posture: { fused_lean_deg: [5, 12], flight_lean_deg: [8, 18], trailing_shoulder_deg: [10, 15], lean_distribution: { PELVIS: 0.45, SPINE: 0.3, CHEST: 0.15, TAIL1: 0.1 }, turn: { gaze_lead_s: 0.12, chest_yaw_share: 0.45, pelvis_yaw_share: 0.35, lower_yaw_share: 0.2 } },
    clearance: { ribcage: { cx: 0, cy: 1.20, cz: 0.0, rx: 0.26, ry: 0.30, rz: 0.20 }, pelvis: { cx: 0, cy: 0.86, cz: 0.0, rx: 0.22, ry: 0.18, rz: 0.18 }, head: { cx: 0, cy: 1.62, cz: 0.02, r: 0.15 }, min_hand_to_chest_m: 0.12, min_elbow_to_ribs_m: 0.08, min_leg_gap_m: 0.04 },
    transformation_beats: { gather: 0.3, release: 0.4, settle: 0.3 }
  };
  var PR = opts.profile || rig.profile || PROFILE_FALLBACK;
  var PR_SRC = opts.profile ? 'lab/rig_profile.json (opts.profile)' : (rig.profile ? 'lab/rig_profile.json (rig.profile)' : 'BUILT-IN FALLBACK — the host did not supply lab/rig_profile.json; keep PROFILE_FALLBACK in step with that file');
  var LIM_SRC = PR.limits_rad || PROFILE_FALLBACK.limits_rad;
  var TP = PR.travel_posture || PROFILE_FALLBACK.travel_posture;
  var CLR = PR.clearance || PROFILE_FALLBACK.clearance;
  var TB = PR.transformation_beats || PROFILE_FALLBACK.transformation_beats;
  var LD = TP.lean_distribution || PROFILE_FALLBACK.travel_posture.lean_distribution;
  var DEG = Math.PI / 180;
  var LEAN_TRAVEL = (TP.fused_lean_deg || [5, 12]).map(function (d) { return d * DEG; });
  var LEAN_FLIGHT = (TP.flight_lean_deg || [8, 18]).map(function (d) { return d * DEG; });
  /* owner interjection 2026-09-18 §7: ONE centralized flight-lean curve (presentation_defaults flight.lean.curve_mps_deg: real horizontal m/s → whole-body degrees). leanDeg(s) = the inclination, leanL(s) = its 0…1 position between hover and boost (drives the arm sweep, leg / taper trail, knee shape and the pelvis-led distribution). LEAN_FLIGHT stays as the fallback when the curve is absent. */
  var FLN = DF.lean || {}; var LEAN_CURVE = Array.isArray(FLN.curve_x_deg) && FLN.curve_x_deg.length >= 2 ? FLN.curve_x_deg : [[0, LEAN_FLIGHT[0] / DEG], [1, LEAN_FLIGHT[1] / DEG]];   /* x = displayed horizontal speed / the form's cruise speed (host play.flight.cruise_mps; fallback table for figures without it) */
  function smoothstep01(u) { u = clamp(u, 0, 1); return u * u * (3 - 2 * u); }
function leanDeg(s) { var c = LEAN_CURVE; if (!(s > c[0][0])) return c[0][1]; for (var i = 1; i < c.length; i++) { if (s <= c[i][0]) { var t = (s - c[i - 1][0]) / Math.max(1e-6, c[i][0] - c[i - 1][0]); return c[i - 1][1] + (c[i][1] - c[i - 1][1]) * t; } } return c[c.length - 1][1]; }
  function leanL(s) { var lo = LEAN_CURVE[0][1], hi = LEAN_CURVE[LEAN_CURVE.length - 1][1]; return clamp((leanDeg(s) - lo) / Math.max(1, hi - lo), 0, 1); }
  var TRAIL = (TP.trailing_shoulder_deg || [10, 15]).map(function (d) { return d * DEG; });
  var B = rig.bones || {}; var E = {}; var tmpE = new THREE.Euler(); var tmpQ = new THREE.Quaternion();
  var qA = new THREE.Quaternion(), qB = new THREE.Quaternion(), eQT = new THREE.Euler();
  function eulerCanon(e) { /* the XYZ solution with the small hinge angles (x, z) */ var a = [e.x, e.y, e.z], b = [a[0] + (a[0] > 0 ? -Math.PI : Math.PI), Math.PI - a[1], a[2] + (a[2] > 0 ? -Math.PI : Math.PI)]; if (b[1] > Math.PI) b[1] -= 2 * Math.PI; if (Math.abs(b[0]) + Math.abs(b[2]) < Math.abs(a[0]) + Math.abs(a[2])) { e.x = b[0]; e.y = b[1]; e.z = b[2]; } return e; }
  function eulerNear(e, ref) { /* of the two XYZ solutions, the one nearest a reference Euler (continuity with the composed pose) */ var a = [e.x, e.y, e.z], b = [a[0] + (a[0] > 0 ? -Math.PI : Math.PI), Math.PI - a[1], a[2] + (a[2] > 0 ? -Math.PI : Math.PI)]; if (b[1] > Math.PI) b[1] -= 2 * Math.PI; var da = Math.abs(a[0] - ref.x) + Math.abs(a[1] - ref.y) + Math.abs(a[2] - ref.z), db = Math.abs(b[0] - ref.x) + Math.abs(b[1] - ref.y) + Math.abs(b[2] - ref.z); if (db < da) { e.x = b[0]; e.y = b[1]; e.z = b[2]; } return e; }
  function twistY(e) { /* rotation about the bone's own axis (y) of an XYZ Euler pose, from its quaternion swing-twist split; equals e.y when x, z are small */ eQT.set(e.x, e.y, e.z, 'XYZ'); qA.setFromEuler(eQT); var t = 2 * Math.atan2(qA.y, qA.w); if (t > Math.PI) t -= 2 * Math.PI; if (t < -Math.PI) t += 2 * Math.PI; return t; }
  function blendEulerQ(e, o, w) { eQT.set(e.x, e.y, e.z, 'XYZ'); qA.setFromEuler(eQT); eQT.set(o[0] || 0, o[1] || 0, o[2] || 0, 'XYZ'); qB.setFromEuler(eQT); qA.slerp(qB, w); eQT.setFromQuaternion(qA, 'XYZ'); eulerCanon(eQT); e.x = eQT.x; e.y = eQT.y; e.z = eQT.z; }
  /* ---- DONOR CLIP PLAYER (S10): baked upper-body clips (deploy/retarget_tripo.mjs → lab/assets/retarget/*.json) fitted to the host's own
     event contract. CAST: STARTUP progress → clip [t_in, t_release], RECOVERY progress → [t_release, end]; HIT: hitAgo → clip time.
     The clip never drives timing, damage or the root; it is one masked layer with pose-aware entry / exit. ---- */
  var DONOR = { clips: {}, enabled: false, active: null };
  function clipPose(clip, tSec, out) { var fps = clip.meta.fps, n = clip.frames.length; var f = Math.max(0, Math.min(n - 1.0001, tSec * fps)); var i0 = Math.floor(f), i1 = Math.min(n - 1, i0 + 1), u = f - i0; var A0 = clip.frames[i0], A1 = clip.frames[i1]; Object.keys(A0).forEach(function (k) { if (!UPPER_MASK[k]) return; var a = A0[k], b = A1[k] || a; eQT.set(a[0], a[1], a[2], 'XYZ'); qA.setFromEuler(eQT); eQT.set(b[0], b[1], b[2], 'XYZ'); qB.setFromEuler(eQT); qA.slerp(qB, u); eQT.setFromQuaternion(qA, 'XYZ'); eulerCanon(eQT); out[k] = [eQT.x, eQT.y, eQT.z]; }); return out; }
  Object.keys(B).forEach(function (k) { E[k] = { x: 0, y: 0, z: 0 }; });
  /* ---- resolved per-bone limit table (a bone inherits its base key: FOREARM_L → FOREARM_x) ---- */
  var SAFETY = 2.6; var LIMITS = {};
  Object.keys(B).forEach(function (k) { var base = k.replace(/_(L|R)$/, ''); var e = {}; ['x', 'y', 'z'].forEach(function (ax) { var r = LIM_SRC[base + '_' + ax]; e[ax] = (r && r.length === 2) ? [r[0], r[1]] : null; }); LIMITS[k] = e; });
  /* ---- measured segment lengths in metres (bone rest offsets × figure scale) — stride, knee lift and clearance are fitted to the ACTUAL body, never to constants ---- */
  var SC = rig.scale || 1;
  function bonePos(k) { var b = B[k]; return b && b.position ? b.position : null; }
  function segLen(childKey, fb) { var p = bonePos(childKey); if (!p) return fb; var l = Math.sqrt(p.x * p.x + p.y * p.y + p.z * p.z) * SC; return l > 1e-4 ? l : fb; }
  function restPos(chain) { var x = 0, y = 0, z = 0; for (var i = 0; i < chain.length; i++) { var p = bonePos(chain[i]); if (p) { x += p.x; y += p.y; z += p.z; } } return { x: x * SC, y: y * SC, z: z * SC }; }
  var SEG = { thigh: segLen('SHIN_L', 0.42), shin: segLen('TIP_L', 0.40), upperarm: segLen('FOREARM_L', 0.30), forearm: segLen('HAND_L', 0.26), hand: segLen('FINGERS_L', 0.10) };
  SEG.leg = SEG.thigh + SEG.shin; SEG.arm = SEG.upperarm + SEG.forearm;
  var SHOULDER = { L: restPos(['PELVIS', 'LUMBAR', 'SPINE', 'SPINE2', 'CHEST', 'CLAV_L', 'SCAP_L', 'UPPERARM_L']), R: restPos(['PELVIS', 'LUMBAR', 'SPINE', 'SPINE2', 'CHEST', 'CLAV_R', 'SCAP_R', 'UPPERARM_R']) };   /* rig v5 chain (missing bones are skipped) */
  var HIP_GAP = Math.abs((bonePos('THIGH_L') ? bonePos('THIGH_L').x : 0) - (bonePos('THIGH_R') ? bonePos('THIGH_R').x : 0)) * SC;
  /* abduction sign kept consistent with this file's own authored convention (L opens with +z, R with −z) so existing tuned poses are untouched */
  function outSign(side) { return side === 'L' ? 1 : -1; }
  /* the same sign in the RIG's space, for the passes that edit composed values directly instead of authoring them */
  function rigOutSign(side) { return outSign(side) * (MIRROR_SIDES ? -1 : 1); }
  var seed = (rig.seed !== undefined ? rig.seed : (opts.seed !== undefined ? opts.seed : Math.random())) * 1000 + 17; var rnd = function () { seed = (seed * 9301 + 49297) % 233280; return seed / 233280; };
  function rr(range, fb) { return range && range.length === 2 ? range[0] + rnd() * (range[1] - range[0]) : fb; }
  function ease(x) { x = Math.max(0, Math.min(1, x)); return x < 0.5 ? 2 * x * x : 1 - Math.pow(-2 * x + 2, 2) / 2; }
  function clamp(x, a, b) { return Math.max(a, Math.min(b, x)); }
  /* soft approach: the last 15 % of a range compresses and the value approaches the limit asymptotically — never a snap between limits */
  function soft(v, r) { if (!r) return clamp(v, -SAFETY, SAFETY); var lo = r[0], hi = r[1], span = hi - lo; if (!(span > 1e-6)) return clamp(v, lo, hi); var m = span * 0.15; if (v > hi - m) { var t = (v - (hi - m)) / m; return hi - m + m * (1 - Math.exp(-t)); } if (v < lo + m) { var t2 = ((lo + m) - v) / m; return lo + m - m * (1 - Math.exp(-t2)); } return v; }
  function rotXYZ(v, e) { var cx = Math.cos(e.x), sx = Math.sin(e.x), cy = Math.cos(e.y), sy = Math.sin(e.y), cz = Math.cos(e.z), sz = Math.sin(e.z); var x1 = v.x * cz - v.y * sz, y1 = v.x * sz + v.y * cz, z1 = v.z; var x2 = x1 * cy + z1 * sy, y2 = y1, z2 = -x1 * sy + z1 * cy; return { x: x2, y: y2 * cx - z2 * sx, z: y2 * sx + z2 * cx }; }
  function inside(p, vol, pad) { if (!vol) return 0; var dx = (p.x - vol.cx) / (vol.rx + pad), dy = (p.y - vol.cy) / (vol.ry + pad), dz = (p.z - vol.cz) / (vol.rz + pad); var d = dx * dx + dy * dy + dz * dz; return d < 1 ? 1 - d : 0; }
  var st = { t: rnd() * 10, prevSpeed: 0, prevSpeedMps: 0, brake: 0, yawPrev: null, turn: 0, turnAnt: 0, calm: 0, airborne: false, inert: { key: null, off: {}, t: 1, dur: 0.22 }, layer: null, layerN: 0, support: { L: null, R: null }, applied: {}, balanceT: rnd() * 9,
    breath: rr(DA.breath_cycle_s, 5), breathT: rnd(), blinkNext: rr(DA.blink_interval_s, 4), blinkT: 0, blink: 0, blinkPhase: 0,
    gaze: { x: 0, y: 0, tx: 0, ty: 0, next: rr(DA.gaze_interval_s, 4), t: 0, headX: 0, headY: 0 },
    vary: { kind: null, t: 0, dur: 0, next: rr(DA.idle_adjust_interval_s, 10), amp: 1 },
    gait: { phase: 0, events: [], supportL: 1, supportR: 1, settle: 0, blend: 0 },
    emote: { id: null, key: null, t: 0, seg: 'entry', loop: 0, done: false, role: 'A', t0: null },
    face: { layers: { EMOTE: { name: null, w: 0 }, COMBAT: { name: null, w: 0 }, REACTION: { name: null, w: 0, until: 0 } }, out: {} },
    transformT: 0, transformState: null, seam: 0, central: 1, tips: 0, packed: 1, showLegs: false, lastHit: 99, beams: { fused: 1, L: 0, R: 0 }, brace: 0, pose: {}, clearFix: 0 };
  /* SIDE CONVENTION — the one place it is resolved.
     The procedural poses in this file are authored with the left arm opening on +z (see outSign). The delivered rigs place
     `_L` bones at model −x, which is the mirror of that, so on such a rig the authored y/z of a side bone are negated on
     write: the file keeps one authoring convention and the bones keep the asset's. Authored clips (animData) are already
     written in the ASSET's convention and go in through setRaw, which never mirrors — mirroring them would undo that work. */
  var SIDE_RE = /_(L|R)$/;
  var MIRROR_SIDES = (function () { var p = bonePos('UPPERARM_L'); return !!(p && p.x < 0); })();
  function setRaw(k, x, y, z, w) { var e = E[k]; if (!e) return; var f = w === undefined ? 1 : w; e.x += x * f; e.y += (y || 0) * f; e.z += (z || 0) * f; }
  function set(k, x, y, z, w) { if (MIRROR_SIDES && SIDE_RE.test(k)) { y = -(y || 0); z = -(z || 0); } setRaw(k, x, y, z, w); }
  function setBones(map, w) { if (!map) return; Object.keys(map).forEach(function (k) { var v = map[k]; if (v && E[k]) setRaw(k, v[0] || 0, v[1] || 0, v[2] || 0, w); }); }
  function patternOf(id) { id = String(id || ''); if (/BARBELL_PRESS_THROW|PRESS_THROW/.test(id)) return 'BAR_PRESS'; if (/KATANA|X_WAVE/.test(id)) return 'KATANA'; if (/OVERHEAD/.test(id)) return 'OVERHEAD'; if (/CHEST_FLY|CLAP|CLAMP/.test(id)) return 'CHESTFLY';   /* B8 §12: X-WAVE = the katana-extension arc, the telekinetic CLAMP = the chest-fly adduction arc, PLATE SHOT = the generic cast; M2 PRESS_THROW = a true two-hand overhead press */ if (/CURL/.test(id)) return 'CURL'; if (/UPRIGHT_ROW|BILATERAL/.test(id)) return 'UPRIGHT_ROW'; if (/PUSH|SHOULDER/.test(id)) return 'PUSH'; if (/PULL|ROW/.test(id)) return 'PULL'; if (/HINGE|GROUND_PULSE/.test(id)) return 'HINGE'; if (/SQUAT|LUNGE|DRIVE/.test(id)) return 'SQUAT'; if (/ROTATION|DIAGONAL|PIVOT/.test(id)) return 'ROTATION'; return 'CAST'; }
  var HANDS = (AD && AD.HAND_POSES) || { FIST: { FINGERS: 1.3, HAND: 0.1 }, OPEN: { FINGERS: 0.05, HAND: -0.1 }, RELAXED: { FINGERS: 0.35, HAND: 0.05 }, GUARD: { FINGERS: 1.1, HAND: 0.2 }, CAST: { FINGERS: 0.15, HAND: -0.35 }, DIRECT: { FINGERS: 0.6, HAND: -0.2 } };
  function handVal(p, key, side, fb) { if (!p) return fb; if (p[key] !== undefined) return p[key]; if (p[key + '_' + side] !== undefined) return p[key + '_' + side]; if (p[key + '_L'] !== undefined) return p[key + '_L']; return fb; }
  function hand(side, pose, w) { var p = HANDS[pose] || HANDS.RELAXED; var f = handVal(p, 'FINGERS', side, 0.3); var h = handVal(p, 'HAND', side, 0); set('FINGERS_' + side, f, 0, 0, w); set('HAND_' + side, h, 0, 0, w); }
  var EXPR = (AD && AD.EXPRESSIONS) || { CALM: { face: { brow_l: 0, brow_r: 0, cheek_l: 0.05, cheek_r: 0.05, jaw: 0.02, smile: 0.08 } }, CURIOUS: { face: { brow_l: 0.35, brow_r: 0.25, cheek_l: 0.05, cheek_r: 0.05, jaw: 0.04, smile: 0.05 } }, FOCUS: { face: { brow_l: -0.35, brow_r: -0.35, cheek_l: 0.1, cheek_r: 0.1, jaw: 0, smile: 0, blink_bias: 0.15 } }, EXERT: { face: { brow_l: -0.55, brow_r: -0.55, cheek_l: 0.35, cheek_r: 0.35, jaw: 0.35, smile: 0, blink_bias: 0.3 } }, FRIENDLY: { face: { brow_l: 0.2, brow_r: 0.2, cheek_l: 0.4, cheek_r: 0.4, jaw: 0.06, smile: 0.55 } }, AMUSED: { face: { brow_l: 0.15, brow_r: 0.15, cheek_l: 0.6, cheek_r: 0.6, jaw: 0.18, smile: 0.7, blink_bias: 0.35 } }, PLAYFUL: { face: { brow_l: 0.35, brow_r: -0.05, cheek_l: 0.3, cheek_r: 0.45, jaw: 0.05, smile: 0.45 } }, UNCERTAIN: { face: { brow_l: 0.4, brow_r: -0.1, cheek_l: 0.05, cheek_r: 0.05, jaw: 0.08, smile: 0.05 } }, WINCE: { face: { brow_l: -0.5, brow_r: -0.5, cheek_l: 0.55, cheek_r: 0.55, jaw: 0.2, smile: 0, blink_bias: 0.85 }, hold_s: 0.45 }, CELEBRATE: { face: { brow_l: 0.45, brow_r: 0.45, cheek_l: 0.6, cheek_r: 0.6, jaw: 0.3, smile: 0.7 } }, DISAPPOINT: { face: { brow_l: 0.25, brow_r: 0.25, cheek_l: 0, cheek_r: 0, jaw: 0.05, smile: 0, blink_bias: 0.3 } } };
  var FACE_KEYS = ['brow_l', 'brow_r', 'cheek_l', 'cheek_r', 'jaw', 'smile', 'blink_bias', 'gaze_x', 'gaze_y'];
  function faceOf(name) { var e = EXPR[name]; return e && e.face ? e.face : (EXPR.CALM && EXPR.CALM.face) || {}; }
  function stepFace(dt, emoteFace, combatName, reactionName) {
    var L = st.face.layers; var bs = clamp(dt / Math.max(0.05, DFC.expression_blend_s || 0.18), 0, 1);
    if (reactionName) { L.REACTION.name = reactionName; L.REACTION.until = st.t + ((EXPR[reactionName] && EXPR[reactionName].hold_s) || DFC.wince_s || 0.45); }
    var reactOn = L.REACTION.name && st.t < L.REACTION.until; L.REACTION.w += ((reactOn ? 1 : 0) - L.REACTION.w) * bs * 1.6;
    if (combatName) L.COMBAT.name = combatName; L.COMBAT.w += ((combatName ? 1 : 0) - L.COMBAT.w) * bs;
    if (emoteFace) L.EMOTE.name = emoteFace; L.EMOTE.w += ((emoteFace ? 1 : 0) - L.EMOTE.w) * bs;
    var out = {}; FACE_KEYS.forEach(function (k) { out[k] = 0; });
    function mix(face, w) { if (!face || w <= 0) return; FACE_KEYS.forEach(function (k) { if (face[k] !== undefined) out[k] = out[k] * (1 - w) + face[k] * w; }); }
    mix(faceOf('CALM'), 1); if (st.vary.kind === 'GLANCE') mix(faceOf('CURIOUS'), 0.45 * st.calm);
    if (L.EMOTE.w > 0.001) mix(typeof L.EMOTE.name === 'string' ? faceOf(L.EMOTE.name) : L.EMOTE.name, L.EMOTE.w);
    if (L.COMBAT.w > 0.001) mix(faceOf(L.COMBAT.name), L.COMBAT.w); if (L.REACTION.w > 0.001) mix(faceOf(L.REACTION.name), L.REACTION.w);
    st.face.out = out; return out;
  }
  /* ---- 12 BODY CLEARANCE: coarse posing volumes only (never combat hitboxes). The reference is the RELAXED REST ARM, not the bare
     ellipsoid: on a real body the hanging elbow already sits beside the ribs, so the rule that means something is "no pose may drive the
     elbow or hand DEEPER into the torso than the relaxed arm already sits". Two cheap steps per arm per frame, nothing iterative. ---- */
  function armPts(side, ua, fa) {
    var sh = SHOULDER[side]; var el = rotXYZ({ x: 0, y: -SEG.upperarm, z: 0 }, ua); var elbow = { x: sh.x + el.x, y: sh.y + el.y, z: sh.z + el.z };
    var fw = rotXYZ(rotXYZ({ x: 0, y: -SEG.forearm, z: 0 }, fa), ua);
    return { elbow: elbow, hand: { x: elbow.x + fw.x, y: elbow.y + fw.y, z: elbow.z + fw.z } };
  }
  var PAD_E = CLR.min_elbow_to_ribs_m || 0.08, PAD_H = CLR.min_hand_to_chest_m || 0.12;
  function depthE(p) { return Math.max(inside(p, CLR.ribcage, PAD_E), inside(p, CLR.pelvis, PAD_E)); }
  function depthH(p) { return Math.max(inside(p, CLR.ribcage, PAD_H), inside(p, CLR.pelvis, PAD_H)); }
  var REST_DEPTH = {}; ['L', 'R'].forEach(function (side) { var z = { x: 0, y: 0, z: 0 }; var pt = armPts(side, z, z); REST_DEPTH[side] = { e: depthE(pt.elbow), h: depthH(pt.hand) }; });
  /* ---- 12b DONOR ARM CLEARANCE (S10): a retargeted cross-body gather that puts the hand/elbow inside the Athlete's deeper chest is corrected
     LOCALLY: the hand is pushed out of the coarse ribcage/pelvis volumes along their gradient and the arm is re-solved with 2-bone IK keeping the
     elbow on its current side; the result is blended in by depth (no pop), the forearm keeps its pronation. Coarse posing aid only. ---- */
  function volGrad(p) { var g = { x: 0, y: 0, z: 0 }; [[CLR.ribcage, PAD_H], [CLR.pelvis, PAD_H]].forEach(function (vp) { var vol = vp[0], pad = vp[1]; if (!vol || !inside(p, vol, pad)) return; var rx = vol.rx + pad, ry = vol.ry + pad, rz = vol.rz + pad; g.x += (p.x - vol.cx) / (rx * rx); g.y += (p.y - vol.cy) / (ry * ry); g.z += (p.z - vol.cz) / (rz * rz); }); var l = Math.hypot(g.x, g.y, g.z) || 1; return { x: g.x / l, y: g.y / l, z: g.z / l }; }
  function frontZ(p, pad) { /* z of the front face of the ribcage / pelvis volumes at (x, y); null when outside their x-y footprint */ var best = null; [[CLR.ribcage, pad], [CLR.pelvis, pad]].forEach(function (vp) { var vol = vp[0]; if (!vol) return; var rx = vol.rx + vp[1], ry = vol.ry + vp[1], rz = vol.rz + vp[1]; var dx = (p.x - vol.cx) / rx, dy = (p.y - vol.cy) / ry; var q = 1 - dx * dx - dy * dy; if (q <= 0) return; var z = vol.cz - rz * Math.sqrt(q); if (best === null || z < best) best = z; }); return best; }
  function clearArmIK(side) {
    var ua = E['UPPERARM_' + side], fa = E['FOREARM_' + side]; if (!ua || !fa) return 0; var sc = E['SCAP_' + side]; var r0 = REST_DEPTH[side];
    var uaT = sc ? { x: ua.x + sc.x, y: ua.y, z: ua.z + sc.z } : { x: ua.x, y: ua.y, z: ua.z }; var pt = armPts(side, uaT, fa); var over = Math.max(depthH(pt.hand) - r0.h, depthE(pt.elbow) - r0.e); if (over <= 0.01) return 0;
    var h = { x: pt.hand.x, y: pt.hand.y, z: pt.hand.z }; var fz = frontZ(h, PAD_H); if (fz !== null && h.z > fz) h.z = fz;   /* the sweep stays IN FRONT of the chest: the hand is brought to the volume's front face at its own height / lateral position (forward = -z) */
    var eb = { x: pt.elbow.x, y: pt.elbow.y, z: pt.elbow.z }; var fe = frontZ(eb, PAD_E); if (fe !== null && eb.z > fe) eb.z = fe;   /* and so does the elbow (it also becomes the IK pole) */
    var S = SHOULDER[side], L1 = SEG.upperarm, L2 = SEG.forearm; var dx = h.x - S.x, dy = h.y - S.y, dz = h.z - S.z; var d = Math.hypot(dx, dy, dz); var dmax = (L1 + L2) * 0.995; if (d > dmax) { var sc2 = dmax / d; dx *= sc2; dy *= sc2; dz *= sc2; d = dmax; } if (d < 1e-4) return 0;
    var ux = dx / d, uy = dy / d, uz = dz / d; var a2 = (L1 * L1 - L2 * L2 + d * d) / (2 * d); var rr = Math.sqrt(Math.max(0, L1 * L1 - a2 * a2));
    var ex = eb.x - S.x, ey = eb.y - S.y, ez = eb.z - S.z; var dotu = ex * ux + ey * uy + ez * uz; var px = ex - ux * dotu, py = ey - uy * dotu, pz = ez - uz * dotu; var pl = Math.hypot(px, py, pz); if (pl < 1e-4) { px = rigOutSign(side) * -1; py = -0.3; pz = 0; pl = Math.hypot(px, py, pz); } px /= pl; py /= pl; pz /= pl;
    var Ep = { x: S.x + ux * a2 + px * rr, y: S.y + uy * a2 + py * rr, z: S.z + uz * a2 + pz * rr };
    var u = { x: (Ep.x - S.x) / L1, y: (Ep.y - S.y) / L1, z: (Ep.z - S.z) / L1 }; var f = { x: (h.x - Ep.x) / L2, y: (h.y - Ep.y) / L2, z: (h.z - Ep.z) / L2 };
    var n = { x: u.y * f.z - u.z * f.y, y: u.z * f.x - u.x * f.z, z: u.x * f.y - u.y * f.x }; var nl = Math.hypot(n.x, n.y, n.z); if (nl < 1e-5) { n = { x: 1, y: 0, z: 0 }; nl = 1; } n.x /= nl; n.y /= nl; n.z /= nl;
    var theta = Math.acos(Math.max(-1, Math.min(1, u.x * f.x + u.y * f.y + u.z * f.z)));   /* elbow flexion: (0,-1,0) rotated by +theta about local x reaches the forearm direction */
    var yx = -u.x, yy = -u.y, yz = -u.z; var zx = n.y * yz - n.z * yy, zy = n.z * yx - n.x * yz, zz = n.x * yy - n.y * yx;   /* frame columns: x = hinge, y = -bone dir, z = x × y */
    var m11 = n.x, m12 = yx, m13 = zx, m21 = n.y, m22 = yy, m23 = zy, m31 = n.z, m32 = yz, m33 = zz; var tr = m11 + m22 + m33, qw, qx, qy, qz; if (tr > 0) { var s0 = Math.sqrt(tr + 1) * 2; qw = 0.25 * s0; qx = (m32 - m23) / s0; qy = (m13 - m31) / s0; qz = (m21 - m12) / s0; } else if (m11 > m22 && m11 > m33) { var s1 = Math.sqrt(1 + m11 - m22 - m33) * 2; qw = (m32 - m23) / s1; qx = 0.25 * s1; qy = (m12 + m21) / s1; qz = (m13 + m31) / s1; } else if (m22 > m33) { var s2 = Math.sqrt(1 + m22 - m11 - m33) * 2; qw = (m13 - m31) / s2; qx = (m12 + m21) / s2; qy = 0.25 * s2; qz = (m23 + m32) / s2; } else { var s3 = Math.sqrt(1 + m33 - m11 - m22) * 2; qw = (m21 - m12) / s3; qx = (m13 + m31) / s3; qy = (m23 + m32) / s3; qz = 0.25 * s3; }
    var w = Math.max(0, Math.min(1, (over - 0.01) / 0.12));   /* blend in by depth: a grazing pass barely changes, a deep pass is fully re-solved */
    eQT.set(uaT.x, uaT.y, uaT.z, 'XYZ'); qA.setFromEuler(eQT); qB.set(qx, qy, qz, qw); qA.slerp(qB, w); eQT.setFromQuaternion(qA, 'XYZ'); eulerNear(eQT, uaT); ua.x = eQT.x - (sc ? sc.x : 0); ua.y = eQT.y; ua.z = eQT.z - (sc ? sc.z : 0);
    var pron = twistY(fa); eQT.set(fa.x, fa.y, fa.z, 'XYZ'); qA.setFromEuler(eQT); eQT.set(theta, pron, 0, 'XYZ'); qB.setFromEuler(eQT); qA.slerp(qB, w); eQT.setFromQuaternion(qA, 'XYZ'); eulerNear(eQT, fa); fa.x = eQT.x; fa.y = eQT.y; fa.z = eQT.z;
    return 1;
  }
  /* ---- 12c ENDPOINT IK (S13 precision): an emote key that names a wrist target is SOLVED, not guessed. Live FK through the composed trunk +
     girdle (pelvis → chest → clavicle → scapula, quaternions, real rest offsets) gives the glenohumeral pivot; a 2-bone solve (humerus, forearm)
     with the authored pole puts the wrist ON the target; the target is first pushed out of the fitted head / torso volumes unless the key says
     `contact` (hands on hips). The solution is expressed in the scapula's frame (no Euler component games), blended by the key's weight on top
     of the authored travel so the path stays authored and only the END is exact; forearm pronation and the hand pose are kept. ---- */
  /* plain quaternion helpers ([x,y,z,w]) so the solver has no dependency beyond the Euler/Quaternion pair the tests stub */
  function qE(e) { var c1 = Math.cos(e.x / 2), c2 = Math.cos(e.y / 2), c3 = Math.cos(e.z / 2), s1 = Math.sin(e.x / 2), s2 = Math.sin(e.y / 2), s3 = Math.sin(e.z / 2); return [s1 * c2 * c3 + c1 * s2 * s3, c1 * s2 * c3 - s1 * c2 * s3, c1 * c2 * s3 + s1 * s2 * c3, c1 * c2 * c3 - s1 * s2 * s3]; }
  function qM(a, b) { return [a[3] * b[0] + a[0] * b[3] + a[1] * b[2] - a[2] * b[1], a[3] * b[1] - a[0] * b[2] + a[1] * b[3] + a[2] * b[0], a[3] * b[2] + a[0] * b[1] - a[1] * b[0] + a[2] * b[3], a[3] * b[3] - a[0] * b[0] - a[1] * b[1] - a[2] * b[2]]; }
  function qI(q) { return [-q[0], -q[1], -q[2], q[3]]; }
  function qR(q, v) { var x = q[0], y = q[1], z = q[2], w = q[3]; var tx = 2 * (y * v.z - z * v.y), ty = 2 * (z * v.x - x * v.z), tz = 2 * (x * v.y - y * v.x); return { x: v.x + w * tx + (y * tz - z * ty), y: v.y + w * ty + (z * tx - x * tz), z: v.z + w * tz + (x * ty - y * tx) }; }
  function qFromM(m11, m12, m13, m21, m22, m23, m31, m32, m33) { var tr = m11 + m22 + m33, qw, qx, qy, qz; if (tr > 0) { var s0 = Math.sqrt(tr + 1) * 2; qw = 0.25 * s0; qx = (m32 - m23) / s0; qy = (m13 - m31) / s0; qz = (m21 - m12) / s0; } else if (m11 > m22 && m11 > m33) { var s1 = Math.sqrt(1 + m11 - m22 - m33) * 2; qw = (m32 - m23) / s1; qx = 0.25 * s1; qy = (m12 + m21) / s1; qz = (m13 + m31) / s1; } else if (m22 > m33) { var s2 = Math.sqrt(1 + m22 - m11 - m33) * 2; qw = (m13 - m31) / s2; qx = (m12 + m21) / s2; qy = 0.25 * s2; qz = (m23 + m32) / s2; } else { var s3 = Math.sqrt(1 + m33 - m11 - m22) * 2; qw = (m21 - m12) / s3; qx = (m13 + m31) / s3; qy = (m23 + m32) / s3; qz = 0.25 * s3; } return [qx, qy, qz, qw]; }
  var CHAIN_T = ['PELVIS', 'LUMBAR', 'SPINE', 'SPINE2', 'CHEST'];
  function chainFK(names) { /* character-space orientation of the LAST bone's frame and its origin, after all of `names` (rest offsets × scale, composed E[] rotations) */ var q = [0, 0, 0, 1], p = { x: 0, y: 0, z: 0 }; for (var i = 0; i < names.length; i++) { var k = names[i]; var bp = bonePos(k); if (!bp) continue; var v = qR(q, { x: bp.x * SC, y: bp.y * SC, z: bp.z * SC }); p.x += v.x; p.y += v.y; p.z += v.z; var e = E[k]; if (e) q = qM(q, qE(e)); } return { q: q, p: p }; }
  function pushOut(t, contact) { /* keep a target outside the head, and (unless in contact) outside the fitted torso, by the smallest move */ var H = CLR.head_fit || { cx: 0, cy: 1.765, cz: 0, r: 0.115 }; var m = 0.035; var dx = t.x - H.cx, dy = t.y - H.cy, dz = t.z - H.cz; var d = Math.hypot(dx, dy, dz); if (d < H.r + m) { if (d < 1e-4) { dx = 1; dy = 0; dz = 0; d = 1; } var s0 = (H.r + m) / d; t.x = H.cx + dx * s0; t.y = H.cy + dy * s0; t.z = H.cz + dz * s0; } if (contact) return; [CLR.ribcage_fit || CLR.ribcage, CLR.pelvis_fit || CLR.pelvis].forEach(function (vol) { if (!vol) return; var pad = 0.03; var rx = vol.rx + pad, ry = vol.ry + pad, rz = vol.rz + pad; var ex = (t.x - vol.cx) / rx, ey = (t.y - vol.cy) / ry, ez = (t.z - vol.cz) / rz; var q = ex * ex + ey * ey + ez * ez; if (q >= 1) return; var l = Math.sqrt(q) || 1e-4; var k = 1 / l; t.x = vol.cx + ex * k * rx; t.y = vol.cy + ey * k * ry; t.z = vol.cz + ez * k * rz; }); }
  function orthoHinge(dir, hint) { /* unit vector ⟂ dir, nearest to hint */ var d = hint.x * dir.x + hint.y * dir.y + hint.z * dir.z; var hx = hint.x - dir.x * d, hy = hint.y - dir.y * d, hz = hint.z - dir.z * d; var l = Math.hypot(hx, hy, hz); if (l < 1e-5) { hx = dir.y; hy = -dir.x; hz = 0; l = Math.hypot(hx, hy, hz) || 1; } return { x: hx / l, y: hy / l, z: hz / l }; }
  function rotToFrame(a, b, u, n) { /* rotation taking the rest axis a + hinge b onto the world axis u + hinge n: R = [u n u×n]·[a b a×b]ᵀ */ var c = { x: a.y * b.z - a.z * b.y, y: a.z * b.x - a.x * b.z, z: a.x * b.y - a.y * b.x }; var w = { x: u.y * n.z - u.z * n.y, y: u.z * n.x - u.x * n.z, z: u.x * n.y - u.y * n.x }; return qFromM(u.x * a.x + n.x * b.x + w.x * c.x, u.x * a.y + n.x * b.y + w.x * c.y, u.x * a.z + n.x * b.z + w.x * c.z, u.y * a.x + n.y * b.x + w.y * c.x, u.y * a.y + n.y * b.y + w.y * c.y, u.y * a.z + n.y * b.z + w.y * c.z, u.z * a.x + n.z * b.x + w.z * c.x, u.z * a.y + n.z * b.y + w.z * c.y, u.z * a.z + n.z * b.z + w.z * c.z); }
  function emoteArmIK(side, tgt) {
    var ua = E['UPPERARM_' + side], fa = E['FOREARM_' + side]; if (!ua || !fa || !tgt || !(tgt.w > 0)) return 0;
    var restU = bonePos('UPPERARM_' + side), restF = bonePos('FOREARM_' + side), restH = bonePos('HAND_' + side); if (!restU || !restF || !restH) return 0;
    var L1 = Math.hypot(restF.x, restF.y, restF.z) * SC, L2 = Math.hypot(restH.x, restH.y, restH.z) * SC; if (L1 < 1e-4 || L2 < 1e-4) return 0;
    /* live pivot + parent frame (scapula) */
    var sc = chainFK(CHAIN_T.concat(['CLAV_' + side, 'SCAP_' + side])); var off = qR(sc.q, { x: restU.x * SC, y: restU.y * SC, z: restU.z * SC }); var S = { x: sc.p.x + off.x, y: sc.p.y + off.y, z: sc.p.z + off.z };
    var T = { x: tgt.hand[0], y: tgt.hand[1], z: tgt.hand[2] }; var P = { x: tgt.pole[0], y: tgt.pole[1], z: tgt.pole[2] };
    if (tgt.frame === 'CHEST' || tgt.frame === 'PELVIS') { /* a target authored in the TRUNK's frame (twisted poses: side chest, back lat): carried by the composed trunk */ var chain = tgt.frame === 'CHEST' ? CHAIN_T : ['PELVIS']; var fkT = chainFK(chain); var o = restPos(chain); var tl = qR(fkT.q, { x: T.x - o.x, y: T.y - o.y, z: T.z - o.z }); T = { x: fkT.p.x + tl.x, y: fkT.p.y + tl.y, z: fkT.p.z + tl.z }; var pl2 = qR(fkT.q, { x: P.x - o.x, y: P.y - o.y, z: P.z - o.z }); P = { x: fkT.p.x + pl2.x, y: fkT.p.y + pl2.y, z: fkT.p.z + pl2.z }; }
    if (!tgt.frame) pushOut(T, !!tgt.contact);
    var dx = T.x - S.x, dy = T.y - S.y, dz = T.z - S.z; var d = Math.hypot(dx, dy, dz); var dmax = (L1 + L2) * 0.985, dmin = Math.abs(L1 - L2) + 0.02; if (d > dmax) { var k1 = dmax / d; dx *= k1; dy *= k1; dz *= k1; d = dmax; } if (d < dmin) { var k2 = dmin / (d || 1e-4); dx *= k2; dy *= k2; dz *= k2; d = dmin; } if (d < 1e-4) return 0;
    var ux = dx / d, uy = dy / d, uz = dz / d; var a2 = (L1 * L1 - L2 * L2 + d * d) / (2 * d); var rr = Math.sqrt(Math.max(0, L1 * L1 - a2 * a2));
    var ex = P.x - S.x, ey = P.y - S.y, ez = P.z - S.z; var dotu = ex * ux + ey * uy + ez * uz; var px = ex - ux * dotu, py = ey - uy * dotu, pz = ez - uz * dotu; var pl = Math.hypot(px, py, pz); if (pl < 1e-4) { px = rigOutSign(side); py = -0.2; pz = 0; pl = Math.hypot(px, py, pz); } px /= pl; py /= pl; pz /= pl;
    var Ex = S.x + ux * a2 + px * rr, Ey = S.y + uy * a2 + py * rr, Ez = S.z + uz * a2 + pz * rr;   /* elbow */
    var u = { x: (Ex - S.x) / L1, y: (Ey - S.y) / L1, z: (Ez - S.z) / L1 }; var f = { x: (S.x + dx - Ex) / L2, y: (S.y + dy - Ey) / L2, z: (S.z + dz - Ez) / L2 };
    var n = { x: f.y * u.z - f.z * u.y, y: f.z * u.x - f.x * u.z, z: f.x * u.y - f.y * u.x }; var nl = Math.hypot(n.x, n.y, n.z); if (nl < 1e-4) n = orthoHinge(u, { x: 1, y: 0, z: 0 }); else { n.x /= nl; n.y /= nl; n.z /= nl; }   /* hinge = f × u: the elbow flexes by a NEGATIVE rotation about its local x (this rig's FOREARM_x < 0 = flexion), so the humerus twist stays near rest */
    /* rest axes: the humerus bone axis is the forearm rest offset, the forearm bone axis is the hand rest offset; the hinge is rest local x made ⟂ */
    var aU = { x: restF.x * SC / L1, y: restF.y * SC / L1, z: restF.z * SC / L1 }; var bU = orthoHinge(aU, { x: 1, y: 0, z: 0 }); var aF = { x: restH.x * SC / L2, y: restH.y * SC / L2, z: restH.z * SC / L2 }; var bF = orthoHinge(aF, { x: 1, y: 0, z: 0 });
    var qUw = rotToFrame(aU, bU, u, n), qFw = rotToFrame(aF, bF, f, n);
    /* to local: humerus in the scapula frame, forearm in the humerus frame; keep the authored forearm pronation (twist about its own axis) */
    var qUl = qM(qI(sc.q), qUw), qFl = qM(qI(qUw), qFw); var pron = twistY(fa); if (Math.abs(pron) > 1e-3) { var hp = Math.sin(pron / 2); qFl = qM(qFl, [aF.x * hp, aF.y * hp, aF.z * hp, Math.cos(pron / 2)]); }   /* pronation about the forearm's TRUE bone axis (elbow→wrist), so the wrist stays put */
    var w = clamp(tgt.w, 0, 1);
    var tw0 = twistY(ua), tf0 = twistY(fa);
    eQT.set(ua.x, ua.y, ua.z, 'XYZ'); qA.setFromEuler(eQT); qB.set(qUl[0], qUl[1], qUl[2], qUl[3]); qA.slerp(qB, w); eQT.setFromQuaternion(qA, 'XYZ'); eulerFit(eQT, ua, 'UPPERARM'); ua.x = eQT.x; ua.y = eQT.y; ua.z = eQT.z;
    eQT.set(fa.x, fa.y, fa.z, 'XYZ'); qA.setFromEuler(eQT); qB.set(qFl[0], qFl[1], qFl[2], qFl[3]); qA.slerp(qB, w); eQT.setFromQuaternion(qA, 'XYZ'); eulerFit(eQT, fa, 'FOREARM'); fa.x = eQT.x; fa.y = eQT.y; fa.z = eQT.z;
    var tw = E['ARMTWIST_' + side]; if (tw) tw.y += (tw0 - twistY(ua)) * 0.6; var ft = E['FORETWIST_' + side]; if (ft) ft.y += (tf0 - twistY(fa)) * 0.55;   /* twist carriers follow the solved humerus / forearm */
    st.ikDebug = st.ikDebug || {}; st.ikDebug[side] = { target: [+T.x.toFixed(3), +T.y.toFixed(3), +T.z.toFixed(3)], pivot: [+S.x.toFixed(3), +S.y.toFixed(3), +S.z.toFixed(3)], elbow: [+Ex.toFixed(3), +Ey.toFixed(3), +Ez.toFixed(3)], reach: +d.toFixed(3), w: +w.toFixed(2) };
    return 1;
  }
  function eulerFit(e, ref, base) { /* of the two XYZ solutions, the one that FITS the endpoint allowance (least clamp excess); ties → nearest the composed value */ var EPL = PR && PR.pose_overrides && PR.pose_overrides.emote_endpoint ? PR.pose_overrides.emote_endpoint.limits_rad : null; if (!EPL) return eulerNear(e, ref); var a = [e.x, e.y, e.z], b = [a[0] + (a[0] > 0 ? -Math.PI : Math.PI), Math.PI - a[1], a[2] + (a[2] > 0 ? -Math.PI : Math.PI)]; if (b[1] > Math.PI) b[1] -= 2 * Math.PI; if (b[1] < -Math.PI) b[1] += 2 * Math.PI; var ex = function (v) { var s0 = 0; ['x', 'y', 'z'].forEach(function (ax, i) { var r = EPL[base + '_' + ax]; if (!r) return; s0 += Math.max(0, r[0] - v[i], v[i] - r[1]); }); return s0; }; var ea = ex(a), eb = ex(b); if (Math.abs(ea - eb) < 1e-6) return eulerNear(e, ref); var pick = ea < eb ? a : b; e.x = pick[0]; e.y = pick[1]; e.z = pick[2]; return e; }
  function ikActive(side) { return !!(st.emoteIK && st.emoteIK[side] && st.emoteIK[side].w > 0.05); }
  function clearArms() {
    var fixes = 0;
    ['L', 'R'].forEach(function (side) {
      if (st.layer && st.layer.donor && st.layer.weight > 0.5 && st.layer.pose['UPPERARM_' + side]) { fixes += clearArmIK(side); return; }
      if (ikActive(side)) { fixes += emoteArmIK(side, st.emoteIK[side]); return; }   /* an explicit endpoint: solved, never nudged by the coarse heuristic */
      var ua = E['UPPERARM_' + side], fa = E['FOREARM_' + side]; if (!ua || !fa) return; var r0 = REST_DEPTH[side]; var sc = E['SCAP_' + side];
      for (var step = 0; step < 2; step++) {
        var uaT = sc ? { x: ua.x + sc.x, y: ua.y, z: ua.z + sc.z } : ua; var pt = armPts(side, uaT, fa);   /* the coarse model sees the TOTAL humerothoracic rotation (humerus + scapula) */ var oE = depthE(pt.elbow) - r0.e, oH = depthH(pt.hand) - r0.h; var over = Math.max(oE, oH);
        if (over <= 0.01) break;
        var dz = rigOutSign(side) * clamp(over * 1.3 + 0.03, 0.03, 0.30);   /* open the shoulder outward — the smallest change that clears */
        if (st.layer && st.layer.donor) { eQT.set(ua.x, ua.y, ua.z, 'XYZ'); qA.setFromEuler(eQT); eQT.set(0, 0, dz, 'XYZ'); qB.setFromEuler(eQT); qA.multiply(qB); eQT.setFromQuaternion(qA, 'XYZ'); eulerNear(eQT, ua); ua.x = eQT.x; ua.y = eQT.y; ua.z = eQT.z; }   /* donor pose: the same opening as a rotation (q · Rz), valid on any Euler branch */
        else ua.z += dz;
        if (oH > oE) fa.x *= 0.85;                                      /* and let the elbow open rather than drive the hand through the chest */
        fixes++;
      }
    });
    st.clearFix = fixes;
  }
  var api = {
    armDebug: function () { var o = {}; ['L', 'R'].forEach(function (side) { var ua = E['UPPERARM_' + side], fa = E['FOREARM_' + side]; if (!ua || !fa) return; var sc = E['SCAP_' + side]; var uaT = sc ? { x: ua.x + sc.x, y: ua.y, z: ua.z + sc.z } : ua; var pt = armPts(side, uaT, fa); o[side] = { elbow: [+pt.elbow.x.toFixed(2), +pt.elbow.y.toFixed(2), +pt.elbow.z.toFixed(2)], hand: [+pt.hand.x.toFixed(2), +pt.hand.y.toFixed(2), +pt.hand.z.toFixed(2)], depthH: +depthH(pt.hand).toFixed(2), depthE: +depthE(pt.elbow).toFixed(2) }; }); o.fixes = st.clearFix; o.ik = st.ikDebug || null; o.endpoint = st.emoteIK ? { L: !!st.emoteIK.L, R: !!st.emoteIK.R } : null; return o; },
    seam: function () { return st.seam; }, aura: function () { return st.aura || 0; }, setOverride: function (map) { st.override = map || null; }, setDonorClips: function (map, enabled) { DONOR.clips = map || {}; DONOR.enabled = !!enabled; }, donorActive: function () { return DONOR.active; }, setLayer: function (pose, weight, id) { st.layer = pose ? { pose: pose, weight: weight === undefined ? 1 : weight, id: id || (++st.layerN) } : null; }, layer: function () { return st.layer; }, propulsion: function () { return { central: st.central, tips: st.tips }; },
    beams: function () { return { fused: st.beams.fused, L: st.beams.L, R: st.beams.R, seam: st.seam, brace: st.brace }; },
    form: function () { return { packed: st.packed, showLegs: st.showLegs, transforming: !!st.transformState, hoverLift: st.hoverLift || 0 }; },
    /* OWNER DECISION 3: the separated tips hover just above the ground. The lift is proportional to the measured leg and eases through the
       transformation; a tiny bob rides the gait phase and is never negative, so support transfer can't read as a drop. */
    hover: function () { return { lift_m: st.hoverLift || 0, clearance_m: st.hoverClear || 0 }; },
    limits: function () { return { convention: 'INSIDE_ANGLE (straight = 180°); bone values are radians of flexion from rest', source: PR_SRC, table: LIMITS, segments_m: SEG, shoulders_m: SHOULDER, hip_gap_m: +HIP_GAP.toFixed(4), clearance_rest_depth: REST_DEPTH }; },
    pose: function () { return st.pose; },
    face: function () { var o = st.face.out || {}; return { brow_l: o.brow_l || 0, brow_r: o.brow_r || 0, cheek_l: o.cheek_l || 0, cheek_r: o.cheek_r || 0, jaw: o.jaw || 0, smile: o.smile || 0, blink: clamp(Math.max(st.blink, o.blink_bias || 0), 0, 1), gaze_x: st.gaze.x, gaze_y: st.gaze.y, layers: { mood: 'CALM', emote: st.face.layers.EMOTE.w > 0.5 ? st.face.layers.EMOTE.name : null, combat: st.face.layers.COMBAT.w > 0.5 ? st.face.layers.COMBAT.name : null, reaction: st.face.layers.REACTION.w > 0.5 ? st.face.layers.REACTION.name : null } }; },
    attackArc: function () { return st.attackArc || null; },
    rootOffset: function () { return st.rootOff || { y: 0, z: 0 }; },
    curlStrike: function () { return st.curlStrike || null; },   /* planted-feet root offset (metres, local: y down, z back) */
    flightLean: function (s) { return { deg: +leanDeg(s).toFixed(2), l: +leanL(s).toFixed(3), curve: LEAN_CURVE }; }, flightBrake: function () { return { active: st.fbT !== undefined, t: st.fbT === undefined ? null : +st.fbT.toFixed(3), amount: +((st.flyBrake || 0)).toFixed(3), seq: st.fbSeq || 0, cfg: FLN.brake || null }; },   /* the ONE flight-lean curve, for probes */   /* S07: the presentation's root drive / impact pulse read the composed arc (never the other way round) */
    gaitState: function () { var hL = st.support && st.support.L, hR = st.support && st.support.R; return { phase: st.gait.phase, L: st.gait.supportL, R: st.gait.supportR, blend: st.gait.blend, settle: st.gait.settle, speed_mps: st.prevSpeedMps, form: st.showLegs ? 'SPLIT' : 'FUSED', beams: { fused: st.beams.fused, L: st.beams.L, R: st.beams.R }, hold: { L: hL ? [+(hL.behind !== undefined ? hL.behind : hL.z).toFixed(3), +hL.w.toFixed(2)] : null, R: hR ? [+(hR.behind !== undefined ? hR.behind : hR.z).toFixed(3), +hR.w.toFixed(2)] : null }, plant: plantInfo() }; },   /* read-only (never consumes the support events the beam FX needs) — PASS 4b walk-identity trace */
    gait: function () { var ev = st.gait.events; st.gait.events = []; return { phase: st.gait.phase, support: { L: st.gait.supportL, R: st.gait.supportR }, events: ev, speed_mps: st.prevSpeedMps, form: st.showLegs ? 'SPLIT' : 'FUSED', airborne: !!st.airborne, brace: st.brace }; },
    emoteState: function () { return { id: st.emote.id, seg: st.emote.seg, t: +st.emote.t.toFixed(2), loop: st.emote.loop, done: st.emote.done, role: st.emote.role }; },
    alive: function () { return { calm: +st.calm.toFixed(2), breath_s: +st.breath.toFixed(2), breath_phase: +st.breathT.toFixed(2), blink: +st.blink.toFixed(2), next_blink_s: +(st.blinkNext - st.blinkT).toFixed(1), gaze: { x: +st.gaze.x.toFixed(3), y: +st.gaze.y.toFixed(3) }, variation: st.vary.kind, clearance_fixes: st.clearFix, eyes: rig.eyes || 'K3_EMISSIVE_MASK_ONLY (no eye/lid geometry; blink = a material uniform fading the glow, NOT eyelid motion)' }; },
    /* v: adapter view — speed01, speed_mps, backward, yaw, vz, moving, fly, dash, attack{state,progress,category,attack_id,profile}, guard, hitAgo, hitDir, ko, emote, emote_at, emote_target_dir, paired{id,role,t0,partner_id}, host_t, mahloco, transforming, form, altitude, outcome */
    /* SUB-STEPPED so animation time tracks wall clock even when the renderer is slow.
       play.js used to hand this a delta clamped to 0.1 s: at 3 fps (0.33 s frames) the character then advanced 0.1 s of
       animation per frame and the whole performance played at a third speed — the movement looked floaty and late while
       the host kept moving the body at full speed. The clamp is still needed (a stalled tab must not integrate a 10 s
       jump), so the fix is to spend the real elapsed time in bounded steps instead of discarding it. */
    animate: function (v, dtReal, maxStep) {   /* maxStep: the presentation LOD passes a larger sub-step for distant rigs (default 1/30 s) */
      var left = Math.min(0.5, Math.max(0, dtReal || 0)), MAXSTEP = maxStep && maxStep > 1 / 30 ? Math.min(0.25, maxStep) : 1 / 30;
      var out; if (left <= MAXSTEP + 1e-9) out = this.step(v, left); else { while (left > 1e-4) { var s0 = Math.min(MAXSTEP, left); out = this.step(v, s0); left -= s0; } }
      plantPass(v, Math.min(0.5, Math.max(0, dtReal || 0))); return out;
    },
    step: function (v, dt) {
      v = v || {}; dt = clamp(dt, 0, 0.1); st.t += dt; Object.keys(E).forEach(function (k) { E[k].x = 0; E[k].y = 0; E[k].z = 0; });
      var speed01 = clamp(v.speed01 || 0, 0, 1); var speedMps = v.speed_mps !== undefined ? v.speed_mps : speed01 * 5.5; st.prevSpeedMps = speedMps; var accel = (speed01 - st.prevSpeed) / Math.max(dt, 1e-3); st.prevSpeed = speed01; st.brake = clamp(st.brake * Math.max(0, 1 - dt * 4) + (accel < -0.8 ? -accel * dt * 2 : 0), 0, 1);
      if (st.yawPrev === null) st.yawPrev = v.yaw || 0; var dy = Math.atan2(Math.sin((v.yaw || 0) - st.yawPrev), Math.cos((v.yaw || 0) - st.yawPrev)); st.yawPrev = v.yaw || 0; st.turn += ((dy / Math.max(dt, 1e-3)) - st.turn) * Math.min(1, dt * 8); var turn = clamp(st.turn / 3, -1, 1);
      st.turnAnt += (turn - st.turnAnt) * Math.min(1, dt / Math.max(0.03, (TP.turn && TP.turn.gaze_lead_s) || 0.12));   /* gaze/chest lead the turn, the body follows through */
      var TS = TP.turn || { chest_yaw_share: 0.45, pelvis_yaw_share: 0.35, lower_yaw_share: 0.2 };
      var ms = String(v.mahloco || ''); var isTf = /^(UNFUSE|REFUSE)_/.test(ms); var at = v.attack; if (!at) st.atkAllow = null; var hitAgo = v.hitAgo === undefined || v.hitAgo === null ? 99 : v.hitAgo; var split = v.form === 'SPLIT'; var fly = !!v.fly; st.airborne = fly || (v.altitude || 0) > 0.3; var flyX = function (sMps) { var cr = (v.fly_cruise && v.fly_cruise > 0.5) ? v.fly_cruise : (((FLN.cruise_mps_fallback || {})[split ? 'SPLIT' : 'FUSED']) || 7.71); return sMps / cr; };   /* §7: the lean curve's x = speed / cruise */
      var busy = !!(at || v.dash || v.guard || v.ko || hitAgo < 0.6 || isTf); st.calm = clamp(st.calm + ((busy || speed01 > 0.15) ? -dt * (DA.calm_drop_per_s || 3) : dt * (DA.calm_rise_per_s || 1.2)), 0, 1); var calm = st.calm;
      var amp = ({ HEAVY: 1.25, QUICK: 0.85, FLOW: 1.0, PRECISE: 0.9, BALANCED: 1.0 }[at && at.profile && at.profile.style] || 1) * CLS.amp; var rate = 7 * CLS.speed; st.brace = Math.max(0, st.brace - dt * 3);
      /* ───── 1 ALIVE — restrained breathing through ribcage + upper spine, relaxed shoulders, calm face, purposeful pauses ───── */
      st.breathT += dt / st.breath; if (st.breathT >= 1) { st.breathT -= 1; st.breath = rr(DA.breath_cycle_s, 5); } var br = 0.5 - 0.5 * Math.cos(st.breathT * Math.PI * 2); var brs = br * (0.55 + 0.45 * calm) * (busy ? 1.15 : 1);
      set('CHEST', 0.026 * brs - 0.008); set('SPINE', 0.007 * brs); set('NECK', -0.004 * brs); set('CLAV_L', -0.012 * brs, 0, 0.010 * brs); set('CLAV_R', -0.012 * brs, 0, -0.010 * brs); set('UPPERARM_L', 0.012 * brs, 0, 0.05 + 0.008 * brs); set('UPPERARM_R', 0.012 * brs, 0, -0.05 - 0.008 * brs); set('FOREARM_L', -0.10); set('FOREARM_R', -0.10); hand('L', 'RELAXED', 1); hand('R', 'RELAXED', 1);
      st.blinkT += dt; if (st.blinkPhase === 0 && st.blinkT >= st.blinkNext) { st.blinkPhase = 1; st.blinkT = 0; } if (st.blinkPhase === 1) { st.blink = clamp(st.blinkT / (DA.blink_close_s || 0.07), 0, 1); if (st.blink >= 1) { st.blinkPhase = 2; st.blinkT = 0; } } else if (st.blinkPhase === 2) { st.blink = 1 - clamp(st.blinkT / (DA.blink_open_s || 0.11), 0, 1); if (st.blink <= 0) { st.blink = 0; st.blinkPhase = 0; st.blinkT = 0; st.blinkNext = rr(DA.blink_interval_s, 4) * (rnd() < 0.15 ? 0.25 : 1); } }
      var gz = st.gaze; gz.t += dt; if (gz.t >= gz.next) { gz.t = 0; gz.next = rr(DA.gaze_interval_s, 4); var big = rnd() < 0.25; gz.tx = (rnd() - 0.5) * (big ? (DA.gaze_large_rad || 0.24) : (DA.gaze_small_rad || 0.10)); gz.ty = (rnd() - 0.5) * (big ? 0.10 : 0.05); if (rnd() < 0.3) { gz.tx = 0; gz.ty = 0; } }
      if (v.emote_target_dir !== undefined && v.emote_target_dir !== null) { gz.tx = clamp(v.emote_target_dir, -0.6, 0.6); gz.ty = 0; }
      gz.x += (gz.tx - gz.x) * Math.min(1, dt * 6); gz.y += (gz.ty - gz.y) * Math.min(1, dt * 6); gz.headX += (gz.x - gz.headX) * Math.min(1, dt * 2.2); gz.headY += (gz.y - gz.headY) * Math.min(1, dt * 2.2);
      var gzW = v.emote_target_dir !== undefined && v.emote_target_dir !== null ? 1 : calm;   /* attention blends in and out; no craning */
      set('HEAD', gz.headY * 0.45 * gzW + 0.004 * Math.sin(st.t * 0.7), gz.headX * 0.45 * gzW); set('NECK', gz.headY * 0.2 * gzW, gz.headX * 0.2 * gzW); if (Math.abs(gz.headX) > 0.20) set('CHEST', 0, gz.headX * 0.12 * gzW);
      var vy = st.vary; vy.t += dt; if (vy.kind) { var vp = Math.min(1, vy.t / vy.dur); var vw = (vp < 0.25 ? ease(vp / 0.25) : (vp > 0.75 ? 1 - ease((vp - 0.75) / 0.25) : 1)) * calm; if (vy.kind === 'SHIFT') { set('PELVIS', 0, 0, 0.030 * vy.amp * vw); set('SPINE', 0, 0, -0.022 * vy.amp * vw); set('CHEST', 0, 0, -0.010 * vy.amp * vw); set('TAIL1', 0, 0, -0.018 * vy.amp * vw); set('THIGH_L', 0, 0, -0.025 * vy.amp * vw); set('THIGH_R', 0, 0, -0.025 * vy.amp * vw); } else if (vy.kind === 'HANDS') { set('FOREARM_L', -0.12 * vw); set('FOREARM_R', -0.09 * vw); set('HAND_L', 0.10 * vw); set('HAND_R', 0.08 * vw); set('FINGERS_L', -0.20 * vw); set('FINGERS_R', -0.16 * vw); } else if (vy.kind === 'SHOULDER') { set('CLAV_L', -0.035 * vw, 0, 0.030 * vy.amp * vw); set('CLAV_R', -0.035 * vw, 0, -0.030 * vy.amp * vw); set('CHEST', 0.012 * vw); } if (vp >= 1) { vy.kind = null; vy.t = 0; vy.next = rr(DA.idle_adjust_interval_s, 10) * 1.8; } } else if (vy.t >= vy.next && calm > 0.8) { vy.t = 0; vy.kind = ['SHIFT', 'GLANCE', 'GLANCE', 'HANDS', 'SHIFT', 'SHOULDER'][Math.floor(rnd() * 6)]; vy.dur = 3.0 + rnd() * 3.0; vy.amp = (rnd() < 0.5 ? -1 : 1) * 0.7; }   /* motion precision pass E: rarer, smaller, mostly gaze / posture */
      st.balanceT += dt; var balW = 0.5 - 0.5 * Math.cos(st.balanceT * Math.PI * 2 / 9.0); var balSide = Math.sin(st.balanceT * Math.PI * 2 / 9.0); set('PELVIS', 0, 0, 0.018 * balSide * calm); set('LUMBAR', 0, 0, -0.010 * balSide * calm); set('CHEST', 0, 0, -0.008 * balSide * calm); set('CLAV_L', -0.010 * balW * calm); set('CLAV_R', -0.010 * balW * calm);   /* ACTIVE IDLE: a 9 s weight shift through pelvis / lumbar / chest, shoulders settling with it */
      st.central = 1; st.tips = 0; st.seam = 0; st.aura = 0; var beamF = DB.fused_idle || 1, beamL = 0, beamR = 0;
      /* ───── 2 LOCOMOTION ───── */
      var lat = clamp(v.lateral || 0, -1, 1); var latA = Math.abs(lat) > 0.35 && !v.dash && v.locked ? Math.abs(lat) : 0;   /* the lateral glide is a LOCK-ON strafe posture only: in free movement the whole body faces travel (S07 orientation rule) */
      /* OWNER DECISION 2: a held sideways input is a controlled lateral glide (presentation only; never DASH). Preparation 0.18 s in,
         brake 0.28 s out, so a reversal blends through neutral instead of snapping a mirrored pose. */
      var GL = PR.lateral_glide || {}; var gIn = 1 / Math.max(0.05, GL.prep_s || 0.18), gOut = 1 / Math.max(0.05, GL.brake_s || 0.28);
      st.glide = st.glide === undefined ? 0 : st.glide; var gTarget = latA * Math.min(1, speed01 * 1.6);
      st.glide += (gTarget - st.glide) * Math.min(1, dt * (gTarget > st.glide ? gIn : gOut));
      st.glideDir = st.glideDir === undefined ? 0 : st.glideDir; if (latA > 0) st.glideDir += (Math.sign(lat) - st.glideDir) * Math.min(1, dt * 6);
      var back = v.backward ? 1 : 0; var mv = speed01 * (1 - back) * (1 - 0.65 * st.glide); var bk = speed01 * back * (1 - 0.5 * st.glide);
      if (fly) { st.gait.supportL = 0; st.gait.supportR = 0; st.gait.blend = Math.max(0, st.gait.blend - dt * 2.5); }   /* flight REPLACES the ground locomotion layer — the lean is authored once, in 3, and never stacked on top of the travel lean */
      else if (!split) {
        /* FUSED: ONE whole-body lean distributed pelvis → spine → chest → fused lower body, so trunk-to-thigh stays ~170–180° and the waist never hinges alone */
        var lean = (LEAN_TRAVEL[0] + (LEAN_TRAVEL[1] - LEAN_TRAVEL[0]) * mv) * mv;
        set('PELVIS', (LD.PELVIS || 0.45) * lean); set('SPINE', (LD.SPINE || 0.3) * lean); set('CHEST', (LD.CHEST || 0.15) * lean); set('TAIL1', (LD.TAIL1 || 0.1) * lean); set('HEAD', -0.5 * lean);
        var trail = (TRAIL[0] + (TRAIL[1] - TRAIL[0]) * mv) * mv;   /* 10–15° of trailing shoulder, supported by the clavicle — never dragged behind the back */
        set('CLAV_L', -0.25 * trail, 0, 0.12 * trail); set('CLAV_R', -0.25 * trail, 0, -0.12 * trail); set('UPPERARM_L', trail, 0, 0.06 * mv); set('UPPERARM_R', trail, 0, -0.06 * mv); set('FOREARM_L', -0.16 * mv); set('FOREARM_R', -0.16 * mv); set('TAIL2', -0.05 * mv); set('TAIL3', -0.04 * mv);
        var accResp = clamp(accel * 0.12, -0.05, 0.05); set('CHEST', accResp); set('PELVIS', -accResp * 0.5);   /* small coordinated chest/pelvis response to acceleration */
        set('SPINE', -0.10 * bk); set('CHEST', -0.04 * bk); set('HEAD', 0.07 * bk); set('UPPERARM_L', -0.26 * bk); set('UPPERARM_R', -0.26 * bk); set('FOREARM_L', -0.30 * bk); set('FOREARM_R', -0.30 * bk); set('TAIL1', 0.09 * bk);   /* backward glide: its own restrained posture */
        set('CHEST', 0, -(TS.chest_yaw_share || 0.45) * 0.42 * turn); set('PELVIS', 0, -(TS.pelvis_yaw_share || 0.35) * 0.42 * turn); set('TAIL1', 0, -(TS.lower_yaw_share || 0.2) * 0.42 * turn); set('HEAD', 0, -0.30 * st.turnAnt);
        var stopC = (DT.stop_counter_rad || 0.08) * st.brake; set('PELVIS', -0.5 * stopC); set('SPINE', -0.3 * stopC); set('CHEST', -0.2 * stopC); set('UPPERARM_L', -0.18 * st.brake); set('UPPERARM_R', -0.18 * st.brake);   /* deceleration: upright balance with one short settle */
        beamF = (DB.fused_idle || 1) * (1 + ((DB.fused_travel || 1.25) - 1) * mv) * (1 + 0.12 * st.brake) * (0.95 + 0.05 * Math.sin(st.t * 2.3)); st.gait.supportL = 1; st.gait.supportR = 1; st.gait.blend = Math.max(0, st.gait.blend - dt * 3);
      } else {
        /* SEPARATED: one gait phase from travelled distance; legs and arms are in phase with the support events, stride and knee lift fitted to the MEASURED segments */
        var g = st.gait; var cyc = DG.distance_per_full_cycle_m || 1.9; var prev = g.phase;
        if (st.airborne) { g.blend = Math.max(0, g.blend - dt * 2.5); }   /* in the air the phase FREEZES where it was and resumes on landing — the support sequence never loses its place */
        else if (speedMps > 0.05) { g.phase = (g.phase + Math.PI * 2 * (speedMps / cyc) * dt) % (Math.PI * 2); g.settle = 0; g.blend = Math.min(1, g.blend + dt * 4); }
        else { g.settle = Math.min(1, g.settle + dt / (DG.step_settle_s || 0.25)); var toward = Math.cos(g.phase) >= 0 ? 0 : Math.PI; var dp = Math.atan2(Math.sin(toward - g.phase), Math.cos(toward - g.phase)); g.phase = (g.phase + dp * Math.min(1, dt * 6) + Math.PI * 2) % (Math.PI * 2); g.blend = Math.max(0, g.blend - dt * 2.5); }   /* stopping settles onto the NEAREST support without ever restarting the phase */
        var crossed = function (a, b, x) { var da = Math.atan2(Math.sin(a - x), Math.cos(a - x)), db = Math.atan2(Math.sin(b - x), Math.cos(b - x)); return da < 0 && db >= 0 && (db - da) < Math.PI; };
        if (!st.airborne) { var stg = clamp(0.35 + speedMps / 6, 0.35, 1); if (crossed(prev, g.phase, 0)) g.events.push({ side: 'L', t: st.t, strength: stg }); if (crossed(prev, g.phase, Math.PI)) g.events.push({ side: 'R', t: st.t, strength: stg }); }   /* a support event IS a phase crossing — emitted while walking and while settling to a stop, so the L/R sequence stays even across stop / start / reverse / turn / dash */
        var ph = g.phase; var cph = Math.cos(ph), sph = Math.sin(ph); var sL = 0.5 + 0.5 * cph, sR = 0.5 - 0.5 * cph; g.supportL = sL; g.supportR = sR;
        /* MOTION PRECISION PASS D — long, deliberate, technologically assisted strides (owner: ≈2× the visual stride at half the cadence).
           Cadence is tied to travelled distance (distance_per_full_cycle_m, now 3.6 m): one thrust carries the body visibly farther.
           The thigh follows a HELD-EXTREME curve (|cos|^0.6): quick passage through the thrust, long hover at the ends of the swing.
           Knee flexes at the measured knee through the swing (≈60°) and softens under the support pulse; the pelvis transfers weight
           (roll toward the support tip) with chest counter-rotation and shoulder counter-swing; tips hover, never plant. */
        var stride = clamp(speedMps / (DG.stride_speed_ref_mps || 3), 0, 1.1) * g.blend; var dirS = 1 - back * 2;
        var held = function (c) { return Math.sign(c) * Math.pow(Math.abs(c), 0.6); }; var hc = held(cph);
        var reachMax = Math.min(cyc * 0.25, SEG.leg * 0.78); var swingMax = Math.min(DG.swing_max_rad || 0.82, Math.asin(clamp(reachMax / Math.max(0.05, SEG.leg), 0, 0.95))) * stride;   /* thigh swing fitted to the real leg length */
        var kneeSwing = clamp((DG.knee_swing_rad || 1.05) * stride, 0, 1.25); var softKnee = 0.10 + 0.06 * stride;
        var swL = Math.max(0, -sph), swR = Math.max(0, sph);                               /* swing share per leg (L swings while R supports and vice versa) */
        var ampL = (hc * dirS > 0 ? 1 : 0.68), ampR = (hc * dirS < 0 ? 1 : 0.68);   /* hip flexion reaches farther than extension (a real stride: the trailing leg never goes horizontal) */
        set('THIGH_L', -swingMax * hc * dirS * ampL, 0, outSign('L') * 0.05); set('THIGH_R', swingMax * hc * dirS * ampR, 0, outSign('R') * 0.05);   /* L forward at phase 0 (its support strike), back at π — in phase with the pulses; a small splay keeps the knees apart */
        /* HOVER-SPECIFIC REAR-LEG SWING (closure pass 1): the knee never curls while the thigh is BEHIND the body (that was the run-derived
           heel-kick); it flexes only as the thigh passes under the pelvis to lift the tip clear, and re-extends for the forward reach.
           gate = thigh position 0 (fully back) → 1 (fully forward); flexion ∝ swing · 4g(1−g), peaking under the body. */
        var gL = 0.5 + 0.5 * hc * dirS, gR = 0.5 - 0.5 * hc * dirS; var kL = kneeSwing * swL * 4 * gL * (1 - gL), kR = kneeSwing * swR * 4 * gR * (1 - gR);
        set('SHIN_L', softKnee + kL + 0.08 * stride * sL); set('SHIN_R', softKnee + kR + 0.08 * stride * sR);   /* knee flexes at the real knee only under the body; a soft give under support; long behind, long ahead */
        set('TIP_L', 0.18 * swL * stride * (0.4 + 0.6 * gL) - 0.06 * stride * sL); set('TIP_R', 0.18 * swR * stride * (0.4 + 0.6 * gR) - 0.06 * stride * sR);   /* the swing tip levels up as it comes forward: positive clearance, never a plant */
        /* SUPPORT-TIP CONSTRAINT (motion-matching principle, hover version of foot locking): while a leg carries support its tip HOLDS its
           position over the ground (a hover thrust point, never a plant) — the anchor is carried backwards by the body's own travel and the
           hip-knee-tip chain is solved by two-bone IK in the sagittal plane so the leg reads as pushing the body past a point instead of
           skating. Reach is capped at 0.86 of the measured leg; beyond it the anchor slips (the species glides, it does not root). */
        if (PLANT_ON()) { /* G37 repair: the support hold is solved in WORLD space after the pose is written (see plantPass) — the pelvis-frame drifting hold below is the rollback path (gait.plant = false) */ } else (function () {
          var vzL = speedMps * Math.sqrt(Math.max(0, 1 - lat * lat)) * (back ? -1 : 1), vxL = speedMps * lat;
          var Lt = SEG.thigh, Ls = SEG.shin, reach = (DG.support_reach_frac === undefined ? 0.86 : DG.support_reach_frac) * (Lt + Ls);   /* PASS 5: a plant needs ~0.98 (the held point is at the leg's own length at the extremes of the stride) */
          ['L', 'R'].forEach(function (S) {
            var sup = S === 'L' ? sL : sR; var th = E['THIGH_' + S], sh = E['SHIN_' + S]; if (!th || !sh) return;
            var a0 = th.x, k0 = sh.x;                                                            /* authored sagittal angles (mesh space: +x rotation swings the hanging bone BACK) */
            var fk = function (a, k) { return { z: -Lt * Math.sin(a) - Ls * Math.sin(a + k), y: -Lt * Math.cos(a) - Ls * Math.cos(a + k) }; };
            var tip = fk(a0, k0); var anc = st.support[S];
            var pastExtreme = S === 'L' ? sph > 0 : sph < 0;                                    /* the hold starts at the leg's forward extreme (its thrust point) and lasts while the body passes over it */
            var holdBehind = DG.support_hold_behind_m === undefined ? 0.16 : DG.support_hold_behind_m; if (sup > 0.55 && pastExtreme && !st.airborne && speedMps > 0.4 && !(anc && anc.z < -holdBehind)) {   /* the hold ends once the thrust point has passed ~16 cm behind the hip: the push is done, the leg is free to trail and swing */
              if (!anc) anc = st.support[S] = { z: tip.z, y: tip.y, w: 0 };
              anc.z -= vzL * dt * (DG.support_drift === undefined ? 0.45 : DG.support_drift); anc.w = Math.min(1, anc.w + dt * 9);                      /* hover thrust, not a plant: the held point drifts back at ~45 % of travel (the species glides on its pulse); PASS 5: `gait.support_drift` 1.0 = a world-fixed plant (Spec E foot IK), measured both ways — the owner picks */
              var D = Math.hypot(anc.z, anc.y); if (D > reach) { var kk = reach / D; anc.z *= kk; anc.y *= kk; D = reach; }
              var cosK = clamp((Lt * Lt + Ls * Ls - D * D) / (2 * Lt * Ls), -1, 1); var knee = Math.PI - Math.acos(cosK);   /* flexion from straight */
              var ang = Math.atan2(-anc.z, -anc.y);                                                 /* direction hip -> tip measured from straight down, + = backwards */
              var inner = Math.atan2(Ls * Math.sin(knee), Lt + Ls * Math.cos(knee));               /* thigh leads the tip direction by the knee's offset */
              var thigh = clamp(ang - inner, -1.0, 0.95); var w = sup >= 0.7 ? anc.w : anc.w * (sup - 0.55) / 0.15;
              th.x = a0 + (thigh - a0) * w; sh.x = k0 + (knee - k0) * w;
            } else if (anc) { anc.w -= dt * 6; if (anc.w <= 0) st.support[S] = null; else { var w2 = Math.max(0, anc.w); th.x = a0 + (Math.atan2(-anc.z, -anc.y) - a0) * w2 * 0.5; } }
          });
        })();   /* the swing tip clears the ground and levels for its next support pulse */
        var pr = (DG.pelvis_rot_rad || 0.14) * stride, cr = (DG.counter_rot_rad || 0.11) * stride, sw = (DG.shoulder_sway_rad || 0.06) * stride, as = clamp((DG.arm_swing_rad || 0.42) * stride * (0.7 + 0.3 * speed01), 0, 0.5);
        var roll = (DG.pelvis_roll_rad || 0.07) * stride * (sL - sR);                       /* weight transfer: the pelvis rolls over the supporting tip */
        set('PELVIS', 0.035 * stride, pr * hc, roll); set('SPINE', 0.045 * stride, -cr * 0.5 * hc, -roll * 0.6); set('CHEST', 0.02 * stride + 0.015 * stride * Math.cos(2 * ph), -cr * hc, sw * sph - roll * 0.3); set('CLAV_L', 0, 0, sw * 0.5 * sph); set('CLAV_R', 0, 0, sw * 0.5 * sph); set('HEAD', -0.03 * stride, cr * 0.35 * hc, -sw * 0.4 * sph);   /* pelvis rotation + chest counter-rotation + restrained shoulder sway, steady head */
        set('UPPERARM_L', as * hc + 0.05 * stride, 0, 0.07 * stride); set('UPPERARM_R', -as * hc + 0.05 * stride, 0, -0.07 * stride); set('FOREARM_L', -0.26 * stride - 0.14 * stride * Math.max(0, hc)); set('FOREARM_R', -0.26 * stride - 0.14 * stride * Math.max(0, -hc));   /* opposite arm swing, easy hands */
        if (g.blend > 0.25 && !at) rate = Math.max(rate, 30 * CLS.speed);                   /* the gait is a smooth oscillator already: no pose low-pass on top (7/s halved the swing and lagged it 60°) */
        set('CHEST', 0, -(TS.chest_yaw_share || 0.45) * 0.4 * turn); set('PELVIS', 0, -(TS.pelvis_yaw_share || 0.35) * 0.4 * turn); set('SPINE', -0.05 * st.brake); set('UPPERARM_L', -0.12 * st.brake); set('UPPERARM_R', -0.12 * st.brake);
        var base = DB.split_baseline || 0.3, res = DB.split_swing_residual || 0.12; var behL = Math.max(0, -hc * dirS) * stride, behR = Math.max(0, hc * dirS) * stride;   /* how far each thigh trails behind the body (0 ahead → 1 fully back) */
        beamL = (base * sL + res * (1 - sL)) * (1 - 0.8 * Math.pow(behL, 1.4)); beamR = (base * sR + res * (1 - sR)) * (1 - 0.8 * Math.pow(behR, 1.4)); beamF = 0; st.tips = 0.5;   /* micro-closure: a trailing tip at the end of its push idles down to ~20 %; support pulses (crossing events) are untouched */
        if (st.airborne) { g.supportL = 0; g.supportR = 0; beamL = base * 0.8; beamR = base * 0.8; var fsA = fly ? leanL(flyX(speedMps)) : 0; set('THIGH_L', 0.16 * (1 - fsA)); set('THIGH_R', 0.16 * (1 - fsA)); set('SHIN_L', 0.20 * (1 - fsA)); set('SHIN_R', 0.20 * (1 - fsA)); }   /* airborne: soft tuck at hover / in a fall, fading out with forward flight speed (the flight layer then trails the legs — owner 2026-09-18 §6: one coordinated travel posture, never legs dangling under a leaning chest) */
      }
      var HG = PR.hover_gait || {}; var hClear = Math.max(HG.min_clearance_m || 0.035, (HG.hover_clearance_fraction_of_leg || 0.07) * SEG.leg); st.hoverClear = hClear;
      var hoverTarget = (st.showLegs ? (1 - st.packed) : 0) * (fly ? 0 : 1) * hClear;
      var bob = st.showLegs && speed01 > 0.05 ? (0.35 * hClear + 0.018 * Math.min(1, speed01 * 1.5)) * (0.5 - 0.5 * Math.cos(2 * st.gait.phase)) * Math.min(1, speed01 * 2) : 0;   /* pelvis heave: twice per cycle, ~2 cm at speed (the pelvis rides the support, it is not a fixed beam) */
      st.hoverLift = (st.hoverLift === undefined ? hoverTarget : st.hoverLift) + ((hoverTarget + bob) - (st.hoverLift || 0)) * Math.min(1, dt / Math.max(0.05, HG.support_transfer_smooth_s || 0.16));
      /* ───── 2b LATERAL GLIDE — chest turned slightly toward travel, pelvis follow, a small whole-body bank, the leading arm opening
         toward travel while the trailing arm settles back; shoulders low, hands loose. World +x is model −x (the body is turned 180°
         about Y), so travel toward world-right leads with the model's −x side — the `_L` bones on this rig. ───── */
      if (st.glide > 0.01 && !fly) {
        var gA = st.glide, dirW = st.glideDir;                                   /* +1 = travelling to the character's right (world +x) */
        var modelDir = MIRROR_SIDES ? -dirW : dirW;                             /* model-space x of travel */
        var lead = (modelDir < 0) === MIRROR_SIDES ? 'L' : 'R', trail = lead === 'L' ? 'R' : 'L';
        var chestTurn = ((GL.chest_turn_deg || [10, 16])[0] + ((GL.chest_turn_deg || [10, 16])[1] - (GL.chest_turn_deg || [10, 16])[0]) * speed01) * DEG;
        var pelvTurn = ((GL.pelvis_turn_deg || [5, 8])[0] + ((GL.pelvis_turn_deg || [5, 8])[1] - (GL.pelvis_turn_deg || [5, 8])[0]) * speed01) * DEG;
        var bank = ((GL.bank_deg || [3, 6])[0] + ((GL.bank_deg || [3, 6])[1] - (GL.bank_deg || [3, 6])[0]) * speed01) * DEG;
        setRaw('CHEST', 0, modelDir * chestTurn * gA, -modelDir * bank * 0.45 * gA);        /* yaw toward travel, a little roll into it */
        setRaw('PELVIS', 0, modelDir * pelvTurn * gA, -modelDir * bank * 0.55 * gA);
        setRaw('SPINE', 0.02 * gA, modelDir * 0.04 * gA, -modelDir * bank * 0.3 * gA);
        setRaw('HEAD', 0, modelDir * 0.10 * gA, 0);                                       /* attentive toward where it is going */
        var openL = (GL.lead_arm_open_deg || 18) * DEG, settle = (GL.trail_arm_settle_deg || 10) * DEG;
        set('UPPERARM_' + lead, -0.14 * gA, 0, outSign(lead) * openL * gA); set('FOREARM_' + lead, -0.24 * gA);
        set('UPPERARM_' + trail, settle * 1.4 * gA, 0, outSign(trail) * 0.04 * gA); set('FOREARM_' + trail, -0.12 * gA);
        set('CLAV_' + lead, -0.04 * gA, 0, 0.06 * gA * outSign(lead)); hand(lead, 'RELAXED', gA); hand(trail, 'RELAXED', gA);
        if (split) {   /* compact lateral repositioning: the leading limb abducts a little toward travel, the trailing one tucks under */
          set('THIGH_' + lead, -0.04 * gA, 0, outSign(lead) * 0.14 * gA); set('THIGH_' + trail, 0.03 * gA, 0, -outSign(trail) * 0.05 * gA);
        } else { set('TAIL1', 0, 0, -modelDir * bank * 0.5 * gA); set('TAIL2', 0, 0, -modelDir * bank * 0.3 * gA); }   /* the fused lower body banks with the trunk */
      }
      /* ───── 3 FLIGHT — the same whole-body language as travel ───── */
      /* FLIGHT BRAKE (owner next-pass 2026-09-20 §2 — presentation_defaults.flight.brake): armed while x ≥ trigger_x, fired by a real deceleration (the stick released at speed), one envelope rise → hold → settle, re-armed only after the speed climbs back above retrigger_x */
      var FB = FLN.brake || {}; var fxNow = fly ? flyX(speedMps) : 0; var accelMps = (speedMps - (st.prevSpeedMps2 === undefined ? speedMps : st.prevSpeedMps2)) / Math.max(dt, 1e-3); st.prevSpeedMps2 = speedMps;
      /* B7 §1: the brake is the HOST's one-shot input-edge event (flight.brake_seq) — the envelope plays exactly once per seq; no velocity heuristics, no re-arming from speed, so a hover, a drift or a landing descent can never rock the body */
      var fbSeq = fly ? (v.brake_seq || 0) : 0; if (st.fbSeq === undefined) st.fbSeq = fbSeq; if (fly && fbSeq !== st.fbSeq && st.fbT === undefined) { st.fbT = 0; st.fbAmp = clamp(((v.brake_x || fxNow) - 0.4) / 0.6, 0.35, 1); } st.fbSeq = fbSeq; if (!fly) { st.fbT = undefined; }
      if (st.fbT !== undefined) { st.fbT += dt; var fbR = FB.rise_s || 0.12, fbH = FB.hold_s || 0.08, fbS = FB.settle_s || 0.22; var fbTot = fbR + fbH + fbS; var fbE = st.fbT < fbR ? smoothstep01(st.fbT / fbR) : (st.fbT < fbR + fbH ? 1 : 1 - smoothstep01((st.fbT - fbR - fbH) / fbS)); st.flyBrake = fbE * (st.fbAmp || 1); if (st.fbT >= fbTot) { st.fbT = undefined; st.flyBrake = 0; } } else st.flyBrake = 0;
      if (fly) { var fx = fxNow; var fs = leanL(fx); var incl = leanDeg(fx) * DEG; var bank = (DF.bank_rad || 0.18) * turn * fs;   /* §7: the inclination comes from the ONE curve (hover 6° → normal 24° → boost 40°), never from a fixed pair */
        var LDc = FLN.distribution_cruise || LD, LDb = FLN.distribution_boost || LDc; var shareOf = function (k, dflt) { var c0 = LDc[k] === undefined ? dflt : LDc[k], b0 = LDb[k] === undefined ? c0 : LDb[k]; return c0 + (b0 - c0) * fs; };   /* boost = the Hinge-inspired commitment: the pelvis leads the lean (its share grows with speed), the spine stays long */
        set('PELVIS', shareOf('PELVIS', 0.45) * incl); set('SPINE', shareOf('SPINE', 0.3) * incl, 0, -bank * 0.3); set('CHEST', shareOf('CHEST', 0.15) * incl, 0, -bank * 0.5); set('TAIL1', shareOf('TAIL1', 0.1) * incl); set('HEAD', -(FLN.head_counter || 0.55) * incl, 0, bank * 0.4);
        var armT = (at && !v.ko) ? 0.12 : 1; st.flyArmK = (st.flyArmK === undefined ? 1 : st.flyArmK) + (armT - (st.flyArmK === undefined ? 1 : st.flyArmK)) * Math.min(1, dt * 9); var ATR = FLN.arm_trail_rad || [TRAIL[1], TRAIL[1] + 0.35]; var fa = (ATR[0] + (ATR[1] - ATR[0]) * fs) * st.flyArmK;   /* the arms sweep back with the lean (boost ≈ 54°), opened outward so they never enter the torso */   /* owner 2026-09-18 §6 / §9.2: an attack in flight owns the arms — the flight arm trail yields to the cast pose while pelvis / legs keep the travel posture (upper-body cast over continuing flight support) */ set('CLAV_L', -0.2 * fa, 0, 0.1 * fa); set('CLAV_R', -0.2 * fa, 0, -0.1 * fa); set('UPPERARM_L', fa, 0, ((FLN.arm_open_rad || 0.14) - bank * 0.5) * st.flyArmK); set('UPPERARM_R', fa, 0, (-(FLN.arm_open_rad || 0.14) - bank * 0.5) * st.flyArmK); set('FOREARM_L', -0.12 * st.flyArmK); set('FOREARM_R', -0.12 * st.flyArmK); var T2R = FLN.tail2_rad || [0.05, 0.15]; set('TAIL2', -(T2R[0] + (T2R[1] - T2R[0]) * fs));   /* the fused lower body participates: the taper curves back under the lean */ var legTrail = (FLN.leg_trail_rad || DF.leg_trail_rad || 0.42) * fs, kneeShape = (FLN.knee_shape_rad || DF.knee_shape_rad || 0.34) * fs;   /* 0.34 = the same knee flexion the hover tuck (0.20 + 0.14) had at full speed before: the knee floor for hit reactions is unchanged (rig limits: SHIN.x ≥ −0.03) */ set('THIGH_L', 0.16 * (1 - fs) + legTrail, 0, 0.04 * fs); set('THIGH_R', 0.16 * (1 - fs) + legTrail, 0, -0.04 * fs);   /* THIGH + = hip EXTENSION (measured): the legs trail BEHIND the pitched pelvis, in line with the lean (the old minus sign folded them forward) */ set('SHIN_L', 0.14 * (1 - fs) + kneeShape); set('SHIN_R', 0.14 * (1 - fs) + kneeShape);   /* SPLIT flight (owner 2026-09-18 §6): with speed the thighs EXTEND behind the pelvis in proportion to the body's inclination (hip extension, the legs trail the lean) with a soft knee, so torso, pelvis and legs read as one travel posture; at hover / when braking (fs → 0) the soft tuck returns and the pelvis un-leans with it — no detached pelvis */
        if (v.vz > 0.2) { set('UPPERARM_L', -0.18); set('UPPERARM_R', -0.18); set('SPINE', -0.05); set('PELVIS', -0.03); } else if (v.vz < -0.2) { set('UPPERARM_L', -0.28, 0, 0.30); set('UPPERARM_R', -0.28, 0, -0.30); set('SPINE', 0.04); }
        var brk = st.brake; set('PELVIS', -0.10 * brk); set('SPINE', -0.07 * brk); set('CHEST', -0.05 * brk);
        var fb = st.flyBrake || 0; if (fb > 0) { set('PELVIS', -(FB.pelvis_rad || 0.34) * fb); set('TAIL1', -(FB.tail1_rad || 0.16) * fb); set('TAIL2', -(FB.tail2_rad || 0.1) * fb); if (split) { set('THIGH_L', -(FB.thigh_rad || 0.22) * fb); set('THIGH_R', -(FB.thigh_rad || 0.22) * fb); } set('SPINE', -(FB.spine_rad || 0.08) * fb); set('CHEST', -(FB.chest_rad || 0.05) * fb); set('HEAD', (FB.head_rad || 0.06) * fb); set('UPPERARM_L', -(FB.arm_rad || 0.16) * fb); set('UPPERARM_R', -(FB.arm_rad || 0.16) * fb); }   /* the lower body sweeps forward as ONE unit under the torso, the torso counter-angles back, the head stays level, the arms absorb — then it settles */ beamF = split ? 0 : (DB.fused_travel || 1.25) * (1 + 0.3 * fs); if (split) { beamL = (DB.split_baseline || 0.3) * (1 + 0.5 * fs); beamR = beamL; } st.central = 1.2; }
      /* ───── 4 DASHES — upright torso, leading hand near the chin, elbow inside the limit ───── */
      if (v.dash) { var d = v.dash.dir; var tl = DD.torso_lean_rad || 0.06;
        if (d === 'FORWARD') { set('PELVIS', 0.06); set('SPINE', 0.16 + tl); set('CHEST', 0.05); set('CLAV_R', -0.2, 0, -0.15); set('UPPERARM_R', -1.30, 0, -0.42); set('FOREARM_R', -1.22); hand('R', 'FIST', 1); set('UPPERARM_L', -0.42, 0, 0.34); set('FOREARM_L', -1.05); set('TAIL1', -0.26); set('TAIL2', -0.12); }
        else if (d === 'BACK') { set('PELVIS', -0.05); set('SPINE', -0.16); set('CHEST', -0.05); set('HEAD', 0.09); set('UPPERARM_L', -0.92, 0, 0.36); set('UPPERARM_R', -0.92, 0, -0.36); set('FOREARM_L', -1.15); set('FOREARM_R', -1.15); hand('L', 'GUARD', 1); hand('R', 'GUARD', 1); set('TAIL1', 0.16); }
        else { var side = d === 'LEFT' ? 1 : -1; set('PELVIS', 0, 0, 0.10 * side); set('CHEST', 0, 0, 0.11 * side); set('SPINE', tl, 0, 0.04 * side); set('HEAD', 0, 0.14 * side, -0.09 * side); var lead = side > 0 ? 'L' : 'R', trail2 = side > 0 ? 'R' : 'L'; set('CLAV_' + lead, -0.22, 0, outSign(lead) * 0.2); set('UPPERARM_' + lead, -1.18, 0, outSign(lead) * 0.62); set('FOREARM_' + lead, -1.26); hand(lead, 'FIST', 1); set('UPPERARM_' + trail2, -0.48, 0, outSign(trail2) * 0.58); set('FOREARM_' + trail2, -0.95); hand(trail2, 'OPEN', 1); set('TAIL1', -0.08, 0, 0.11 * side); set('THIGH_' + lead, -0.22); set('THIGH_' + trail2, 0.18); }
        rate = 16 * CLS.speed; st.brace = 1; beamF *= (DB.fused_brace || 1.5); beamL *= 1.6; beamR *= 1.6; }
      /* M2 EQUIPMENT REST: the body must agree with the socket solve. A dumbbell closes the owning palm; a held barbell is carried with both
         palms on the shaft in front of the thighs, wrists neutral and elbows soft. Attacks own the arms after this layer. */
      var heldGear = v.equipment && v.equipment.held; if (heldGear && !at && !v.ko && !v.dash) { if (heldGear.kind === 'DUMBBELL') { if ((heldGear.count || 0) >= 1) { set('UPPERARM_R', -0.13, 0, -0.08); set('FOREARM_R', -0.20); hand('R', 'FIST', 1); } if ((heldGear.count || 0) >= 2) { set('UPPERARM_L', -0.13, 0, 0.08); set('FOREARM_L', -0.20); hand('L', 'FIST', 1); } } else if (heldGear.kind === 'BARBELL') { set('CLAV_L', -0.05, 0, 0.03); set('CLAV_R', -0.05, 0, -0.03); set('UPPERARM_L', -0.20, 0, 0.16); set('UPPERARM_R', -0.20, 0, -0.16); set('FOREARM_L', -0.24); set('FOREARM_R', -0.24); hand('L', 'FIST', 1); hand('R', 'FIST', 1); } }
      /* ───── 5 ATTACKS — preparation → commit → recovery on the host phases, every elbow inside 105° ───── */
      var combatFace = null;
      if (at && !v.ko) { var startup = /CONCENTRIC|STARTUP/.test(at.state); var p = at.progress === undefined ? 0.6 : clamp(at.progress, 0, 1); var pat = patternOf(at.attack_id); if (at.category === 'SPECIAL_MAGIC' && pat !== 'OVERHEAD' && pat !== 'BAR_PRESS' && pat !== 'CHESTFLY' && pat !== 'KATANA') pat = 'CAST'; st.atkAllow = pat === 'OVERHEAD' || pat === 'BAR_PRESS' ? 'overhead_release' : null;   /* owner-authorized exercise Specials keep their biomechanics; only unmapped Specials use the generic CAST support pose */ /* S07 EXERCISE ARC — anticipation/LOAD (fast into the loaded pose, held with a small breathing overshoot until release), then a WHIP
           into the primary force arc, a readable follow-through and a clean recovery back to the guard/idle. Amplitudes are exaggerated
           (X) on purpose: the limit pass after the stack caps them anatomically, so the pose is as big as the joint allows. */
        var X = (AD && AD.ATTACK_EXAGGERATION) || 1.55; var ordinary = at.category === 'PHYSICAL' || at.category === 'PHYSICAL_MAGIC', style = at.profile && at.profile.style || 'BALANCED'; var CM = { QUICK: [0.56, 0.24, 0.52, 18], HEAVY: [0.78, 0.38, 0.68, 13.5], PRECISE: [0.68, 0.30, 0.62, 15], FLOW: [0.72, 0.36, 0.70, 14], BALANCED: [0.68, 0.32, 0.62, 15.5] }, cv = ordinary ? (CM[style] || CM.BALANCED).slice() : [0.72, 0.34, 0.58, 15.5]; if (ordinary && at.category === 'PHYSICAL_MAGIC') { cv[0] = Math.min(0.84, cv[0] + 0.06); cv[1] = Math.min(0.44, cv[1] + 0.04); cv[2] = Math.min(0.76, cv[2] + 0.04); cv[3] -= 0.7; } var loadEnd = cv[0], whipEnd = cv[1], followStart = cv[2], loadFadeEnd = Math.min(0.74, whipEnd + 0.30); var load = ease(Math.min(1, p / loadEnd)); var q = startup ? load * (1 + 0.025 * Math.sin(p * 6.5)) : 1 - ease(Math.min(1, p / loadFadeEnd)); var whip = startup ? 0 : Math.pow(ease(Math.min(1, p / whipEnd)), style === 'HEAVY' ? 0.82 : 0.72); var follow = startup ? 0 : 1 - ease(Math.max(0, (p - followStart) / Math.max(0.1, 1 - followStart)));   /* M6 closeout: host phase lengths, commit, hit, damage and cost remain authoritative. Only ordinary presentation mapping varies by strike style/category: QUICK arrives and clears crisply, HEAVY carries a longer load/finish, PRECISE stays controlled, FLOW uses the broadest tail, and physical-MAHGIC retains a slightly longer readable charge. Exercise pose branches below remain untouched. */ var rel = whip * follow; rate = cv[3] * CLS.speed; var qa = q * X, ra = rel * X; combatFace = startup ? 'FOCUS' : (rel > 0.3 ? 'EXERT' : 'FOCUS'); if (!startup) st.brace = Math.max(st.brace, rel);
        /* -- PUSH = HORIZONTAL PUSH (S13: the rules call this slot HORIZONTAL_PUSH — a two-hand press straight ahead, the bench press / push-press
              pattern in the air). SETUP (load): scapulae RETRACT and sit down, humeri ~60° flared with the elbows just behind the shoulder line,
              elbows folded so both hands sit at the pecs' height, chest up, trunk NEUTRAL (no forward lean), a small athletic knee brace.
              EXECUTION (release): the humeri swing to horizontal while the elbows extend to lockout and the scapulae PROTRACT at the end — the force
              comes out of the shoulder / elbow, with a restrained ~7° trunk drive and leg extension, never a lunge or a collapsing lean.
              FINISH: palms forward at full reach (wrist extended), held by the follow-through, then back to guard. */
        if (pat === 'PUSH') { var barPush = heldGear && heldGear.kind === 'BARBELL', pushDep = barPush ? 0.32 : 0.06, pushFlex = barPush ? -0.68 : -0.92; set('THIGH_L', 0.11 * qa); set('THIGH_R', 0.11 * qa); set('SHIN_L', 0.15 * qa); set('SHIN_R', 0.15 * qa); set('TAIL1', 0.09 * qa); set('TAIL2', 0.06 * qa); set('CHEST', -0.05 * qa); set('HEAD', 0.02 * qa); set('CLAV_L', 0.03 * qa, outSign('L') * 0.26 * qa, -pushDep * qa); set('CLAV_R', 0.03 * qa, outSign('R') * 0.26 * qa, pushDep * qa);   /* retraction plus a deeper loaded-bar depression keeps the shaft at upper-chest height instead of across the face */ set('UPPERARM_L', -0.10 * qa, outSign('L') * -0.22 * qa, 0.42 * qa); set('UPPERARM_R', -0.10 * qa, outSign('R') * -0.22 * qa, -0.42 * qa); set('FOREARM_L', pushFlex * qa); set('FOREARM_R', pushFlex * qa); hand('L', 'FIST', q); hand('R', 'FIST', q);   /* elbows flared ~35°, humeri a touch externally rotated so the forearms point FORWARD: fists ~15 cm ahead of the pecs */
          set('THIGH_L', -0.10 * ra); set('THIGH_R', -0.10 * ra); set('SHIN_L', -0.13 * ra); set('SHIN_R', -0.13 * ra); set('TAIL1', -0.09 * ra); set('TAIL2', -0.05 * ra); set('PELVIS', 0.04 * ra * amp); set('SPINE', 0.04 * ra * amp); set('CHEST', 0.04 * ra * amp); set('HEAD', -0.03 * ra); set('CLAV_L', -0.06 * ra, outSign('L') * -0.44 * ra, 0.04 * ra); set('CLAV_R', -0.06 * ra, outSign('R') * -0.44 * ra, -0.04 * ra);   /* protraction into the lockout */ set('UPPERARM_L', -1.30 * ra * amp, 0, -0.40 * ra); set('UPPERARM_R', -1.30 * ra * amp, 0, 0.40 * ra); set('FOREARM_L', 1.35 * ra); set('FOREARM_R', 1.35 * ra); hand('L', 'OPEN', rel); hand('R', 'OPEN', rel); set('HAND_L', -0.55 * ra); set('HAND_R', -0.55 * ra); }
        /* -- PULL = VERTICAL PULL (S13: the rules call this slot VERTICAL_PULL — a lat pulldown / pull-up in the air). SETUP (load): both arms
              reach up and wide, elbows nearly straight, scapulae elevated, eyes up, trunk tall. EXECUTION (release): the elbows drive DOWN and
              BACK to the ribs while the forearms flex, the scapulae DEPRESS and RETRACT, the chest lifts — the lats do the work; a small stand-tall
              leg extension. FINISH: fists at the lower ribs, elbows behind the torso, chest up. */
        else if (pat === 'PULL') { set('CHEST', -0.06 * qa); set('SPINE', -0.02 * qa); set('HEAD', -0.14 * qa); set('THIGH_L', 0.07 * qa); set('THIGH_R', 0.07 * qa); set('SHIN_L', 0.08 * qa); set('SHIN_R', 0.08 * qa); set('TAIL1', 0.05 * qa); set('CLAV_L', -0.04 * qa, outSign('L') * -0.10 * qa, 0.22 * qa); set('CLAV_R', -0.04 * qa, outSign('R') * -0.10 * qa, -0.22 * qa);   /* elevation (z) + slight protraction */ set('UPPERARM_L', -0.42 * qa, 0, 1.18 * qa); set('UPPERARM_R', -0.42 * qa, 0, -1.18 * qa); set('FOREARM_L', -0.22 * qa); set('FOREARM_R', -0.22 * qa); hand('L', 'OPEN', q); hand('R', 'OPEN', q);
          set('PELVIS', -0.05 * ra); set('SPINE', -0.07 * ra * amp); set('CHEST', -0.13 * ra * amp); set('HEAD', 0.14 * ra); set('THIGH_L', -0.06 * ra); set('THIGH_R', -0.06 * ra); set('TAIL1', -0.09 * ra); set('CLAV_L', 0.04 * ra, outSign('L') * 0.46 * ra, -0.40 * ra); set('CLAV_R', 0.04 * ra, outSign('R') * 0.46 * ra, 0.40 * ra);   /* retraction (y) + depression (z): the clavicle runs along x */ set('UPPERARM_L', 0.34 * ra * amp, 0, -0.36 * ra); set('UPPERARM_R', 0.34 * ra * amp, 0, 0.36 * ra); set('FOREARM_L', -1.20 * ra * amp); set('FOREARM_R', -1.20 * ra * amp); hand('L', 'FIST', rel); hand('R', 'FIST', rel); }   /* finish: elbows down-and-back to the ribs but still ~30° out, fists in front of the upper chest (a pulldown bar at the clavicles) */
        else if (pat === 'UPRIGHT_ROW') { set('UPPERARM_L', -0.30 * qa, 0, 0.10); set('UPPERARM_R', -0.30 * qa, 0, -0.10); set('FOREARM_L', -0.42 * qa); set('FOREARM_R', -0.42 * qa); hand('L', 'FIST', 1); hand('R', 'FIST', 1); set('CLAV_L', -0.28 * ra, outSign('L') * 0.18 * ra, 0.34 * ra); set('CLAV_R', -0.28 * ra, outSign('R') * 0.18 * ra, -0.34 * ra); set('UPPERARM_L', -0.55 * ra, 0, 1.05 * ra); set('UPPERARM_R', -0.55 * ra, 0, -1.05 * ra); set('FOREARM_L', -1.26 * ra); set('FOREARM_R', -1.26 * ra); set('CHEST', -0.05 * ra); }
        /* -- HINGE = a SURFACE-SUPPORTED HIP HINGE (owner interjection 2026-09-18 §8; numbers in presentation_defaults.hinge). LOAD: hips push back
              behind the feet, femurs to ~45°, shins stay vertical, the long neutral spine inclines at the hips, eyes forward, arms hang long.
              RELEASE: the hip drive to a tall lockout, arms to chest height (never overhead), MAHGIC hurled from the hip drive, a floating follow-through. */
        else if (pat === 'HINGE') { var HN = (D.hinge || {}); var fem = HN.femur_rad || 0.58, tor = (st.showLegs ? HN.torso_rad : (HN.torso_fused_rad || HN.torso_rad)) || 0.66, spn = HN.spine_neutral_rad || 0.05, hang = (st.showLegs ? HN.arm_hang_rad : (HN.arm_hang_fused_rad || 0.60)) || 0.60, relArm = HN.release_arm_rad || -1.25; var HA = HN.airborne || {}; var qh = startup ? qa : qa * (1 - whip);   /* owner interjection 2026-09-18 §8: a REAL surface-supported hip hinge (numbers in presentation_defaults.hinge; rig signs measured: PELVIS + pitches forward and carries the legs / taper back, THIGH + = extension, SHIN + = knee flexion, UPPERARM + = arm back). Targets are exact (divided by X). The loaded pose collapses WITH the whip so the drive ends tall. */
          if (!st.airborne) { set('PELVIS', (tor / X) * qh * amp); set('SPINE', (spn / X) * qh); set('CHEST', (spn * 0.6 / X) * qh); set('HEAD', -(0.36 / X) * qh); set('THIGH_L', -((fem + tor) / X) * qh); set('THIGH_R', -((fem + tor) / X) * qh); set('SHIN_L', (fem / X) * qh); set('SHIN_R', (fem / X) * qh); set('TAIL1', -(tor * 0.75 / X) * qh); set('TAIL2', -(tor * 0.25 / X) * qh);   /* femurs forward-down by fem in the WORLD (relative flexion = fem + the pelvis pitch), shins vertical (knee flexion = fem), the fused taper counters the pitch so its tip stays the base under the hips */ }
          else { set('PELVIS', ((HA.pike_pelvis_rad || 0.55) / X) * qh * amp); set('SPINE', (0.06 / X) * qh); set('HEAD', -(0.30 / X) * qh); set('THIGH_L', -((HA.pike_thigh_flex_rad || 0.95) / X) * qh); set('THIGH_R', -((HA.pike_thigh_flex_rad || 0.95) / X) * qh); set('SHIN_L', (0.15 / X) * qh); set('SHIN_R', (0.15 / X) * qh); set('TAIL1', (0.15 / X) * qh);   /* airborne: no floor to load against — a pike (legs fold forward under the pitched body), never a fake planted stance */ }
          set('CLAV_L', 0.06 * qh); set('CLAV_R', 0.06 * qh); set('UPPERARM_L', -(hang / X) * qh, 0, 0.16); set('UPPERARM_R', -(hang / X) * qh, 0, -0.16); set('FOREARM_L', -0.04 * qh); set('FOREARM_R', -0.04 * qh); hand('L', 'FIST', 1); hand('R', 'FIST', 1);   /* the arms hang long under the inclined torso (forward relative to it = vertical in the world) */
          /* RELEASE = the hip drive: pelvis through to a tall lockout (a touch behind vertical), hips and knees extend, glutes finish, the straight arms swing forward to CHEST height (never overhead), the MAHGIC leaves from the hip drive; the follow-through floats the arms before they fall. */
          set('PELVIS', -(0.10 / X) * ra * amp); set('SPINE', -(0.08 / X) * ra * amp); set('CHEST', -(0.06 / X) * ra); set('HEAD', (0.16 / X) * ra); set('THIGH_L', (0.10 / X) * ra); set('THIGH_R', (0.10 / X) * ra); set('TAIL1', (0.06 / X) * ra); set('CLAV_L', -0.10 * ra, 0, 0.12 * ra); set('CLAV_R', -0.10 * ra, 0, -0.12 * ra); set('UPPERARM_L', (relArm / X) * ra * amp, 0, 0.22 * ra); set('UPPERARM_R', (relArm / X) * ra * amp, 0, -0.22 * ra); set('FOREARM_L', -0.06 * ra); set('FOREARM_R', -0.06 * ra); hand('L', 'OPEN', rel); hand('R', 'OPEN', rel); }
        else if (pat === 'BAR_PRESS') { var qb = startup ? qa : qa * (1 - whip);   /* M2 D — TRUE BARBELL OVERHEAD PRESS → THROW. LOAD: shaft at the upper chest, hands just outside shoulder width, elbows directly below it, neutral wrists and a stacked torso. RELEASE: shoulder flexion plus elbow extension presses the same held bar to an overhead lockout; only after host commit does EquipmentManager detach that exact equipment id as the swept projectile. */
          set('THIGH_L', (0.10 / X) * qb); set('THIGH_R', (0.10 / X) * qb); set('SHIN_L', (0.10 / X) * qb); set('SHIN_R', (0.10 / X) * qb); set('PELVIS', -(0.03 / X) * qb); set('SPINE', -(0.04 / X) * qb); set('CHEST', -(0.10 / X) * qb); set('HEAD', (0.08 / X) * qb); set('CLAV_L', -(0.08 / X) * qb, 0, (0.12 / X) * qb); set('CLAV_R', -(0.08 / X) * qb, 0, -(0.12 / X) * qb); set('UPPERARM_L', -(0.82 / X) * qb, 0, (0.46 / X) * qb); set('UPPERARM_R', -(0.82 / X) * qb, 0, -(0.46 / X) * qb); set('FOREARM_L', -(1.36 / X) * qb); set('FOREARM_R', -(1.36 / X) * qb); hand('L', 'FIST', 1); hand('R', 'FIST', 1);
          set('THIGH_L', -(0.08 / X) * ra); set('THIGH_R', -(0.08 / X) * ra); set('SHIN_L', -(0.08 / X) * ra); set('SHIN_R', -(0.08 / X) * ra); set('PELVIS', (0.02 / X) * ra); set('SPINE', -(0.03 / X) * ra); set('CHEST', -(0.05 / X) * ra); set('HEAD', (0.04 / X) * ra); set('CLAV_L', -(0.14 / X) * ra, 0, (0.44 / X) * ra); set('CLAV_R', -(0.14 / X) * ra, 0, -(0.44 / X) * ra); set('UPPERARM_L', -(2.30 / X) * ra, 0, (0.18 / X) * ra); set('UPPERARM_R', -(2.30 / X) * ra, 0, -(0.18 / X) * ra); set('FOREARM_L', -(0.08 / X) * ra); set('FOREARM_R', -(0.08 / X) * ra); set('HAND_L', -(0.04 / X) * ra); set('HAND_R', -(0.04 / X) * ra); hand('L', 'FIST', 1); hand('R', 'FIST', 1); }
        else if (pat === 'OVERHEAD') { var qo = startup ? qa : qa * (1 - whip);   /* SPECIAL #1 OVERHEAD EXTENSION RELEASE (owner 2026-09-18 §10): LOAD = both arms genuinely OVERHEAD (humerus ~135° through the attack's named shoulder allowance), elbows bent so the hands sit behind / above the head, scapulae elevated, chest open (the overhead medicine-ball throw); RELEASE = the triceps extend and the arms snap forward-down to ~80°, the wrists whip and the projectile leaves the fingertips; a small trunk drive, never a lunge. Measured (shoulder_sign_probe 2026-09-19, raw bone values): UPPERARM x − = flexion; through set() (which mirrors z for side bones) L z − / R z + = INWARD; CLAV L z + / R z − = ELEVATION; CLAV x − = protraction. */
          set('UPPERARM_L', -(2.35 / X) * qo, 0, -(0.35 / X) * qo); set('UPPERARM_R', -(2.35 / X) * qo, 0, (0.35 / X) * qo); set('FOREARM_L', -(1.15 / X) * qo); set('FOREARM_R', -(1.15 / X) * qo); set('CHEST', -(0.22 / X) * qo); set('SPINE', -(0.10 / X) * qo); set('HEAD', (0.14 / X) * qo); set('CLAV_L', -(0.12 / X) * qo, 0, (0.40 / X) * qo); set('CLAV_R', -(0.12 / X) * qo, 0, -(0.40 / X) * qo); hand('L', 'FIST', 1); hand('R', 'FIST', 1);
          set('UPPERARM_L', -(1.35 / X) * ra * amp, 0, -(0.30 / X) * ra); set('UPPERARM_R', -(1.35 / X) * ra * amp, 0, (0.30 / X) * ra); set('SPINE', (0.12 / X) * ra * amp);   /* release measured: hands 0.37 m ahead of the chest, 0.20 m above the chest bone, elbows straight, wrists whipped */ set('PELVIS', (0.05 / X) * ra); set('CHEST', (0.06 / X) * ra); set('HEAD', -(0.06 / X) * ra); set('HAND_L', -(0.45 / X) * ra); set('HAND_R', -(0.45 / X) * ra); set('THIGH_L', (0.06 / X) * ra); set('THIGH_R', (0.06 / X) * ra); hand('L', 'OPEN', rel); hand('R', 'OPEN', rel); }
        else if (pat === 'KATANA') { var qk = startup ? qa : qa * (1 - whip);   /* B8 §12 A — KATANA EXTENSION X-WAVE: the cross-cable / katana-extension triceps mechanic. LOAD = shoulders abducted ~45° and flexed a little, elbows folded so the fists CROSS in front of the upper chest (right over left — the drawn blade), chest tall. RELEASE = pure ELBOW EXTENSION driving each hand out and down its own diagonal (the arms open into the X), shoulders held, wrists snap at lockout; no punch, no trunk lunge. UPPERARM z = abduction, FOREARM x = flexion. */
          set('UPPERARM_L', -(0.50 / X) * qk, (0.10 / X) * qk, (0.85 / X) * qk); set('UPPERARM_R', -(0.50 / X) * qk, -(0.10 / X) * qk, -(0.85 / X) * qk); set('FOREARM_L', -(2.25 / X) * qk); set('FOREARM_R', -(2.35 / X) * qk); set('CHEST', -(0.06 / X) * qk); set('SPINE', -(0.03 / X) * qk); set('CLAV_L', 0.02 * qk, outSign('L') * 0.16 * qk, 0.04 * qk); set('CLAV_R', 0.02 * qk, outSign('R') * 0.16 * qk, -0.04 * qk); set('HEAD', (0.05 / X) * qk); hand('L', 'FIST', 1); hand('R', 'FIST', 1);
          set('UPPERARM_L', -(0.38 / X) * ra * amp, (0.06 / X) * ra, (0.85 / X) * ra); set('UPPERARM_R', -(0.38 / X) * ra * amp, -(0.06 / X) * ra, -(0.85 / X) * ra); set('FOREARM_L', -(0.15 / X) * ra); set('FOREARM_R', -(0.15 / X) * ra);   /* the upper arm holds its abduction; only the ELBOW opens (2.3 → 0.15 rad) — the hands travel from the sternum out and down the diagonals */   /* lockout: the forearms extend along the diagonals (~0.12 rad residual bend), shoulders stay at their abduction */ set('HAND_L', -(0.35 / X) * ra); set('HAND_R', -(0.35 / X) * ra); set('CHEST', (0.05 / X) * ra); set('SPINE', (0.04 / X) * ra); set('PELVIS', (0.03 / X) * ra); set('THIGH_L', (0.04 / X) * ra); set('THIGH_R', (0.04 / X) * ra); hand('L', 'FIST', 1); hand('R', 'FIST', 1); }
        else if (pat === 'CHESTFLY') { var qc = startup ? qa : qa * (1 - whip);   /* SPECIAL #2 CHEST FLY CLAP RELEASE (§11): LOAD = arms out wide at shoulder height with a slight FIXED elbow bend, chest open; RELEASE = real horizontal ADDUCTION — the arms sweep forward to meet in front of the chest and the CLAP is the release; the elbow bend never changes (a fly, not a press). UPPERARM z = abduction. */
          set('UPPERARM_L', -(0.15 / X) * qc, 0, (1.10 / X) * qc); set('UPPERARM_R', -(0.15 / X) * qc, 0, -(1.10 / X) * qc); set('FOREARM_L', -(0.28 / X) * qc); set('FOREARM_R', -(0.28 / X) * qc); set('CHEST', -(0.08 / X) * qc);   /* measured (shoulder_sign_probe 2026-09-19): through set() L z + (R −) = OUTWARD; x −0.15 / z 1.10 puts the hands 1.28 m apart at SHOULDER height (0.20 m above the chest bone, 0.09 m ahead) with the fixed 16° elbow bend — a horizontal fly, not a Y (the earlier −0.60 / 1.30 lifted the hands 0.40 m above the chest) */ set('CLAV_L', 0.04 * qc, outSign('L') * 0.30 * qc, 0.06 * qc); set('CLAV_R', 0.04 * qc, outSign('R') * 0.30 * qc, -0.06 * qc); set('HEAD', (0.02 / X) * qc); hand('L', 'OPEN', 1); hand('R', 'OPEN', 1);
          set('UPPERARM_L', -(1.35 / X) * ra * amp, 0, -(0.62 / X) * ra); set('UPPERARM_R', -(1.35 / X) * ra * amp, 0, (0.62 / X) * ra); set('FOREARM_L', -(0.28 / X) * ra); set('FOREARM_R', -(0.28 / X) * ra);   /* the ADDUCTION: z swings inward (− for L through set()) until the palms meet ~0.12 m apart (hand bones), 0.35 m ahead of the chest at shoulder height = the CLAP (measured 2026-09-19: 0.75 rad put the hand bones 0.02 m apart = palms through each other; the HEAVY profile's amp deepens the flexion); the elbow bend is unchanged */ set('CLAV_L', -0.06 * ra, outSign('L') * -0.40 * ra, 0); set('CLAV_R', -0.06 * ra, outSign('R') * -0.40 * ra, 0); set('CHEST', (0.05 / X) * ra); set('SPINE', (0.05 / X) * ra); hand('L', 'OPEN', rel); hand('R', 'OPEN', rel); }
        else if (pat === 'CURL') { /* PHYSICAL MAHGIC EIGHT-STRIKE ALTERNATING CURL BARRAGE (§12): LOAD = a braced stance, fists low at the sides, elbows pinned, a short forward lean (the approach); STRIKES = eight alternating biceps curls timed to the host's strike schedule (presentation_defaults.barrage) — the striking forearm flexes to the shoulder while the other returns, the shoulder follows a little; the tail drops both fists. No generic punches: the elbow stays pinned, the hand rises in the curl plane. */
          var apF = DFH.approach_fraction === undefined ? 0.157 : DFH.approach_fraction, spF = DFH.strike_fraction === undefined ? 0.765 : DFH.strike_fraction; var hs = at.strike && Number.isFinite(at.strike.done) ? at.strike.done : null; if (!st.curl || st.curl.id !== at.attack_id + ':' + (at.state || '')) st.curl = { id: at.attack_id + ':' + (at.state || ''), done: 0, t: 9 }; if (hs !== null && hs !== st.curl.done) { st.curl.done = hs; st.curl.t = 0; } st.curl.t += dt; var u8 = startup ? 0 : clamp((p - apF) / spF, 0, 1) * 8; var kc, bumpC; if (hs !== null) { kc = Math.max(0, Math.min(7, hs - 1)); var tc = st.curl.t; bumpC = (startup || hs < 1) ? 0 : (tc < 0.05 ? 1 : Math.max(0, 1 - (tc - 0.05) / 0.12)); }   /* HOST-driven: each landed strike snaps the striking arm to the top of its curl and it settles over 0.12 s — the next arm winds up below */ else { kc = Math.min(7, Math.floor(u8)); bumpC = (startup || u8 >= 8) ? 0 : Math.sin(Math.PI * (u8 - Math.floor(u8))); }   /* fallback: the authored schedule fractions */ var windK = (startup || hs === null || hs >= 8) ? 0 : 0.45; var leftK = (kc % 2 === 0) ? bumpC : windK, rightK = (kc % 2 === 1) ? bumpC : windK; var tailF = startup ? 0 : clamp((p - apF - spF) / Math.max(0.05, 1 - apF - spF), 0, 1); var qs = startup ? qa : X * (1 - tailF);   /* the stance holds through the strikes and releases in the tail */
          set('SPINE', (0.14 / X) * qs); set('PELVIS', (0.06 / X) * qs); set('HEAD', -(0.08 / X) * qs); set('THIGH_L', -(0.10 / X) * qs); set('THIGH_R', -(0.10 / X) * qs); set('SHIN_L', (0.16 / X) * qs); set('SHIN_R', (0.16 / X) * qs); set('TAIL1', (0.06 / X) * qs);
          set('UPPERARM_L', -(0.30 / X) * qs - 0.25 * leftK, 0, 0.12); set('UPPERARM_R', -(0.30 / X) * qs - 0.25 * rightK, 0, -0.12); set('FOREARM_L', -(0.20 / X) * qs - 1.15 * leftK); set('FOREARM_R', -(0.20 / X) * qs - 1.15 * rightK); set('CHEST', 0, 0.10 * (rightK - leftK), 0); hand('L', 'FIST', 1); hand('R', 'FIST', 1); st.brace = Math.max(st.brace, bumpC); st.curlStrike = { k: kc, f: +bumpC.toFixed(2), arm: kc % 2 === 0 ? 'L' : 'R', host: hs }; }
        else if (pat === 'SQUAT') { set('PELVIS', 0.14 * qa); set('SPINE', 0.11 * qa); set('THIGH_L', -0.52 * qa); set('THIGH_R', -0.52 * qa); set('SHIN_L', 0.85 * qa); set('SHIN_R', 0.85 * qa);   /* phase D sign audit: THIGH + is hip EXTENSION on this rig — a squat FLEXES the hips (negative) while the knees bend (SHIN +); the planted-feet root offset drops the hips */ set('TAIL1', 0.28 * qa); set('TAIL2', 0.32 * qa); set('UPPERARM_L', -0.46 * qa, 0, 0.20); set('UPPERARM_R', -0.46 * qa, 0, -0.20); set('FOREARM_L', -1.05 * qa); set('FOREARM_R', -1.05 * qa); set('PELVIS', -0.09 * ra); set('THIGH_L', 0.18 * ra); set('THIGH_R', 0.18 * ra); set('TAIL1', -0.14 * ra); set('UPPERARM_L', -1.30 * ra, 0, 0.28); set('UPPERARM_R', -1.30 * ra, 0, -0.28); hand('L', 'OPEN', rel); hand('R', 'OPEN', rel); }
        /* -- ROTATION = CORE / WOODCHOP. LOAD: a big wind-up away from the strike side - pelvis, spine, chest and head all coil, both arms
              carried high on the far side, weight loaded on the far leg. RELEASE: the pelvis leads, the spine and chest whip through past the
              strike line, both arms chop across and down at chest height; FOLLOW-THROUGH over-rotates before settling. */
        else if (pat === 'ROTATION') { set('PELVIS', 0.06 * qa, -0.34 * qa); set('SPINE', 0.08 * qa, -0.36 * qa * amp); set('CHEST', 0.04 * qa, -0.52 * qa * amp); set('HEAD', 0, -0.20 * qa); set('THIGH_L', 0.12 * qa); set('THIGH_R', 0.12 * qa); set('SHIN_L', 0.16 * qa); set('SHIN_R', 0.16 * qa); set('TAIL1', 0, -0.14 * qa); set('UPPERARM_R', -1.30 * qa, 0.30 * qa, -0.80 * qa); set('FOREARM_R', -0.95 * qa);   /* rig v5: the humerus is 18 % longer, the wind-up crosses less to keep the elbow off the sternum */ hand('R', 'FIST', 1); set('UPPERARM_L', -1.15 * qa, 0, 0.20 * qa); set('FOREARM_L', -1.05 * qa); hand('L', 'FIST', 1);   /* the far arm carries high and forward on its own side (elbow stays clear of the ribs) */
          set('PELVIS', -0.06 * ra, 0.52 * ra * amp); set('SPINE', -0.04 * ra, 0.50 * ra * amp); set('CHEST', 0, 0.78 * ra * amp); set('HEAD', 0, 0.36 * ra); set('TAIL1', 0, 0.26 * ra); set('THIGH_R', 0, 0.16 * ra); set('THIGH_L', 0, 0.10 * ra); set('UPPERARM_R', -0.95 * ra, 0.55 * ra, -1.20 * ra * amp); set('FOREARM_R', -0.10 * ra); set('UPPERARM_L', -0.70 * ra, -0.30 * ra, 0.90 * ra); set('FOREARM_L', -0.20 * ra); }
        else { set('UPPERARM_L', -1.25 * qa, 0, 0.34); set('UPPERARM_R', -1.25 * qa, 0, -0.34); set('FOREARM_L', -0.62 * qa); set('FOREARM_R', -0.62 * qa); set('HAND_L', 0, 0, 0.35 * qa); set('HAND_R', 0, 0, -0.35 * qa); hand('L', 'CAST', q); hand('R', 'CAST', q); set('SPINE', -0.05 * qa); set('HEAD', -0.10 * qa); set('UPPERARM_L', -1.30 * ra, 0, 0.16); set('UPPERARM_R', -1.30 * ra, 0, -0.16); set('FOREARM_L', -0.10 * ra); set('FOREARM_R', -0.10 * ra); hand('L', 'DIRECT', rel); hand('R', 'DIRECT', rel); set('SPINE', 0.13 * ra); set('CHEST', 0.06 * ra); }
        beamF *= 1 + ((DB.fused_brace || 1.5) - 1) * Math.max(q * 0.4, rel); st.attackArc = { pat: pat, startup: startup, load: startup ? q : 0, release: rel, whip: whip, cadence: ordinary ? style + ':' + at.category : 'SPECIAL_AUTHORED', envelope: { load_end: loadEnd, whip_end: whipEnd, finish_end: followStart, filter_rate: cv[3] } }; }
      /* ───── 6 GUARDS — protective forearms in front of the chest, elbows outside the ribs ───── */
      if (v.guard === 'PHYSICAL') { set('SPINE', 0.11); set('CLAV_L', -0.12, 0, 0.18); set('CLAV_R', -0.12, 0, -0.18); set('UPPERARM_L', -0.86, 0, 0.46); set('UPPERARM_R', -0.86, 0, -0.46); set('FOREARM_L', -1.28, 0, -0.42); set('FOREARM_R', -1.28, 0, 0.42); hand('L', 'GUARD', 1); hand('R', 'GUARD', 1); set('HEAD', 0.11); rate = 12 * CLS.speed; combatFace = combatFace || 'FOCUS'; }
      else if (v.guard === 'MAGICAL') { set('UPPERARM_L', -1.20, 0, 0.88); set('UPPERARM_R', -1.20, 0, -0.88); set('FOREARM_L', -0.50); set('FOREARM_R', -0.50); set('HAND_L', 0, 0, 0.45); set('HAND_R', 0, 0, -0.45); hand('L', 'CAST', 1); hand('R', 'CAST', 1); set('SPINE', -0.04); rate = 12 * CLS.speed; combatFace = combatFace || 'FOCUS'; }
      /* ───── 7 EMOTES: authored clips, additive; paired roles on the shared host timeline (the limit pass below applies to them too) ───── */
      var emFace = null; var em = st.emote; var emId = v.emote && typeof v.emote === 'object' ? v.emote.emote_id : v.emote; var emKey = emId ? emId + '@' + (v.emote_at === undefined || v.emote_at === null ? '' : v.emote_at) : null;
      if (emKey && emKey !== em.key) { em.key = emKey; em.id = emId; em.t = 0; em.seg = 'entry'; em.loop = 0; em.done = false; em.role = v.paired && v.paired.role ? v.paired.role : 'A'; em.t0 = v.paired && v.paired.t0 !== undefined && v.paired.t0 !== null ? v.paired.t0 : null; em.synced = false; }
      if (!emId && em.key) { em.key = null; if (em.id && !em.done && em.seg !== 'exit') { em.seg = 'exit'; em.t = 0; } }
      if (!em.id) st.emoteIK = null;
      if (em.id) { var def = AD && AD.EMOTES ? AD.EMOTES[em.id] : null; var fv = def && def.form_variants && def.form_variants[split ? 'SPLIT' : 'FUSED']; var segs = def ? (def.roles ? (def.roles[em.role] || def.roles.A) : def.segments) : null; if (fv && segs) segs = { entry: fv.entry || segs.entry, loop: fv.loop || segs.loop, exit: fv.exit || segs.exit };
        if (segs && AD.sampleClip) { var clip = segs[em.seg]; if (!clip && em.seg === 'loop') { em.seg = 'exit'; em.t = 0; clip = segs.exit; } if (!clip && em.seg === 'exit') { em.done = true; em.id = null; }
          if (clip) { if (em.t0 !== null && !em.synced && v.host_t !== undefined && v.host_t !== null) { em.t = Math.max(0, v.host_t - em.t0); em.synced = true; }
            em.t += dt * CLS.speed; var u = clamp(em.t / Math.max(0.05, clip.duration_s), 0, 1); var s = AD.sampleClip(clip, u); setBones(s.bones, 1); st.emoteIK = s.ik || null; emFace = s.face || def.face || null; if (s.beam !== undefined && s.beam !== null) { beamF *= s.beam; beamL *= s.beam; beamR *= s.beam; if (s.beam > 1.1 && split) st.brace = Math.max(st.brace, 0.5); }
            if (u >= 1) { if (em.seg === 'entry') { if (segs.loop && (def.loops === 'until_cancel' || (def.loops || 0) > 0)) { em.seg = 'loop'; em.t = 0; em.loop = 0; } else { em.seg = 'exit'; em.t = 0; } } else if (em.seg === 'loop') { em.loop++; if (def.loops === 'until_cancel' || em.loop < (def.loops || 1)) em.t = 0; else { em.seg = 'exit'; em.t = 0; } } else { em.done = true; em.id = null; } } rate = Math.max(rate, 14 * CLS.speed); }
        } else { em.t += dt; var eT = em.t; var life = em.id === 'WAVE' ? 2.2 : (em.id === 'NOD' ? 1.2 : 1.6); var w = eT < 0.25 ? ease(eT / 0.25) : (eT > life - 0.35 ? 1 - ease((eT - (life - 0.35)) / 0.35) : 1); if (eT >= life) { em.done = true; em.id = null; w = 0; }
          if (em.id === 'WAVE') { set('CLAV_R', -0.12, 0, -0.28, w); set('UPPERARM_R', -0.35, 0, -1.55, w); set('FOREARM_R', -1.05, 0, -0.25, w); set('HAND_R', 0, 0, -0.45 * Math.sin(eQT * 11), w); hand('R', 'OPEN', w); set('HEAD', 0, -0.18, 0.07, w); set('CHEST', 0, -0.10, -0.04, w); emFace = 'FRIENDLY'; } else if (em.id === 'NOD') { set('HEAD', 0.28 * Math.max(0, Math.sin(eQT * 6.5)), 0, 0, w); set('NECK', 0.07 * Math.max(0, Math.sin(eQT * 6.5)), 0, 0, w); emFace = 'FRIENDLY'; } else if (em.id) { set('SPINE', 0.12, 0, 0, w); set('UPPERARM_L', -0.72, 0, 0.42, w); set('UPPERARM_R', -0.72, 0, -0.42, w); set('FOREARM_L', -1.25, 0, 0, w); set('FOREARM_R', -1.25, 0, 0, w); hand('L', 'FIST', w); hand('R', 'FIST', w); emFace = 'PLAYFUL'; } rate = Math.max(rate, 14); } }
      /* ───── 8 HIT REACTION ───── */
      var reaction = null; if (hitAgo < 0.4 && !v.ko) { var hq = 1 - ease(hitAgo / 0.4); var hd = v.hitDir || 0; set('SPINE', -0.26 * hq * Math.cos(hd), 0, 0.18 * hq * Math.sin(hd)); set('CHEST', -0.11 * hq); set('HEAD', -0.22 * hq, 0.14 * hq * Math.sin(hd)); set('UPPERARM_L', -0.44 * hq, 0, 0.44 * hq); set('UPPERARM_R', -0.44 * hq, 0, -0.44 * hq); set('FOREARM_L', -0.80 * hq); set('FOREARM_R', -0.80 * hq); rate = 20; if (hitAgo < st.lastHit) reaction = 'WINCE'; } st.lastHit = hitAgo;
      /* ───── 9 TRANSFORMATION — GATHER (compact rounded silhouette) → RELEASE (controlled star, limbs separating) → SETTLE, inside the host's own phase durations ───── */
      if (isTf) { if (st.transformState !== ms) { st.transformState = ms; st.transformT = 0; } st.transformT += dt; var PH = { UNFUSE_ENTER: 0.05, UNFUSE_CURL: 0.25, UNFUSE_CHARGE: 0.6, UNFUSE_BURST: 0.12, UNFUSE_STAR: 0.18, UNFUSE_RECOVER: 0.3, REFUSE_ENTER: 0.05, REFUSE_KNEES_UP: 0.2, REFUSE_ALIGN: 0.15, REFUSE_SNAP: 0.08, REFUSE_RECOVER: 0.25 }; var pp = Math.min(1, st.transformT / (PH[ms] || 0.3)); var e2 = ease(pp); combatFace = 'EXERT';
        /* OWNER DECISION 4 (motion master B): GATHER = both knees high toward the navel, pelvic tuck, restrained rounding, forearms
           crossed in an X in front of the chest with one just ahead of the other, hands clear of the face. The old tuck folded the
           lower body BACKWARD (positive THIGH/TAIL x on a hanging bone swings it behind the body); knees come FORWARD on negative x.
           The transformation carries its own joint allowance (rig_profile pose_overrides.transformation) — the 75° elbow cap does
           not govern this pose. Authored in canonical space (set mirrors per side). */
        var gather = function (c) {
          /* a pelvic TUCK is a posterior tilt (the pelvis rolls under toward the knees: negative x on the upward-pointing pelvis bone);
             the trunk stays close to upright and rounds only through the spine, so the knees read as gathered at the navel rather
             than the body diving forward over trailing legs */
          set('PELVIS', -0.16 * c); set('SPINE', 0.32 * c); set('CHEST', 0.12 * c); set('NECK', 0.06 * c); set('HEAD', -0.06 * c);
          set('TAIL1', -0.12 * c); set('TAIL2', -0.08 * c); set('TAIL3', -0.04 * c);                                   /* the fused lower body is hidden during the gather (packed legs show); the tail chain only anchors the fused beam, so it stays under the body */
          set('THIGH_L', -1.85 * c, 0, outSign('L') * 0.08 * c); set('THIGH_R', -1.85 * c, 0, outSign('R') * 0.08 * c);   /* knees to the belly button */
          set('SHIN_L', 2.05 * c); set('SHIN_R', 2.05 * c);                                                            /* shins folded under the thighs */
          /* the X: shoulders flexed forward and adducted across the chest, elbows deep; the left forearm sits just ahead of the right */
          /* fitted by forward kinematics on the delivered skeleton (16_TESTS/gameplay_rig_pose_fk.test.mjs): shoulders flexed 52°, internally
             rotated 95°, elbows held 19 cm out beside the lower pectorals (clear of the 16 cm torso half-width) and 69° bent, so the forearms
             lie ON the chest (pectoral front is 0.175 m ahead of the CHEST bone) and the hands meet at the sternum. On this body the X is
             limited by humeral rotation, not by the elbow. Canonical space: y and z are mirrored per side by set(). */
          set('CLAV_L', -0.06 * c, -outSign('L') * 0.14 * c, 0.05 * c); set('CLAV_R', -0.06 * c, -outSign('R') * 0.14 * c, -0.05 * c);
          set('UPPERARM_L', -0.84 * c, -outSign('L') * 1.62 * c, outSign('L') * 0.03 * c); set('UPPERARM_R', -0.74 * c, -outSign('R') * 1.62 * c, outSign('R') * 0.03 * c);   /* refit for the v4.1 shoulder joint (6 mm further out): a touch less flexion, elbows a touch wider, hands still meet at the sternum */
          set('FOREARM_L', -1.17 * c, 0, 0); set('FOREARM_R', -1.09 * c, 0, 0);
          set('HAND_L', 0.12 * c); set('HAND_R', 0.12 * c); hand('L', 'RELAXED', c); hand('R', 'RELAXED', c);
          st.tfGather = Math.max(st.tfGather || 0, c);
        };
        /* RELEASE = a quick readable star: arms open wide on the shoulder's safe path, elbows nearly straight, legs spread. Follow-through
           comes from the phase easing (the burst overshoots 6 % then relaxes), never from a held T-pose. */
        var star = function (c) {
          set('SPINE', -0.14 * c); set('CHEST', -0.12 * c); set('NECK', -0.06 * c); set('HEAD', -0.10 * c); set('PELVIS', -0.08 * c);
          set('CLAV_L', -0.16 * c, 0, 0.34 * c); set('CLAV_R', -0.16 * c, 0, -0.34 * c);
          set('UPPERARM_L', -0.22 * c, 0, 1.42 * c); set('UPPERARM_R', -0.22 * c, 0, -1.42 * c); set('FOREARM_L', -0.16 * c); set('FOREARM_R', -0.16 * c); hand('L', 'OPEN', c); hand('R', 'OPEN', c);
          set('THIGH_L', -0.18 * c, 0, outSign('L') * 0.42 * c); set('THIGH_R', -0.18 * c, 0, outSign('R') * 0.42 * c); set('SHIN_L', 0.14 * c); set('SHIN_R', 0.14 * c);
        };
        var over = function (p) { return p < 0.7 ? p / 0.7 * 1.06 : 1.06 - 0.06 * ((p - 0.7) / 0.3); };   /* 6 % overshoot then relax */
        st.tfGather = 0;
        /* SEPARATE: the packed leg pair (same silhouette as the fused body) is shown from the first beat, so the knees can gather as one
           mass without tearing a fused bridge; the split happens at the burst with the star. */
        if (ms === 'UNFUSE_ENTER') { gather(0.35 * e2); st.central = 1 - 0.25 * e2; beamF = (DB.fused_idle || 1) * (1 - 0.15 * e2); st.packed = 1 - 0.45 * e2; st.showLegs = true; }   /* the pair starts to part as the knees gather: two legs hug, never one folded slab */
        else if (ms === 'UNFUSE_CURL') { gather(0.35 + 0.65 * e2); st.seam = 0.45 * e2; st.aura = 0.12 * e2; st.central = 0.75 - 0.45 * e2; beamF = (DB.fused_idle || 1) * (0.85 - 0.45 * e2); st.packed = 0.55 - 0.30 * e2; st.showLegs = true; }
        else if (ms === 'UNFUSE_CHARGE') { var tr = 0.015 * Math.sin(st.t * 34) * e2; gather(1); set('SPINE', tr); set('CHEST', -tr); st.seam = 0.45 + 0.55 * e2; st.aura = 0.12 + 0.55 * e2 + 0.06 * Math.sin(st.t * 21) * e2; st.central = 0.3; beamF = 0.4; st.packed = 0.25; st.showLegs = true; }
        else if (ms === 'UNFUSE_BURST') { var ov = over(pp); gather(1 - e2); star(ov); st.seam = 1; st.aura = 0.7 + 0.6 * Math.sin(Math.PI * Math.min(1, pp * 1.4)); st.central = 0; st.packed = 0.25 * (1 - e2); st.showLegs = true; beamF = 0.3 * (1 - e2); beamL = 0.35 * e2; beamR = 0.35 * e2; rate = 26; }
        else if (ms === 'UNFUSE_STAR') { star(1 - 0.35 * e2); st.seam = 1 - e2; st.aura = 0.7 * (1 - e2) + 0.12; st.central = 0; st.tips = 0.5 * e2; st.packed = 0; st.showLegs = true; beamF = 0; beamL = (DB.split_step_peak ? DB.split_step_peak[1] : 0.65) * e2; beamR = beamL; rate = 22; }
        else if (ms === 'UNFUSE_RECOVER') { star(0.65 * (1 - e2)); st.aura = 0.12 * (1 - e2); st.central = 0; st.tips = 0.5; st.packed = 0; st.showLegs = true; beamF = 0; beamL = (DB.split_baseline || 0.3); beamR = beamL; }   /* settles straight into the separated hover */
        /* FUSE: the same body in reverse — legs draw together (packed → 1), knees lift into the gather, then the fused taper takes over */
        else if (ms === 'REFUSE_ENTER') { gather(0.25 * e2); st.tips = 0.5; st.central = 0; st.packed = 0.15 * e2; st.showLegs = true; beamL = 0.3; beamR = 0.3; }
        else if (ms === 'REFUSE_KNEES_UP') { var k2 = 0.25 + 0.75 * e2; gather(k2); st.tips = 0.5 - 0.3 * k2; st.central = 0; st.packed = 0.15 + 0.55 * e2; st.showLegs = true; beamL = 0.3 * (1 - 0.5 * k2); beamR = beamL; rate = 20; }
        else if (ms === 'REFUSE_ALIGN') { gather(1); st.seam = 0.5 * e2; st.aura = 0.3 * e2; st.tips = 0.2; st.central = 0; st.packed = 0.7 + 0.3 * e2; st.showLegs = true; beamL = 0.15; beamR = 0.15; }
        else if (ms === 'REFUSE_SNAP') { gather(1 - e2); st.seam = 0.35 * (1 - e2); st.aura = 0.3 * (1 - e2); st.tips = 0; st.central = e2; st.packed = 1; st.showLegs = pp < 0.85; beamF = (DB.fused_idle || 1) * e2; beamL = 0.15 * (1 - e2); beamR = beamL; rate = 26; }   /* PASS 4B review (G35): the packed legs straighten fully through the SNAP and the swap to the fused taper happens at 85 % of the beat, when both shapes agree — the old swap at 60 % (knees still 61 % gathered against a nearly straight tail) measured as an 81/255 pixel pop */
        else if (ms === 'REFUSE_RECOVER') { gather(0.05 * (1 - e2)); set('CHEST', 0.06 * e2); set('CLAV_L', -0.05 * e2, 0, 0.04 * e2); set('CLAV_R', -0.05 * e2, 0, -0.04 * e2); st.central = 1; st.tips = 0; st.packed = 1; st.showLegs = false; beamF = DB.fused_idle || 1; }   /* reopens into the poised fused stance */ }
      else { st.transformState = null; st.tfGather = 0; st.packed = split ? 0 : 1; st.showLegs = split; }
      /* ───── 10 KO / outcome ───── */
      if (v.ko) { set('UPPERARM_L', 0, 0, 1.05); set('UPPERARM_R', 0, 0, -1.05); set('HEAD', 0.28); hand('L', 'RELAXED', 1); hand('R', 'RELAXED', 1); rate = 6; beamF = 0.15; beamL = 0.05; beamR = 0.05; combatFace = 'DISAPPOINT'; }
      if (v.outcome === 'WIN') combatFace = 'CELEBRATE'; else if (v.outcome === 'LOSS') combatFace = 'DISAPPOINT';
      st.beams.fused = beamF; st.beams.L = beamL; st.beams.R = beamR;
      /* ───── face channels → bones ───── */
      var f = stepFace(dt, emFace, combatFace, reaction); var bm = DFC.brow_max_rad || 0.1, cm = DFC.cheek_max || 0.6, jm = DFC.jaw_max_rad || 0.12, sm = clamp(f.smile || 0, 0, DFC.smile_max || 0.7);
      set('BROW_L', -(f.brow_l || 0) * bm); set('BROW_R', -(f.brow_r || 0) * bm); set('CHEEK_L', -((f.cheek_l || 0) + 0.6 * sm) * cm * 0.12); set('CHEEK_R', -((f.cheek_r || 0) + 0.6 * sm) * cm * 0.12); set('JAW', ((f.jaw || 0) + 0.15 * sm) * jm);
      /* DEV / EVIDENCE: pose overrides are injected here so the girdle rhythm, twist carriers, trunk distribution, limits and correctives all apply to them */
      if (st.override) Object.keys(st.override).forEach(function (k) { if (E[k]) { var o = st.override[k]; E[k].x = o[0] || 0; E[k].y = o[1] || 0; E[k].z = o[2] || 0; } });
      /* ───── 10a UPPER-BODY MOTION LAYER (S10 hybrid contract): a retargeted donor pose — pelvis-relative rest deltas already converted to this
         rig's parent/rest frames by the retarget step — blended over the MAH base pose for the UPPER mask only. Lower-body / root channels are
         never accepted here (the mask filters them), so hover gait, split legs and form changes stay MAH-owned. ONE composition per frame:
         base locomotion/action → this layer (masked, weighted) → girdle rhythm (its clavicle share fades out where the layer supplies the
         clavicle; scapular support stays unless the layer supplies SCAP) → twist carriers → trunk distribution → inertialization → correctives. */
      if (DONOR.enabled) { var dc = null, dt2 = 0, dw = 0, did = null;
        if (at && !v.ko && (function () { var pd = patternOf(at.attack_id); return pd === 'CAST' || (at.category === 'SPECIAL_MAGIC' && pd !== 'OVERHEAD' && pd !== 'BAR_PRESS' && pd !== 'CHESTFLY' && pd !== 'KATANA'); })() && DONOR.clips.CAST) {   /* the donor stages only generic casts; authored exercise Specials own their arms */ var C = DONOR.clips.CAST, T = C.meta.fit || { t_in: 0.3, t_release: 2.6 }; var pr = at.progress === undefined ? 0.6 : clamp(at.progress, 0, 1); var startup = /CONCENTRIC|STARTUP/.test(at.state); dc = C; if (startup) { st.donorRec = 0; dt2 = T.t_in + (T.t_release - T.t_in) * pr; } else { st.donorRec = (st.donorRec || 0) + dt * (T.recovery_rate || 1.6); dt2 = Math.min(C.meta.duration_s, T.t_release + st.donorRec); }   /* STARTUP is progress-locked so the release lands on the host's commit; RECOVERY plays at a bounded real-time rate (no snap when the host recovery is short) and fades with the phase */ dw = startup ? Math.min(1, pr / 0.12) : (1 - ease(Math.max(0, (pr - 0.55) / 0.45))); did = 'CAST:' + at.attack_id; }
        else if (hitAgo < 0.6 && !v.ko && DONOR.clips.HIT) { var Ch = DONOR.clips.HIT; dc = Ch; dt2 = Math.min(Ch.meta.duration_s, hitAgo * (Ch.meta.fit && Ch.meta.fit.speed || 2.0)); dw = Math.min(1, hitAgo / 0.06) * (hitAgo < 0.35 ? 1 : 1 - ease((hitAgo - 0.35) / 0.25)); did = 'HIT'; }
        if (dc) { var pose = clipPose(dc, dt2, {});
          if (did === 'HIT') { /* S13 DIRECTION on the donor reaction (the clip is a blow from the front-left): a blow from the right mirrors the arms, a
             blow from the side adds trunk yaw/roll + head turn toward it, a blow from behind snaps the head/chest BACK instead of forward */ var hd = v.hitDir || 0; var sn = Math.sin(hd), cs = Math.cos(hd); if (hd > 0.35 && hd < 2.8) { var swap = function (a, b) { var pa = pose[a], pb = pose[b]; if (pa && pb) { pose[a] = [pb[0], -pb[1], -pb[2]]; pose[b] = [pa[0], -pa[1], -pa[2]]; } }; swap('UPPERARM_L', 'UPPERARM_R'); swap('FOREARM_L', 'FOREARM_R'); }
            if (pose.SPINE) { pose.SPINE[1] += 0.22 * sn; pose.SPINE[2] += 0.14 * sn; } if (pose.HEAD) pose.HEAD[1] += 0.16 * sn; if (cs < -0.5) { var back = -cs; ['SPINE', 'SPINE2', 'CHEST', 'HEAD', 'NECK'].forEach(function (k) { if (pose[k]) pose[k][0] *= 1 - 1.6 * back; }); } }
          if (!st.layer || st.layer.id !== did) st.layer = { pose: pose, weight: dw, id: did, donor: true }; else { st.layer.pose = pose; st.layer.weight = dw; } DONOR.active = did; }
        else if (st.layer && st.layer.donor) { st.layer = null; DONOR.active = null; } }
      var LAYER = st.layer; var layerW = LAYER ? Math.max(0, Math.min(1, LAYER.weight)) : 0; var layerHas = function (k) { return !!(LAYER && layerW > 0 && LAYER.pose[k]); };
      if (layerW > 0) Object.keys(LAYER.pose).forEach(function (k) { if (!E[k] || !UPPER_MASK[k]) return; var o = LAYER.pose[k]; blendEulerQ(E[k], o, layerW); });   /* quaternion-space blend: donor forearm/humerus twists sit near the XYZ gimbal, a component lerp would wander */
      /* ───── 10b SHOULDER GIRDLE: scapulohumeral rhythm. The deltoid cap is bound to the humerus (rig v4), so the arm carries
         its own muscle; here the girdle follows it: past ~30° of abduction the clavicle elevates about a third as fast as the
         arm rises, forward flexion protracts it, and the chest answers with a small roll toward the raised side. Rig-space
         values (post-mirror), written with setRaw so the side convention cannot double-apply. ───── */
      ['L', 'R'].forEach(function (side) {
        var ua = E['UPPERARM_' + side], cl = E['CLAV_' + side]; if (!ua || !cl) return;
        var abd = Math.max(0, ua.z * rigOutSign(side));                   /* radians of abduction, side-normalised */
        var flex = Math.max(0, -ua.x);                                     /* forward flexion (a hanging bone swings forward on -x) */
        var elev = 0.33 * Math.max(0, abd - 0.5) + 0.18 * Math.max(0, flex - 0.6);
        var prot = 0.22 * Math.max(0, flex - 0.5) + 0.06 * Math.max(0, abd - 0.9);
        var clavKeep = layerHas('CLAV_' + side) ? 1 - layerW : 1;   /* DOF ownership: the donor clavicle replaces the procedural clavicle rhythm, never stacks on it */
        setRaw('CLAV_' + side, 0, rigOutSign(side) * prot * clavKeep, rigOutSign(side) * elev * clavKeep);
        if (E.CHEST) setRaw('CHEST', 0, 0, -rigOutSign(side) * 0.08 * Math.min(1, Math.max(0, abd - 0.7)));   /* the ribcage leans a little under a high arm (capped: never a 17° side-bend) */
        /* SCAPULA (rig v5): nonlinear scapulohumeral rhythm — little below 30°, then upward rotation ~1:2.2 to 90°, ~1:1.6 above, plus
           posterior tilt past 100° and a touch of external axial rotation; the girdle visibly carries the high arm. */
        var elevT = Math.max(abd, flex); var upRot = 0.45 * Math.max(0, elevT - 0.5) + 0.25 * Math.max(0, elevT - 1.6); var postTilt = -0.18 * Math.max(0, elevT - 1.75); var axial = 0.10 * Math.max(0, elevT - 1.2);
        /* the authored UPPERARM value is the TOTAL humerothoracic elevation: the scapular share is taken FROM the humerus, not added on top
           (90° overhead = ~60° glenohumeral + ~30° scapulothoracic), so every tuned pose keeps its silhouette while the girdle visibly rotates */
        var scapKeep = layerHas('SCAP_' + side) ? 1 - layerW : 1; upRot *= scapKeep; postTilt *= scapKeep; axial *= scapKeep;   /* scapular support stays unless the donor actually supplies a scapula */
        if (LAYER && LAYER.donor && layerW > 0.5 && LAYER.pose['UPPERARM_' + side]) { /* DONOR-OWNED HUMERUS: elevation read from the rotation; the scapula takes its share by pre-rotation (SCAP · SCAP⁻¹·UA), so the arm's world line is untouched and nothing is applied twice */
          eQT.set(ua.x, ua.y, ua.z, 'XYZ'); qA.setFromEuler(eQT); var dir = { x: 0, y: -1, z: 0 }; var dv = rotXYZ(dir, ua); var elevQ = Math.acos(Math.max(-1, Math.min(1, -dv.y))); var upQ = 0.45 * Math.max(0, elevQ - 0.5) + 0.25 * Math.max(0, elevQ - 1.6); upQ *= scapKeep; var lateral = dv.x * rigOutSign(side) * -1; var frontal = -dv.z; var sx = postTilt * scapKeep, sy = rigOutSign(side) * axial * scapKeep, sz = 0; if (lateral >= Math.abs(frontal)) sz = rigOutSign(side) * upQ; else { sx += -0.6 * upQ; sz = lateral > 0.05 ? rigOutSign(side) * upQ * 0.35 : 0; } setRaw('SCAP_' + side, sx, sy, sz);
          var sc = E['SCAP_' + side]; eQT.set(sc.x, sc.y, sc.z, 'XYZ'); qB.setFromEuler(eQT); qB.invert(); qB.multiply(qA); eQT.setFromQuaternion(qB, 'XYZ'); eulerCanon(eQT); ua.x = eQT.x; ua.y = eQT.y; ua.z = eQT.z; }
        else if (abd >= flex) { setRaw('SCAP_' + side, postTilt, rigOutSign(side) * axial, rigOutSign(side) * upRot); ua.z -= rigOutSign(side) * upRot * 0.9; }
        else { var abdOpen = ua.z * rigOutSign(side) > 0.05; setRaw('SCAP_' + side, postTilt - 0.6 * upRot, rigOutSign(side) * axial, abdOpen ? rigOutSign(side) * upRot * 0.35 : 0); ua.x += 0.6 * upRot; if (abdOpen) ua.z -= rigOutSign(side) * upRot * 0.3; }   /* an adducted (cross-body) arm keeps its line: no frontal share */
        if (E['CLAV_' + side] && E['SCAP_' + side]) { cl.y *= 0.66; cl.z *= 0.66; }   /* v5 clavicle is rooted at the sternum (12 cm lever vs 8 cm): same authored angle would move the shoulder 1.5× as far */
        if (E.SPINE2) setRaw('SPINE2', -0.03 * Math.max(0, elevT - 1.4), 0, -rigOutSign(side) * 0.04 * Math.max(0, elevT - 1.4));   /* upper thorax answers overhead reach */
      });
      /* TWIST CARRIERS (rig v5): axial rotation of the humerus / forearm is split so the deltoid keeps its shape while the distal segment turns */
      ['L', 'R'].forEach(function (side) { var ua = E['UPPERARM_' + side], tw = E['ARMTWIST_' + side]; if (ua && tw) tw.y -= twistY(ua) * 0.6; var fa = E['FOREARM_' + side], ft = E['FORETWIST_' + side]; if (fa && ft) ft.y -= twistY(fa) * 0.55; });   /* twist read from the rotation itself (swing-twist about the bone axis), not from the Euler y slot — a donor pose's exact XYZ solution may carry twist in x/z */   /* COUNTER-twist carriers bound to the PROXIMAL half: the chain (elbow / hand) keeps the full rotation, the deltoid / elbow skin turns only 40-45 % of it */
      /* DISTRIBUTED TRUNK (rig v5): every authored SPINE / CHEST value propagates through LUMBAR / SPINE2 so flexion, side-bend and
         rotation read along the whole trunk instead of at two hinges (totals unchanged: children inherit) */
      if (E.LUMBAR && E.SPINE) { E.LUMBAR.x += E.SPINE.x * 0.40; E.LUMBAR.y += E.SPINE.y * 0.35; E.LUMBAR.z += E.SPINE.z * 0.40; E.SPINE.x *= 0.60; E.SPINE.y *= 0.65; E.SPINE.z *= 0.60; }
      if (E.SPINE2 && E.CHEST) { E.SPINE2.x += E.CHEST.x * 0.38; E.SPINE2.y += E.CHEST.y * 0.40; E.SPINE2.z += E.CHEST.z * 0.40; E.CHEST.x *= 0.62; E.CHEST.y *= 0.60; E.CHEST.z *= 0.60; }   /* totals preserved (children inherit): the authored trunk motion is spread over four segments instead of two hinges */
      /* the transformation's gather carries its own allowance (rig_profile pose_overrides.transformation): knees to the navel and a deep
         crossed elbow are the owner's decision for this pose alone; every other state keeps the shared table */
      var TFL = (st.tfGather || 0) > 0.02 && PR && PR.pose_overrides && PR.pose_overrides.transformation ? PR.pose_overrides.transformation.limits_rad : null;
      var EPL = PR && PR.pose_overrides && PR.pose_overrides.emote_endpoint ? PR.pose_overrides.emote_endpoint.limits_rad : null;
      var emDef = st.emote && st.emote.id && AD && AD.EMOTES ? AD.EMOTES[st.emote.id] : null; var EML = emDef && emDef.allowance && PR && PR.pose_overrides && PR.pose_overrides[emDef.allowance] ? PR.pose_overrides[emDef.allowance].limits_rad : null;   /* an emote's own named allowance (e.g. PERCH knees) */
      var ATL = st.atkAllow && PR && PR.pose_overrides && PR.pose_overrides[st.atkAllow] ? PR.pose_overrides[st.atkAllow].limits_rad : null;   /* an attack's own named allowance (rig_profile pose_overrides.overhead_release) — both arms, that attack's frames only */ var limOf = function (k) { var L = LIMITS[k]; if (!L) return L; var base = k.replace(/_(L|R)$/, ''); var side = /_L$/.test(k) ? 'L' : (/_R$/.test(k) ? 'R' : null); if (ATL && side && base === 'UPPERARM' && (ATL[base + '_x'] || ATL[base + '_y'] || ATL[base + '_z'])) return { x: ATL[base + '_x'] || L.x, y: ATL[base + '_y'] || L.y, z: ATL[base + '_z'] || L.z }; if (EPL && side && (base === 'UPPERARM' || base === 'FOREARM') && ikActive(side)) return { x: EPL[base + '_x'] || L.x, y: EPL[base + '_y'] || L.y, z: EPL[base + '_z'] || L.z };   /* endpoint-solved arm: the named allowance (rig_profile pose_overrides.emote_endpoint) */ if (EML && (EML[base + '_x'] || EML[base + '_y'] || EML[base + '_z'])) return { x: EML[base + '_x'] || L.x, y: EML[base + '_y'] || L.y, z: EML[base + '_z'] || L.z }; if (!TFL) return L; return { x: TFL[base + '_x'] || L.x, y: TFL[base + '_y'] || L.y, z: TFL[base + '_z'] || L.z }; };
      /* ───── 11 JOINT LIMITS: soft approach into the last 15 % of every range, then a hard safety clamp — every layer above inherits this ───── */
      var donorOwned = function (k) { return !!(st.layer && st.layer.donor && st.layer.weight > 0.5 && st.layer.pose[k]); };   /* a retargeted human pose is already anatomical; the authored Euler ranges (e.g. humeral twist ±34°) were written for the procedural poses and would mangle its exact XYZ solution — clearance (12) still runs on it */
      Object.keys(E).forEach(function (k) { var e = E[k], L = limOf(k); if (donorOwned(k)) { e.x = clamp(e.x, -Math.PI, Math.PI); e.y = clamp(e.y, -Math.PI, Math.PI); e.z = clamp(e.z, -Math.PI, Math.PI); return; } if (!L) { e.x = clamp(e.x, -SAFETY, SAFETY); e.y = clamp(e.y, -SAFETY, SAFETY); e.z = clamp(e.z, -SAFETY, SAFETY); return; } e.x = soft(e.x, L.x); e.y = soft(e.y, L.y); e.z = soft(e.z, L.z); });
      /* ───── 12 BODY CLEARANCE, then one final safety clamp so a correction can never push a joint back out of range ───── */
      if ((st.tfGather || 0) < 0.3) clearArms();   /* the transformation's X is fitted by FK and verified in the pose test; the coarse ellipsoid would push it open */
      Object.keys(E).forEach(function (k) { var e = E[k], L = limOf(k); if (!L || donorOwned(k)) return; if (L.x) e.x = clamp(e.x, L.x[0], L.x[1]); if (L.y) e.y = clamp(e.y, L.y[0], L.y[1]); if (L.z) e.z = clamp(e.z, L.z[0], L.z[1]); });
      /* ───── PLANTED FEET (owner interjection 2026-09-18 §8): a standing pose that flexes the hips (hinge, squat) keeps the feet where they are — the
           ROOT moves back and down by the amount the leg pose would otherwise lift the foot (thigh / shin lengths from THIS rig). Split form only,
           standing (speed01 < 0.15), not airborne, not transforming; forward is local −Z so "back" is +z. Read by the adapter through rootOffset(). ───── */
      var roT = { y: 0, z: 0 }; if (st.showLegs && !st.airborne && !st.transformState && speed01 < 0.15 && E.PELVIS && E.THIGH_L && E.THIGH_R && E.SHIN_L && E.SHIN_R) { var thR = -(E.PELVIS.x + 0.5 * (E.THIGH_L.x + E.THIGH_R.x)), shR = 0.5 * (E.SHIN_L.x + E.SHIN_R.x);   /* the femur's WORLD angle forward of vertical: the pelvis pitch carries the thighs (children) back, THIGH + is extension */ if (Math.abs(thR) > 0.02) { var LtP = SEG.thigh * SC, LsP = SEG.shin * SC, saP = thR - shR;   /* the shin's world angle forward of vertical (SHIN + folds it back); lengths in metres */ var LtipM = (rig.tipExtLocal || 0.0296) * SC; var tipA = saP - 0.5 * ((E.TIP_L ? E.TIP_L.x : 0) + (E.TIP_R ? E.TIP_R.x : 0));   /* the tip's world angle: the shin's minus the TIP fold */ roT.y = -Math.max(0, (LtP + LsP + LtipM) - LtP * Math.cos(thR) - LsP * Math.cos(saP) - LtipM * Math.cos(tipA)); roT.z = LtP * Math.sin(thR) + LsP * Math.sin(saP) + LtipM * Math.sin(tipA); } }   /* JOB A 2026-09-19: the pointed end is part of the planted leg — with the deeper RDL its tilt was the 8 cm dip */
      if (!st.rootOff) st.rootOff = { y: 0, z: 0 }; var kRo = Math.min(1, dt * 14); st.rootOff.y += (roT.y - st.rootOff.y) * kRo; st.rootOff.z += (roT.z - st.rootOff.z) * kRo;
      /* ───── apply: ONE composed pose per character per frame ───── */
      /* INERTIALIZATION: when the discrete state changes (attack / guard / emote / form / airborne / hit), the offset between the last
         applied pose and the new composed pose is captured and decayed with a quintic over ~0.22 s (0.12 s for attacks) — continuity of
         pose and velocity without a low-pass on the continuous layers (the gait keeps its own oscillator). */
      var inert = st.inert; var ikey = [at ? at.attack_id + ':' + at.state : '', v.guard || '', emKey || '', v.form || '', st.airborne ? 'A' : 'G', hitAgo < (DONOR.enabled && DONOR.clips.HIT ? 0.6 : 0.4) ? 'H' : '', v.dash ? 'D' : '', isTf ? ms : '', st.layer ? 'L' + st.layer.id : ''].join('|');   /* a layer entry / exit / swap is a state change too: the captured offset blends it in and out */
      if (inert.key !== null && ikey !== inert.key) { var offs = {}; Object.keys(B).forEach(function (k) { var ap = st.applied[k]; if (!ap) return; var e = E[k]; eQT.set(ap[0], ap[1], ap[2], 'XYZ'); qA.setFromEuler(eQT); eQT.set(e.x, e.y, e.z, 'XYZ'); qB.setFromEuler(eQT); qB.invert(); qA.multiply(qB); offs[k] = [qA.x, qA.y, qA.z, qA.w]; }); inert.off = offs; inert.t = 0; inert.dur = at ? 0.12 : (isTf ? 0.16 : 0.22); }   /* the captured offset is a ROTATION (prev · new⁻¹), so a pose whose exact Euler solution sits on another branch still blends as the small rotation it is */
      inert.key = ikey; inert.t = Math.min(1, inert.t + dt / Math.max(0.05, inert.dur)); var q5 = 1 - inert.t; var decay = q5 * q5 * q5 * (q5 * (6 * q5 - 15) + 10) * 0 + Math.pow(q5, 3);   /* cubic-out decay of the captured offset */
      if (!st.showLegs && !st.transformState) { ['THIGH_L', 'THIGH_R'].forEach(function (k) { if (E[k]) { E[k].x = 0; E[k].y = 0; E[k].z = 0; } }); }   /* FUSED: the split legs are hidden and, since dev_0.15 (hip connection), the pelvis shell carries a graded THIGH share in its SPLIT skin set (the fused set has none) — the THIGH bones rest so no invisible leg pose can ever reach the fused body; SHIN / TIP carry nothing of the body and keep their composed pose (knee floor for hit reactions unchanged) */
      var a = Math.min(1, dt * Math.max(rate, 24)); var pose = {};
      Object.keys(B).forEach(function (k) { var e = E[k]; var o = inert.off[k]; tmpE.set(e.x, e.y, e.z, 'XYZ'); tmpQ.setFromEuler(tmpE); if (o && decay > 0.001) { qA.set(0, 0, 0, 1); qB.set(o[0], o[1], o[2], o[3]); qA.slerp(qB, decay); qA.multiply(tmpQ); tmpQ.copy(qA); eQT.setFromQuaternion(tmpQ, 'XYZ'); eulerNear(eQT, e); pose[k] = [eQT.x, eQT.y, eQT.z]; } else pose[k] = [e.x, e.y, e.z]; B[k].quaternion.slerp(tmpQ, a); st.applied[k] = pose[k]; });   /* the reported pose keeps the composed (limit-checked) Euler; only an inertialized frame re-derives it, on the branch nearest the composed value */
      st.pose = pose;
    }
  };
  /* ───── G37 REPAIR — WORLD-SPACE SUPPORT HOLD (the declared pointed contact of this hovering species) ─────
     DECLARED CONTACT: one per step and side. It starts at the support event (the tip's forward extreme = the beam pulse) and lasts while the
     hip passes over the held point; the measured point is the TIP'S POINTED END (joint + tip_ext along the tip bone), the support surface is
     the beam's foot on the ground below it (the tip itself hovers — OWNER DECISION motion master B: no floor strike). While held, the end is
     WORLD-FIXED: the anchor is captured in world coordinates from the pose actually written this frame (so capture is continuous — no pop,
     no weight ramp) and the leg is re-solved every frame by a 3D two-bone IK in the pelvis frame against that fixed point, written straight
     onto the bones AFTER the rate filter (the filtered pendulum pose would lag the anchor by ~2 frames = 10 cm at walk speed). The hold ends
     when the point leaves the leg's reach or passes gait.plant_hold_behind_m behind the hip; the release blends back to the pendulum over
     gait.plant_release_s (outside the contact interval). The old pelvis-frame hold drifted the point at 45 % of travel by construction
     (support_drift) — that drift is what PASS 5 measured as 42.8 cm; it is kept as the rollback path (gait.plant = false). Pace, cadence,
     stride, hover clearance and the beam visuals are unchanged. Runs only on a real THREE bone hierarchy (node tests use stub bones). */
  var PL = { L: null, R: null }; var plV = null, plT = null, plH = null, plD = null, plP = null, plPP = null, plTD = null, plK = null, plSD = null, plM = null, plQ0 = null, plQ1 = null, plQ2 = null, plQ3 = null, plF = null, plE = null, plAx = null;
  function PLANT_ON() { return DG.plant !== false; }
  function plantReady() { var t = B.THIGH_L; return !!(t && t.parent && t.updateWorldMatrix && t.parent.matrixWorld && B.SHIN_L && B.TIP_L && B.THIGH_R && B.SHIN_R && B.TIP_R); }
  function plantInfo() { var o = {}; ['L', 'R'].forEach(function (S) { var a = PL[S]; o[S] = a ? { x: +a.x.toFixed(4), y: +a.y.toFixed(4), z: +a.z.toFixed(4), ground_y: a.gy === null ? null : +a.gy.toFixed(3), w: +a.w.toFixed(2), hold_ms: Math.round((st.t - a.t0) * 1000), behind_m: +a.behind.toFixed(3), reach: +a.reach.toFixed(3), clamped: !!a.clamped } : null; }); o.enabled = PLANT_ON(); o.ready = plantReady(); o.tip_ext_local = TIP_EXT; return o; }
  var TIP_EXT = rig.tipExtLocal || 0.0296;   /* the pointed end below the TIP joint, in bone units (dev_0.14: TIP joint 0.054 m above the lowest point ÷ scale 1.82461) */
  /* q such that q·restDir = dir, with the twist about `dir` chosen so the bone's rest forward (+z) stays as close as possible to `fwd` */
  function aimQuat(q, restDir, dir, fwd) { q.setFromUnitVectors(restDir, dir); plF.set(0, 0, 1).applyQuaternion(q); plF.addScaledVector(dir, -plF.dot(dir)); plE.copy(fwd).addScaledVector(dir, -fwd.dot(dir)); if (plF.lengthSq() < 1e-8 || plE.lengthSq() < 1e-8) return q; plF.normalize(); plE.normalize(); var c = clamp(plF.dot(plE), -1, 1); plAx.crossVectors(plF, plE); var sgn = plAx.dot(dir) < 0 ? -1 : 1; var ang = Math.acos(c) * sgn; plQ3.setFromAxisAngle(dir, ang); q.premultiply(plQ3); return q; }
  function plantPass(v, dt) {
    if (!PLANT_ON() || !plantReady()) { PL.L = PL.R = null; return; }
    if (!plV) { plV = new THREE.Vector3(); plT = new THREE.Vector3(); plH = new THREE.Vector3(); plD = new THREE.Vector3(); plP = new THREE.Vector3(); plPP = new THREE.Vector3(); plTD = new THREE.Vector3(); plK = new THREE.Vector3(); plSD = new THREE.Vector3(); plM = new THREE.Matrix4(); plQ0 = new THREE.Quaternion(); plQ1 = new THREE.Quaternion(); plQ2 = new THREE.Quaternion(); plQ3 = new THREE.Quaternion(); plF = new THREE.Vector3(); plE = new THREE.Vector3(); plAx = new THREE.Vector3(); }
    var speedMps = v.speed_mps !== undefined ? v.speed_mps : (v.speed01 || 0) * 5.5; var fly = !!v.fly;
    var active = st.showLegs && !st.airborne && !fly && !st.transformState && (st.packed || 0) < 0.05 && speedMps > (DG.plant_min_speed_mps || 0.4) && !(st.emote && st.emote.id);
    var reachFrac = DG.plant_reach_frac === undefined ? 0.985 : DG.plant_reach_frac, holdBehind = DG.plant_hold_behind_m === undefined ? 0.42 : DG.plant_hold_behind_m, relS = DG.plant_release_s === undefined ? 0.08 : DG.plant_release_s;
    var pelvis = B.THIGH_L.parent; pelvis.updateWorldMatrix(true, false); plM.copy(pelvis.matrixWorld).invert();
    var Lt = B.SHIN_L.position.length();   /* the shin segment runs from the knee to the pointed END through the tip bone AS WRITTEN this frame (its own rotation kept): the IK is continuous with the pendulum at capture */
    /* travel direction in world (for the 'behind' test) from the yaw the adapter passes (root yaw = the displayed facing) */
    var yaw = v.yaw || 0; var back = v.backward ? -1 : 1; var lat = clamp(v.lateral || 0, -1, 1); var tx = (Math.sin(yaw) * back * Math.sqrt(Math.max(0, 1 - lat * lat)) + Math.cos(yaw) * lat), tz = (-Math.cos(yaw) * back * Math.sqrt(Math.max(0, 1 - lat * lat)) + Math.sin(yaw) * lat);
    ['L', 'R'].forEach(function (S) {
      var th = B['THIGH_' + S], sh = B['SHIN_' + S], tp = B['TIP_' + S]; var a = PL[S]; var sup = S === 'L' ? st.gait.supportL : st.gait.supportR;
      plE.set(0, -TIP_EXT, 0).applyQuaternion(tp.quaternion).add(tp.position); var Ls = plE.length(); var Lmax = (Lt + Ls) * reachFrac;   /* knee → END in shin-local (tip rotation included) */
      if (a && a.rel === null) { /* held: does the hold end this frame? (the hip this frame — root already placed by the adapter, pelvis chain already written) */
        th.getWorldPosition(plH); var dx = a.x - plH.x, dz = a.z - plH.z; a.behind = -(dx * tx + dz * tz); var dist3 = Math.sqrt(dx * dx + (a.y - plH.y) * (a.y - plH.y) + dz * dz); a.reach = dist3 / ((Lt + Ls) * SC);
        if (!active || a.behind > holdBehind || dist3 > (Lt + Ls) * SC * 1.02 || sup < 0.02) a.rel = 0; }
      if (a && a.rel !== null) { /* releasing: from the pose the IK wrote last, back into the written pendulum pose over plant_release_s, then forget the anchor */ a.rel += dt; var w = 1 - Math.min(1, a.rel / Math.max(0.02, relS)); if (w <= 0) { PL[S] = null; return; } th.quaternion.slerp(a.qT, w); sh.quaternion.slerp(a.qS, w); tp.quaternion.slerp(a.qP, w); a.w = 0; return; }   /* the blend-out is visual continuity, not contact: the reported hold weight is 0 from the release frame on (the contact interval = w ≥ 0.9 ends when the hold ends, whatever the frame rate) */
      if (!active) return;
      /* capture at the support event: the pendulum's forward extreme (support pulse at its peak) — the pointed end's world position as written this frame */
      if (!a) { if (sup < 0.97) return; tp.updateWorldMatrix(true, false); plV.set(0, -TIP_EXT, 0).applyMatrix4(tp.matrixWorld); th.getWorldPosition(plH); plD.subVectors(plV, plH); var D0 = plD.length(), Dmax = Lmax * SC; if (D0 > Dmax) plV.copy(plH).addScaledVector(plD, Dmax / D0);   /* a point captured beyond the leg's reach is pulled in along the hip line (≤ 1 % of the leg): the knee never snaps at capture */ a = PL[S] = { x: plV.x, y: plV.y, z: plV.z, gy: v.groundAt ? v.groundAt(plV.x, plV.z, plV.y) : null, t0: st.t, w: 0, behind: 0, reach: 0, clamped: false, rel: null, eng: 0, qT: new THREE.Quaternion(), qS: new THREE.Quaternion(), qP: new THREE.Quaternion() }; }
      /* ---- 3D two-bone IK in the pelvis frame against the world-fixed end ---- */
      plT.set(a.x, a.y, a.z).applyMatrix4(plM); plH.copy(th.position); plP.set(0, 0, 1);   /* pole: the pelvis' own forward — the knee bends forward */
      plD.subVectors(plT, plH); var D = plD.length(); a.clamped = false; if (D > Lmax) { plD.multiplyScalar(Lmax / D); D = Lmax; plT.copy(plH).add(plD); a.clamped = true; } if (D < 1e-5) return; plD.normalize();
      var cosK = clamp((Lt * Lt + Ls * Ls - D * D) / (2 * Lt * Ls), -1, 1); var flex = Math.PI - Math.acos(cosK); var inner = Math.atan2(Ls * Math.sin(flex), Lt + Ls * Math.cos(flex));
      plPP.copy(plP).addScaledVector(plD, -plP.dot(plD)); if (plPP.lengthSq() < 1e-6) plPP.set(1, 0, 0).addScaledVector(plD, -plD.x); plPP.normalize();
      plTD.copy(plD).multiplyScalar(Math.cos(inner)).addScaledVector(plPP, Math.sin(inner)).normalize();   /* thigh direction: the hip→end line rotated toward the pole by the knee's offset */
      plK.copy(plH).addScaledVector(plTD, Lt); plSD.subVectors(plT, plK).normalize();                         /* knee, then the shin direction */
      /* minimal swing from the pose the pendulum wrote: each bone is rotated by the smallest rotation that takes its CURRENT end-direction onto the solved one, so its twist (splay, roll) is untouched and capture is continuous in orientation as well as position */
      plE.copy(sh.position).normalize().applyQuaternion(th.quaternion); plQ3.setFromUnitVectors(plE, plTD); plQ1.copy(th.quaternion).premultiply(plQ3);                                          /* thigh (pelvis-relative) */
      plQ2.copy(plQ1).invert(); plV.copy(plSD).applyQuaternion(plQ2); plE.set(0, -TIP_EXT, 0).applyQuaternion(tp.quaternion).add(tp.position).normalize().applyQuaternion(sh.quaternion); plQ3.setFromUnitVectors(plE, plV); plQ0.copy(sh.quaternion).premultiply(plQ3);   /* shin (thigh-relative): its written end-direction (through the tip as written) swung onto the solved shin line */
      a.eng = Math.min(1, a.eng + dt / Math.max(0.01, DG.plant_engage_s === undefined ? 0.04 : DG.plant_engage_s)); var wIn = a.eng * a.eng * (3 - 2 * a.eng);   /* the IK pose eases in over plant_engage_s from the written pendulum pose — no knee snap at capture; the contact interval counts from weight ≥ 0.9 */
      th.quaternion.slerp(plQ1, wIn); sh.quaternion.slerp(plQ0, wIn);                                                                                        /* the tip keeps the rotation the pendulum wrote: the END offset it defines is what the shin was aimed with */
      a.w = wIn; a.qT.copy(th.quaternion); a.qS.copy(sh.quaternion); a.qP.copy(tp.quaternion);
      [['THIGH_' + S, th], ['SHIN_' + S, sh], ['TIP_' + S, tp]].forEach(function (pr) { eQT.setFromQuaternion(pr[1].quaternion, 'XYZ'); st.applied[pr[0]] = [eQT.x, eQT.y, eQT.z]; if (st.pose) st.pose[pr[0]] = st.applied[pr[0]]; });
    });
    st.support.L = PL.L ? { z: -PL.L.behind, w: PL.L.w, behind: PL.L.behind } : null; st.support.R = PL.R ? { z: -PL.R.behind, w: PL.R.w, behind: PL.R.behind } : null;
  }
  api.plant = plantInfo;
  return api;
}
