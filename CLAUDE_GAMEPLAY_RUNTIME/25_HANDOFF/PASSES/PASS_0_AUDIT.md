# PASS 0 — AUDIT ONLY (record)

Written 2026-09-17 by the gameplay-lane Claude session, from the read-only audit that preceded PASS 1 (zero edits were made during
PASS 0; this file is the durable record of its output, written when the checkpoint folder was created). Evidence words: *inspected* = read
code / looked at an image; *rendered/measured* = ran it and captured numbers. iPhone: NOT tested (Jah only).

Brief: `C:\Users\jahsu\Downloads\MAHWORLD_ALL_PASSES_MASTER_PROMPT.md` (PART I continuous execution; PART II = the supplied source brief,
Specs A–H, Section 2 non-negotiables, Section 12 pass order, Section 13 report format). Repo: `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS`
(git, branch master; PASS 0 was audited at tag `s13_convergence` = commit 74b3ec5). Runtime root: `CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY` (R).

## Section 12 PASS 0 output (every quoted item)

- **R170 fileset:** NOT in the workspace. Searched the repo and the sibling folders (`MAHWORLD_CLAUDE_CHARACTER`, the Codex paste folder) for
  "R170" / an R170 brief — nothing. This brief "sits on top of R170"; where R170 is referenced (control grammar, movement states, macro world
  lock, "Control Lab exposes real runtime values") the S13 runtime's existing grammar was treated as the in-force baseline and no macro-world
  change was made.
- **Entrypoint:** `R/lab/play.html` → `R/lab/play.js` (ES modules, vendored three.js 0.185.1 at `R/../vendor/three/three.module.min.js`,
  no bundler). Static site build: `R/deploy/build_static_demo.mjs` → `R/deploy/static_dist` (flat Netlify root, in-page authority
  `R/deploy/static/static_host.js`, edge gate `R/deploy/static/edge/gate.js`).
- **Scene owner paths:** `R/lab/fieldScene.js` (field, floors, sky, sun, clouds, fog, landmark GLBs + collision, `cameraDistance` boom probe),
  `R/lab/cityScene.js` (procedural sample city around the field, instanced fins), `R/lab/assets/buildings/_decimated/*.glb` (runtime
  landmark meshes; source GLBs uploaded by Jah), `R/deploy/build_district.mjs` (district manifest).
- **Camera file:** `R/lab/play.js` — module-level `cam` object (yaw / pitch / dist / applied / manual / manualT / follow / latch / kick / pivotY),
  follow block (centred rear follow of the displayed root yaw, `CAM_RETURN_S` single ease constant, `CAM_FRAME_FRAC`, `CAM_TURN_CAP`), drag
  = free look via `camDrag / camDragEnd / camZoom / lookAllowed / camFraming`; touch look zone `#tc-world` in `play.html` with the CAMERA
  role in `R/lab/touchControls.js`. Dev API: `MAHWORLD_PLAY.cam / camFollow / camLook / cameraDebug`.
- **Input / HUD files:** `R/lab/touchControls.js` (joystick + look zone + buttons, emulated-pointer tests only), `R/lab/gameHud.js` (HUD,
  guide cards, dev panel), `R/lab/rulesHud.js` (combat / hit HUD), `R/lab/gameMenu.js` (Q menu, TAB emote radial), keyboard bindings in
  `play.js` (`addEventListener('keydown' …)`: W A S D, Space, F, H, E R T Y, G / Shift+G, TAB, Q, F3, Backquote after PASS 1).
- **Animation system type:** procedural additive rig animator — `R/lab/RigAnimator.js` (38-joint XYZ-Euler bone rig, gait / flight / attack
  arcs / emote endpoint IK, `emoteArmIK`, `eulerFit`), authored emote clips in `R/lab/animData.js` (keyed bone clips + `IK()` endpoints),
  donor Tripo clips as retargeted JSON (`R/lab/assets/retarget/tripo_*_upper_v1.json`, UPPER_MASK only). No `THREE.AnimationMixer` /
  `AnimationAction` is used anywhere in `R/lab` (inspected: zero matches) — Spec E's "additive blending / crossFadeTo" language maps onto
  the animator's own layer blending, not the three.js mixer.
- **VFX files:** `R/lab/PresentationAdapter.js` (glow sprites, PROJECTILE / ZONE / IMPACT effects, particles, seam / aura, hit flash),
  `R/lab/BeamFx.js` (MAHGIC beams), `R/lab/MuscleLayer.js`, `strikePulse` in `play.js`. Materials: MeshBasic / Sprite / Points with
  additive blending; no post-processing, no shadow maps in this runtime.
- **Separated-legs code path + lane:** the fused body and the split legs are generated by the gameplay lane's rig generator
  `R/lab/assets/make_class_rig_v5.mjs` (`--legs surgical`) with `R/lab/assets/rig_legs_surgical.mjs`; runtime switching is
  `rig.setForm(packed, showLegs)` (GlbCharacter.js) driven by `RigAnimator.form()`. Athlete_M anatomy / Character-Creator sources are
  the OTHER lane (Astra / Codex; `MAHWORLD_CLAUDE_CHARACTER` session) — never edited here. Spec D's premise ("separated legs") vs the
  fused anatomy: there is no separate leg mesh in the source; the faithful path is a true midline clip of `fused_lower` inside the gameplay-lane
  generator (already the S13 approach) → Spec D remains an owner-lane GATE before any anatomy change.
- **Test harness status:** plain-node tests `CLAUDE_GAMEPLAY_RUNTIME/16_TESTS/*.test.mjs` (24 files at PASS 0, all runnable with
  `node <file>`); headless-Chrome probes `R/deploy/probe_*.mjs` through `R/deploy/probe_lib.mjs` (Chrome for Testing / local Chrome,
  `--headless=new --use-angle=d3d11`, emulated touch via synthetic pointer events); static smoke `R/deploy/test_static_demo.mjs`; recorder
  `R/deploy/record_s13.mjs`. **No Control Lab existed** (frame graph / percentiles / dropped %) — PASS 1 added one. **No dropped-frame
  definition existed** anywhere in the project (grep for "dropped", "frame budget", "hitch" thresholds: only a `PERF.slow` counter for
  frames > 50 ms). **No bone angular-velocity threshold exists** (Spec E's "nothing twitches" has no number yet) — NOT INVENTED.
- **Baseline numbers on the test route** (rendered/measured, headless Chrome, unlocked frame pacing, this laptop, before any PASS 1
  edit; the baseline build is the S13 static root): desktop 1120×700 route p50 16.6 / p95 38.5 / p99 47.0 / max 692 ms, 585 916 tris
  (`25_HANDOFF/PASSES/perf/route_before_pass1_desktop_unlocked.json`). Re-measured back-to-back with PASS 1 under lighter machine load:
  p50 6.2 / p95 9.1 / p99 11.2 / max 158 ms (`route_before_pass1_desktop_run2.json`) — the harness numbers scale with machine load, so only
  back-to-back pairs are compared. The iPhone recording's unique-frame rate was NOT used as a baseline.
- **Cross-lane files needed:** none for PASS 1–3. Spec D (PASS 4) may need the anatomy lane's decision on the midline clip →
  GATE. Spec F (PASS 6) needs Jah's recordings (human input, not a file).
- **Spec H tooling decision:** custom shaders / sprites on the vendored three.js (no new dependency). `three.quarks` would be a new
  dependency → needs approval; not taken.
- **Camera-drag conflict (recorded, not changed):** Spec B says swipe on the "empty right-side area"; Spec C says "right half"; Jah's
  earlier S13 instruction was "anywhere unobstructed, including the left, except the joystick" (current runtime: `#tc-world` left:42% →
  right edge). One bounded choice is asked at the PASS 2 gate before this is changed.

## Acceptance
Every quoted PASS 0 output above is accounted for; unknowns are stated as unmet (R170 fileset absent; dropped-frame definition and bone
angular-velocity threshold do not exist and were not invented). Checkpoint: internal → continued to PASS 1.
