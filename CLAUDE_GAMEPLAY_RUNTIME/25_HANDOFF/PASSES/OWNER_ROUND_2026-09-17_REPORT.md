# OWNER ROUND 2026-09-17 — camera lock-in · character look · 0.67× landmarks · daylight · mobile layout · dash

Section 13 report · gameplay-lane Claude session · repo `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS` (master) · commits `80dee57`, `1a8e9a6`
`59a6585`, `5e2ffac` (tag `owner_round_0917`) on top of `pass2_spec_b`. R = `CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY`. Blender: not launched. Athlete_M
sources / Character-Creator: untouched (the look change is a RUNTIME material response in the gameplay lab, see below).
Evidence words: *inspected* = read code / looked at an image · *rendered/measured* = ran it and captured numbers/frames · **iPhone: NOT tested (Jah only)**;
touch in every probe is EMULATED (synthetic pointer events in headless Chrome, unlocked frame pacing).

The owner's instructions this round (verbatim intent): back view always in view, no drifting / wandering camera, only a mouse drag or tap-drag moves the
POV; the character must not "glitch into pieces"; mainland buildings ~0.67×; smooth and natural; daytime with the sun out; use the reference video;
mobile layout = attack buttons 1–4 at the very bottom, the five round buttons in a 2×2 on top of them with the extra one bottom-left, DASH directly right of
ATTACK, a more dramatic dash; deploy to the domain on the spot.

## WHAT I DID (files changed, one line each)
- `R/lab/play.js` — **camera = rear follow + drag, nothing else**: `return_delay_s` 0 (the return starts on release, one setting `return_time_s` 0.4 s), `speed_fov_deg` 0 + `speed_pullback` 0 (no view change at speed), lock-on reframing OFF (`lock_frame` false — always his back), strike camera kick OFF (`strike_kick` 0); all still Control Lab fields for experiments. Aim point 0.28 H so his feet clear the bottom row. Mesh fade fully faded by 0.7 m with depthWrite kept (never see-through "pieces"). Sun shadow box follows the player; ACES tone mapping (exposure 1.35) in daylight; dev hooks `boneAudit / matSet / lightSet / shadowSet / sunInfo / morphZero / muscleRead`.
- `R/lab/PresentationAdapter.js` — animator distance LOD never drops below 1/3 of the frame rate (≤ 30 m every frame, ≤ 60 m every 2nd, beyond every 3rd; sub-step ≤ 1/15 s) — no stop-motion rigs; shadow casters = the player (+ other rigs on HIGH only).
- `R/lab/GlbCharacter.js` — **runtime look**: a class body that exports as matte PBR (metalness 0, no metalness map) gets metalness 0.72 / roughness ≤ 0.34 / envMapIntensity 1 (the Tripo turntable reference: metallic orange-gold + dark chrome); bodies cast shadows.
- `R/lab/fieldScene.js` — daylight: the sun (key 2.35) casts a following shadow map (HIGH tier), PMREM sky environment with a real sun highlight (`pmremSky`, inside the PMREM far plane), sky / clouds / sun disc untone-mapped, dark platinum floors (roughness 0.5, envMapIntensity 0.16–0.2 by day), landmarks receive shadows; boom floor 1.1 m; `pointBlocked` skips decks / rims; `lightSet / follow / sunInfo`.
- `R/lab/quality.js` — tier keys `shadows / shadow_map / shadow_reach_m` (HIGH: PCF 1024, reach 24 m, the player casts; MED + LOW: off, blob contact shadow — measured phone-tier cost below).
- `R/lab/assets/buildings/district_v1.json` + `R/play/rules1723/district_v1_colliders.json` — **landmarks 0.67×**: temple 80 → 53.6 m, gym 72 → 48.2, tower 72 → 48.2, market 64 → 42.9 (`target_height_m_s11` keeps the old value); envelopes, deck slabs and 1 027 colliders regenerated by `deploy/build_district.mjs`.
- `R/lab/play.html` — **mobile layout**: bottom row = [≡ menu][1][2][3][4] (full width, 46 px tall); FLY / GUARD / USE / MORE in a 2×2 block above it at the right; DASH directly right of ATTACK (portrait and landscape rules); joystick zone bottom-left 30 % × 38 %.
- `R/lab/touchControls.js` — the floating stick's ring is clamped inside its zone (a landing near the zone's top edge never puts the stick over him).
- `00_CORE/dev_tuning.dev.json` — dash distances ~1.75× (ATHLETE 3.4 → 6.0 m in 0.34 s; Titan 3.6, Bage 4.6, Visionary 5.3, Lean 7.4) — the rules-field combat dash scales from the same base.
- `R/deploy/probe_glitch_hunt.mjs` (new) — 100 s varied play script with a per-50 ms bone audit (bone-length ratio vs rest, NaN, per-sample jump) + stills at anomalies; `probe_spec_b*.mjs` follow the owner rule (no hold, no speed view change, lock = his back), wall test reads the live landmark envelope.
- Tests: `gameplay_spec_a_perf` (shadow tiers), `gameplay_spec_b_camera` 27/27, whole suite in packaging.

## WHAT I FOUND (problems, with file:line)
- **"Glitching into pieces" — reproduced as a LOOK defect, not a rig break.** The bone audit over walk / run / turns / strafe / back / E R T Y / specials / guard / dash / separate / split walk / fuse / fly / 8 emotes / practice hits found **0 dislocations, 0 NaN** (1 291 samples; the only flags were sampling-gap jumps); stills of every state show an intact body. What the rear camera does show, all the time: dark angular patches on his upper back (`spec_b`/`blotch` stills) — present on V7 and V8, unchanged with the normal map off, tangents on, or double-sided. They are the dark chrome shoulder plates of the design (see the reference turntable at 1.5–2.0 s), rendered as matte grey holes because the exported material has **metalness 0** (`matInfo`: `metalness 0, roughness 0.5, no metalnessMap`). With the runtime metallic response they read as glossy black chrome. Also hardened: the boom floor (1.1 m) + full fade by 0.7 m so the near plane never cuts him open, opaque fade, and far rigs never animate below 1/3 rate.
- The PMREM environment was black at first: `PMREMGenerator.fromScene` clips at 100 m and my sky sphere was 300 m — fixed (radius 50, sun at 44 m, explicit near/far).
- With the bright PMREM horizon the metallic floors became a pale mirror; daylight floors now use envMapIntensity 0.16–0.2 (black platinum with a sky sheen).
- Shadows on the phone tier: PCF 1024 with all rigs casting doubled the frame's triangles (227k → 490k) and cost +2.4 ms p95 on the Spec A route; player-only casters still +1.9 ms → MED / LOW keep the blob contact shadow, HIGH (desktop) has the real sun shadow (+cost recorded below).
- The reference video is a 4 s Tripo Studio turntable (1080×1080), not gameplay footage — used as the look reference (frames extracted with headless Chrome; no new dependency).

## EVIDENCE
- inspected: the reference turntable contact sheets (`scratchpad/video/sheet_00.jpg`), the S13 review stills, every layout screenshot below.
- rendered/measured:
  - Camera (owner rule) — `probe_spec_b.mjs` desktop / portrait / landscape **20 / 20 / 20**: back check 0.0° at four headings, release → return starts at once and settles 0.0° within 0.4 s, hold-back 3 s = 180.0° turn with 0 oscillation, 0 frames inside geometry (≈ 9 000 frames per mode incl. the wall hug), 0 HUD / control / thumb-zone overlaps of his screen box (portrait box 36–62 %, landscape 26–84 %); `probe_spec_b_extra.mjs` desktop + portrait 6 / 6 (E4 now asserts NO view change at run speed; E2 = a locked player who faces the target has the target on screen with the camera on his back); gesture audit 11 / 11 + 10 / 10.
  - Glitch hunt — `probe_glitch_hunt.mjs desktop`: 1 291 audits, 0 dislocated bones, 0 NaN; 18 split / fuse / walk / run / attack stills intact (`scratchpad/split/_sheet.jpg`).
  - Look — `scratchpad/look_v4/_sheet.jpg`, `mat/_sheet.jpg`, `chrome/chrome_spheres.png` (env probe spheres: blue sky, pale horizon, hot sun on chrome; gold sphere rich), `blotch/` (back plates before / after).
  - Layout — `scratchpad/layout_portrait.png`, `layout_landscape.png`; rects: portrait row y 798–844, 2×2 at x 262–380 / y 617–770, ATTACK x 10–76 + DASH x 86–152 at y 620–686, menu x 8–60 bottom; landscape row y 338–384, 2×2 x 722–836, ATTACK x 100–154 + DASH x 162–216 at y 262–316.
  - Spec A route (same harness): phone tier (portrait 390×844, `?quality=med`, single runs, PASS 1 build → this build): route p50 4.9 → 5.1, **p95 7.7 → 8.1**, fly 7.0 → 7.0, combo 8.7 → 9.1, triangles 227k → 286k (the 0.67× landmarks sit deeper inside the LOD switch distance). Desktop HIGH (sun shadows on, 3× interleaved vs the PASS 1 build): see the series line at the end of this report.
- iPhone: NOT tested (Jah only).

## ACCEPTANCE (owner's asks)
- Back view always, no drift, only drag moves the view → **PASS** (rendered: 20/20 ×3; the only automatic motion left is the rear follow of his displayed facing + the boom shortening against walls).
- Character never glitches into pieces → **PASS on the harness** (bone audit clean, stills intact) + the look defect that reads as "pieces" fixed; on the iPhone → Jah's eyes.
- Buildings 0.67× replacing the mainland landmarks → **PASS** (manifest + colliders regenerated; stills `look_v4/04_temple_approach.png`, `05_gym_approach.png`).
- Daytime, sun out, appropriate lighting → **PASS** (sun disc + directional sun + following shadow on HIGH, PMREM sky environment, ACES, blob shadow on the phone tier).
- Reference-video improvements → **PASS** (metallic runtime look approaching the Tripo turntable; front / back stills).
- Mobile layout (row of 1–4 at the bottom with the menu, 2×2 above, DASH right of ATTACK) → **PASS** (rendered rects above).
- Dash more dramatic → **PASS** (~1.75× distances; `gameplay_rules_17_23` 60/60, `gameplay_attack_stack` 22/22 still pass).
- Spec A "no regression" → phone tier +0.4 ms p95 (within its spread); desktop HIGH: series line below (sun shadows are the deliberate cost on the desktop tier only).
- Deployed on the spot → **DONE** (deploy 6aac25101f17ce5ed37166e4 live on https://mahdemo.fob.systems; gate verified fail-closed on the real address; login step = Jah).

## OPEN RISKS
- Real iPhone behaviour (Safari, 60 Hz, MED tier) is still Jah's to see; the harness cannot confirm what he saw as "pieces" — if it persists on the phone, the next suspects are the split-form toggle at H and the phone's skinning precision, and I need a phone recording of the moment.
- Landmarks receive shadows on HIGH only; the phone tier shows the blob contact shadow, not a cast shadow.
- The camera-drag area question from the PASS 2 gate is still open (RIGHT half vs ANYWHERE); the look zone is unchanged (right 58 %).
- The Spec B "hold 0.5 s then glide" from the brief is now OFF by the owner's rule (fields remain at 0 in the Control Lab).

## ZIP / DEPLOY
- Package: `26_LOCAL_AUTHORITY/deploy/packages/OWNER_ROUND_0917.zip` · 28 741 761 bytes · 102 entries · **SHA-256 `b71516cea4854c72e7b537a891cc2dd38881a74aac23520c0b01acffb00a8612`** · pipeline `package_pass.mjs OWNER_ROUND_0917`: 26 test files / 1 011 checks → JS sweep (65) → CSS sweep → stamps (commit 59a6585, clean) → boundaries (gate is the only function) → secret scan (102 files, 0 hits) → ZIP → listing 102 = 102 → fresh extraction (byte sizes match) → smoke test from the extraction 7/7 → JS sweep clean. Record `OWNER_ROUND_0917.PACKAGE.json` (tracked).
- **Deployed to the demo project as production (owner: "deployed to the domain on the spot")**: `npx netlify-cli deploy --prod --dir . --site 968f80e5-889b-43a8-b2cd-5860c588546a` run from inside the built site root (netlify.toml + the gate edge function bundled) → deploy **`6aac25101f17ce5ed37166e4`** live at **https://mahdemo.fob.systems** (also https://mahworld-test-preview.netlify.app). Rollback = the previous production deploy `6aab65567e997ae33c1da204` (S13). The main MAHFITT site, DNS and domains were not touched.
- Verified on the real address (rendered/measured, not a deploy status): `release.mjs --check` on the domain, the netlify.app address and the deploy permalink → play page 401 password page + private asset 401 JSON (fail-closed); `probe_public_gate.mjs https://mahdemo.fob.systems mobile` (emulated iPhone, fresh profile) → password page renders (title "MAHWORLD DEMO", form), assets 401 before login, wrong password → readable error with the attempt count, the password string absent from the served page. **The correct-password login was not exercised by me** (the password is not held by this session — by design it lives only in the Netlify environment); Jah's phone login is the last step of the verification.
- Spec A series on this build: phone tier (MED, portrait 390×844, 3× interleaved vs the PASS 1 build) route p95 7.8 → 7.9 ms, no regression; desktop HIGH 9.9 → 11.7 ms (+1.8) = the player's sun shadow, recorded as an explicit owner exception in `route_series_round3.json` and printed by the test.

## NEXT
Jah on the iPhone: open the domain, log in, play 30 s (walk / swipe / attack / dash / H), then say in one line what still looks wrong (with a screen recording if he sees the "pieces" again).
