# RULES 17–23 IN THE TEST FIELD — REPORT (2026-09-14, DEVELOPMENT, loopback only)

Owner packet: `C:\Users\jahsu\Downloads\MAHWORLD_RULES_17_23_CLAUDE_PACKET.zip` (`MAHWORLD_RULES_17_23.md`, `MAHWORLD_RULES_17_23_STARTER.json`, `CLAUDE_IMPLEMENT_17_23.md`, `validate_rules.py`; packet sha256 `37d7bf70ca830bed…`, copied read-only to `26_LOCAL_AUTHORITY/play/rules1723/packet/`). Ruleset `MW_DEV_2026_09_14_R17_23_V0_1`. Design validator (packet): 47/47. This report records what was actually implemented, executed and browser-tested; balance is not established. The local server is NOT running after this session — relaunch below. HARD STOP.

## FIELD OPENS / ACTUAL LOCAL URL

```
cd C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS\CLAUDE_GAMEPLAY_RUNTIME
node 26_LOCAL_AUTHORITY\lab_host_server.mjs --dev-hooks
```
Open the printed `http://127.0.0.1:<OS-assigned port>/lab/play.html` with `?field=1` (for example `http://127.0.0.1:52946/lab/play.html?field=1`). Port 8140 is never used. Second player: `?field=1&account=PLAYER_B`. `--memory` for a throwaway store; no `DEV_DATA_ONLY` store was created by this work.

## EXACT LAUNCH AND CONTROLS

Keyboard / mouse: WASD move · left-drag orbit, wheel zoom (never turns the aim; F faces the camera) · **Z** cycles the category tab (Physical / Physical Magic / Special Magic) · **1–4** selects a chip (tap = select, never fires) · **hold a chip ≈ 350 ms** = compact details panel (≤ 30 % of the viewport, scrolls, does not pause; Esc / ✕ closes and nothing leaks into combat) · **J or click** performs the selected action · **Tab** cycles the aim hint (nearest eligible in front) · **Shift+J** = confirm an ally-targeted Discord Mark · Q dash · Space / C / V fly / descend / land · T fuse-split · G / H physical / magical guard · **I / U / O** incoming physical / mixed / magic fixture · **B** 1v1 vs synthetics (2v2 / 3v3 buttons in the panel) · **M** end match · **Y** spawn / remove a damaged practice ally · **N** BAGE niche set · R reset practice · class picker in the panel (development fixture; persisted class ids are never renamed). Touch: left stick, right-half orbit, MOD held + swipe = dash, buttons ATTACK / GUARD PHYS / GUARD MAGIC / FLY / FUSE-SPLIT / INTERACT / WAVE / RESET / INCOMING; category tabs and chips are tappable (hold for details). No right-click anywhere. Lost focus, hidden tab and cancelled touches release inputs.

## FIVE CLASS PROFILES / THREE CATEGORIES / TWO RESOURCES

Data-driven from `rules_17_23.dev.json` (owner starter JSON + `_runtime_mapping`): Athlete 100/70/60 · Titan 90/100/40 · BAGE 35/40/100 · Visionary 65/45.5/90 · Lean 55/56/48 potency; HP 1000/1100/1150/1020/900; reductions, phase multipliers (startup / recovery only, clamped 0.12–3 s, recovery floor 0.10 s), dash table, combat capacity / regen (2-s delay, no regen while casting or guarding). Mahloco, flight pool and forms untouched. Categories are explicit tabs; Physical costs nothing; PM / SM and dashes spend combat energy; flight keeps its own pool and is refused in team matches. Aliases Mage / Balance Mage → BAGE, Lien → Lean; no sixth class; M/F share one profile (tested). Movement multiplier applies to the room integrator (class travel speed × strongest slow × self-buff), never below 40 % from slows, dashes unaffected.

## STARTER AND NICHE SKILLS ACTUALLY WORKING (executed in Node and/or browser)

Shared exemplars PA/PM × PUSH · PULL · HINGE · ROTATION mapped onto existing pattern ids (HORIZONTAL_PUSH, VERTICAL_PULL, HINGE, ROTATION); Titan's `TITAN_SHOULDER` / `TITAN_GROUND_PULSE` replace its PUSH / HINGE infused chips (majority-infused catalogue 6 PM vs 4 PA, verified; Physical stays pure). Athlete: Vector Shot, Trackburst (dissipatable projectiles), Set Position (self buff). Titan: Iron Pulse (area at commit), Anchor Brace. BAGE: Mend (heal), Rally (team buff), Sapping Mist (fixed zone), Delayed Seal (trap → following DoT); niche set Vanish, Phase Shell, Discord Mark, Last Pulse. Visionary: Ankle Load (following slow), Chain Circuit (fixed zone + slow inside), Barbell Drop (aimed area), Cable Snare (trap → following DoT + slow). Lean: Slip Bolt (projectile), Quickstep Echo (movement buff), Tether Flick (following slow). Every one of the 30 skills is castable through the same pipeline; the Node suite executes strikes, PM strikes, both projectiles, Mend, Mist, Seal, Ankle, Cable, Vanish, Shell, Discord, Last Pulse; the browser run executes strikes, mixed incoming, Mend, Mist, Ankle, Vanish, Shell, Discord. Rally / Set Position / Brace / Iron Pulse / Chain / Barbell / Bolt / Echo / Tether were selected and castable but their outcomes were not individually asserted in this pass.

## GUARDS / INTERRUPTIONS / LOWERING

Damage applied once: raw × potency ÷ 100 (× 1.25 periodic for Visionary, periodic only) → 60/40 split (PM) → guard per component (90 %, 120° frontal cone) → class resistance per component. Packet example verified: a mixed 100 from a potency-100 attacker leaves 46 behind physical guard and 64 behind magical guard before armor. Dissipation only for declared pure-magic projectiles on a frontal magical guard (verified; approach direction, not overlap position). Applied DoT is never blocked by a guard. Guard requests during a committed attack are refused; a dash releases guard; guards are one exclusive choice; guarding blocks a new attack start. Lowering is automatic (startup → commit → recovery); a valid stagger (development poise test: pre-mitigation hit ≥ 6 % max HP × poise/100) fizzles an uncommitted cast: 25 % of the reservation consumed, 75 % released, fizzle cooldown max(0.5 s, 25 % cd); committed damage and released autonomous projectiles are never rewound; invalid / repeated commands cost nothing.

## HEALING / TRAPS / FOLLOWING EFFECTS

BAGE Mend: living self / allies, same context (practice team outside matches, same team inside), capped at missing HP (self at full heals 0), never through resistance; enemies, spectators / non-field accounts, and cross-context targets refused; no resurrection. Visionary has no heal / lifesteal (flagged reading). Zones tick on their recorded cadence (first tick after the interval, whole tick count, no reentry tick); traps trigger once and attach a following DoT (+ slow); DoT families BAGE_DECAY / VISIONARY_LOAD coexist, same family strongest-only with refresh; slows strongest-only, ≥ 40 % movement; construct caps refuse without charge; owner KO / match end / reset clear constructs; invisible enemies are omitted from the snapshot and never auto-targeted.

## TEAM FIXTURES / DISCORD / SELF-KO OUTCOME

`TEAM_MATCH` START(size 1–3) → JOIN (real players) → BEGIN (synthetics fill; deterministic classes; passive or simple approach-and-strike AI). All-opponents-KO wins, no in-round respawn, simultaneous elimination = `DRAW_DEV` (no winner, no reward); spectators are not combatants. Discord Mark: enemy or explicitly confirmed ally, 4 s; the marked actor's VOLUNTARY strikes may hit original teammates at 50 % (snapshot at commit, both actors logged), team identity / aim / healing eligibility unchanged, never self, disabled outside matches. Last Pulse: 2.5 s × class; interrupted before commit → neither effect; at commit self-KO (a committed cost) + burst resolve in one batch before victory evaluation → DRAW_DEV verified when both teams fall together.

## OD-29 / REPLAY REGRESSION

Team extension (development): any active combatant disconnect pauses the whole round (casts, statuses, cooldowns, entities frozen); duplicate notices keep the original deadline; resume only when all missing return; a late reconnect after 10 elapsed seconds with zero simulation ticks ends the match NO_CONTEST exactly once (no winner, no reward); disconnect expiry is never a draw. Duel-lab OD-29, RECOVERY_HELD_DEV, receipts, deduplication unchanged (5A 46/46 · OD-29 26/26 · 5B 46/46). Ruleset freeze: `RULESET_FREEZE` journal record per new match; crash + replay with a mutated live table and a poisoned fresh clock reproduces damage, costs, statuses, entities and match state with zero inconsistencies and never reads the clock; a journal missing the record reports `RULESET_MISSING` (unresolved, HELD). QA replay issues no rewards.

## ACTUAL BROWSER EVIDENCE

Driver: `26_LOCAL_AUTHORITY/evidence/play/cdp_playtest.mjs --rules` — the installed headless Chrome driven through the DevTools protocol with real key, mouse and multi-touch events (`Emulation.setDeviceMetricsOverride` / `setTouchEmulationEnabled` for the phone layouts); the visible class picker, category tabs, chips (tap / long-press), ATTACK / GUARD / RESET buttons and keys were used; state read back through the page's read-only `window.MAHWORLD_PLAY` handle; step screenshots + `transcript.json` per layout under `evidence/play/cdp_rules_<layout>/`, batch log `cdp_rules_runs.log`. Chrome emulation, not a physical phone.

| run | class picker (five profiles through the HUD) | tabs / chips / details | strike hit & miss | guards vs mixed incoming | heal (BAGE Mend) | Mist zone / Ankle Load | niche fixture in a 1v1 | reset | errors |
|---|---|---|---|---|---|---|---|---|---|
| desktop 1600×900, keyboard + mouse, two rounds | TITAN hp 1100 ⚡95 PM0 TITAN_SHOULDER, BAGE hp 1150 ⚡120 PM0 PM_PUSH, VISIONARY hp 1020 ⚡110 PM0 PM_PUSH, LEAN hp 900 ⚡100 PM0 PM_PUSH, ATHLETE hp 1000 ⚡100 PM0 PM_PUSH | category PHYSICAL_MAGIC / PM_PULL selected by tap with 0 casts; long-press panel opened True; J after closing cast once | hit 1000→916; out of range refused | physical guard took 34.2 · magical guard took 47 (same mixed hit) | aim None · None · ally None→None | zone True · following slow True | match ACTIVE · invisible True · immune True · enemy marked True | 2 reset(s) → hp 1150 ⚡120 entities 0 statuses 0 xp 0 | 0 |
| portrait 390×844 (emulated touch, DPR 3) | TITAN hp 1100 ⚡95 PM0 TITAN_SHOULDER, BAGE hp 1150 ⚡120 PM0 PM_PUSH, VISIONARY hp 1020 ⚡110 PM0 PM_PUSH, LEAN hp 900 ⚡100 PM0 PM_PUSH, ATHLETE hp 1000 ⚡100 PM0 PM_PUSH | category PHYSICAL_MAGIC / PM_PULL selected by tap with 0 casts; long-press panel opened True; J after closing cast once | hit 1000→916; out of range refused | physical guard took 34.2 · magical guard took 47 (same mixed hit) | aim None · None · ally None→None | zone True · following slow True | match ACTIVE · invisible True · immune True · enemy marked True | 1 reset(s) → hp 1150 ⚡120 entities 0 statuses 0 xp 0 | 0 |
| landscape 844×390 (emulated touch) | TITAN hp 1100 ⚡95 PM0 TITAN_SHOULDER, BAGE hp 1150 ⚡120 PM0 PM_PUSH, VISIONARY hp 1020 ⚡110 PM0 PM_PUSH, LEAN hp 900 ⚡100 PM0 PM_PUSH, ATHLETE hp 1000 ⚡100 PM0 PM_PUSH | category PHYSICAL_MAGIC / PM_PULL selected by tap with 0 casts; long-press panel opened True; J after closing cast once | hit 1000→916; out of range refused | physical guard took 34.2 · magical guard took 47 (same mixed hit) | aim None · None · ally None→None | zone True · following slow True | match ACTIVE · invisible True · immune True · enemy marked True | 1 reset(s) → hp 1150 ⚡120 entities 0 statuses 0 xp 0 | 0 |

Skills executed in the browser: PA_PUSH / PA_PULL selection and strikes, PM_PUSH incoming (mixed) behind both guards, ATHLETE_VECTOR incoming (dissipation is Node-verified), BAGE_MEND, BAGE_MIST, VISIONARY_ANKLE, BAGE_VANISH, BAGE_SHELL, BAGE_DISCORD (enemy target) inside a 1v1 with a passive synthetic; the class picker cycled Titan / BAGE / Visionary / Lean / Athlete with the expected profiles and chips. Earlier attempts are preserved unchanged (`cdp_rules_first_attempt/`, `cdp_rules_second_attempt/`, `cdp_rules_third_attempt/`): they found three real page defects, each fixed with a small change — chips rebuilt every tick turned taps into long-presses and dropped tab clicks (structure now built once, pointer handling delegated), the details panel rebuilt every tick so its close tap was lost on touch (now rebuilt only when the skill changes), and the landscape control centre could reach the stick / the details card covered the ATTACK button (bounded height, left-anchored card). One cosmetic leftover: in portrait the collapsed DEV PANEL summary overlaps the control centre's lower text (no functional effect).

**Visually reviewed by me (screenshots read back):** tabs / chips / cast bar / details card, class labels on the stand-ins, zone disc, projectile, KO / invisible / immune / discord marks, portrait and landscape layouts.

## ASSET SLOT AND LIMITATIONS

Stand-in only through `lab/AssetSlot.js` / `PresentationAdapter.js` (new presentation kinds: projectile, zone, trap, impact; KO / invisible / immune / discord flags). No released GAME-TEST EXPORT exists; native checks PENDING; Astra folders not scanned. Limitations: in portrait the collapsed DEV PANEL summary overlaps the control centre's lower text (cosmetic); development balance only; synthetic AI is deliberately simple; the practice dummy is passive except on request; Rally / Set Position / Brace / Iron Pulse / Chain / Barbell / Bolt / Echo / Tether outcomes were not individually asserted; touch evidence is Chrome emulation; the details panel is a single-skill card (no per-gesture animation); the two-client team path was exercised in Node (real PLAYER_A + PLAYER_B teammates), not in a two-browser screenshot.

## PROPOSED DEFAULTS / OPEN DECISIONS

Adopted development defaults (ledger DECISION 2026-09-14 · answers 17–23): every table value, melee unit 2.5 m, 60/40 split, 90 % guards / 120° cone, poise threshold 6 %, fizzle 25 %, construct caps, synthetic AI, DRAW_DEV, incoming-fixture mapping. Two flagged readings: Visionary "not allowed to field" = no healing; Lean 80 % = 80 % of ATHLETE's matching magic. Open: workout multiplier, specialization vs reset, 4→8 unlock schedule, progression / unlocks, final energy and skill names, parry tuning, form-cost reference, airborne PvE policy, live-loadout changes, shipping topology, host-failure outcome, BAGE's remaining trap slots, Last Pulse final unlock, OD-02 / OD-21.

## CHANGED FILES

| file | before (end of the mechanics-field stages) | after |
|---|---|---|
| `26_LOCAL_AUTHORITY/play/PlayMode.js` (rules engine delegation, FIELD attach / detach, casts, dash / movement / guard / flight hooks, session events) | `da4c4bcf…` | `c5917df5…` |
| `26_LOCAL_AUTHORITY/Protocol.js` (+ RULES_SELECT / RULES_CAST / RULES_CLASS / RULES_NICHE / TEAM_MATCH / FIELD_ALLY, MIXED incoming) | `3b7292cf…` | `10f070cb…` |
| `26_LOCAL_AUTHORITY/DuelHost.js` (RULESET_FREEZE journal + replay, session hooks, `dev.play_internals`) | `423c92f5…` | `f68bceb4…` |
| `03_RESOURCES/Resource.js` (optional per-instance max / regen / delay overrides; defaults unchanged) | `3fe289ef…` | `9e77d322…` |
| `00_CORE/dev_tuning.dev.json` (FIELD room → rules field, dummy position) | `09ff6dd4…` | `84e8399f…` |
| `26_LOCAL_AUTHORITY/lab/play.js` (rules HUD wiring, cast routing, entity rendering, flags) | `b3183f42…` | `5fbb68f3…` |
| `26_LOCAL_AUTHORITY/lab/play.html` (control-centre containers + CSS) | `0ba5870d…` | `e3014e70…` |
| `26_LOCAL_AUTHORITY/lab/PresentationAdapter.js` (projectile / zone / trap / impact kinds; KO / invisible / immune / discord flags) | `a478ae6d…` | `c00b9a2f…` |
| `16_TESTS/gameplay_play_sample.test.mjs` (§14 rewritten for the rules field) | `eb08d94c…` | `59df3698…` |
| `26_LOCAL_AUTHORITY/COMMAND_AUTHORITY_CONTRACT.md` (append-only addendum) | `6dfa8788…` | `c32cc40a…` |
| `CLAUDE_GAMEPLAY_FOUNDATION/15_OPEN_DECISIONS/GAMEPLAY_OPEN_DECISIONS.md` (append-only DECISION line) | `764b9580…` | `1012cb52…` |
| `26_LOCAL_AUTHORITY/evidence/play/cdp_playtest.mjs` (+ `--rules` sequence) | `5d4c8fee…` | `2ad4ad32…` |

New: `26_LOCAL_AUTHORITY/play/rules1723/RulesField.js` `fa38f403…` · `RulesMath.js` `10387440…` · `rules_17_23.dev.json` `ab383643…` · `packet/*` (read-only copy of the owner packet) · `lab/rulesHud.js` `dc30f8bd…` · `16_TESTS/gameplay_rules_17_23.test.mjs` `65ca0ecc…` · `evidence/play/cdp_rules_*` · this report. Untouched (hash-verified earlier, not edited now): `DurableStore.js`, `RewardReceipts.js`, `LabClient.js`, `MahlocoAdapter.js`, `EccentricRuntime.js`, `DuelSession.js`, `lab/index.html`, `lab/lab.js`, `AssetSlot.js`. `DEV_DATA_ONLY` still absent. Memory / resume updated. Rollback: restore the twelve modified files from the "before" hashes and delete the new files.
