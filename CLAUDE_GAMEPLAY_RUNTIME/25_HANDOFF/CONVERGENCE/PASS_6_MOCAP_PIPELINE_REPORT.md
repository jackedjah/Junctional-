PASS 6 — SPEC F MOCAP PIPELINE (the in-repository part) · 2026-09-18 · STATUS: built and self-checked; the GATE is Jah recording

Report separation: (1) in progress before the addendum = PASS 5 (Spec E) measurement slice, still open on two owner decisions; (2) appended by the addendum = nothing for this pass; (3) completed now = the clip manifest, the validator, the import helper, the one-page capture checklist, the Control Lab placeholder listing, the test that proves what the validator checks; (4) remaining = Jah’s recordings (outside the repo), then validate → render → repeat the affected Spec E checks; PASS 7–9.

WHAT I BUILT (no external accounts, no purchases, no credits, no new captured material claimed):
  26_LOCAL_AUTHORITY/lab/mocap/clip_manifest.json   ONE manifest (shipped in the static build): the DISCOVERED skeleton (the 38 joints of dev_0.14, identical on every class rig), the track convention (Euler XYZ radians on the identity-bind rest axes = what RigAnimator.setLayer / setDonorClips consume; ROOT translation for root-motion clips), masks (UPPER / LOWER_SPLIT / LOWER_FUSED / FULL_SPLIT / FULL_FUSED / UPPER_DONOR), the human → MAH bone-map template (Mixamo / Rokoko / DeepMotion names; feet → tips, toes dropped on purpose — this species hovers), the loop convention (the repeat frame is carried), and the capture list v1 (24 clips: locomotion 8, six gym patterns × regression / base / progression + strike, flight 4, guard, hits, knockdown / get-up, cast staging, finish) with state · pattern · tier · loop · root-motion flag · contact frames · cancel windows · status. Statuses: 22 MISSING, 2 PLACEHOLDER (the S10 Tripo donor clips `cast_a_spell` / `hit_to_head` — upper-body-only stand-ins from another body, tagged with their real coverage `UPPER_DONOR` and the mask a recording must cover), 0 CAPTURED (that word is the owner’s after the render review; the import helper can only write VALIDATED).
  26_LOCAL_AUTHORITY/mocap/validate_clip.mjs        `node mocap/validate_clip.mjs <clip.json|clip.glb> --id <ID> [--json]` — what it ACTUALLY checks: bone map complete for the clip’s mask · no missing / non-finite track values · no T-pose (identity-rest) frames · contact frames (FK on the rig’s rest skeleton: the tips’ world height per frame, contacts = local minima; a locomotion clip needs ≥ 2 per leg with ≤ 5 cm height spread) · loop seams (first = last frame ≤ 0.05 rad per bone, root height ≤ 2 cm) · duration ≥ the manifest minimum, fps ≥ 24. Reads the runtime clip JSON or a GLB whose animation targets MAH joint names (a Blender retarget export). Its verdict says "structure only — motion quality is not judged here".
  26_LOCAL_AUTHORITY/mocap/clip_io.mjs               shared reader (GLB animation → Euler frames, quaternion helpers, rest-skeleton FK); no three.js.
  26_LOCAL_AUTHORITY/mocap/import_clip.mjs           `node mocap/import_clip.mjs <retargeted.glb> --id <ID> [--register]` — runs the validator first and REFUSES a failing clip; writes the runtime clip JSON (the S10 donor-clip shape the animator already plays); `--register` sets the manifest entry to VALIDATED with the measured contacts. Retargeting itself stays where Spec F puts it (AI mocap → Blender clean-up → retarget onto the Mr. Mah skeleton, outside the repo; or three.js SkeletonUtils.retargetClip with the manifest’s bone map in the browser) — the helper ingests the result.
  26_LOCAL_AUTHORITY/mocap/make_checklist.mjs → CAPTURE_CHECKLIST.md   one page, plain words, generated from the manifest: setup (takes, framing, slate), every clip with its duration / loop note / tiers / what to do, placeholders named as placeholders and kept on the list, and what is NOT on Jah (no accounts or purchases from Claude Code).
  lab/play.js (Control Lab MOTION → "mocap clips")   reads the shipped manifest and shows "0 recorded / 24 wanted · 2 PLACEHOLDER: HIT_SMALL (Tripo preset biped hit_to_head), CAST_STAGING (Tripo preset biped cast_a_spell) · 22 MISSING (Jah records)" — verified in the built page (`?lab=1`).
  16_TESTS/gameplay_mocap_pipeline.test.mjs (18 checks)   the manifest fields and honest statuses; the skeleton is the discovered rig; Spec F v1 coverage; the validator on synthetic clips — a well-formed walk loop passes, a single T-pose frame fails (named), a dropped joint fails the bone map (named), a NaN fails the tracks, a broken seam fails, a root-motion clip without ROOT_POS fails, a 5 s idle fails the 60 s minimum; the two real placeholders pass for the mask they actually cover; the import helper refuses a failing clip and leaves the manifest untouched, and writes a passing one in the runtime shape; the checklist is the generator’s own output; the Control Lab line and the build copy exist.

SELF-CHECK (brief §PASS 6, verbatim items): Spec F manifest fields → PASS · complete bone mapping (the template + the per-clip mask check) → PASS · missing tracks → PASS (checked per bone per frame) · T-pose frames → PASS (checked) · contact-frame validity → PASS (FK-derived, reported per tip) · loop seams → PASS (checked with the stated convention) · truthful PLACEHOLDER identification in the manifest and Control Lab → PASS. Demonstrated: what the validator proves is structure; it does not and cannot prove motion quality — the render and Jah’s eye do.

HUMAN GATE — the capture checklist (`26_LOCAL_AUTHORITY/mocap/CAPTURE_CHECKLIST.md`): recordings already supplied = NONE (the only motion files in the repo are the Tripo donor presets, which are placeholders); requested = the 22 MISSING clips (locomotion first: LOC_IDLE_CALM, LOC_WALK, LOC_START, LOC_STOP, LOC_TURN_IN_PLACE — these unlock the Spec E foot-slide / idle / transition checks on real motion). When GLBs come back: `validate_clip` → `import_clip --register` → render → rerun `probe_motion_e.mjs` / `probe_walk_identity.mjs` on the clip-driven motion before any "complete" claim.
RESOURCES: node 22 (validator, helper, checklist); the dev_0.14 rig GLB (read only, FK rest skeleton); the S10 Tripo donor JSONs (read only, validated as placeholders). Rokoko / DeepMotion / Move.ai / Blender: NAMED for Jah’s outside-repo step, NOT USED here (no accounts, no credits, Blender slot still reserved).
