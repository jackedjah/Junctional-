# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-24 (post-M6 quality continuation active)

## Post-M6 quality continuation — protected private candidate released
- New visible build `ASTRA_M6Q_PRIVATE_20260924`; immutable Netlify draft `6ab594d507b647b41d599401`: `https://6ab594d507b647b41d599401--mahworld-test-preview.netlify.app`. API state is `ready`, context `deploy-preview`, `published_at = null`; one edge gate is present.
- Exact package: 181 entries / 122,976,086 bytes / SHA-256 `72e754ac8e6a9587531e8e3bac4a6b67ff154533dd79b2a799b44a8e0eff5c06`. The single authorized pipeline passed 56 programs / 1,518 checks, 104-file syntax, boundary/secret scans, ZIP integrity, exact fresh extraction, 7/7 extracted smoke and extracted syntax.
- Hosted protection is verified: play `401` password page, private asset `401 application/json`, wrong password refused with attempts remaining, and the served page contains no password value. `MAHDEMO_PW` was unavailable, so authenticated hosted gameplay is explicitly **NOT RUN**.
- Production remains deploy `6aac25101f17ce5ed37166e4`; M6 remains ready/unpublished at `6ab586ec9ad382fa1b83f1be`; M5C remains ready/unpublished at `6ab454bf3a60c4bf88b1334a`. None was promoted, replaced or altered.
- Release source checkpoint before packaging is `abae621c0c40c4b14cb3985377dcd6e434aa7c52`. Durable release record: `astra_post_m6_quality/private_candidate_release.json`. Physical-phone acceptance and the two recorded presentation limitations remain owner/open.
- **Next exact action:** owner phone review of this private candidate. Do not promote production without a new explicit instruction.

## Post-M6 quality continuation — checkpoint 02 world/gameplay presentation
- GitHub continuity: checkpoint 01 is verified at `80b96391c29e275cdac1c3b238b713b90c5e15d3` on `backup/mahworld-m6-20260924T190351Z`. M6, M5C, production, assets, JOB A/B and Character/MAHFITT/S08 boundaries remain preserved.
- The foreground pass is now joined by lower/longer atmospheric cloud carriers, a subdued connected distant cloud bank in place of the bright circular skyland strip, shouldered far massifs/towers, continuous bridge arches, and added macro-ridge strata. The registered actual Moon remains authoritative.
- Equipment presentation now samples the source-rig HAND→FINGERS span for the visible palm center while retaining the host grip/ownership/projectile authority. A representative physical-MAHGIC barbell pose received bounded presentation tuning; damage, costs, cooldowns, cadence profiles and exercise selection are unchanged.
- One bounded NIGHT visual/action run completed 11 views with zero console/runtime errors. It rechecked the floor/junction, Moon/cloud/horizon, source Dogkie, blue MAH GYM/canopy aura, resource break + two drops/refills, complete L0 equipment and representative physical/physical-MAHGIC actions. Static build remains clean at 180 files / 207.1 MB; the preserved focused checks are 98/98.
- Honest visual remainder: after two focused pose attempts, the accepted `PA_PUSH` load still places the bar shaft through the rear neck/head silhouette even though two-hand midpoint/grip metrics are zero-error. The close resource prompt is readable and no longer clipped but remains visually prominent. These are recorded, not hidden or called complete.
- Next: push this checkpoint, then run the authorized release gate/package exactly once from the preserved source and deploy one new protected unpublished candidate. No production promotion or physical-phone claim.

## Post-M6 quality continuation — checkpoint 01 foreground surface/resource presentation
- Continuity: work continues from the verified M6 release/recovery tip `fa5576a0cb988eb226a880340aeeff8e0f9cdb98` on `backup/mahworld-m6-20260924T190351Z`. The M6 candidate, production, current assets, JOB A/B requirements, and Character/MAHFITT/S08 boundaries remain unchanged.
- `terrain.js` keeps the authored one-owner 128-segment junction topology, but reduces the region-overlay dominance and adds subtle world-space satin grain/veining to opaque road cores. The causeway now reads as a reflective constructed surface instead of a flat saturated slab; no collider, route, ownership, width, or junction height changed.
- `gear.js` keeps all 41 authoritative resource nodes and the existing attack/drop/refill logic, but restrains white/emissive clipping, adds denser low-growth color variation and a softly lobed geology bed, and limits the instructional sprite to a smaller near-only view (`16 m`, five-metre fade).
- Minimal check only: edited JavaScript syntax and `git diff --check` pass; the static build reports 180 files / 207.1 MB and a clean private-content scan. The reused M6 floor/junction view completed with zero unresolved/runtime errors; inspected image SHA-256 is `fcff1f60623dab7aec91b931ec96c0816f0e6e1875421a8224ec52b1a184c9de`.
- Next: propagate the same premium restraint into the cloud/horizon shell, then run the bounded matched-view gameplay/gear check. No package or deployment has run at this checkpoint.

## M6 protected private preview — owner phone review next
- New visible build `ASTRA_M6_PRIVATE_20260924`; immutable Netlify draft `6ab586ec9ad382fa1b83f1be`: `https://6ab586ec9ad382fa1b83f1be--mahworld-test-preview.netlify.app`. API state is `ready`, context `deploy-preview`, `published_at = null`.
- Exact package: 181 entries / 122,974,744 bytes / SHA-256 `82d5433350d9785e665fe9b5b74b88e07ddbf2c40a1c9e36c3a1a4fea87ab02c`. The mandatory pipeline passed 56 programs / 1,518 checks, 104-file JavaScript syntax, secret/private-boundary scans, ZIP integrity, exact fresh extraction, 7/7 extraction smoke, and extracted JavaScript syntax.
- The extracted package also passed the existing bounded M6 runtime proof with 0 console errors / 0 unresolved items: resource bush broken, three drops and three refills collected; two dumbbell palm errors `0`; barbell midpoint error `0` at `0.68 m` hand span. The ordinary-controller junction proof reached within `0.009 m` of `CW_GYM / RP_FOREST_W` with no console errors.
- Protection verified independently: play is `401` with the password form, a representative asset is `401 application/json`, a wrong password is refused, and no password string is served. `MAHDEMO_PW` was unavailable, so authenticated hosted READY/gameplay is explicitly **NOT RUN**.
- Production remains deploy `6aac25101f17ce5ed37166e4`; M5C remains ready at deploy `6ab454bf3a60c4bf88b1334a`. Neither was promoted, replaced, or altered. The M6 ZIP remains local and is not committed; its reproducible record is `astra_m6_release/private_candidate_release.json`.
- Scoped release source/evidence checkpoint `4c67128617fde811c3bdc506b74f2ae8fdfa3157` is pushed on `backup/mahworld-m6-20260924T190351Z`; it is a fast-forward descendant of verified pre-release checkpoint `6fa4e33e9ee4d35171bf3eaa4cff5606c559999e`.
- **Next exact action:** owner physical-phone review only. Suggested path: repaired junction → blue-aura MAH GYM → attack/collect one crystal bush → dumbbell/barbell grip plus physical/physical-MAHGIC action → Moon/cloud/Dogkie view. Do not promote to production without a new explicit instruction.

## Emergency recovery checkpoint — verified remote backup + bounded M6 junction closure
- GitHub recovery destination is the verified **PUBLIC** repository `https://github.com/jackedjah/Junctional-`, branch `backup/mahworld-m6-20260924T190351Z`. It is a scoped recovery branch; default/production branches were not rewritten or merged.
- Source checkpoint `bdc43b60d56c22253aeddf2eac996a5394128407` contains the reviewed current M1–M6 source/context snapshot, build/dependency records, handoffs, requirements and owner recovery directive. Asset checkpoint `a25277ebe6afc378af63ed19735a53f2c400327b` adds all required recovery bytes.
- Asset readback is verified from a fresh sparse clone of the exact remote commit: 98/98 files, 1,454,701,148/1,454,701,148 bytes, zero missing, zero SHA-256 mismatches and zero LFS pointers. This includes 81 runtime derivative/authoring files and 17 required original/building-source files. Restricted footage, secrets, browser/Netlify state and separate Character/MAHFITT/S08 assets remain intentionally excluded.
- The `CW_GYM` / `RP_FOREST_W` dark fragments were isolated to the terrain road junction geometry, not shadows, player, region tint or a global height issue. Clipped ribbons ended on a true circle while 20-sided inscribed frame/core junction discs left repeated dark chord gaps. `terrain.js` now uses one shared 128-segment junction/ring boundary (maximum geometric gap below 3.1 mm); draw calls, collision, route authority, floor colors and all other M6 work are unchanged.
- Local proof is intentionally bounded: fresh static build/private-content scan clean; matched NIGHT and oblique views clean; a 6.25 s ordinary-controller crossing reached `0.188 m` from the authored junction; zero unexpected console errors. Evidence: `astra_m6_closeout/junction_isolation/` and `astra_m6_closeout/junction_repair_proof/`.
- Cloud execution remains **NOT TESTED**. No package, Netlify deploy, production publish, alias/access change or physical-phone claim was made. M5C remains the hosted comparison.

## M6 focused polish — bounded closeout (current)
- Continuity: this is the same intentionally dirty M1–M5C gameplay worktree. All earlier code, evidence, packages, protected candidates, and the production deployment remain preserved. This pass did not reset, clean, stash, stage, commit, package, deploy, publish, change access protection, or enter the Character/MAHFITT/S08/Blender lanes.
- Ground cohesion: `terrain.js` now renders class influence as broad signed-distance vertex gradients on one coherent surface rather than stacked color slabs. Road pieces blend into averaged junction colors, and ambient floor shards form denser, tighter patches with fewer isolated pointy fragments.
- Resource ecology: the registry now carries 41 intentionally distributed crystal-bush patches, including the safe newcomer node and all sanctuary families. `gear.js` renders 64 compact detail-1 shards per patch in one instanced system, adds restrained material breathing, and preserves the existing attack/drop/refill/inventory authority and readable prompt/map language.
- Moon, clouds, and horizon: the registered actual Moon derivative remains authoritative and now sits high at `0.28 rad`. Low/mid/high clouds are deeper, much wider than tall, and use old-soft elliptical silhouettes while retaining analytic moonlight transmission. Night cloud highlights now key from the Moon direction rather than incorrectly retaining the daytime Sun direction; world sheets use 40-edge carriers and the close celestial veil uses 42 edges to soften visible facets. Distant ridges gained layered shoulder facets so the unreachable shell reads as a larger intentional world.
- Aura/building language: blunt horizontal façade bands were removed. The existing single-draw arcane-leakage field now also selects seven existing canopy anchors and two sparse free accents (33 anchors / 321 motes total at runtime), with canopy/free opacity reduced to `0.48`/`0.40` of the structure mist and slow `0.009–0.018` drift. It creates no second particle blanket or rectangular band. MAH GYM uses blue accents. Imported-building source derivatives, corrected scale, real entrance alignment, collision, and grand interiors were preserved; their existing materials received bounded texture anisotropy and richer environment response.
- Equipment, combat, and creatures: HIGH/MED equipment uses the existing L0 derivatives with improved bounded PBR response and the existing exact palm sockets/two-hand shaft solution; ownership/projectile/special logic is unchanged. `RigAnimator.js` now gives QUICK/HEAVY/PRECISE/FLOW/BALANCED their own anticipation/whip/finish/filter envelopes, with a small additional PHYSICAL_MAGIC readability offset instead of one shared duration; `PresentationAdapter.js` mirrors that distinction for the fallback preview. Exercise-specific pose branches, host phase/damage/cost/cooldown authority, and biomechanics remain unchanged. Dogkie again preserves its original GLB albedo/material identity; class color is confined to restrained aura/contact effects.
- Practicality: resource flora remains instanced, structure aura is one point-system draw, clouds reuse generated textures, and no new dependency or rendering subsystem was introduced. The building scale/doors/interiors, host gameplay numbers, M5C package, prior private candidates, and production were intentionally not touched.
- Minimal closeout check only, per owner direction: edited-source/probe syntax and `git diff --check` pass. One bounded local NIGHT session produced 11 frames with 0 runtime/console errors and proved a 41 × 64-shard resource field, `RB_NEXUS_INTRO` break, three drops and three `REFILL_PICKED` events, two zero-error dumbbell palm grips, one zero-error/two-hand barbell grip, and an accepted `PA_PUSH` in real `PHYSICAL_MAGIC` mode (`BALANCED:PHYSICAL_MAGIC`, envelope `0.74/0.36/0.66`, filter `14.8`). The full report and inspected frames are under `astra_m6_closeout/visual_check/`.
- Visually checked in those frames: blended class ground and the authored `CW_GYM`/`RP_FOREST_W` junction; actual distant Moon plus layered clouds; dense bush before/broken/refill states; source-color Dogkie; blue MAH GYM treatment; selected canopy motes; complete L0 equipment geometry and socketed load/release poses; and a complete fused character at rest. These are still-frame/short-sequence local checks, not physical-phone or exhaustive attack acceptance.
- Closed 2026-09-24: the recorded dark circular/angular `CW_GYM`/`RP_FOREST_W` fragments were traced to the 20-sided junction-disc/true-circle ribbon boundary mismatch and removed with the bounded 128-segment shared boundary. The old `floor_recheck` image remains baseline evidence; current proof is in `junction_repair_proof/`.
- **Next exact action:** stop for owner review. The next larger milestone is a separately authorized protected M6 preview. Do not package, deploy, promote, or publish from this checkpoint without that instruction.

## M5 owner corrections — checkpoint 03 (protected private candidate)
- New private build: `ASTRA_M5C_PRIVATE_20260923`; deploy `6ab454bf3a60c4bf88b1334a`; protected draft URL `https://6ab454bf3a60c4bf88b1334a--mahworld-test-preview.netlify.app`.
- Package is complete from the current preserved dirty worktree: 56/56 test programs, 1,518/1,518 checks, 104-file JS syntax sweep, clean private-content/secret scans, 181 archive entries, and two independent fresh-extraction smoke runs at 7/7. ZIP SHA-256 is `9eb0816871a258a408a7b453e8d4982dc851708a48eccd90e2411494ac9d1571`; package-record SHA-256 is `99fdc974b655473b9c2cea8d47353dc813752d9b939161d365f423d3528d7159`.
- Hosted protection is verified: root `303 no-store`; play route `401 text/html` with the password form; representative JS, `BUILD_INFO.json`, and actual-Moon GLB are `401 application/json DEMO_LOGIN_REQUIRED`; all carry `no-store`/`noindex`. `MAHDEMO_PW` was unavailable, so authenticated hosted gameplay is not claimed.
- Production remains deploy `6aac25101f17ce5ed37166e4`, unchanged. Prior private M5 comparison candidate `6ab3abd739fc958c75339988` remains ready and unchanged. No production promotion, alias, password removal, reset, clean, stash, stage, or commit occurred.
- Integrated route remains 20/20 at SHA-256 `55a3137e20e0f82d2e64b04a4b06a4eded7f5a1a21770bf23cebb3a75470270e`. The 900-second throttled emulated-phone soak completed READY with no context loss, runtime errors/rejections, console/page errors, fatal condition, or early termination; its supplemental 5/7 correction-coverage result is retained honestly as a probe-routing limitation, and the owner directed delivery without another investigation pass.
- Portal returns now preserve the actual entry ground as their height reference, preventing Market exits from snapping to the overlapping `23.8 m` roof deck; focused building regression is 14/14. Physical-phone acceptance remains owner-only.
- Durable records: `astra_m5_corrections/private_candidate_release.json` (SHA-256 `d33bc948b51e2b4dd89bff61654c20b4ec3ed6ab95587b9351bcada460d9a850`) and `astra_m5_corrections/checkpoint_03_protected_candidate.json`.

## M5 owner corrections — checkpoint 02 (integrated local closure)
- Continuity/preservation: work remains in the same intentionally dirty M1–M5 worktree on `master` at HEAD `3185a25f80a23131fbdb2062fca9a1a1ffb7684f`. The improved trees, M1–M5 fixes/evidence, unfinished JOB A/B queue, Character/MAHFITT/S08/Blender boundaries, immutable M5 candidate, and production deploy remain untouched. No reset, clean, stash, stage, commit, production publish, or gate removal occurred.
- OP01–OP10 are fixed and verified locally. Imported buildings use source-traced seam/border-preserving derivatives at the once-only `0.6×` scale, source-aligned entrances, and grand functional interiors; roads use one owned topology; structural proxies block reachable ridges/monoliths/building walls while preserving passes, ramps, bridges and doors; crystal bushes pass zero/partial/full/full+999 normal-play loops; the actual owner Moon derivative is sky-distant with coherent moving-cloud light.
- OP11 remains `REPORTED_NOT_PHYSICALLY_REPRODUCED_OPTIMIZED_MEASURED_LOCAL`: redundant surface work was removed and defined desktop/phone-emulated profiles were measured, but no physical-phone smoothness claim is made. OP12 is fixed and verified in emulation without replacing the improved close tree models.
- Generated authority is synchronized at world-registry SHA-256 `825f6943b093d17df65522c1b1cb398b33bf9c2790bde2183ede3376aa757f5f`: 28 base + 792 district + 9,457 world = 10,277 mapping shapes.
- Accepted runtime evidence is desktop/phone visual gates `20/20` each, interiors `26/26`, desktop owner corrections `62/62` (SHA-256 `f586b35415ef0f1939e1dacf53042c3913a96b6d09af0661a88f34b6a91663cd`), phone-emulated owner corrections `62/62` (SHA-256 `9382801a31f9d5ee25cb673da18d43f51835bbb805e27dc96d5068987e9b52c2`), and the ordinary playable route `20/20` (SHA-256 `55a3137e20e0f82d2e64b04a4b06a4eded7f5a1a21770bf23cebb3a75470270e`), with no fatal or unexpected console errors.
- Tree LOD evidence is `100/100` settled checks across gold/blue/red/pink/purple, DAY/NIGHT and desktop/phone emulation. L0/L1 share family colors, switch inward/outward at `26/30 m`, expose no impostor, and retain stable forest programs/resources.
- The Titan route now uses the normal low-flight counter at `11.46 m` for the intentionally proximity-provoked lake fish—above their launch envelope and below the Phoenix's `30 m` aggression threshold—then lands beyond the leash. It records zero defeat and a complete landing; source/report audit confirms no Titan-stage reset, heal, or placement shortcut. The report's sole accepted `PRACTICE_RESET` occurs much earlier for the explicit resource-drain fixture.
- Source honesty: the building derivatives are not exact raw parity or recovered missing originals; textures were resampled and unsupported extensions removed. Their interiors are authored functional spaces, and structural collision is bounded proxy authority rather than per-triangle collision.
- Release state: the integrated local gates are closed. The 900-second soak, package, fresh extraction, and new protected non-production deployment remain pending. Physical-phone acceptance remains Jah's. Durable record: `25_HANDOFF/CONVERGENCE/astra_m5_corrections/checkpoint_02_integrated_corrections.json`.

## M5 owner corrections — checkpoint 01 (surface / collision / refill)
- Continuity/preservation: implementation continues in the same intentionally dirty M1–M5 worktree at HEAD `3185a25`; the existing protected M5 candidate/build/package/evidence remains immutable for comparison. No reset, stash, clean, commit, production publish, password removal, Character/MAHFITT/S08/Blender edit, or source-fidelity restart occurred.
- OP05/OP06 are code-reproduced and structurally fixed pending runtime visual acceptance: authored roads now compile into one cached topology graph. T/X/Y/cross-path intersections split once, coincident pieces have one deterministic owner, named forecourts remain explicit nodes, and sampled ribbon ends meet one junction boundary rather than extending overlapping coplanar rectangles. Road cores are opaque owned surfaces.
- OP10 is code-reproduced and headless-fixed pending route captures: every reachable rendered monolith cluster now has bounded main plus two satellite proxies matching `terrain.js`; the old host mapped only the main crystal. Swept normal/full-distance tests block on a satellite while the real Titan Moon Pass centre and ramps remain traversable.
- OP09 is headless end-to-end fixed pending real pointer/touch visual proof: the existing authoritative node/drop/refill/inventory system is reused; `RB_NEXUS_INTRO` adds one safe newcomer patch, all five sanctuary lists remain populated, the bush is a denser 48-facet compact cluster with dark interior/selected glow, and existing UI now supplies a restrained nearby hint plus map diamond/legend. Zero-MAHGIC physical availability and full+999 overflow retention pass.
- Generated authority is synchronized at registry SHA-256 `5512b124…419ab9`, 251 world shapes and 1,526 mapping shapes. Focused tests pass: correction tranche `9/9`, B8 equipment `25/25`, world JOB B `34/34`, preserved M3 route `13/13`. Exact record: `25_HANDOFF/CONVERGENCE/astra_m5_corrections/checkpoint_01_surface_collision_refill.json`.
- Current next action: finish imported-building scale/fidelity/doors/interiors, then actual Moon/cloud/light and far-tree continuity/performance; perform integrated visual gates before the one full regression/soak/package/protected-deploy closure. Physical-phone acceptance remains owner-only.

## Astra M5 integration checkpoint — current working tree, read first
- Continuity: this remains the same intentionally uncommitted M1→M2→M3→M4→M5 worktree on `master` at source HEAD `3185a25f80a23131fbdb2062fca9a1a1ffb7684f`. All earlier fixes and evidence remain in place; the M1 fidelity audit was not repeated. Character, MAHFITT, S08, and Blender-owned work remains untouched. No reset, clean, stash, replacement project, production publish, or live-demo replacement occurred.
- Integration hardening: M5 adds the persisted FULL/MINIMAL HUD toggle while retaining essential vitals and controls, a visible package/build stamp, safe exterior door returns `0.55 m` outward from the wall, and defensive room teardown that detaches and uniquely disposes room-owned geometry/material/texture/skeleton resources. The same bounded interior topology rebuilds on immediate repeat entry.
- Full candidate proof: `25_HANDOFF/CONVERGENCE/astra_m5/candidate_route_release_final_green/` contains the ordinary local `static_dist` MED/NIGHT acceptance run, its report, and 35 screenshots. It passed **20/20** checks in `563,182 ms`: 54 required ordered normal-controller waypoints plus 11 auxiliary legs, all five sanctuary arrivals within `1.2 m`, zero defeat/respawn samples, and cleanup back in FIELD, grounded/FUSED, NIGHT, FULL HUD, with no Guide, equipment, cast, emote, guard, lock, menu, or confirmation residue.
- Gameplay coverage: the route proves READY/restore gating; all six real menu bodies; FULL→MINIMAL→FULL HUD persistence; fused and split exercise/form transitions; true two-dumbbell and two-hand barbell grips; authoritative projectile identity, hit/miss, return, and no-duplicate behavior; single-envelope flight braking/hover/landing; persistent manual camera orbit and façade obstruction recovery; elevated split-form travel; and accepted low-flight sanctuary transfers.
- World transactions: civic training is entered/exited twice immediately, then the Gold Temple, clothing shop, and Blue Tower complete real prompt/confirm entry and exit transactions. Each exterior return is `0.55 m`; the immediate repeat disposes all 65 room-owned objects, 60 meshes, 60 geometries, 15 materials, and 2 textures and leaves the room-owned group at zero before rebuilding the same room key/topology.
- Resources/ecology/time: literal zero Combat MAHGIC remains zero without passive regeneration; three physical resource harvests refill MAHGIC then inventory, and inventory USE restores a later spend. Contiguous observations prove Dogkie, Visionary Phoenix, Bage fish, and passive-horse motion. NIGHT→DAY→NIGHT kept host simulation and cloud drift moving; DAY p95/max was `19.2/261.7 ms`, NIGHT `34.8/43.4 ms`, and shader programs stayed `168→168` for both switches.
- Error honesty: acceptance recorded 0 unexpected runtime errors, 0 page errors, and 0 console errors. Its raw runtime log retains 13 expected favicon/older-avatar-manifest fallback 404 lines, explicitly filtered by the probe and non-blocking to READY.
- Sustained emulated-phone evidence: `25_HANDOFF/CONVERGENCE/astra_m5/soak_phone_emulated_final/stability.json` is the final `900 s`, `4×` CPU-throttled, MED-tier `393×852` run with 828 samples and 35 coverage phases. It ended READY with no context loss/restore, authority errors/rejections, page errors, console errors, fatal condition, or early termination. Four benign Direct3D shader warnings were retained.
- Performance/device honesty: the final throttled soak frame distribution was p50/p95/p99/max `66.6/100/149.4/5179 ms`; the maximum is the startup gap. This proves sustained functional stability under emulated stress, not 30 fps, physical GPU/memory/thermal behavior, or zero retained-resource growth. Physical-phone acceptance remains pending Jah's actual test.
- Remaining scoped limitations: route travel proves the exact controller waypoints and collision path, not every metre of every road footprint. Resistance band behavior remains intentionally manifest/render/dismiss-only because no owner-approved band move set exists. Renderer-wide counters remain diagnostic because compared samples occupy different world/room states; only room-owned teardown is asserted exactly. Raw ornate-temple parity remains unavailable because its original source GLB is absent.
- **Next exact action:** Jah opens the protected candidate URL below on the real phone and reports acceptance. Do not promote it to production without a separate explicit instruction.

## Astra M5 protected private candidate — final release checkpoint
- Build/package identifier: `ASTRA_M5_PRIVATE_20260923`. Archive: `26_LOCAL_AUTHORITY/deploy/packages/ASTRA_M5_PRIVATE_20260923.zip`; record: `26_LOCAL_AUTHORITY/deploy/packages/ASTRA_M5_PRIVATE_20260923.PACKAGE.json`; 175 entries / 65,008,929 bytes / SHA-256 `ebcad0ed993899f66c7477a1b3f1cde57740b32047fd0c02b60cf71e85055995`.
- Packaging verification: the complete pipeline passed 51 test programs / 1,461 checks; generated collider/registry checks, 102-file JavaScript and CSS checks, private-content/boundary/secret scans, archive verification, built-in fresh-extraction smoke `7/7`, second fresh-extraction smoke `7/7`, and extracted syntax checks all passed. The package records the intentional dirty M1–M5 worktree and base HEAD `3185a25` rather than mislabeling the base commit as the complete source.
- Protected NON-PRODUCTION draft: deploy `6ab3abd739fc958c75339988` → `https://6ab3abd739fc958c75339988--mahworld-test-preview.netlify.app`; Netlify site `968f80e5-889b-43a8-b2cd-5860c588546a`; state `ready`; context `deploy-preview`; `published_at = null`; edge protection active. No alias was created.
- Gate verification: root redirects with `303` and `no-store`; `/play` returns password-form HTML `401` with `no-store`; representative JavaScript, `BUILD_INFO.json`, and GLB requests return `DEMO_LOGIN_REQUIRED` JSON `401` with `no-store`. Correct-password hosted loading was not run because `MAHDEMO_PW` is unavailable in this environment; only the unauthenticated protection boundary is claimed remotely.
- Production remained unchanged before and after deployment: published deploy `6aac25101f17ce5ed37166e4`, published at `2026-09-17T17:36:28.139Z`. No production promotion, live-demo replacement, alias change, or gate removal occurred. An initial unpublished draft that omitted the edge function was rejected and deleted before its URL was shared.
- Final interaction evidence is desktop targeting `25/25` and mobile targeting `25/25` with no direct-lock fallback, page/runtime errors, or console errors. Floor hard-flip maxima at 24-bit depth were desktop `0.005% / 0.286% / 0.201%` and phone-sized `0% / 0.311% / 0%` for causeway / plaza edge / gym forecourt.
- Remaining acceptance: Jah's physical-phone test is still required. The 4× CPU soak is not a physical fps/GPU/memory/thermal claim; renderer-wide samples do not establish zero retained growth; exact route waypoints do not prove every metre of every road footprint; band behavior remains manifest-only pending owner-approved moves; raw ornate-temple parity remains unavailable.
- Durable release record: `25_HANDOFF/CONVERGENCE/astra_m5/private_candidate_release.json`.

## Astra M4 checkpoint — current working tree, read first
- Continuity: this is still the same intentionally uncommitted M1→M2→M3→M4 worktree on `master` at source HEAD `3185a25f80a23131fbdb2062fca9a1a1ffb7684f`. M1–M3 code and evidence are preserved; the M1 fidelity audit was not repeated. Character, MAHFITT, S08, and Blender-owned files remain outside this lane. No reset, clean, stash, new project, live replacement, or production publish occurred.
- Mainland/world contract: the real field remains exactly `350 × 348.5 m = 121,975 m²`, now covered by five complete sanctuary contracts (Athlete, Titan, Lean, Visionary, Bage). The rendered path hierarchy is one connected network of 31 paths plus three explicit physical bridge links. It retains the M3 default `QR_MOONLIT_TITAN_CIRCUIT` exactly and adds `QR_FIVE_SANCTUARY_NIGHT_CIRCUIT` only as the M4 review route.
- Sanctuary completeness: every contract resolves an arrival, visible heart/building identity, water with fish, bounded Dogkie territory, exactly two horses, a Phoenix anchor, resource nodes, vertical element, and NIGHT identity. The Bage ordered gardens now have an eight-record population. The full 54-record Dogkie set is a dormant pool, capped at 24 awake globally and 12 per sanctuary; the separate aggression cap is unchanged.
- World truth/readability: all three canal crossings are explicit collision-backed graph links; six bridge landings have wayfinders. The four original civic plaza façades now own real doorway transactions and bounded rooms (`CIVIC_TRAINING_HALL`, `CIVIC_ARENA_HALL`, `CIVIC_MENTOR_HALL`, `CIVIC_EXCHANGE_HALL`); the Exchange is information-only and adds no economy. There are 14 registered buildings and 10 authoritative ENTER doors. Street fixtures follow the actual selected path polylines. The full mainland map derives its bounds from `field.walls`, uses collision-aware labels, and no longer clips the north/edge regions.
- Terrain/sky authority: region colour is one single-owner 8 m field tessellation with water holes rather than overlapping tint sheets. Low/mid gray-blue cloud bodies depth-test/write and occlude; high cirrus remains a light non-occluding veil. `NORTH_FALL` is explicitly scenic-only/unreachable instead of masquerading as gameplay water.
- Generated authority: `world_registry_v1.json` SHA-256 `2b3a3f2f34b1c9d1ab59be3fc0726f5a1bec69e0ce577fd228ab03951c24ba6c`; generated world mapping is synchronized at 219 world shapes across 31 zones (1,494 total mapping shapes = 28 base + 1,247 district + 219 world). Placement is 121 trees / 160 fixtures; static output is 174 files / 93,524,282 bytes with 24 derivatives / 41.5 MB and a clean private-content scan. `BUILD_INFO.json` now reports the current M4 contract and registry hash instead of the stale B8 counts.
- Proof: `25_HANDOFF/JOB_B/evidence/astra_m4/after/` contains 30 real-runtime sanctuary/road/bridge/building/map captures, the normal no-debug phone-sized Titan route capture, `sanctuary_proof.json`, and `frametime.json`. Result: world READY; phone navigation reached in one pass; 0 page errors; 0 console errors. Targeted regression is M4 `12/12`, preserved M3 `12/12`, interiors `24/24`, world JOB B `34/34`, creature combat `22/22`, and rules 17–23 `60/60`.
- Performance honesty: desktop-relative world-on p50/p95 is plaza `16.4/22.8 ms`, canal run `20.5/32.0 ms`, forest `16.8/24.6 ms`; stored pre-M4 values were `16.5/24.2`, `22.4/34.0`, and `17.6/25.0 ms`. This is not a physical-phone claim. Physical-phone acceptance remains owner-only.
- GitHub add-on reuse: no new dependency was introduced. The existing pinned Three.js `0.185.1` MIT vendor plus same-revision loaders/utilities and the already-integrated meshoptimizer derivative workflow remain the reused implementation base.
- **Next exact action:** M5 only: full regression, sustained route/load validation, clean-extraction packaging, protected non-production Netlify draft deployment, and live-deploy before/after verification. The owner explicitly superseded the earlier no-package/no-deploy hold for this tested private candidate only. Never use `--prod`, never replace the live demo, and never remove the password gate.

## Astra M3 checkpoint — current working tree, read first
- Continuity: this is the same intentionally uncommitted M1→M2→M3 worktree. All M1 fidelity fixes/evidence and all M2 motion/equipment fixes/evidence remain in place. No source-fidelity audit was repeated; no restart, replacement project, character-source edit, commit, package, deployment, or live publish was performed.
- Complete route: the actual playable-world default quality route is `QR_MOONLIT_TITAN_CIRCUIT`: civic forecourt (`FLOOR`) → Tower Causeway (`ROAD`) → `FOREST_NE` / `RB_NE_PATH` and its attackable blue resource patch (`FOREST_RESOURCE`) → fish-populated `TITAN_LAKE` with four shores and a wade bed (`POND`) → the collision-backed, two-ramp `TITAN_MOON_PASS` between four mountain monoliths (`MOUNTAIN`) → the named `MAH MATCH` forecourt, `MATCH_DOOR`, and enter/exit-capable `MATCH_HALL` (`BUILDING`) under the live night sky (`SKY`). It is one continuous real route, not a teleport or replacement navigation layer.
- Night-first quality: first-visit and route preview now default to `NIGHT`; URL and saved player choice still take precedence. The READY gate compiles both `DAY` and `NIGHT` world programs without persisting or visibly presenting the transient warm state, then restores the selected initial state. The normal world map overlays the route on the same live roads and landmarks.
- World authority: generated registry, client ground math, host movement, and `world_v1_colliders.json` agree on the `2.8 m` pass top, monotonic north/south ramps, lips, four mountain walls, route roads, forest/resource node, pond, and building door. Collider generation is synchronized at 223 world shapes / 1,498 mapping shapes against the exact registry hash.
- Rendered proof: `25_HANDOFF/JOB_B/evidence/astra_m3/` contains `m3_night_route_report.json` plus eleven captures from the forecourt/map through road, forest/resource, lake, pass approach/top, MAH MATCH forecourt/interior, DAY prewarm proof, and the final NIGHT route. The probe is **11 passed / 0 failed**: fresh page starts NIGHT; prewarm completed `DAY,NIGHT` and restored NIGHT; terrain/roads, 125 trees, five water bodies, two ridge bands, atmosphere, signage and hall were live; the controller walked the real pass and building approach; MATCH_HALL enter/exit worked; DAY→NIGHT caused no continuing shader-program surge; zero unexpected runtime/console errors.
- Regression proof: all **49 / 49** test programs under `16_TESTS` pass, including core `125/125`, M2 equipment/motion `9/9`, B8 equipment `25/25`, targeting/Phoenix `12/12`, M3 route `12/12`, creature combat `22/22`, and world JOB B `34/34`. Static-host smoke is `7/7`; collider synchronization and edited-script syntax checks pass. The transferred lab server now resolves its read-only Three.js runtime from the preserved in-project vendor copy instead of a stale machine-specific path. The existing local static output remains 174 files / 89.1 MB with 24 derivatives / 41.5 MB and a clean private-content scan.
- Device honesty: desktop rendering and phone-sized emulation are local evidence only. Physical-phone acceptance remains open to the owner.
- **Next exact action:** preserve this M3 checkpoint for owner/device review or the next explicitly assigned milestone. Do not create a release package, draft deployment, or live publish until the owner expressly authorizes it. The missing raw ornate-temple source remains the precise M1 provenance gap.

## Astra M2 checkpoint — current working tree, read first
- Continuity: this is the same repository/worktree and uncommitted continuation of M1. Every M1 source-fidelity repair and evidence artifact is preserved; no source-fidelity audit was repeated, and there was no reset, replacement project, character-source edit, package, deployment, or live publish.
- Motion and deformation: split-leg idle/run/flight now preserves independent leg transforms and measured hip→knee / knee→tip lengths with zero thigh/shin scale drift in the rendered probe. The flight transition retains the existing single authoritative brake. Athlete biomechanics now include measured RDL hinge/loading, chest-fly arc, cross-cable/Katana loading, overhead press, and a rack→lockout barbell press/throw path without donor-pose override.
- True grips: dumbbells resolve from the owning hand socket and sit across the palm; the barbell is solved from both palm sockets with its shaft through both hands, plates outside the body, and the held rest in front of the thighs. Rendered grip errors are zero at the sampled frames; bar hand span is about `0.684 m`.
- Equipment authority: specials publish `cast_id`, `projectile_id`, and exact `equipment_ids`. Lifecycle is explicit (`HELD`/`HOVERING` → `IN_SPECIAL` → `PROJECTILE` where applicable → `RETURNING` → prior state), mid-special dismiss is refused, swept collision and once-only damage remain authoritative, clamp escape is an honest miss, and pre-/post-commit refund behavior is tested. X-Wave uses its actual dumbbells, Plate Shot retains the owning bar while plates travel, and the new Athlete overhead press releases the actual held barbell as the projectile before returning it.
- Creatures: Dogkie uses fused-unit surface glide with no paw/step cycle and only lifts during attack (`0.07 m` travel hover, `0.28 m` attack maximum in the probe). Phoenix is always terrain-relative airborne with a `10 m` minimum clearance and bounded `WINDUP → DIVE → AERIAL_PASS → CLIMB`; the obsolete ground `SWEEP` path is removed.
- Proof: `25_HANDOFF/JOB_A/evidence/astra_m2/` contains `m2_motion_report.json` plus ten rendered captures covering split run/RDL/flight, dumbbell and barbell grips, X-Wave/Katana loading, press rack/release, Dogkie attack lift, and Phoenix airborne pass. The acceptance probe is **10 passed / 0 failed**. Focused tests are `25/25` equipment, `9/9` M2 equipment/motion, and `12/12` targeting/Phoenix; JSON and edited scripts pass syntax validation. Static build remains 174 files / 89.1 MB with 24 derivatives / 41.5 MB and a clean private-content scan.
- Device honesty: desktop rendering and phone-sized emulation are local evidence only. Physical-phone acceptance remains open to the owner.
- M2 next-action note is superseded by the completed M3 checkpoint above. Preserve the M2 authority/lifecycle/biomechanics fixes and its rendered evidence.

## Astra M1 checkpoint — current working tree, read first
- Continuity: same repository/worktree/branch, no restart, reset, new project, character/Blender edit, package, deployment, or live publish. Takeover audit is in `ASTRA_TAKEOVER_STATE.md`; HEAD remains `3185a25f80a23131fbdb2062fca9a1a1ffb7684f` and this checkpoint is intentionally uncommitted.
- Source-fidelity repair: the old tree L0/L1 (`14,000 / 5,000` tris, representative UVs, recomputed normals) was proven to tear the source silhouette and create black holes. It has been replaced in place by glTF-Transform 4.5.0 meshoptimizer derivatives that lock topological borders and preserve source normals/UVs: L0 `14,792` tris / SHA `0bfb4e…5608b`; L1 `7,392` tris / SHA `2bbecd…723d8`. Raw source `treevariaint.glb` remains immutable at SHA `d2a7db…adbde0`.
- Equipment repair: the legacy barbell/dumbbell derivatives visibly smeared source detail. Attribute-preserving replacements are barbell L0/L1 `40,005 / 11,995` tris (`d4a5b2…8a26 / 3de2a4…2e97`) and dumbbell L0/L1 `39,992 / 12,000` tris (`e40a44…6f33 / 7bfc85…2fee`), with 1024² base/normal maps. Dumbbells now sit on the bench cap at registry `y=0.62` instead of being buried at `y=0.50`; the host collider is unchanged.
- Creature repair: raw fish/horse/Phoenix and L0 were faithful, while all legacy skinned L1/L2s tore into shards. Source-attribute-preserving replacements are fish `6,498 / 5,150` tris (`c48e6a…ae83 / c3a954…6132`), horse `8,794 / 8,794` (`399f73…3411`), and Phoenix `8,234 / 8,234` (`7e4840…115`). The repeated horse/Phoenix plateau is intentional: lower requested ratios crossed the error-0.01 fidelity ceiling. Fish preserves its exported inverse-bind relationship and uses host root motion plus shader undulation without unsafe skeleton rebasing.
- Fidelity evidence: `25_HANDOFF/JOB_B/evidence/astra_m1/` contains raw/neutral views, rejected candidates, selected comparisons, and matched live gameplay day/night views. `final_gameplay_remaining_desktop/` and `final_gameplay_remaining_mobile/` each contain 12 PNGs plus an exact-ID report for fish rise/breach + swim, horse, Phoenix, barbell, and dumbbell. Dogkie L0 is geometry-identical to raw; the tree is the other repaired defect. Exact temple source parity is not claimed because the original ornate-temple GLB was not found in the bounded Downloads search.
- Stability/performance: selected dense-forest desktop `885,965` rendered tris, `17.697 ms` mean; selected phone-sized emulation `602,008` tris, `13.108 ms` mean / `13.7 ms` p95. A final same-geometry repeat on the loaded machine recorded desktop `24.571 ms` mean / `47.8 ms` p95 and phone-sized `15.064 ms` mean / `22.7 ms` p95, both with zero runtime errors. Final phone-sized floor flips: temple causeway `0%`, plaza edge `0.07%`, gym forecourt `0%`; desktop causeway max `0.002%`. Static build 174 files / 89.1 MB, 24 derivatives / 41.5 MB, private-content scan clean. Six source hashes, 18 authoritative derivative hashes/byte sizes, and all 15 shipped derivative hashes match; only the intentionally unshipped legacy tree/barbell/dumbbell L2s are absent. Five scripts pass syntax checks, seven manifests parse, `git diff --check` passes, and gameplay core is `125 passed, 0 failed`.
- M1 next-action note is superseded by the M2 checkpoint above. Preserve B8 maxima, lifecycle fixes, texture caps, ground authority, and physical-phone honesty. The raw ornate-temple source remains the only M1 provenance blocker.

---

## Latest checkpoint (read this first)
- HEAD = the B8 docs commit on **f7b487a** (B8 source) ← 0391db1 (P0 leak fixes) ← 03f4a51 (soak fix) ← 7a55cec (re-pins + probes) ← 1fe57e1 (B8 world) ← cee13b6 (B8 P0 / creatures / resources / equipment) ← 02c40d8 (B7 docs) ← c945a50 (B7) ← … Linear, nothing reset. B4 / B5 / B6 / B7 forensic checkpoints stay.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B8.zip`** · 62 406 039 bytes · 175 entries · sha256 `d0a1d36bf8628d98d72779757dd90e576912a8d75856b050b3880cb82498dc15` · from f7b487a (shipped tree = that commit; only untracked docs / evidence were dirty) · pipeline 0–7e PASS · 47 suites / 1423 checks green · record `CONSOLIDATED_B8.PACKAGE.json`.
- DRAFT (the one review URL, no live promotion): **https://b8-killer-delta--mahworld-test-preview.netlify.app** (deploy `6ab084e0cea9648ad9644896`, permalink `https://6ab084e0cea9648ad9644896--mahworld-test-preview.netlify.app`) — deployed from a fresh `tar -xf` of the ZIP, site 968f80e5-…, never `--prod`; verified from this machine: root 303, play 401 password page (text/html), assets 401 JSON, `release.mjs --check` OK. Login proof NOT RUN (`MAHDEMO_PW` not set). LIVE https://mahdemo.fob.systems unchanged: published deploy 6aac25101f17ce5ed37166e4 before and after.
- REPORT: `25_HANDOFF/CONVERGENCE/B8_KILLER_DIRECTION_DELTA_REPORT.md` (owner format: CURRENT JOB STATE / PHONE CRASH FINDING · MITIGATION / RESOURCE COUNTS / DOGKIE / FISH / PHOENIX / TITAN / HORSE / FAIRY EQUIPMENT / MAHGIC ECONOMY / PATHS · MAP / BUILDINGS / SKY · NIGHT / WORLD MACRO / TESTS / NEW DRAFT / LIVE DEPLOY CONFIRMED UNCHANGED / REMAINING RISKS). Evidence: `25_HANDOFF/JOB_A/evidence/b8/` (equipment probe shots + reports, targeting / controls / brake / flicker, soaks B8_p0 320 s + B8_final 330 s) and `25_HANDOFF/JOB_B/evidence/b8/` (world probe shots, macro captures).
- WHAT B8 DID: P0 = texture cap per tier (533 → 190 MB), menu blur gone + ⅓-rate render behind the panel, crash breadcrumbs ring + recovery report, READY veil, and the round-trip audit's leak fix (each NPC remount left 2 skeleton bone textures ≈ 20 / building visit, since P8 — closed) · Dogkie packs (9 territories / 46 seeds), multi-tone crystal, glide + laser contacts, 4 hits · fish 5 hits + legibility · phoenix wing driver + clearance steering · Titan +0.3048 m · horse graze fix · resources ×2 via data, no passive combat regen, guard sheet × health · FAIRY EQUIPMENT (host `play/EquipmentManager.js` + `lab/world/gear.js`: dumbbells / barbell = the gym derivatives, band = procedural tube; tap-to-equip, held ×1.8, floating-gear SPECIAL layer: X-Wave / Telekinetic Clamp / Plate Shot; guide charge → refills; crystal-bush nodes → drops; refill economy cap 999; HUD gear row) · free-roam projectiles resolve WILD / RESOURCE at the hit · route registry (names / from / to / type) + 19 wayfinders (`lab/world/signage.js`) + map · building registry (10) + names at the doors + TEMPLE_HALL / TOWER_HALL shell interiors · live DAY / NIGHT (≡ SETTINGS → TIME, `P.timeOfDay`), sun 6.5° at 33° elevation, moon 6.2° · `lab/world/macro.js`: two ridge bands with real passes, TITAN_FALL + NORTH_FALL, three sky conduits · façade energy windows · host determinism (hashed crit roll + drop scatter: replay reproduces).
- PHONE PERFORMANCE (emulation, 4×, 330 s with menu stress + the B8 gate phases): ctx lost 0, errors 0, heap 35–116 MB, textures 118 → 201, programs 112 → 179 (one-time night set), 15.2 fps avg, max gap 5.3 s (first night compile). Building round-trips: textures 118 → 140 → 140 → 140.
- TOOLS added: `deploy/joba/probe_equipment.mjs [--mobile]`, `deploy/jobb/probe_world_b8.mjs [--mobile]`, soak phases `b8_*`, `P.timeOfDay()`, `P.adapterApi()`, `P.crashReport()`; probes wait for the READY gate.
- FROZEN BUT QUEUED (nothing discarded): JOB A movement-quality audit, separated perf route, JOB A package, RDL residual, the botany verdict; owner decisions: third-dumbbell affinity per class, a band move set, bespoke temple / tower interiors, the concurrent-aggro cap if packs read as a dogpile.
- DISK: C: ≈ 610 MB free. Safe for the OWNER to clean: `deploy/packages/CONSOLIDATED_B2.zip`, `CONSOLIDATED_B3.zip` (older candidates, reachable in git history by commit); never `deploy/probe_out/video`.

## What Jah does (≤ 3 baby steps)
1. Phone, the **B8 draft**: play 3–5 minutes, open the MAHWORLD panel several times (also in the red region) — does the reload come back? If it does, dev panel → crash report (the breadcrumb ring) and tell me the last lines.
2. Tap ✧ GUIDE, then DB×2 in the gear row: do two dumbbells float in front? Tap one (held), tap a crystal bush (lock) and hit slot 1; try the X-WAVE from the SPECIAL slot with both floating; ≡ SETTINGS → TIME for night. Say what reads wrong (size, place, colour, feel).
3. (unchanged) `MAHDEMO_PW` in this machine's environment for the login proof; BLENDER RELEASED when the character lane is free.

---
## Previous head (kept for lineage)
## Latest checkpoint (read this first)
- HEAD = the B7 docs commit on **c945a50** (B7 source) ← 5394060 (B6 access hotfix docs) ← 2bdc4c2 (B6 docs) ← 438cbbb (B6 stability) ← … Linear, nothing reset. B4 / B5 / B6 forensic checkpoints stay.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B7.zip`** · 62 332 852 bytes · 169 entries · sha256 `004262d92d8b961e79b21dba2ff22bce8df6f67be2266e7a146d3941cbcb688c` · from c945a50 (clean) · pipeline 0–7e PASS · suite 46 files / 1 398 checks / 0 failing · fresh-extraction smoke 7 / 7.
- DRAFT (the one review URL, no live promotion): **https://b7-phone-delta--mahworld-test-preview.netlify.app** (deploy `6ab0351b6a0a4e1c1bb537f6`, permalink `https://6ab0351b6a0a4e1c1bb537f6--mahworld-test-preview.netlify.app`) — deployed from a NEW fresh extraction; verified externally (root 303 → play 401 password HTML page, assets 401 JSON, no "Site not found"); correct-password login **NOT RUN** (no `MAHDEMO_PW` on this machine). Two ASCII hyphens `--` in the URL. LIVE unchanged: published deploy `6aac25101f17ce5ed37166e4`.
- REPORT: `25_HANDOFF/CONVERGENCE/B7_PHONE_REVIEW_DELTA_REPORT.md` (owner §14 format: CURRENT JOB STATE / IMPLEMENTED / FLIGHT BRAKE / MONSTER TARGETING / LEVEL-HP UI / ARCHITECTURE CLEANUP / SKY / FISH / PHONE PERFORMANCE / TESTS / NEW DRAFT / LIVE DEPLOY / REMAINING OWNER DECISIONS). Evidence: `25_HANDOFF/JOB_A/evidence/b7/` (targeting, phone controls + pill shot, 300 s + LOW soaks, flicker, brake) and `25_HANDOFF/JOB_B/evidence/b7/arch_before|arch_after/` (rods → integrated colour, sky, fish, trail).
- WHAT B7 DID: §1 brake = host input-edge one-shot (`flight.brake` tuning) · §2 tap locks the body the finger LANDED on, height-aware LOS, fish swirl inside reach (`swirl_radius_m 2.3`), phoenix SWEEP pass (`sweep_s / sweep_radius_m`), airborne look-up (`pitch_min_flight`), lock marker on creature figures · §3 `lab/world/entityStatus.js` (NAME · LV · HP on every active creature / wildlife figure) · §4 size-driven levels (`ecology.levels`, `level_override`): DOGKIE 5 / HORSE 35 / FISH 35 / PHOENIX 75 · §5 mode pill left of FLY · §6/§7 rods + floating diamonds removed; conduits / façade bands / insets / crown-riding diamonds; civic cyan · §8 `lab/world/atmosphere.js` scattering dome + fog coherence + sun-lit sheets + STRATUS_LOW + far depth hierarchy · §9 fish population LOD (208 agents + 120 silhouettes, 2 draw calls) · §10 C continuous trail inlay.
- PHONE PERFORMANCE (emulation, 4×): 300 s soak 19.2 fps avg, tick 41 ms/s, load gap 3.7 s, programs 104 → 108, textures ≤ 158, heap 32–116 MB, ctx lost 0, errors 0; LOW 70 s the same shape; flicker 0–0.1 %. One earlier soak died at 115 s with the disk at ≈ 280 MB free (renderer death) — rerun clean after freeing this job's temp dirs.
- TOOLS added: `deploy/joba/probe_targeting.mjs [--mobile]` (tap-lock / status / damage / cleanup per species), `deploy/jobb/arch_views.mjs <label>` (13 before / after views), `16_TESTS/gameplay_b7_targeting.test.mjs`. Dev accessors: `P.camDrag`, `P.adapterGet`, `worldB.registry()`, `creatures().statusOf(id)`, `wildlife().statusOf(id)`.
- FROZEN BUT QUEUED (nothing discarded): JOB A movement-quality audit, separated perf route, JOB A package, RDL residuals; JOB B sanctuary polish, HUD minimal mode, architecture animation refinements, world expansion.
- DISK: C: ≈ 290 MB free after packaging. Safe for the OWNER to clean: `deploy/packages/CONSOLIDATED_B2.zip`, `CONSOLIDATED_B3.zip` (≈ 62 MB each); Temp `puppeteer_dev_chrome_profile-*`, `Temp/claude/C--Users-jahsu` (another session's scratch, 465 MB). NOT `deploy/probe_out/video/` (the only copies of the V04–V08 review videos). The test suite's disposable `Temp/mahworld-5b-test-*` dirs were this job's and were removed.

## What Jah does (≤ 3 baby steps)
1. Phone, the **B7 draft**: tap a Dogkie, a fish at a pond, the horse; fly up near the highland, drag to look UP at the phoenix and tap it — does the lock take, do LV / HP plates show on every nearby monster, does slot 1 land? (The phoenix is answered in its low sweep after each dive.)
2. Look at the Titan tower / the temple / the skyline: rods gone, colour on the buildings, sky depth, fish in the ponds, the north-east trail no longer striped? (yes / no, where.)
3. (unchanged) `MAHDEMO_PW` in this machine's environment for the login proof; level split horse / fish if wanted (`level_override`); BLENDER RELEASED.

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-20 12:50 UTC (owner STABILITY GATE absorbed → CONSOLIDATED_B6; feature work stays frozen until the owner's phone confirms; then JOB A resumes)

## Latest checkpoint (read this first)
- HEAD = the B6 docs commit on **438cbbb** (stability fixes) ← 9fe02e0 (B5 docs) ← 5f11888 (review-pass fixes) ← 8614aa0 ← a88e656 ← 5f46891 (B4 docs). Linear, nothing reset. B5 stays the forensic baseline.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B6.zip`** · 62 315 989 bytes · 167 entries · sha256 `f2d70c3f61ff3bb22b48b0c7c05d3497025a094ba03c3fdafcbc8f04fdf85aba` · from 438cbbb · pipeline 0–7e PASS · suite 45 files / 1 381 / 0 · fresh-extraction smoke 7 / 7.
- DRAFT (the one review URL, no live promotion) — **B6 ACCESS HOTFIX 2026-09-20 14:40 UTC**: the owner's iPhone got Netlify's "Site not found" on the first B6 permalink; that URL answers 303 → 401 password page from every independent check here (curl with an iPhone UA, a headless mobile Chrome: password form renders, wrong password handled, assets 401), while the same URL with an em dash / en dash / single hyphen in place of `--` returns Netlify's 404 "Not Found - Request ID" — iOS smart punctuation converting `--` is the probable cause. B6 was re-deployed from a FRESH extraction of the same package (sha `f2d70c3f…85aba`, commit 438cbbb) as a NEW non-production draft with a short alias: **https://b6-stability--mahworld-test-preview.netlify.app** (deploy `6aafedd6b0540a7fd03c1857`, permalink https://6aafedd6b0540a7fd03c1857--mahworld-test-preview.netlify.app). Externally verified: root and root/ → 303 to the play page, play page → 401 password page (renders on mobile), BUILD_INFO / play.js → 401 JSON, no "Site not found", no redirect to a foreign host. Correct-password login: BLOCKED on this machine (no `MAHDEMO_PW` — the password exists only as a hash on Netlify; run `MAHDEMO_PW=… node deploy/probe_public_gate.mjs https://b6-stability--mahworld-test-preview.netlify.app mobile` with the owner-supplied env var to record it). Live = published deploy 6aac25101f17ce5ed37166e4 untouched (verified before and after).
- REPORT: `25_HANDOFF/CONVERGENCE/STABILITY_GATE_0920_REPORT.md` in the owner's §21 format (REPRODUCED FAILURE / ROOT CAUSES / FLOOR FLICKER / FIXES / MOBILE CHANGES / BEFORE / AFTER / LONGEST STABLE RUN / CONTEXT LOSS / CANDIDATE / LIVE / RISKS); evidence `25_HANDOFF/JOB_A/evidence/stability_b6/`.
- WHAT WAS WRONG (measured): (1) every host wildlife synthetic (fish / horse / phoenix, ~30) was ALSO mounted as a full Athlete rig by `rulesHud.renderEntities` — 6 220 scene objects, 240 skinned meshes, 590 k–1.2 M tris; (2) ~20 world shader programs compiled on first sight during play (0.2–0.8 s stalls each on a phone); (3) the region tint sheets sat exactly at the floor's height (0.012) with the camera near plane 0.1 → z-fighting on a 16-bit phone depth buffer. Fixes: wildlife excluded from the class-body mount; a world warm-up (`compileAsync` at READY + hidden warm figures); near 0.4 + the ground stack 0.012 < 0.03 < 0.045 with polygon offsets. After: 2 685 objects, 357 k tris at the plaza, 104 programs ready at load, 300 s soak at 4× CPU throttle with 0 context loss / 0 errors.
- STABILITY TOOLS (`deploy/joba/`): `probe_phone_stability.mjs --throttle 4 --seconds 300 [--tier LOW] [--query '&nomod=meadow']` (the soak: context-loss listeners, per-second resources / heap / tick / rAF, the route), `probe_floor_flicker.mjs` (consecutive-frame ground diff), `probe_build_time.mjs` (per-module build ms + main-thread long tasks), `probe_warm_play.mjs` (programs compiled during play), `probe_scene_census.mjs`, `probe_dom_gc.mjs`, `time_world_modules.mjs`. `?nomod=…` skips client world modules; `P.warmState()`; `worldB.debug().build_ms`.
- STANDING RULE (owner 2026-09-20): PLAYABILITY > MORE FEATURES. Desktop smoke / "no console errors" is not the gate; the phone is. If B6 still fails on the phone: next levers = deferring the world build until the first input, parsing the tree derivative in a worker, a creature animation LOD, per-tier populations.
- FROZEN BUT QUEUED (nothing discarded): JOB A movement-quality audit, separated perf route, JOB A package, RDL residuals; JOB B sanctuary polish, HUD minimal mode, architecture animation refinements, world expansion.
- DISK: C: ≈ 470 MB free. Safe for the OWNER to clean: `deploy/packages/CONSOLIDATED_B2.zip`, `CONSOLIDATED_B3.zip`; Temp `puppeteer_dev_chrome_profile-*`. NOT `deploy/probe_out/video/` (the only copies of the V04–V08 review videos).

## What Jah does (≤ 3 baby steps)
1. Phone, the **B6 draft**: play for two minutes — walk west into the forest, fight a Dogkie, fly, land. Does it stay alive? (If it dies, note roughly when and where — that points at the load-time levers listed above.)
2. Look at the ground while walking and turning at the plaza and on the temple causeway: still flickering? (yes / no, where.)
3. (unchanged) evidence files / stat source / decisions / BLENDER RELEASED.

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-20 09:45 UTC (B5: the review pass over the sanctuaries + the SHIELD sheet + the impact-frame hold → CONSOLIDATED_B5; JOB A continues)

## Latest checkpoint (read this first)
- HEAD = the B5 docs commit on **5f11888** (review-pass fixes) ← 8614aa0 (tempo audit + impact hold, horses grounded) ← a88e656 (SHIELD sheet) ← 5f46891 (B4 docs) ← 8c57c00 (B4). Linear, nothing reset.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B5.zip`** · 62 314 087 bytes · 167 entries · sha256 `a7ba93f05af7848e34b8fb000cb0eb5527697deb8533ce84ea47921ef05d5ed8` · from 5f11888 (the only dirty path = the report) · pipeline 0–7e PASS · suite 45 files / 1 381 / 0 · fresh-extraction smoke 7 / 7.
- DRAFT (the one review URL, no live promotion): **https://6aafa87364d396f38a2786a7--mahworld-test-preview.netlify.app** (gate 401 PASS; correct-password login NOT RUN). B4 draft (before the review fixes) https://6aaf9a9ed3d661699d465367--mahworld-test-preview.netlify.app. Live https://mahdemo.fob.systems = published deploy 6aac25101f17ce5ed37166e4 untouched (verified after each draft).
- REPORT: `25_HANDOFF/JOB_B/JOB_B_NEXTPASS_0920_REPORT.md` — the B4 body + the **B5 addendum** (the 21 confirmed review findings and their fixes, the SHIELD sheet, the impact-frame hold, the B5 candidate). Evidence: `evidence/sanctuary_proof/`, `evidence/combat_proof/`, `evidence/frametime_b4/`, `../JOB_A/evidence/flight_brake_guide/`, `../JOB_A/evidence/guard_sheet/`, `../JOB_A/evidence/attack_tempo/`.
- WHAT CHANGED SINCE B4 (what the player sees): the guard is a translucent SHEET on the body (platinum / gold) that dims with hits and shatters on the 2nd–3rd — no ring; attacks hold their contact pose a few frames longer with the flash on the pose; the Visionary highland has ramps north / south / east and a real west wall; creatures on terraces fight properly; pond corners are shore; the Bage basins have fish; the market road goes around the temple; no crystals on the roads or in front of doors.
- ONE REGISTRY, ONE PLACEMENT (unchanged): edit `deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_world_colliders.mjs` (then `--check`) → `node deploy/build_static_demo.mjs`; probes one Chrome at a time; `?nomod=meadow,architecture` attribution; `P.devPlay('PLACE', {x, z})` in the static demo (no account_id override; land first — a PLACE mid-descent keeps the flight state).
- TERRACE RULES (new): a terrace above 1.6 m gets wall LIPS on its ramp-less sides (`terraceColliders`), ramps may carry a z / x range; creatures spawn at `groundHeight(x, z, terraceLevel)` and tick with their own height as the reference (a platform above them is never their ground); walkable tops never block line of sight.
- GUARD SHEET = DATA: `rules_17_23.dev.json` guard.sheet {hits_baseline 2.5, regen_s 6, break_lock_s 1.2} (provisional); the field sizes it from the baseline hit; `guard_sheet {frac, broken}` in the play snapshot and the rules view.
- KNOWN LIMITS: phone perf unmeasured (the desktop was loaded; C: was at 100 %); horse / phoenix / fish still root + shader motion (asset lane); round-façade seams stand free of the tapering upper walls; "no dead space" partial (east range / coast walk); class houses are exterior destinations only.
- NEXT (JOB A): the movement-quality audit (pelvis participation, real knees, no duplicate writers / clip restarts), the separated perf route on a quiet machine, the JOB A output package; then the RDL residuals.

## What Jah does (≤ 3 baby steps)
1. Phone, the B5 draft: hold GUARD beside a Dogkie in the west forest — watch the sheet dim and shatter on the 2nd–3rd bite (then hold MAHGIC guard: gold). Keep / change: hits to break, how visible.
2. Slot 1 (H-PUSH) at a Dogkie a few times: does the contact now READ (pose held, flash on the pose) without the button feeling late? One line.
3. Fly to the Visionary highland (west), walk its ramps, fight the Dogkies up there; then the Titan gate → tower → ledge. One line: do the five sanctuaries read as five classes? (unchanged: evidence files / stat source / decisions / BLENDER RELEASED.)

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-20 08:50 UTC (owner NEXT-PASS DIRECTIVE + ARCHITECTURE add-on + DOGKIE add-on absorbed in three passes → CONSOLIDATED_B4; JOB A resumes)

## Latest checkpoint (read this first)
- HEAD = the B4 docs commit on **8c57c00** (tests re-pin) ← 39346f6 (P3b populations / probe plumbing / tier scaling / slot-canon texts) ← 1fcf4b9 (P3 five class sanctuaries) ← a911928 (P2 flight brake + guide formation + Dogkie add-on) ← 79930d0 (P1 creature combat) ← 16096a6 (B3 docs) ← ad7e312 (B3). Linear, nothing reset.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B4.zip`** · 62 307 537 bytes · 167 entries · sha256 `a7260934896f4534add4a2223a2181d65ca78cde53720bc3e3381ba0afb4fff5` · from 8c57c00 (the only dirty path was this report) · pipeline 0–7e PASS · suite 44 files / 1 369 / 0 · fresh-extraction smoke 7 / 7 (zero page / console errors).
- DRAFT (the one review URL, no live promotion): **https://6aaf9a9ed3d661699d465367--mahworld-test-preview.netlify.app** (gate 401 PASS on play / BUILD_INFO / architecture.js / meadow.js, root 303 → gate; correct-password login NOT RUN). Live https://mahdemo.fob.systems = published deploy 6aac25101f17ce5ed37166e4 untouched (verified after the draft through the site record).
- REPORT: `25_HANDOFF/JOB_B/JOB_B_NEXTPASS_0920_REPORT.md` (status statement; what changed per directive section §2–§26 + the two add-ons; the §28–§31 acceptance checklists with what was proven and how; preserved; measured — suite, real-runtime proofs `evidence/sanctuary_proof/` (24 shots + the EMULATED phone frame), `evidence/combat_proof/`, `../JOB_A/evidence/flight_brake_guide/`, frame time + per-module attribution `evidence/frametime_b4/`; blockers; files; candidate; ≤ 3 owner actions).
- WHAT THE PLAYER SEES NOW (on top of B3): FIVE CLASS SANCTUARIES (ATHLETE gold temple + NE terrace · TITAN blue tower + gate pillars + lake + mountain ledge · LEAN crimson spire house on the NW terrace · VISIONARY amethyst overlook on the new west highland + pool · BAGE rose market + twin basins with platinum rings) joined by 20 m causeways ending in forecourts with door-aligned spurs, an outer loop, crystal MEADOWS in the family tint, root skirts / energy seams / hovering square-diamond crowns / entrance diamonds on every landmark; Dogkies (deep Titan blue, +12 %) 8–12 per sanctuary that CHASE and BITE in free roam (111 / hit, 5 hits to kill, aggro cap 3, dissolve + respawn), horses that feint, fish that breach and launch, phoenixes that dive on flyers; HP 0 → DEFEATED → respawn at the safe point; full-speed flight release = a visible brake; Mrs. Mah on the left shoulder facing the heading.
- ONE REGISTRY, ONE PLACEMENT (unchanged workflow): edit `deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_world_colliders.mjs` (then `--check`) → `node deploy/build_static_demo.mjs`; probes one Chrome at a time: `deploy/jobb/world_smoke.mjs`, `sanctuary_proof.mjs`, `combat_proof.mjs`, `frametime.mjs`, `frametime_modules.mjs` (`?nomod=meadow,architecture,…` attribution), `deploy/joba/probe_flight_brake_guide.mjs`. In the static demo `P.devPlay('PLACE', {x, z})` reaches the in-page host's dev ops (no account_id override).
- CREATURE BALANCE = DATA: `registry.ecology.species.*.combat` (hits_to_kill / hits_to_kill_player …) → `play/world/CreatureCombat.js` at spawn from the ATHLETE PA_PUSH baseline (84 applied); crit = `rules_17_23.dev.json` damage.critical (0.03 / 1.5, provisional); the WILD context lives in `RulesField.contextOf`.
- KNOWN LIMITS (report §blockers): phone perf unmeasured (the population near the temple / market is the heaviest new cost — creature animation LOD / per-tier population are the levers); horse / phoenix / fish still root + shader motion (asset lane); round-façade seams stand free of the tapering upper walls; "no dead space" partial on the east range / coast walk; class houses are exterior destinations only.
- DISK: the machine's C: drive was at 100 % during packaging (only my own probe temp / scratch was cleared — nothing of the owner's); keep probe_out small.
- NEXT (JOB A, resumes): SHIELD body-silhouette sheet, impact-frame tempo audit, movement-quality audit, the separated perf route on a quiet machine, the JOB A output package; then the RDL residuals.

## What Jah does (≤ 3 baby steps)
1. Phone, the B4 draft: walk west into the amethyst forest and fight the first Dogkie (tap it, slots 1–4; let one kill you once → DEFEATED → respawn). One line: keep / change (hits to kill, bite damage, aggro).
2. Fly: north-east through the TITAN gate to the tower, land on the mountain LEDGE; west to the VISIONARY overlook; north-west onto the LEAN spire platform; look at the market basins. One line: do the five colours / houses read as five classes?
3. Full-speed flight, release the stick (the brake); Mrs. Mah on the shoulder. Keep / change. (unchanged: evidence files / stat source / decisions / BLENDER RELEASED.)

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-20 04:20 UTC (owner OFFICIAL DEMO WORLD PASS redirect absorbed into JOB B → CONSOLIDATED_B3; JOB A resumes)

## Latest checkpoint (read this first)
- HEAD = the B3 docs commit on **ad7e312** (JOB B redirect: regions / paths / ground detail / crown terraces / tree lock / sky + wind / sea ripple body / fish · horse · phoenix ecology / guide motion / map layer) ← c2ffaa4 (JOB A checkpoint A2) ← b532ca9 (B2 docs) ← 5eca38a. Linear, nothing reset.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B3.zip`** · 62 254 873 bytes · 163 entries · sha256 `c8c50b2bde270200212978f705d1c9b0f02fa573ccaf17f596ea6c23364fd32a` · from ad7e312 (clean) · pipeline 0–7e PASS · suite 43 files / 1 348 / 0 · fresh-extraction smoke 7 / 7 (zero page / console errors).
- DRAFT (the one review URL, no live promotion): **https://6aaf5ad0d894998b44e52723--mahworld-test-preview.netlify.app** (gate 401 PASS on play / BUILD_INFO / the wildlife module / a creature derivative; correct-password login NOT RUN). Live https://mahdemo.fob.systems = 6aac25101f17ce5ed37166e4 untouched (verified after the draft).
- REPORT: `25_HANDOFF/JOB_B/JOB_B_REDIRECT_0919_REPORT.md` (status statement CURRENT JOB / COMPLETED BEFORE / QUEUED INTO / A-B CONFLICTS; what changed per package phase; preserved; measured — tests, real-runtime proof `evidence/redirect_proof/` (25 shots incl. the phone-size EMULATED frame), rig truth `evidence/redirect_rigs/`, frame time; blockers; files; candidate; ≤ 3 owner actions). Older reports unchanged.
- WHAT THE PLAYER SEES NOW (on top of B2): six named regions with ONE crystal family each (plaza gold, west forests amethyst, open range verdant, the north crown crimson, the south coast rose) — trees locked to platinum / heat-grey trunks with crystal only at the extremities, crystal shard clusters + dust on the ground, satin causeways with restrained emissive seams, regional paths, dashed discovery trails, region monoliths; two raised CROWN TERRACES behind the west / east bridges (walk up the ramps; the host ground follows); layered clouds + a mist ring drifting on one wind; an 11-island archipelago; fish schools breaching in the sea, two solitary crystal horses on the range, a golden phoenix circuit at ~90 m every couple of minutes; Mrs. Mah hovers / drifts / settles in the species language; the map shows regions, paths and labels.
- RIG TRUTH (read before touching the new creatures): the horse and phoenix GLBs are rigid (all weights on the root), the fish partial — motion is root + vertex-shader deformation (`lab/world/wildlife.js`); `lab/world/creatureRig.js` re-bases the empty-node bind poses at load. Real articulation needs the asset lane (Blender slot still reserved).
- ONE REGISTRY, ONE PLACEMENT (unchanged workflow): edit `deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_world_colliders.mjs` → `node deploy/build_static_demo.mjs`; probes one Chrome at a time: `deploy/jobb/world_smoke.mjs` (console errors now counted), `redirect_proof.mjs`, `rig_view.mjs <out> FISH|BIRD|QUADRUPED <glb>`, `frametime.mjs`.
- KNOWN LIMITS (report §blockers): no sea / islet collider beyond the beach ring (a flyer landing there stands on y 0); far islands read monochrome in the haze; phone perf unmeasured (emulation only); pet acquisition is metadata only.
- NEXT (JOB A, resumes): SHIELD body-silhouette sheet, impact-frame tempo audit, movement-quality audit, the separated perf route on a quiet machine, the JOB A output package; then the RDL residuals (hands below knees, tip dip, fused release timing).

## What Jah does (≤ 3 baby steps)
1. Phone, B3 draft: north over the west bridge → up the ramp onto the crown terrace (look back: crimson trees, the red-seamed path); west into the amethyst forest (tree lock up close). One line: keep / change (family colours, terrace height).
2. Fly south past the beach and wait for a breach (≤ 22 s), then east of the tower for the horse; the phoenix arrives on its own (~90 m over the north) within ~2.5 min. Say if the creatures' scale / rarity feel right.
3. (unchanged) evidence files / stat source / decisions / BLENDER RELEASED (the horse + phoenix need a real rig from the asset lane).

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-20 01:40 UTC (owner water / land note + JOB A phone controls → CONSOLIDATED_B2; JOB A continues)

## Latest checkpoint (read this first)
- HEAD = the B2 docs commit on **5eca38a** (beach shorelines / coast / islands / calm water + JOB A phone controls) ← 4f9a5a5 (B1 docs) ← 599fc13 (JOB B code) ← a370fb1 (C7 docs). Linear, nothing reset.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B2.zip`** · 51 896 114 bytes · 147 entries · sha256 `489bc249e0103a0bfc3866a54e3f283134f373357a890f08d45bb7a817116cc8` · from 5eca38a (clean) · pipeline 0–7e PASS · suite 43 files / 1 340 / 0 · smoke 7 / 7.
- DRAFT (the one review URL, no live promotion): **https://6aaf353db7fae6d0aa4df21d--mahworld-test-preview.netlify.app** (gate 401 PASS; correct-password login NOT RUN). Live = 6aac25101f17ce5ed37166e4 untouched.
- REPORTS: `25_HANDOFF/JOB_B/OWNER_NOTE_0919_WATER_LAND_REPORT.md` (calm water, the mainland as one island with a beach coast + sea + five islands, canal beaches with lapping foam), `25_HANDOFF/JOB_A/JOB_A_PROGRESS_REPORT.md` (one FLY control, SEPARATE / FUSE corner, category strip, MAH GUIDE toggle — probe 14 / 14 emulated), plus the B1 reports (`JOB_B_WORLD_REPORT.md`, `INTERJECTION_ENV_GUIDE_0919_REPORT.md`).
- WHAT THE PLAYER SEES NOW (on top of B1): the mainland ends at a coast with a beach ring into a blue sea, islands on the horizon; the canal has sandy sloping banks (walk in, wade, walk out anywhere), calm water with restrained ripples, foam lapping; the phone island has ONE FLY button (no DOWN / LAND), SEPARATE / FUSE where DASH was, `◂ PHYSICAL ▸` under the slots (tap / swipe), ✧ GUIDE under Health.
- NEXT (JOB A): the future-character import contract (semantic adapter), movement-quality + RDL femur-back measurement, SHIELD body sheet, impact tempo, the separated perf route on a quiet machine, JOB A packaging / report.

## What Jah does (≤ 3 baby steps)
1. Phone, B2 draft: FLY hold / release / tap / tap / hold; SEPARATE ↔ FUSE; swipe the category strip; ✧ GUIDE. One line each.
2. Walk into the canal (beach, foam) and look out from the plaza edge (coast, sea, islands). "keep" or a change (wider beach?).
3. (unchanged) evidence files / stat source / decisions / BLENDER RELEASED.

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-20 00:30 UTC (JOB B WORLD + the ENVIRONMENT / GUIDE-SCALE interjection done → CONSOLIDATED_B1; JOB A is next)

## Latest checkpoint (read this first)
- HEAD = the B1 docs commit on **599fc13** (JOB B code + data + evidence) ← a370fb1 (C7 docs) ← 4a17efd (hotfix intactness) ← 715579d. Linear, nothing reset.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B1.zip`** · 51 884 608 bytes · 146 entries · sha256 `c7fd0f71ad6142ea3595e3d698ab69cf0243f3e388f1ba14f32d8e98aea0e368` · from commit 599fc13 (clean) · pipeline 0–7e PASS · suite 43 files / 1 340 checks / 0 failing · fresh-extraction smoke 7 / 7.
- DRAFT (the one review URL, no live promotion): **https://6aaf261b588dbc75aeb59f8c--mahworld-test-preview.netlify.app** (deploy 6aaf261b…; gate 401 on play / BUILD_INFO / world module / derivative = unauthenticated protection PASS; correct-password login NOT RUN). Live https://mahdemo.fob.systems = 6aac25101f17ce5ed37166e4 untouched (verified after the draft).
- THE REPORTS: `25_HANDOFF/JOB_B/JOB_B_WORLD_REPORT.md` (owner §26 form: registry diff, source→derivative table with hashes, placements, lighting / materials, spawn / guide behaviour, entry proofs, water / bridge proof, navigation regressions, budget + frame time, candidate, ≤ 3 owner actions, WAITING_FOR_JOB_A hooks, residuals) and `25_HANDOFF/JOB_B/INTERJECTION_ENV_GUIDE_0919_REPORT.md` (the queued interjection: Mrs. Mah measured at 3.2× the reference in the real rear-follow camera, GPU ripple water with wading / landing / creature / hook impulses, float pads, haze fog, crown sway). Evidence: `25_HANDOFF/JOB_B/evidence/` (sheets + JSON; the raw PNGs are in the ignored `deploy/probe_out/jobb/`).
- WHAT THE PLAYER SEES NOW: the JOB B world — five dark-sapphire forest beds (157 trees, LOD + impostors, sway), the north canal (wadeable, rippling, 11 float pads) with three bridges, 78 cyan fixtures (3 hopping lights at night), the registry-sized sun / moon, the black MAH MATCH hall (enterable, match-capable, spectator tiers) + outdoor court, the projector beacon and gym props, Dogkies waking in the west forest (ALERT in free roam, bite in PRACTICE), Mr / Mrs Mah guides (dev API `worldLayer().guides().setGuide('MRS')` until JOB A's toggle), the cleared plaza, platinum looks; everything from C7 (rig V11, the slot-only phone island) unchanged.
- ONE REGISTRY, ONE PLACEMENT: edit `deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_registry.mjs` → `node deploy/jobb/build_world_colliders.mjs` (splices rules + rooms; `--check` in the JOB B test) → `node deploy/build_static_demo.mjs`. Probes (one Chrome at a time): `deploy/jobb/world_smoke.mjs`, `world_budget.mjs [--noworld]`, `world_extras.mjs [--night]`, `world_closeups.mjs`, `water_proof.mjs [--night] [--mobile]`, `frametime.mjs`, `plan_view.mjs <dir> --tag <t>`, `asset_view.mjs`; the derivative pipeline `deploy/jobb/derive_glb.py` (`--skinned-lod` for rigged sources).
- PROVISIONAL / OWNER: Mrs. Mah 0.75 m (≈ 3.2× the reference — keep or a number); the Dogkie kit (pack strength) and morphology; the far-water tiling / foam polish; the separated perf route must be rerun on a quiet machine (foreign Chrome load contaminated it).

## What Jah does (≤ 3 baby steps)
1. Phone, on the B1 draft: north over the main bridge (step into the canal on the way — wade, ripples, pads), west into the forest until the Dogkies wake (PRACTICE on to fight one), east to the MAH MATCH door → ENTER → EXIT. One line each: keep / change.
2. One number for Mrs. Mah if 0.75 m is not it.
3. (unchanged) the four evidence files + the Fitness Class Archetypes source · points per level 3 vs 4 · PASS_7 cells · botany verdict · "BLENDER RELEASED".

## Exact next action for the agent
1. **JOB A** (owner: "DO JOB B FIRST; AND THEN MAHWORLD JOB A"): FLY single control (DOWN / LAND removed), DASH → SEPARATE / FUSE, category text + arrows + swipe strip, the tiny GUIDE toggle under Health (→ `guides().setVisible` / `options()`), the future-character import contract (semantic adapter), click-to-move 30 m provisional, movement quality, flight posture curve, RDL femur-back, three categories 4/2/2, shield sheet, impact tempo, Training Room, stats, the separated performance route, packaging / report — never spending budget on the V11 mesh's cosmetic holes.
2. JOB A also owns: the match boundary field visual + FORFEIT contract on the MAH MATCH court / hall, projectile → `water.impulse` hook, the gym TRAINING_STATION interaction.
3. Keep the intactness gate in every chain; re-run `probe_perf_separated.mjs` when the machine is quiet.

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-19 16:40 UTC (interjection phases D–H + the 2026-09-19 hotfix done → C7; completion report written)

## Latest checkpoint (read this first)
- HEAD = the C7 docs commit on 4a17efd (hotfix intactness code + evidence) ← 715579d (phases D–G + hotfix B code) ← beb9946 (C6 docs) ← 06b7418 (phase B). Linear, nothing reset. Working tree clean after the docs commit.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_C7.zip`** · 31 342 949 bytes · 116 entries · sha256 `18172f155b98e6ccd5aebbdda1b27d7353d317b97fc3397ef64aef22eb97cb33` · from commit 4a17efd (clean) · pipeline 0–7e PASS · suite 42 files / 1 319 checks / 0 failing · smoke 7/7 from the extraction. DRAFT `6aaeb66e5b7bbc1b49221b84` → https://6aaeb66e5b7bbc1b49221b84--mahworld-test-preview.netlify.app (gate 401 unauthenticated PASS; correct-password login NOT RUN; live 6aac25101f17ce5ed37166e4 untouched).
- THE ONE REPORT: `CONSOLIDATED_PHASE_REPORT_6.md` (the owner's 11-item completion form). Interjection reports: `INTERJECTION_HOTFIX_0919_REPORT.md` (§8 form: intactness cause + repair + residual, the ATTACK button deletion), `INTERJECTION_PHASES_D_G_REPORT.md` (§23 form). Native contract for the residual: `SHOULDER_GIRDLE_NATIVE_REPAIR_CONTRACT.md` (same class as the hip: quilt topology, Character / Blender lane; Blender slot still RESERVED).
- WHAT THE PLAYER SEES NOW: rig ATHLETE_M_V11 (dev_0.16 — seam-welded shoulder weights; V10 / V9 rollback via `?avatar=`); the phone island without any ATTACK button (quick slots 1–4 tap = execute, swipe = the move's arrow; GUARD / DASH / FLY / ≡ / ⚔); the Hinge as a real hinge; flight lean from one curve with `+` = boost; the three new Athlete attacks (overhead rod, chest-fly ring, eight-curl barrage with the combo lock); ⚔ COMBAT (Training Room card) / STATS (five stats, source not found) / SKILLS (progression nodes + catalog status); casts dispatch in flight.
- INTACTNESS STATUS (honest): 17 of 18 matrix poses judged intact from the sheets (`intact/`), with pale / tan plates at the front deltoids in production at some arm poses (the S10 axilla loft patches without atlas UVs — neutral rows clean); the DOUBLE-BICEPS emote is NOT intact at rear-¾ (a covered hole in the quilt) → native lane. Never call the character accepted: Jah's phone verdict is the acceptance.
- OWNER EVIDENCE STILL MISSING ON DISK: the interjection `.md`, IMG_1118 / IMG_1117, the 23-28-36 recording, the Fitness Class Archetypes stat source.
- HOW TO RE-VERIFY: `cd 26_LOCAL_AUTHORITY && node deploy/build_static_demo.mjs`, then the chain script pattern (E · MESH · INTACT · D · F · HUD ×3 · COMBAT-AFTER · PERF — one probe at a time, never two Chromes on the GPU; `probe_intactness.mjs --only <pose,…>` reshoots poses; `--soft` = software rendering); `cd CLAUDE_GAMEPLAY_RUNTIME && node 16_TESTS/<file>`; package `node 26_LOCAL_AUTHORITY/deploy/package_pass.mjs CONSOLIDATED_C8`; draft = Expand-Archive the ZIP to a fresh folder → `npx netlify-cli deploy --cwd . --dir . --site 968f80e5-889b-43a8-b2cd-5860c588546a --message …` (no `--prod`).

## What Jah does (≤ 3 baby steps)
1. Phone, on the C7 draft (portrait, then landscape): PRACTICE on → tap slot 1, swipe slot 2 in its arrow, tap the category label → PHYS MAHGIC slot 2 (Curl Barrage) at the dummy → SPECIAL slots (Overhead Release, Chest Fly Clap) → hold FLY + `+` (the boost lean) → ⚔ (COMBAT / STATS / SKILLS). One line each: keep / change. Look at the shoulders during the push and the specials.
2. One path: the four evidence files + the Fitness Class Archetypes stat source.
3. (only when ready) points per level 3 vs 4 · `PASS_7_COMBAT_DECISIONS_REQUEST.md` cells · the botany specimen · "BLENDER RELEASED" for the two native contracts (hip, shoulder girdle).

## Exact next action for the agent (independent work while the packets wait)
1. Derivative chain: give the S10 axilla loft patches atlas UVs (`rig_axilla.mjs`, sampled from the adjacent skin) so they stop rendering as tan plates — regenerate through make_class_rig_v5 → hip_connect → shoulder_smooth as dev_0.17 with the same measurements (mesh_seams 0 differing, gaps ≤ 2 mm, intactness matrix) — only if the chain reproduces byte-identically otherwise.
2. Keep the intactness gate in every chain; add the double-biceps rear-¾ to the report until V12 (native) lands.
3. Open lanes unchanged: plaza cleanup, water / bridges, perimeter botany (specimen gate), Q07, the `?` cells.

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-19 05:20 UTC (owner interjection 2026-09-18 phases A–C done → C6; phases D–H next)

## Latest checkpoint (read this first)
- HEAD = the C6 docs commit on 06b7418 (interjection phase B: portrait HUD / control topology) ← 1332b66 (interjection reconciliation) ← 72432d4 (lane E host: Training Room field) ← 05670da (C5 docs) ← f4e0fb3 (phase D MAHGIC). Linear, nothing reset.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_C6.zip`** · 28 973 051 bytes · 111 entries · sha256 `33387cdfbc3a50d5dd9ff79c1731a660737e4a8a91758bc044cc25a11d29a994` · from commit 06b7418 (clean) · pipeline 39 files / 1 282 checks / 0 failing · smoke 7/7 from the extraction. DRAFT `6aae1706fc3faf715799be5b` → https://6aae1706fc3faf715799be5b--mahworld-test-preview.netlify.app (unauthenticated gate 401 PASS on play / gameHud.js / BUILD_INFO / dustFx.js / rig; correct-password login NOT RUN). Live untouched.
- WHAT THE PLAYER SEES NOW (phase B, report `INTERJECTION_PHASE_B_REPORT.md`, evidence `hud_b/`): bare bars (HEALTH + LV upper-left, the two MAHGIC bars upper-right), no top-right category card (a small `PHYSICAL ▸` label above PRIMARY; tap = next), no LOCKED panel (world-space `LV n` tag; `LV n · LOCKED` in gold + the health bar only in practice / match), small state line; touch: MAP · SPEED · CAM arc over a 124 px stick; right island = quick 1–4 arc (tap = execute, hold = arm) / PRIMARY two-wide + FLY + ≡ / GUARD + DASH in the corner / ⚔ → COMBAT tab; faces 42 px with measured 4 px hit halos. Rollback `?hud=dock`. The separate touch MAHGIC button is gone (PRIMARY casts when a MAHGIC category is armed).
- PHASE C GATE DONE (`LOWER_BODY_NATIVE_REPAIR_CONTRACT.md`): the hip fault is topological (shell hem 5.75 cm below the femoral head over dome-topped thighs, no shared loop) — runtime hip work STOPPED, the bounded native repair contract is ready for the Character / Blender lane; V10 (dev_0.15) stays the shipped default, NOT approved; Blender slot RESERVED.
- OWNER EVIDENCE STILL MISSING ON DISK: the interjection `.md`, IMG_1118 / IMG_1117, the 23-28-36 recording, and the Fitness Class Archetypes stat source (searched three roots; only the canon's identity sentences exist). Phase F will show "class baseline: source not found" unless Jah points at the file.
- NEXT (interjection §21 order): D Hinge reauthor + speed-driven flight lean (one centralized curve in RigAnimator, boost = deeper commitment, fused lower body participates, arms never inside the torso, camera never coupled) → E Special #1 OVERHEAD EXTENSION RELEASE, Special #2 CHEST FLY CLAP RELEASE, Physical MAHGIC EIGHT-STRIKE CURL BARRAGE (rules field: skills + bindings + combo-lock with immunity / breakout / guard / knockdown; dustFx formations) → F ⚔ STATS (five stats 0–999, points OWNER_DECISION 3 vs 4, branching FUTURE / LOCKED nodes) → G remaining catalog (owner `?` cells stay open) → H regression (click-to-move, camera, Training Room real-touch entry, environment, perf separated) + candidate C7. Still open from the 09-18 assignment: the Training Room client panel (tutorial steps / replay / inspect as a room state of ⚔ / ≡), plaza cleanup, water / bridges, perimeter botany (specimen gate).

## What Jah does (≤ 3 baby steps)
1. Phone, on the C6 draft (portrait, then landscape): thumb the island — DASH (corner), GUARD, PRIMARY hold + swipe, a quick-slot tap, a quick-slot HOLD (arms it), the category label, ⚔; then MAP / SPEED / CAM over the joystick. One line: "keep" or "move X / bigger X".
2. One path: where the four evidence files and the Fitness Class Archetypes stat source are.
3. (unchanged) `PASS_7_COMBAT_DECISIONS_REQUEST.md` `?` cells and the botany specimen verdict — only when ready.

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-19 04:10 UTC (enhancement phases B + C + D done → C5; lanes E–H next)

## Latest checkpoint (read this first)
- HEAD = the C5 docs commit on f4e0fb3 (phase D) ← 69c74dc (report 4) ← fcf553a (phase C) ← 28853d6 (phase B). Linear, nothing reset. Working tree clean after the C4 commits.
- Candidate **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_C5.zip`** · 28 966 378 bytes · 111 entries · sha256 `488e57c4785a15986891b917769988838818589eb9c9ca5dc69a1f1e557d8419` · packaged from commit f4e0fb3 (clean) · pipeline complete (37 files / 1 252 checks; fresh extraction; smoke 7/7). DRAFT `6aae0732563b514ab3213f21` → https://6aae0732563b514ab3213f21--mahworld-test-preview.netlify.app (unauthenticated protection PASS: play 401 · asset 401 · BUILD_INFO 401 · dustFx.js 401; correct-password loading NOT RUN). Previous: **`CONSOLIDATED_C4.zip`** · 28 956 501 bytes · 110 entries · sha256 `91a3cf12bbd96aa7df9bf67074b606a1acd125682be78cd3af1854edc4dd7a7f` · packaged from commit fcf553a (clean) · pipeline complete (36 files / 1 236 checks; fresh extraction; smoke 7/7). DRAFT `6aadfab9367fc0f2649b4f3d` → https://6aadfab9367fc0f2649b4f3d--mahworld-test-preview.netlify.app (unauthenticated protection PASS: play 401 · asset 401 · BUILD_INFO 401 · dev_0.15 GLB 401; correct-password loading NOT RUN). C3 (161da1a3…, draft 6aaddb1f…), C2, C1 remain up. Live `6aac25101f17ce5ed37166e4` UNCHANGED; rollback `6aab65567e997ae33c1da204`.
- WHAT CHANGED THE PLAYER FEELS: (1) the camera heading no longer chases his back — drag to look, release keeps it, ⟲ CAM = one reframe (rollback `?cam_follow=1`); (2) click / tap on empty ground within ~30 m walks / levitates / flies him there through the same controller (marker = the accepted point; stick / dash / altitude / attack take over; a tap while steering is refused); the Map tab also takes taps; (3) default avatar ATHLETE_M_V10 = dev_0.15 (dev_0.14 + hip connection, weights only; `?avatar=ATHLETE_M_V9` rolls back): the pelvis shell follows the thighs in the separated form, the fused form is byte-identical; (4) split flight: legs trail with speed; (5) the yellow balls at the hands are gone (they were the hand-energy sprites forced on by the ghost loop).
- Reports: `CONSOLIDATED_PHASE_REPORT_5.md` (§16 form, phase D), `CONSOLIDATED_PHASE_REPORT_4.md` (§16 form, phases A–C), `ENHANCEMENT_RECONCILIATION_0918.md` (superseded checks + confirmed causes + state per phase), `_3.md` (C2 review answers), `OWNER_DECISIONS_C2.md`, `PASS_7_COMBAT_DECISIONS_REQUEST.md` (still fully open).
- Evidence folders: `nav/` (probe_nav.json + n*/m* stills), `lower_body/` (probe_hip_connect.json, sheet_hip_connect_ab.png, sheet_hip_closeup_ab.png, V9/V10 stills, hipseam_dev_0.14.json, hands_flight_front34_after.png), `motion_e/` (g37_sequence_* rerun on V10), `interior/`, `botany/`, `hud/`.
- LANE D DONE (granular MAHGIC + casting anywhere; evidence `mahgic/`): casts work in flight; outcomes height-aware; ATHLETE_CASCADE original candidate (opt-in via LOADOUT, not equipped); the dust language is an original candidate for Jah's verdict (sheet_mahgic_closeup.png).
- NEXT LANES (dependency order §14): E Training Room (GYM_DOOR reuse; per-room rules field for the practice dummy) → F remaining approved catalog (owner `?` cells stay open) → G HUD translucency / compact, plaza cleanup, water / bridges, perimeter botany → H integrated routes + separate review candidate. Lane D design notes: rules PROJECTILE already terminates on wall / first hit / range (RulesField); presentation must retire the trail on terminal; PM_* skills are BODY_STRIKE (limb-gathered particles + contact burst, no projectile); ATHLETE_VECTOR / TRACK are the only Athlete projectiles (pattern = Jah's decision, keep UNASSIGNED).

## What Jah does (≤ 3 baby steps per packet)
1. **Packet C (tree, artistic):** look at `25_HANDOFF/CONVERGENCE/botany/REVIEW_PACKET_C.png` (6 panels) or open the draft on the phone: the specimen is 3 m ahead-right of spawn, the cluster of six at the plaza's south-west. Say "tree family approved" / "change: …" / "reject". (Scale, geometry, winding and cost are already measured — the verdict is about the look.)
2. **Packets A + B (phone, artistic + feel):** on the C3 draft, portrait then landscape — walk (the separated walk now holds each tip in place while the body passes; say if the held point should sit lower), drag / release / ⟲ CAM, FLY / DOWN / LAND, MAP, ≡ → EMOTES, SEPARATE (H) → legs front / side / back → FUSE; then walk to the MAH GYM door (west, the lit pad in front of its steps) → ENTER → look around → EXIT; and the two MARKET doors (north, in front of the market steps) → BROWSE at the counter. One line each.
3. **Decision (c) (exercise choices):** `PASS_7_COMBAT_DECISIONS_REQUEST.md` — the `?` cells, Vector Shot / BLOCK BREAK patterns, tick the proposed defaults. (a) fused glide = settled by the master §11.1; (b) hover vs planted = resolved as engineering; (d) camera spring = withdrawn.

## Exact next action for the agent (independent work while the packets wait)
1. PASS 7 independent parts (tap/hold single dispatch + zero double fires, the counter loop as PROPOSED, buffered input with cancel windows, measured hit-stop, tier data with `?` cells refused as UNASSIGNED_TIER, transition log + tests) — no dependency on the tree verdict or on any contact assumption.
2. P9 preparation: the extended integrated route as one probe (spawn → HUD → Map → walk/turn/stop → + / Dash → obstacle → orbit / reset → hold ascent → exhaust / landing → split / walk / fuse → attacks → emotes → cluster → landmark → GYM ENTER / interior / EXIT → shops) with interruption cases, run on the C2 build; the common Spec A route series on C2 (perf regression vs the B2 series).
3. Botany after "approved" only: wire `placement_v1.json` → registry migration that replaces the laser plants (U12 exclusions), the far-LOD switch at 30 m, then the no-flora vs distribution workload trace (U15). Before that: nothing scatters.
4. Housekeeping: C: 6.7 GB free; `deploy/probe_out/video/` (293 MB) and `deploy/packages/fresh_*` folders are regenerable.

---
## Previous head (kept for lineage)
# CONTINUE HERE — MAHWORLD convergence (gameplay lane) · updated 2026-09-18 night (PASS 4A / 4B / 4b done; Cards A + B open on CONVERGENCE_B)

The older `25_HANDOFF/PASSES/CONTINUE_HERE.md` (PASS 2 Spec B gate + owner round) is history; this file is the live one.

## Where things are
- Repo / worktree: `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS` (git, `master`). Tags: `s13_convergence` → `pass1_spec_a` → `pass2_spec_b` → `owner_round_0917` (dbaa132, the live demo's build). Convergence commits: 19dfe07 / 731e0fa (PASS 1), 483b5d4 / 7bc7c09 (PASS 2), a9d86d1 (PASS 2b), then the PASS 2b-review + PASS 3 commit (see `git log`).
- Baseline worktree for A/B measurements: `C:\mw_base` = `owner_round_0917` (built `C:\mw_base\...\deploy\static_dist`).
- Runtime root R = `CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY`; rebuild `node deploy/build_static_demo.mjs` (from R); tests `cd CLAUDE_GAMEPLAY_RUNTIME && node 16_TESTS/<file>` (29 files); package `node 26_LOCAL_AUTHORITY/deploy/package_pass.mjs <label>`.
- Reports: `25_HANDOFF/CONVERGENCE/PASS_0_RECONCILIATION.md`, `PASS_1_FLIGHT_QUALITY_REPORT.md`, `PASS_2_CAMERA_REPORT.md`, `PASS_2B_PRESENTATION_PIVOT_REPORT.md`, `PASS_3_HUD_REPORT.md`, `ADDENDUM_QUEUE.md`, `RESOURCE_USE_RECORD.md`. Evidence: `25_HANDOFF/CONVERGENCE/{flight,camera,pivot,hud}/`, `25_HANDOFF/PASSES/spec_b/`, `25_HANDOFF/PASSES/perf/`.
- The live demo (`6aac25101f17ce5ed37166e4`, https://mahdemo.fob.systems) is UNCHANGED since the owner round; rollback `6aab65567e997ae33c1da204`. No password is stored anywhere here; `MAHDEMO_PW` is not set in this session (correct-password login: NOT RUN).
- Owner addendum references `v12044gd0000dam5b47og65j5h5c8730.mp4` and `MAHWORLD_NEW_REFERENCE_STUDY`: NOT on this PC (checked 2026-09-17/18) — re-check before PASS 5.

## Done (headless Chrome, touch EMULATED; iPhone NOT tested)
PASS 1 flight (F01 perch landing, O4 hold-fly, host/visual collider sync F09, real roof heights) · PASS 2 camera (O1 persistent orbit + explicit reset, O2 both sides, pitch-up recovery, fade restore) · PASS 2b pivot slice (framing 31 % / 28 %, turn spring, latched movement frame, far world, matched capture) · PASS 3 HUD (class tokens, bevelled cluster, MAHGIC, + speed, MAP, one utility sheet, no permanent USE) + its review round · PASS 4A fidelity (phone render scale 2×, anisotropy, texture patches = source-side) · PASS 4B faithful separated legs (dev_0.14 = ATHLETE_M_V9) · PASS 4b walk identity (pace harmonized; the fused glide has no step rhythm — owner decision).

## Cards A + B gate (camera · flight · HUD · pivot · legs · walk) — what Jah does (≤ 3 baby steps)
1. Open the newest candidate on the phone: https://6aad2eeae77aff15879b2553--mahworld-test-preview.netlify.app (a password-gated DRAFT of `CONVERGENCE_B2.zip`, sha256 6da02039…, commit 4f7aadd — the live demo is untouched; B (6aad158c…) had a hip ledge on the separated legs, A2 (6aacfc2f…) is the HUD without the legs / pace work — both superseded).
2. Portrait then landscape: walk with the stick (the pace is now the separated walk, `+` is the run), drag the camera on either side and let go, tap ⟲ CAM, hold FLY / release / DOWN / LAND, tap MAP, ≡ → EMOTES → tap an emote, then SEPARATE (≡ SETTINGS or H): look at the legs from front / side / back, walk, FUSE back. One line each on: framing, turn feel, cluster reach, hues, sharpness, the legs (thighs / knees / calves / tips / inner faces / crotch), the transformation, and the pace (too slow? — one word flips it back).
3. Send one screenshot of `?lab=1` (Control Lab) from the phone — its `buffer W × H px (render scale %)` line and the frame average are the PASS 4A phone cost.
Decisions the owner holds: (c) PASS 7 combat — fill `PASS_7_COMBAT_DECISIONS_REQUEST.md` (the `?` cells, Vector Shot / BLOCK BREAK patterns, tick the proposed defaults). (a) WALK IDENTITY — the fused glide now matches the separated walk’s pace / start / stop but has no step rhythm (it glides, per the earlier rule); say "glide is fine" or "give the fused form the step rhythm". (b) FOOT CONTACT — the separated tips hover (the held point drifts ~41 cm per contact at walk); Spec E asks for ≤ 2 cm foot slide; say "keep the hover" or "plant the tips" (a world-space contact solve, ~a day).

## QUEUED (2026-09-18 15:10): the UNIFIED CONVERGENCE MASTER + the video / links bundle — see `ADDENDUM_QUEUE.md` (last section). The consolidated phase runs from the Cards A + B gate; the prior roadmap items (PASS 5 decision, PASS 7–9) fold into the master's P0–P9 in dependency order.

## Exact next action for the agent
1. (done) `CONVERGENCE_B2.zip` packaged and draft-deployed (gate 401 verified); the live demo is not replaced until Jah says so after the phone review. Commits: 17331c3 (PASS 3 review + 4A), a6ff97b (A2), d5a996b (PASS 4B + 4b), 42ce141 (B), fc364ca (PASS 5 measurements), 8c263e2 (PASS 4B review round + interpolation), 4f7aadd (B2). Reports: `PASS_4B_LEGS_WALK_REPORT.md` (with the review round), `PASS_5_MOTION_E_REPORT.md` (in progress).
2. Continue independent work while waiting: PASS 6 (Spec F) is BUILT — manifest / validator / import helper / checklist / Control Lab line (`PASS_6_MOCAP_PIPELINE_REPORT.md`); its gate is Jah recording (`26_LOCAL_AUTHORITY/mocap/CAPTURE_CHECKLIST.md`). PASS 5 remaining = the planted-contact decision (owner). PASS 7 combat: the bounded decision request is written (`PASS_7_COMBAT_DECISIONS_REQUEST.md` — the exercise table, Vector Shot / BLOCK BREAK patterns, the proposed defaults); the independent part (tap / hold grammar, counter loop as PROPOSED, buffer, hit-stop, tier data with `?` cells refused, transition log + tests) is the next build step → 8 VFX → 9 town / world per `ADDENDUM_QUEUE.md`.
3. Re-check for the addendum reference files (`v12044gd0000dam5b47og65j5h5c8730.mp4`, `MAHWORLD_NEW_REFERENCE_STUDY`) before PASS 5 — still NOT on this PC at the last check.
4. Housekeeping: the C: drive hit 0 bytes free during this session (probes hung); ~2 GB was freed from the npm cache + stale probe profiles only. Nothing of Jah's was touched — Jah decides what else to clear (Documents 68 GB, AppData 25 GB). Check `Get-PSDrive C` before long probe chains.
