diff --git a/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/deploy/world_preview/capture.mjs b/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/deploy/world_preview/capture.mjs
index 7538b57..d4b385e 100644
--- a/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/deploy/world_preview/capture.mjs
+++ b/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/deploy/world_preview/capture.mjs
@@ -39,7 +39,9 @@ export var VIEWS = [
   ['V25', 'ATHLETE district paving close', [-30, 1.8, 146], [-30, 0, 162]],
   ['V26', 'canal bank and water edge close', [20, 2.2, 204], [40, 0, 226]],
   ['V27', 'ridge face close from the TITAN ledge', [166, 14, 130], [230, 40, 150]],
-  ['V28', 'civic roofline, piers and service bay (Mentor Spire rear)', [46, 11, -12], [29, 16, 0]]
+  ['V28', 'civic roofline, piers and service bay (Mentor Spire rear)', [46, 11, -12], [29, 16, 0]],
+  ['P1', 'PILOT ridge wall at ~10 m (north canal)', [40, 1.8, 297], [43, 6, 308]],
+  ['P2', 'PILOT ridge wall at ~25 m (north canal)', [33, 3, 283], [44, 9, 306]]
 ];
 
 function mime(p) { return ({ '.html': 'text/html', '.js': 'text/javascript', '.mjs': 'text/javascript', '.json': 'application/json', '.glb': 'model/gltf-binary', '.png': 'image/png', '.jpg': 'image/jpeg', '.css': 'text/css', '.bin': 'application/octet-stream', '.ktx2': 'image/ktx2' })[path.extname(p).toLowerCase()] || 'application/octet-stream'; }
diff --git a/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/macro.js b/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/macro.js
index fb5da98..9397878 100644
--- a/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/macro.js
+++ b/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/macro.js
@@ -1,5 +1,6 @@
 import { ridgeStations, ridgeFaceSegment } from './ridgeLayout.js';
 import { applyGeology } from './surfaceDetail.js';
+var PILOT_TEX = null; function pilotDetail() { return PILOT_TEX; }   /* SCENARIO PILOT: set in createMacro (needs THREE) */
 
 /* JOB B world module — MACRO GEOGRAPHY (owner B8 §16–§18 "floor down, world up"). Data: registry.macro.
    MOUNTAINS: two ridge bands between the sanctuaries and the far massifs (NEAR 300–420 m, MID 470–560 m) — each ONE merged ribbon mesh
@@ -14,7 +15,7 @@ import { applyGeology } from './surfaceDetail.js';
    1 mist + 1 beam instanced mesh ≈ 8 draw calls, no per-frame allocation, LOW tier drops the mist. */
 var FAM = { gold: 0xe6c36a, blue: 0x2c62d8, red: 0xd8404a, purple: 0x8a5fd6, pink: 0xff5fb0, platinum: 0xdfe6ee, cyan: 0xdfe8ff };   /* colour law 2026-09-25: the civic 'cyan' key is neutral white light */
 export function createMacro(ctx) {
-  var THREE = ctx.THREE, log = ctx.log || function () { }; var group = null; var reg = null; var M = null; var own = []; var ridges = []; var falls = []; var beams = null; var clock = 0; var night = !!ctx.night; var beamMat = null, sheetMats = [], foamMats = [], mistSprites = [];
+  var THREE = ctx.THREE, log = ctx.log || function () { }; if (!PILOT_TEX) { PILOT_TEX = new THREE.TextureLoader().load(new URL('../assets/world/pilot/rock_detail.png', import.meta.url).href); PILOT_TEX.wrapS = PILOT_TEX.wrapT = THREE.RepeatWrapping; PILOT_TEX.colorSpace = THREE.NoColorSpace; PILOT_TEX.anisotropy = 4; PILOT_TEX.name = 'SCENARIO_PILOT_ROCK_DETAIL'; } var group = null; var reg = null; var M = null; var own = []; var ridges = []; var falls = []; var beams = null; var clock = 0; var night = !!ctx.night; var beamMat = null, sheetMats = [], foamMats = [], mistSprites = [];
   function tier() { try { return ctx.quality && ctx.quality.tier ? ctx.quality.tier() : 'MED'; } catch (e) { return 'MED'; } }
   function rnd(seed) { var s = (seed >>> 0) || 1; return function () { s = (s * 1664525 + 1013904223) >>> 0; return s / 4294967296; }; }
   /* ---- ridge ribbon: bearings around the map, profile height h(b) with passes; a fan of peaks (triangles) from a base ring to a crest ring ---- */
@@ -44,7 +45,7 @@ export function createMacro(ctx) {
       push(uoA.x, uoA.y, uoA.z, cuoA, nOut); push(crestA.x, crestA.y, crestA.z, cA, nOut); push(crestB.x, crestB.y, crestB.z, cB, nOut); push(uoA.x, uoA.y, uoA.z, cuoA, nOut); push(crestB.x, crestB.y, crestB.z, cB, nOut); push(uoB.x, uoB.y, uoB.z, cuoB, nOut); }
     var g = new THREE.BufferGeometry(); g.setAttribute('position', new THREE.Float32BufferAttribute(pos, 3)); g.setAttribute('color', new THREE.Float32BufferAttribute(col, 3)); g.setAttribute('normal', new THREE.Float32BufferAttribute(nrm, 3)); own.push(g);
     var mat = new THREE.MeshStandardMaterial({ vertexColors: true, roughness: 0.82, metalness: 0.08, flatShading: true, envMapIntensity: 0.28 });   /* pivot: faceted rock that answers the Sun */ own.push(mat);
-    applyGeology(THREE, mat, { snowY: R.h_max * 0.6, strata: 2.6, tier: tier() }); var mesh = new THREE.Mesh(g, mat); mesh.name = 'MACRO_RIDGE_' + R.id; mesh.frustumCulled = false; mesh.receiveShadow = false; mesh.userData.noMerge = true; group.add(mesh); ridges.push({ R: R, mesh: mesh, tris: pos.length / 9 }); return mesh; }
+    applyGeology(THREE, mat, { snowY: R.h_max * 0.6, strata: 2.6, tier: tier(), detail: { map: pilotDetail(), tile_m: 36, k: 1 } });   /* SCENARIO PILOT (isolated comparison) */ var mesh = new THREE.Mesh(g, mat); mesh.name = 'MACRO_RIDGE_' + R.id; mesh.frustumCulled = false; mesh.receiveShadow = false; mesh.userData.noMerge = true; group.add(mesh); ridges.push({ R: R, mesh: mesh, tris: pos.length / 9 }); return mesh; }
   /* M8C: the ridge faces use the shared GEOLOGY pipeline (lab/world/surfaceDetail.js applyGeology — strata, ledges, fracture joints, clefts,
      macro / micro tone, a derivative bump so every feature catches the Sun, snow on shallow high faces). Shader-only: the inner face stays
      exactly on the owner OP10 collision plane. */
diff --git a/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/surfaceDetail.js b/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/surfaceDetail.js
index 6eae885..932ad4f 100644
--- a/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/surfaceDetail.js
+++ b/CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY/lab/world/surfaceDetail.js
@@ -198,8 +198,8 @@ var GEO_FUNCS = [
 
 export function applyGeology(THREE, mat, spec) {
   if (!mat || (PATCHED ? PATCHED.has(mat) : (mat.userData && mat.userData.surfaceDetail))) return mat;
-  var S = Object.assign({ snowY: 1e5, strata: 2.6, major: 0, ledge: 0.32, joint: 4.5, fracture: 1, cleft: 1, macro: 0.16, grain: 0.07, bump: 1, lit: true, tier: 'HIGH', lod: [60, 900], bumpLod: [40, 260] }, spec || {});
-  var LOW = S.tier === 'LOW', lit = S.lit && !!mat.isMeshStandardMaterial, MAJ = S.major || S.strata * 3.2;
+  var S = Object.assign({ snowY: 1e5, strata: 2.6, major: 0, ledge: 0.32, joint: 4.5, fracture: 1, cleft: 1, macro: 0.16, grain: 0.07, bump: 1, lit: true, tier: 'HIGH', lod: [60, 900], bumpLod: [40, 260], detail: null }, spec || {});
+  var LOW = S.tier === 'LOW', lit = S.lit && !!mat.isMeshStandardMaterial, MAJ = S.major || S.strata * 3.2, DET = !!(S.detail && S.detail.map) && lit && !LOW;   /* SCENARIO PILOT (isolated comparison, not canonical): a luminance detail map */
   /* v2 (M8C iteration 2): the first cut read as a crazed-glaze web (an isotropic Voronoi everywhere) with dotted "stitching" where thin ledge
      lips went through the derivative bump. Rock now reads in three orders, each only where it can resolve: MAJOR BEDS (≈ 8 m: value zoning
      and ledges — a lit lip above a ledge boundary, a shadowed undercut below it), MINOR BEDS (the bedding lines) with JOINTS that stop at the
@@ -225,17 +225,20 @@ export function applyGeology(THREE, mat, spec) {
     'float gCl = geoN(vec2(gUV.x * 0.028, 3.7)) * 0.7 + geoN(vec2(gUV.x * 0.07, vSdW.y * 0.004)) * 0.3; float gCleft = pow(smoothstep(0.55, 0.95, gCl), 2.0) * ' + f(S.cleft) + ';',
     'float gGrain = (geoN(gUV * 1.1) * 0.6 + geoN(gUV * 3.3) * 0.4 - 0.5) * gNear;',
     'gH += -gCleft * 1.6 + gGrain * 0.35; gTone *= (1.0 - gJoint * 0.34) * (1.0 - gCrack * 0.38) * (1.0 - gCleft * 0.42) * (1.0 + gGrain * ' + f(S.grain * 2) + '); gRough *= 1.0 + gJoint * 0.12 + gCrack * 0.12 + gCleft * 0.08 + gGrain * 0.1;');
+  if (DET) body.push(   /* SCENARIO PILOT: the photographic detail rides the procedural beds — v follows the warped bedding, u shifts per major bed (breaks tile repetition); value, roughness and the bump height only, fading out by 220 m */
+    'vec2 gDuv = vec2(gUV.x, vSdW.y + gWarp) / ' + f(S.detail.tile_m || 36) + ' + vec2(sdHash(vec2(gBM, 3.7)), 0.0); vec4 gD = texture2D(uGeoDetail, gDuv); float gDk = ' + f(S.detail.k || 1) + ' * (1.0 - smoothstep(150.0, 600.0, gDist));',
+    'gTone *= 1.0 + (gD.r - 0.5) * 1.0 * gDk; gRough *= 1.0 + (gD.a - 0.5) * 0.5 * gDk;');   /* variant C (meso scale) / B: no texture height in the derivative bump (it aliased into speckle); the map's own normal channels perturb the face instead (below) */
   body.push('float gMacro = geoFbm(vSdW.xz * 0.004 + vec2(vSdW.y * 0.002)) - 0.5; gTone *= 1.0 + gMacro * ' + f(S.macro * 2) + ';');
   if (S.snowY < 1e4) body.push('float gUp = gAn.y; float gSnow = smoothstep(0.55, 0.8, gUp + gLip * 0.25) * smoothstep(' + f(S.snowY - 18) + ', ' + f(S.snowY + 14) + ', vSdW.y + (geoFbm(vSdW.xz * 0.02) - 0.5) * 36.0);');
   else body.push('float gSnow = 0.0;');
   body.push('diffuseColor.rgb *= mix(gTone, 1.0, gSnow * 0.9);', 'diffuseColor.rgb = mix(diffuseColor.rgb, vec3(0.9, 0.93, 0.97) * diffuse, gSnow * 0.88);');
   var frag = function (fs) {
-    fs = fs.replace('#include <common>', '#include <common>\n' + HELPERS + '\n' + GEO_FUNCS).replace('#include <color_fragment>', body.join('\n'));
+    fs = fs.replace('#include <common>', '#include <common>\n' + HELPERS + '\n' + GEO_FUNCS + (DET ? '\nuniform sampler2D uGeoDetail;' : '')).replace('#include <color_fragment>', body.join('\n'));
     if (lit) { fs = fs.replace('#include <roughnessmap_fragment>', '#include <roughnessmap_fragment>\nroughnessFactor = clamp(mix(roughnessFactor * gRough, 0.55, gSnow), 0.3, 1.0);');
-      if (!LOW && S.bump > 0) fs = fs.replace('#include <normal_fragment_maps>', '#include <normal_fragment_maps>\nnormal = geoBump(-vViewPosition, normal, gH * ' + f(S.bump) + ' * (1.0 - smoothstep(' + f(S.bumpLod[0]) + ', ' + f(S.bumpLod[1]) + ', gDist)));'); }
+      if (!LOW && S.bump > 0) fs = fs.replace('#include <normal_fragment_maps>', '#include <normal_fragment_maps>\nnormal = geoBump(-vViewPosition, normal, gH * ' + f(S.bump) + ' * (1.0 - smoothstep(' + f(S.bumpLod[0]) + ', ' + f(S.bumpLod[1]) + ', gDist)));' + (DET ? '\n{ vec3 gT = gAn.x > gAn.z ? vec3(0.0, 0.0, 1.0) : vec3(1.0, 0.0, 0.0); vec2 gNt = (gD.gb - 0.5) * 2.0; float gNk = 0.5 * gDk * (1.0 - smoothstep(40.0, 250.0, gDist)); normal = normalize(normal + (mat3(viewMatrix) * (gT * gNt.x + vec3(0.0, 1.0, 0.0) * gNt.y)) * gNk); }' : '')); }
     return fs; };
-  var prevOBC = mat.onBeforeCompile, prevKey = mat.customProgramCacheKey; var key = 'mahworld-geo-' + [S.snowY < 1e4 ? Math.round(S.snowY) : 'x', S.strata, S.ledge, S.fracture, S.cleft, S.macro, S.grain, S.bump, lit ? 'L' : 'U', LOW ? 'lo' : 'hi', S.lod.join('x'), S.bumpLod.join('x'), MAJ, S.joint, 'v2b'].join('_');
-  mat.onBeforeCompile = function (sh, r) { if (prevOBC) prevOBC.call(this, sh, r);
+  var prevOBC = mat.onBeforeCompile, prevKey = mat.customProgramCacheKey; var key = 'mahworld-geo-' + [S.snowY < 1e4 ? Math.round(S.snowY) : 'x', S.strata, S.ledge, S.fracture, S.cleft, S.macro, S.grain, S.bump, lit ? 'L' : 'U', LOW ? 'lo' : 'hi', S.lod.join('x'), S.bumpLod.join('x'), MAJ, S.joint, 'v2b', DET ? 'dt' : 'nd'].join('_');
+  mat.onBeforeCompile = function (sh, r) { if (prevOBC) prevOBC.call(this, sh, r); if (DET) sh.uniforms.uGeoDetail = { value: S.detail.map };
     sh.vertexShader = sh.vertexShader.replace('#include <common>', '#include <common>\nvarying vec3 vSdW; varying vec3 vSdN;').replace('#include <project_vertex>', ['#include <project_vertex>',
       '{ vec4 sdP = vec4(transformed, 1.0);', '#ifdef USE_INSTANCING', '  sdP = instanceMatrix * sdP;', '#endif', '  vSdW = (modelMatrix * sdP).xyz; vSdN = vec3(0.0, 1.0, 0.0); }'].join('\n'));
     sh.fragmentShader = frag(sh.fragmentShader); };
