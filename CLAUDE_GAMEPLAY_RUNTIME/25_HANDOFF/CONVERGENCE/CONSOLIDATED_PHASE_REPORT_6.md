CONSOLIDATED PHASE REPORT 6 — owner full-job completion directive 2026-09-19 (the 11-item form) · gameplay / demo lane · 2026-09-19 · candidate CONSOLIDATED_C7

1. WHAT WAS COMPLETED (since C6 = 06b7418 / beb9946)
  · Interjection phase D — Hinge reauthored as a surface-supported hinge (planted feet through a root offset, hips back, neutral spine, hip drive, MAHGIC from the hip); ONE speed-driven flight lean curve (hover 6° · normal 24° · boost 40° at 1.3 × cruise), boost = `run` in powered flight (host `flight.boost_factor` 1.3 PROVISIONAL), pelvis-led commitment with trailing legs and arms, camera never coupled.
  · Phase E — Special #1 OVERHEAD EXTENSION RELEASE (rod projectile), Special #2 CHEST FLY CLAP RELEASE (ring projectile, a gather on each hand), Physical MAHGIC EIGHT-STRIKE ALTERNATING CURL BARRAGE (in-range approach, 8 host-scheduled alternating curls, COMBO_LOCK 1.0 s integrated with immunity / KO / guard-in-cone / reapply cooldown / caster stagger; no infinite stun).
  · Phase F — the five-stat family FORCE · POWER · ENGINE · CONTROL · RESILIENCE (0–999) as a data contract + ⚔ STATS card (baselines SOURCE_NOT_FOUND, points per level OWNER_DECISION 3 | 4, preview only), branching PROGRESSION with FUTURE / LOCKED nodes.
  · Phase G — the whole approved catalog as a status table (140 class × mode × pattern cells: BOUND / OWNER_DECISION / FUTURE; nothing invented, nothing enabled) + the ⚔ SKILLS card.
  · Lane E client — the TRAINING ROOM panel (event-driven tutorial steps, ¼-speed INSPECT replay, RESET) inside ⚔ COMBAT.
  · HOTFIX A — character intactness: diagnosed (two measured causes), repaired at the source (ATHLETE_M_V11 = dev_0.16 graded + seam-welded shoulder weights), gated in the chain, one residual handed to the native lane (see 4 / 5).
  · HOTFIX B — the generic phone ATTACK "hold + swipe" button deleted; quick slots 1–4 are the attack controls through the one dispatcher (see 6).
  · Phase H — the separated performance measurement and the combat-after-transitions regression (see 8 / 9); the F01 flight test route made robust to the boost.

2. OWNER INTERJECTIONS INTEGRATED (all, in order): the ULTRA MASTER INTERJECTION 2026-09-18 §21 A–H (A reconcile · B portrait HUD · C lower-body gate — done at C6; D–H — this candidate); the QUEUED HOTFIX INTERJECTION 2026-09-19 (§1–§4 intactness, §5–§6 attack button + island reflow, §7 acceptance, §8 report form); the FULL-JOB COMPLETION DIRECTIVE 2026-09-19 (this report, one candidate). Precedence applied: the hotfix supersedes phase B's PRIMARY ATTACK / CAST button (`INTERJECTION_PHASE_B_REPORT.md` amended); nothing older was resurrected.

3. FILES / SYSTEMS MATERIALLY CHANGED (commits 715579d → 4a17efd; docs follow)
  · Presentation: `lab/RigAnimator.js` (Hinge, flight lean curve, OVERHEAD / CHESTFLY / CURL patterns, Special pattern resolution, donor gating, attack-scoped shoulder allowance), `lab/PresentationAdapter.js` (root offset, rod / ring projectiles, second gather, world tags with COMBO), `lab/dustFx.js` (rod / ring wave shapes), `lab/presentation_defaults.json` (flight.lean, hinge, barrage), `lab/rig_profile.json` (`pose_overrides.overhead_release`).
  · Host: `play/PlayMode.js` (flight boost, lunge step, snapshot cruise / boost), `play/rules1723/RulesField.js` (MULTI_STRIKE, combo-lock, refusals, records; the view's AIRBORNE block removed), `play/rules1723/rules_17_23.dev.json` (3 skills, 3 bindings, 3 profiles, barrage effect, catalog_status), `00_CORE/dev_tuning.dev.json` (boost factor), `00_CORE/class_stats.dev.json` (new).
  · HUD / input: `lab/play.html`, `lab/gameHud.js`, `lab/touchControls.js`, `lab/play.js` (ATTACK button + modifier + gesture path deleted; per-slot tap / swipe; `directAttack(slot, source, dir)`; menu-open gate; training hooks), `lab/gameMenu.js` (COMBAT / STATS / SKILLS cards, ruleset copy path), `lab/rulesHud.js`.
  · Asset: `lab/assets/rig_shoulder_smooth.mjs` (new, the derivative pass) → `lab/assets/athlete_m_preview/dev_0.16/` (ATHLETE_M_V11, default; V10 / V9 kept); `deploy/build_static_demo.mjs` (ships dev_0.16 + the ruleset / class stats / rooms copies).
  · Diagnostics (kept, intentional): `deploy/probe_intactness.mjs`, `probe_mesh_stretch.mjs`, `mesh_components.mjs`, `mesh_seams.mjs`, `probe_mesh_caps.mjs`, `probe_hinge_flight.mjs`, `probe_attacks_phase_e.mjs`, `probe_sword_stats.mjs`, `probe_combat_after.mjs`, `probe_perf_separated.mjs`, `probe_hud_portrait.mjs` (no ATTACK control), `probe_lib.mjs` (CDP timeouts, −0 / NaN values).
  · Tests: `16_TESTS/gameplay_motion_phase_d`, `gameplay_attacks_phase_e`, `gameplay_stats_phase_f` (new); `gameplay_hud_phase_b`, `gameplay_hud_pass3`, `gameplay_direct_ui`, `gameplay_flight_f01`, `gameplay_hip_connect`, `gameplay_legs_faithful`, `gameplay_rules_17_23`, `gameplay_loadout_422`, `gameplay_mahgic_anywhere` (updated).

4. ROOT CAUSES FOUND (major defects)
  · Shoulder tearing (owner screenshots): (1) TOPOLOGY — the body is a quilt of 1 485 patches with coincident, unwelded borders and overlapping edges; (2) SKIN WEIGHTING — a 0.4 UPPERARM weight step across one 8–11 mm edge at the deltoid / armpit, and left / right scapula weights crossing the spine midline. Measured with the mesh re-skinned on the CPU at the frozen runtime poses (edges stretching 2.5–8.6×; overlap pairs opening up to 8 cm).
  · The two new Specials never played their exercise: every SPECIAL_MAGIC cast was forced to the generic CAST pose with the Tripo cast-a-spell donor clip blended over it (one-handed throw).
  · Casts in flight refused from the HUD: the rules view still carried an AIRBORNE state block (the last remnant of the flight blanket lock) although the host accepted the cast.
  · The blank intactness stills (198 frames): a settled yaw of −0 came back as `undefined` through the DevTools protocol and poisoned the probe's camera — a probe fault, fixed in `probe_lib`; the game never had a NaN camera (the dev hook now ignores non-finite input anyway).
  · The old F01 flight test's ceiling step: the phase-D boost moved its route onto a roof (test route fixed, no criterion changed).

5. CHARACTER INTACTNESS — what was done (full detail: `INTERJECTION_HOTFIX_0919_REPORT.md`; native contract: `SHOULDER_GIRDLE_NATIVE_REPAIR_CONTRACT.md`)
  Repair (weights only, derivative workflow, no Blender): seam-welded Laplacian smoothing around the glenohumeral and armpit centres, an overlap weld of same-facing patch borders, mid-back scapula symmetry → ATHLETE_M_V11. Measured: seam weight mismatches 0; overlap gaps > 12 mm 0 at every pose (max 2 mm); stretched edges rotation release 359 → 4, chest fly 115 → 2, double biceps 160 → 39, overhead 109 → 94 at the new 135° load (max 3.4×).
  INTACTNESS TABLE (V11, `intact/sheet_intact_<pose>.jpg`, A production + VFX / B VFX off / C neutral / E wireframe × front / side / rear-¾ / rear, frozen frames, MANUAL LOOK; judged by the agent from the sheets — Jah's phone verdict is the acceptance):
    idle INTACT · push load INTACT (pale plates at the front deltoids in production only, C clean — the S10 axilla loft patches, unmapped in the atlas, see 11) · push release INTACT · pull release INTACT · hinge load INTACT (same pale plates in A) · rotation release INTACT (no see-through; a small pale fold at the rear deltoid) · overhead load INTACT (a small pale fragment at the armpit side in A) · overhead release INTACT (small white flakes at the right shoulder in A) · chest fly load INTACT · chest fly clap INTACT · curl strike INTACT · guard INTACT · dash INTACT (pale fragments at the deltoids in A) · flight cruise INTACT (the A-front frame is a probe artifact: the camera collided at the moment; B / C front clean) · flight boost INTACT (pale shards at both front deltoids in A / B, C clean) · split mid INTACT · split walk INTACT in row A (rows B / C / E are not the same instant: the separated legs follow the host gait, not the held animator — the D probe and the walk-identity evidence cover the legs) · DOUBLE BICEPS emote NOT INTACT at rear-¾ (a hole at the right rear shoulder / lat, the background shows; C confirms it is geometry) → native lane.
  Not covered by anything: no bloom, dense MAHGIC, farther camera, slower attack, cover plate, fake shell, double-sided material or hiding.

6. ATTACK CONTROLS — what changed
  Deleted: the phone ATTACK / CAST "hold + swipe" button, its CSS and dock rollback rules, the touch layer's attack modifier and gesture role, the page's gesture / attackTap callbacks, the armed-move gesture dispatcher, the HOLD-a-slot-to-arm two-step. Nothing replaced it: row C's two left cells are empty (open world, camera-drag space); the small category label sits under the slot arc (tap = next category).
  Quick slots 1–4 ARE the attack controls: identity + category colour + cooldown / unavailable state; TAP = execute; SWIPE ≥ 24 px from the slot = that move's own arrow (a wrong arrow is refused with the expected arrow, no cast). ONE dispatcher for slot tap / slot swipe / E R T Y / ⚔ ARM: `directAttack(slot, source, dir)` → `rulesHud.castSlot` → RULES_SELECT + RULES_CAST on the in-page authority; exactly one cast start per tap; no dispatch while the ≡ / ⚔ surface is open (menus own their input). GUARD, DASH (hold + swipe kept), FLY, ≡, ⚔ directly reachable, 42 / 36 px faces with 4 px hit halos, no overlaps. Desktop keeps V · E R T Y · M · B.

7. CAMERA / FLIGHT / HUD / COMBAT — what changed
  Camera: unchanged in behaviour (persistent manual orbit, ⟲ CAM reset, position follow, no heading follow, obstruction by boom shortening — PASS 2 truth kept); never coupled to the flight lean (probe D10).
  Flight: hold-to-ascend / release holds altitude unchanged (F01 29 / 29, probe B9); the ONE lean curve; boost on `run`; casts in flight now dispatch from the slots (host unchanged: outcomes height-aware).
  HUD: phase B topology minus the ATTACK button; ⚔ COMBAT (+ TRAINING ROOM card), STATS, SKILLS (PROGRESSION + CATALOG STATUS); class-themed Athlete gold kept; no permanent USE button (contextual prompts: probe B11).
  Combat: three new skills / bindings / profiles, MULTI_STRIKE + COMBO_LOCK on the existing authority; catalog status data; no parallel hit detection, no capacity change, no live rebalance.

8. RUNTIME / DEVICE TESTING PERFORMED (all headless Chrome on this PC; touch EMULATED; iPhone NOT tested by the agent)
  Page probes on the C7 build (records in `25_HANDOFF/CONVERGENCE/`): intactness matrix 18 poses × 4 modes × 4 angles + close-ups (`intact/`), mesh stretch / gaps / seams (`intact/mesh_stretch.json`), attacks E 9 / 9 (`attacks_e/`), motion D 14 / 14 (`motion_d/`), stats F portrait 4 / 4 + desktop 4 / 4 (`stats_f/`), HUD portrait 17 / 17 · landscape 17 / 17 · desktop 5 / 5 (`hud_b/`), combat after menus / rotation / focus loss / flight / landing / split-fuse / Training Room 12 / 12 (`hud_b/probe_combat_after.json`), performance separated (`perf_h/`).
  PERFORMANCE (desktop GPU headless Chrome, normal speed, a straight walking leg; five runs on this build while the PC ran other work): INPUT DELAY host 31–53 ms / displayed 147–148 ms (the 120 ms presentation delay + the 20 Hz tick) · ANIMATION STEPPING 0 zero-advance frames every run, speed coefficient of variation 0.21 / 0.21 / 0.44 / 0.43 / pass / pass (the 0.35 line was crossed twice on a loaded machine) · FRAME STALLS p95 10.2 / 13.6 / 27.7 / 17.4 / 29.3 ms, p99 ≤ 33 ms, 0 frames > 100 ms in every run · CAMERA JITTER RMS 0.89 / 1.12 / 3.32 / 2.10 / 2.71 px per frame² (the metric is per frame, so it rises with the frame time; the 2.5 line was crossed twice) · STATE DELAY 91–94 ms at 3.6–3.8 m/s, nothing accumulating. Honest reading: no stalls and no stepping in any run; the two threshold crossings track the machine's frame time, not a code change (no per-frame work was added in C7); a phone number is Jah's measurement.
  Gate on the draft: play 401 · BUILD_INFO 401 · dustFx.js 401 · the V11 rig 401 (unauthenticated protection PASS); correct-password login NOT RUN.

9. REGRESSION TESTS: node suite 42 files · 1 319 checks · 0 failing on 4a17efd (rerun inside the packaging pipeline); package pipeline stages 0–7e all PASS; smoke 7 / 7 from the fresh extraction.

10. NEWEST REVIEW CANDIDATE
  ZIP  C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS\CLAUDE_GAMEPLAY_RUNTIME\26_LOCAL_AUTHORITY\deploy\packages\CONSOLIDATED_C7.zip · 31 342 949 bytes · 116 entries · SHA-256 18172f155b98e6ccd5aebbdda1b27d7353d317b97fc3397ef64aef22eb97cb33 · packaged from commit 4a17efd (clean) · record CONSOLIDATED_C7.PACKAGE.json.
  DRAFT https://6aaeb66e5b7bbc1b49221b84--mahworld-test-preview.netlify.app (netlify-cli draft of the fresh C7 extraction; build label BUILD_INFO commit 4a17efd = ≡ → SETTINGS about line / dev panel; rig ATHLETE_M_V11 dev_0.16; rollbacks `?avatar=ATHLETE_M_V10`, `?hud=dock`, `?cam_follow=1`). No live promotion: https://mahdemo.fob.systems = 6aac25101f17ce5ed37166e4 untouched. Previous candidates C6 (33387cdf…, draft 6aae1706…) / C5 / C4 remain.
  Docs after the packaged commit (documentation only, explained): this report, `INTERJECTION_HOTFIX_0919_REPORT.md` (candidate lines), `INTERJECTION_PHASES_D_G_REPORT.md`, `SHOULDER_GIRDLE_NATIVE_REPAIR_CONTRACT.md`, `CONTINUE_HERE.md`.

11. REMAINING ISSUES — truly unresolved (not buried)
  · Intactness residual: the DOUBLE-BICEPS emote at rear-¾ (a covered hole in the quilt topology) — native lane, bounded contract; and, in production only, pale / tan plates at the front deltoids at some arm poses (push load, hinge load, dash, flight boost): the S10 axilla loft patches (`rig_axilla.mjs`, inner-arm / lat patches lofted without atlas UVs) and the v5 shoulder correctives show as unmapped colour when the arm lifts — a texture-coordinate fault of those derivative patches (neutral rows are clean); fix = UVs for the loft patches in the derivative chain or the native weld (both listed in the contract). Not hidden.
  · Physical-phone acceptance (artistic + feel) — Jah only; emulated touch is not iPhone acceptance.
  · Owner-side inputs still missing on this machine: the four evidence files (interjection `.md`, IMG_1118 / IMG_1117, the 23-28-36 recording) and the Fitness Class Archetypes stat source (the STATS card says SOURCE_NOT_FOUND instead of inventing numbers).
  · Owner decisions open: points per level 3 vs 4; `PASS_7_COMBAT_DECISIONS_REQUEST.md` cells (tiers, Vector Shot / BLOCK BREAK patterns); the botany specimen; the lower-body (phase C) and shoulder-girdle native repairs wait for the Blender slot.
  · Performance on a phone: not measured by the agent.
  · The curl barrage's gather dust reads dense at the 2.6 m close-up (the sheet's A-front frame) — the language accepted at gameplay distance in lane D; a look for Jah, not a regression.
