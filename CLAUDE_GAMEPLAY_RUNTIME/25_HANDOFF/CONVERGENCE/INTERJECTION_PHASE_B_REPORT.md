# OWNER INTERJECTION 2026-09-18 — PHASE B REPORT (§23 form) · portrait HUD / control topology · gameplay / demo lane · 2026-09-19

> **AMENDED 2026-09-19 by the owner hotfix interjection §5 / §6 (record kept as written below for lineage).** The PRIMARY ATTACK / CAST button this report introduced (two quick-slots wide, hold + swipe = the armed move's gesture, HOLD a quick slot = arm it) is **DELETED** and not replaced by any generic control. The numbered quick slots 1–4 are now the attack controls themselves: TAP a slot = execute that move; SWIPE from a slot (≥ 24 px) = that move's own arrow gesture, refused as WRONG SWIPE when it is not the move's arrow; there is no hold-to-arm two-step and no attack modifier in the touch layer. Row C's two left cells stay EMPTY (open world, camera-drag space); the small category label sits under the quick-slot arc (tap = next category). Every path (slot tap, slot swipe, E / R / T / Y, the ⚔ COMBAT ARM + slot) enters the ONE dispatcher `directAttack(slot, source, dir)` → `rulesHud.castSlot` → RULES_SELECT + RULES_CAST on the host. Desktop keeps its keyboard / mouse bindings (V · E R T Y · M · B). Evidence + report: `INTERJECTION_HOTFIX_0919_REPORT.md`, `hud_b/` C7 records.

CURRENT JOB PRESERVED:
  The running job is the owner assignment 2026-09-18 (lanes A–H) with the ULTRA MASTER INTERJECTION folded in at the lane E host checkpoint (72432d4 → reconciliation 1332b66). Nothing was restarted: lanes B (camera + tap-to-move), C (hip connection, V10 = dev_0.15), D (granular MAHGIC + casting anywhere) and the Training Room host field are intact and regression-tested in this suite. This checkpoint = interjection §21 B (the portrait HUD / control topology as ONE reversible semantic layout pass). §21 C–H are NOT started (listed under NEXT). Commit: 06b7418 (code) — the docs commit after it is documentation only.

BUILD / ASSET IDENTITY:
  Static build from commit 06b7418 (BUILD_INFO.json carries the commit; the visible label is the ≡ → SETTINGS about line + the dev panel). Athlete rig dev_0.15 (ATHLETE_M_V10 — NOT owner-approved; §6 lower body is phase C) with dev_0.14 (V9) shipped for rollback; residents v4; three.js 0.185.1; ruleset rules_17_23.dev 31 skills / 68 bindings (unchanged in this phase). Host change: ONE field — `level` in the rules others view (the source of the world-space tag). No purchase, install, subscription, credential, Blender launch or live action.

VIDEO REVIEW:
  NOT DONE — `ScreenRecording_09-18-2026 23-28-36_1.mp4`, `IMG_1118.jpeg`, `IMG_1117.jpeg` and the interjection `.md` are not on this machine (searched Downloads, Documents\Codex and the working roots on 2026-09-19). The interjection text in chat is the authority used. Nothing is claimed about the recording's contents; the timestamps in §1 stay unread until the file exists.

HUD (what actually changed — all in `lab/play.html` + `lab/gameHud.js` + `lab/gameMenu.js`; the old layout survives behind `?hud=dock`):
  §3.1 bars — the shared dark-glass card is gone: HEALTH (with `LV n`) upper-left, COMBAT MAHGIC + FLIGHT MAHGIC upper-right in the space the attack selector freed; 5 px bars with a thin platinum edge, 8 px labels with text-shadow, no backdrop blur; depleted / critical: HEALTH ≤ 25 % turns red and blinks, COMBAT ≤ 25 % amber, FLIGHT keeps its warning blink; landing / forced-landing / KO warnings unchanged (still shown).
  §3.2 category card — the top-right PHYSICAL / PHYS MAHGIC / SPECIAL MAHGIC dock + drawer no longer renders; the armed category is a 9 px gold label directly above PRIMARY (`PHYSICAL ▸` — tap = next category, through the ONE selection function); the desktop shows the same label above its bottom bar (Z still cycles).
  §3.3 target — the LOCKED panel is gone from the DOM (it exists only with `?hud=dock`). World-space tag under the actor's name: free roam `LV 1`; practice / match + lock `LV 1 · LOCKED` in gold (+ `behind cover` / `too far` while the lock is dropping); the health bar above the actor shows ONLY in PRACTICE / MATCH (`in_duel` now follows the rules context — in free roam the dummy's bar, which used to be always on, is hidden); the gold lock corner indicators stay; ⚔ COMBAT lists the lock with UNLOCK.
  §3.4 state — `FREE ROAM · HARMLESS` / `PRACTICE BATTLE` / `MAH MATCH — DAMAGE ACTIVE` is a 9 px letter-spaced line (8.5 px on phones, placed under the bars so it never covers the HEALTH value); toasts / warnings stack under it.
  §4 controls (touch) — LEFT: joystick ring 124 px (was 112; landscape 108, was 96; the stick radius follows the styled ring) with MAP · SPEED · CAM in a shallow mirrored arc above it (left and right at the same height, SPEED 6 px higher). RIGHT combat island, rows from the bottom: row D `· · GUARD DASH` (former 3 / 4 spots; DOWN (hold) · LAND fill the two free cells while airborne — FLY never moves), row C `PRIMARY── FLY ≡` (PRIMARY two slots wide, reads ATTACK or CAST after the armed category, hold + swipe = the armed move's gesture, tap = fallback; FLY direct hold; ≡ lower-right above DASH), the category label, row B quick slots `1 2 3 4` as a shallow arc (2 · 3 raised 6 px; tap = execute the slot directly, HOLD ≈ 450 ms = arm it for PRIMARY), row A ⚔ above slot 4. Removed on touch: the separate MAHGIC quick-cast button (PRIMARY casts when a MAHGIC category is armed; M and the desktop MAHGIC button remain) — reversible if Jah wants it back.
  §4 VISIBLE vs HIT bounds (measured by `elementFromPoint` outward from every edge, probe B3, portrait and landscape): cells (quick 1–4, GUARD, DASH, FLY, MAP, SPEED, CAM) face 42 × 42 px (0.83 × the 50 px PASS 3 cells) → hit 50 × 50; PRIMARY face 92 × 42 → hit 100 × 50; ≡ / ⚔ face 36 × 36 → hit 44 × 44. The hit halo is a transparent 4 px ring on every side; gaps are 8 px, so halos touch and never overlap (probe B4: 0 face overlaps, 0 halo overlaps); the arc answers its own taps inside the stick zone (MAP hit-tested); the contextual LIFT prompt and the framed body overlap nothing (B10 / B11).
  §4.3 reconciliation of the eight-equipped capacity (documented model, capacity untouched): the host keeps 4 PHYSICAL + 2 PHYS MAHGIC + 2 SPECIAL MAHGIC equipped (loadout unchanged). The quick slots 1–4 are the equipped moves of the ARMED category (PHYSICAL shows 4; a MAHGIC category shows 2 + two empty faces). ONE armed move per category (the host's `selection`): PRIMARY performs the armed move of the armed category; tap on a quick slot executes that slot directly without arming (the direct-slot rule of E / R / T / Y); hold arms it; the label cycles the category; ⚔ COMBAT shows all 8 with ARM and the armed row; LOADOUT (unchanged) equips.
  §5 ⚔ — a compact SWORD button opens the ≡ menu on a new first tab COMBAT (armed category with equipped counts, the 8 equipped rows with movement pattern · resolved stack · gesture arrow · cost · availability + ARM, the lock + UNLOCK, practice START / STOP, the reconciliation line). SKILLS · STATS · LOADOUT are the existing tabs (the five-stat family is phase F).
  Portrait proof: `25_HANDOFF/CONVERGENCE/hud_b/hud_b_portrait_{ground,lock,sword,air,rollback}.png` (390 × 844), landscape `hud_b_landscape_{ground,lock,sword,air}.png` (844 × 390), desktop `hud_b_desktop_{ground,sword,lock}.png`; records `hud_b_{portrait,landscape,desktop}.json` — probe `deploy/probe_hud_portrait.mjs`: portrait 17 / 17, landscape 17 / 17, desktop 5 / 5, touch EMULATED (headless Chrome mobile emulation); the iPhone check is Jah's only.
  Rollback: `?hud=dock` restores the PASS 3 cluster, the top-right dock, the LOCKED panel and the 112 px stick on the same build (probe B12 PASS).

LOWER BODY: the §6.1 gate RAN at this checkpoint (phase C, `LOWER_BODY_NATIVE_REPAIR_CONTRACT.md`). CONFIRMED cause = TOPOLOGY, not weights: the derivative is two closed surfaces at the hip — a pelvis shell cut horizontally 5.75 cm below the femoral head (110-vertex hem ring at y 0.394) over dome-topped separate thighs (no shared edge loop; ring gap median 7.7 cm, normal mismatch 109°); the fused lower primitive is cut on the same line, so the SOURCE is cut there. Weights are not the fault (the arm-chain shares an earlier band table showed in the pelvis bands were the hands hanging beside the hips; 0 pelvis / glute vertices carry arm weight). V10's matched weights synchronize the hem with the thigh (the ≤ 2 cm hem metric) but cannot make one glute → thigh surface or a real crease. Native / runtime operations: runtime hip work STOPS (no dev_0.16 weighting pass); the bounded native-source repair contract (one continuous pelvis-glute-crease-thigh surface per side with a shared anatomical loop, natively authored PELVIS ↔ THIGH weights, fused silhouette untouched, same GLB layout for make_class_rig_v5) goes to the Character / Blender lane; the Blender slot stays RESERVED. Remaining visible fault: the hem / shell in the separated form (V10 shipped, NOT approved; `?avatar=ATHLETE_M_V9` rolls back).
FLIGHT: NOT STARTED (phase D). ATTACKS: NOT STARTED (Hinge reauthor, Special #1 / #2, curl barrage = phases D / E). MAHGIC: unchanged since report 5 (granular dust for PUSH / PULL / GATHER / CURTAIN; new formations come with phases D / E).
STATS / SKILLS: NOT STARTED as UI (phase F). Canonical source status (honest): the "MAHWORLD Fitness Class Archetypes" source is NOT on this machine — a search of the three reference roots for RESILIENCE / Archetype finds only the canon's identity sentences (TITAN maximum force / strength / resilience / power; ATHLETE explosive all-round …) — no numeric FORCE · POWER · ENGINE · CONTROL · RESILIENCE baselines. Unless Jah points at the file, the STATS panel will show "class baseline: source not found" rather than invented numbers; 3-vs-4 points per level stays OWNER_DECISION.

PERFORMANCE: not re-measured in this checkpoint (HUD-only change, no new VFX or combat load; the probe reports zero page errors in all three modes). The separated input-delay / animation-stepping / stall / camera-jitter / state-delay measurement is phase H; physical phone = Jah only.

PACKAGE: C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS\CLAUDE_GAMEPLAY_RUNTIME\26_LOCAL_AUTHORITY\deploy\packages\CONSOLIDATED_C6.zip · 28 973 051 bytes · 111 entries · SHA-256 33387cdfbc3a50d5dd9ff79c1731a660737e4a8a91758bc044cc25a11d29a994 · packaged from commit 06b7418 (clean; build stamp 2026-09-19T04:54:04Z) · pipeline: 39 test files / 1 282 checks / 0 failing, JS sweep 71 files, CSS sweep, stamps, backend boundaries, secret scan (111 files), ZIP integrity 111 = 111, fresh extraction byte-identical, smoke 7 / 7 from the extraction, JS sweep from the extraction clean · record CONSOLIDATED_C6.PACKAGE.json.

CANDIDATE: ONE review URL — DRAFT https://6aae1706fc3faf715799be5b--mahworld-test-preview.netlify.app (netlify-cli draft of a fresh Expand-Archive extraction of CONSOLIDATED_C6.zip, 111 files; BUILD_INFO commit 06b7418 = the visible build label in ≡ → SETTINGS / DEV). Gate: unauthenticated GET → 401 on /play, gameHud.js, BUILD_INFO.json, dustFx.js and the dev_0.15 rig (protection PASS, its own line); correct-password login NOT RUN. Self-contained; nothing points at production. Rollback = C5 (488e57c4…, draft 6aae0732…) / C4 / C3 / live.

LIVE: unchanged (https://mahdemo.fob.systems = 6aac25101f17ce5ed37166e4, untouched; no live promotion in this checkpoint).

NEXT (≤ 3 owner actions):
  1. Jah (phone, on the C6 draft, portrait then landscape, ≤ 2 minutes): thumb the island — DASH (corner), GUARD, PRIMARY hold + swipe, a quick-slot tap and a quick-slot HOLD (arm), the category label, ⚔; then MAP / SPEED / CAM over the joystick. One line: "keep" or "move X / bigger X".
  2. Jah (one path): where the four evidence files are (`…INTERJECTION_UI_MOTION_ATTACKS_STATS.md`, IMG_1118, IMG_1117, the 23-28-36 recording) and where the Fitness Class Archetypes stat source lives — phase F cannot show numbers without it.
  Independent work continues without waiting: phase C (§6.1 gate on the runtime derivative + the native repair contract), then D (Hinge + flight lean), E (the three attacks), F, G, H.
