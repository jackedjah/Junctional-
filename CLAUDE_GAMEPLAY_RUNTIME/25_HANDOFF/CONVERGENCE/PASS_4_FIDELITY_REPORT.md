PASS 4A — source-to-runtime fidelity diagnosis and the render-side repairs (O5 / G25 / G26 / G28) · 2026-09-18

Report separation: (1) in progress before the addendum = PASS 3 HUD + its review round (done, see PASS_3_HUD_REPORT.md); (2) appended by the addendum = walk identity (PASS 4b, waits for the separated-leg derivative), sky / far-background (2b slice done, PASS 9 full), presentation pivot (2b done); (3) completed now = PASS 4A below (diagnosis + render-side fixes); (4) remaining = PASS 4B faithful separated legs (O6 / G32–G35) → 4b walk identity → 5 → 6 → 7 → 8 → 9, gates A / B / C.

WHAT I MEASURED (headless Chrome, `node deploy/probe_fidelity.mjs --dpr 1|2|3`, 390 × 844 CSS px, MED tier = the phone default; touch not needed):
  · buffer chain BEFORE (MED cap 1.0): the drawing buffer was 331 × 717 px at every device pixel ratio (adaptive 0.85 on the harness) — 85 % of the device pixels at DPR 1, 43 % at DPR 2, 28 % at DPR 3 (a 1170 × 2532 phone). His gameplay-distance box: 322 buffer px tall at DPR 1, 188 at DPR 2, 193 at DPR 3, i.e. the phone stretched a ~190-px-tall figure over ~950 device pixels (≈ 5× upscale). That is the primary "pixelated / low-resolution" cause (G28) — the assets were never sampled at the screen's resolution.
  · textures: the character's runtime derivative uses a 2048 base colour + 2048 normal + 1024 emissive, all with anisotropy 1 (trilinear only); the gym landmark's 2048 base had anisotropy 8 but its 1024 normal map had 1; the native training hall has no textures (procedural materials, 1.8 k triangles). Renderer: ACES, exposure 1.35, sRGB output, WebGL2, antialias on, max anisotropy 16.
  · geometry (G26): neutral opaque shading and wireframe at close range (back / side / front) show continuous surfaces — no holes, no missing polygons; the runtime body is 18 315 triangles / 6 materials (`fidelity/fid_dpr1_close_back_neutral.png`, `_wireframe.png`).
  · the pale / tan hard-edged patches on the upper back (F03): they persist with the normal map disabled (`fidelity/mat_E_normal0.png`) and with every material variant (metalness 0.5–0.72, roughness 0.34–0.5, env 0.5–1.0) — so they are not a lighting artefact of the runtime look. A block comparison of the runtime 2048 base map against the character owner's 8K source (`Mah_Athlete_M_AM08_FINGER_COLOR_TEST_8K.png`, 1024-cell grid, `fidelity/tex_source_vs_runtime_1024.jpg`) shows the same atlas: max block difference 11.2 / 255, 0 cells above 40. The runtime map is a faithful downsample; the patches are painted in the SOURCE atlas (flat tan / grey UV islands behind the shoulder blades). → a CHARACTER-LANE finding (Astra / Character Claude); nothing in the gameplay lane can fix it without touching the protected source, so it is reported, not patched.
  · gym landmark softness at close range (F08): a 48 m building on one 2048 map ≈ 2.3 cm per texel at best, and the runtime derivative's normal map is 1024 — soft by construction at arm's length. A 4096 base for the landmark you can walk into is a PASS 9 (town / interior) decision (memory: 64 MB per 4096 RGBA map on the phone).

WHAT I CHANGED (gameplay lane, presentation only; no source asset touched):
  26_LOCAL_AUTHORITY/lab/quality.js       MED cap 1.0 → 2.0 (floor 0.6 → 1.0), HIGH cap 1.5 → 3.0 = the full device ratio (floor 1.0; desktop / opt-in tier), LOW cap 0.75 → 1.0 (floor 0.6). A 3× phone now renders 780 × 1688 (the adaptive ratio still drops 0.25 per step above a 21 ms average frame and settles at 1.0 at worst, never at 0.6). Reversible in one line; the Control Lab / `?quality=` overrides are unchanged.
  26_LOCAL_AUTHORITY/lab/GlbCharacter.js  the character's base / normal / emissive (and any roughness / metalness) maps get anisotropy 8 on the material clone (the shared runtime texture, not the GLB).
  26_LOCAL_AUTHORITY/lab/fieldScene.js    landmark normal maps get anisotropy 8 too (the base already had it).
  26_LOCAL_AUTHORITY/lab/play.js          dev hooks `renderChain()` (device px, drawing buffer, render scale, tier caps, antialias, tone mapping, exposure, anisotropy), `textureAudit()` (me / gym / native), `shadeMode('production'|'neutral'|'wire')`; `rendererDebug()` keeps returning the renderer (the route probe depends on it).
  deploy/probe_fidelity.mjs (new)         the diagnosis probe (buffer chain, texture audit, 4 views × 3 shadings, native 1:1 crop of his box read from the drawing buffer); deploy/probe_route.mjs gained `ROUTE_DPR` / `ROUTE_MOBILE` for phone-like device pixels.

COST (harness = laptop GPU through ANGLE d3d11, NOT a phone; the phone measurement is Card A's): the Spec A route at 390 × 844, emulated DPR 2, MED tier — cap 1.0: route p50 5.6 / p95 9.0 ms; cap 2.0 (buffer 780 × 1688): p50 10.1 / p95 15.1 ms (`25_HANDOFF/PASSES/perf/route_pass4_cap1_dpr2.json`, `route_pass4_cap2_dpr2.json`). Under the 16.7 ms budget at p95 on this GPU; a slower phone GPU is caught by the adaptive drop (floor 1.0 = the old behaviour on a 1× device, 2–3× better than the old floor on a 3× phone). The DPR-1 route series (conv1 / conv2b2 / conv3) are unaffected by the cap (min(cap, 1) = 1) and remain the regression baseline.

EVIDENCE (`25_HANDOFF/CONVERGENCE/fidelity/`): `fidelity_dpr1/2/3.json` (after), `fidelity_dpr3_before_cap.json` (before), `fid_dpr3_native_crop_before_cap.png` (125 × 209 buffer px) vs `fid_dpr3_native_crop_after_cap.png` (197 × 338 on the harness — the adaptive ratio sat at 1.0 there; a phone at 2.0 doubles it again), `g28_native_before_after_dpr3.png` (both upscaled to the phone's ~966 device px like the compositor does), `fid_dpr1_close_back_{production,neutral,wireframe}.png`, `fid_dpr1_gameplay_{production,wireframe}.png`, `mat_A_current_m072_r034.png` / `mat_E_normal0.png` (the patches with the normal map on / off), `tex_source_vs_runtime_1024.jpg` (source 8K vs runtime 2048 block diff).

ACCEPTANCE (brief §PASS 4, the diagnosis half): fidelity diagnosis with the exact loss stage named → PASS (the render scale on phones; the texture patches are source-side; geometry continuous) · render-side fix with a perf check → PASS on the harness, phone cost = Card A · G26 surface continuity → PASS (neutral + wire) · G28 native crops → PASS (before / after) · the separated-leg derivative (O6 / G32–G35) → NOT IN THIS HALF (PASS 4B, next).

OPEN RISKS: the real phone frame time at 2× (Card A: `?lab=1` shows `buffer W × H px (render scale %)` and the frame average); the source-atlas patches remain until the character lane repaints those islands; the gym's 2048 / 1024 maps stay soft at arm's length until PASS 9 decides on 4096 for the enterable landmark.
SUPERSEDED: the MED cap 1.0 (Spec A, 2026-09-16, chosen when the phone frame time was unknown) → 2.0 with floor 1.0 (measured cost above).
RESOURCES: three.js 0.185.1 texture anisotropy / renderer pixel ratio (vendored); headless Chrome device-metrics emulation (DPR 1 / 2 / 3); the owner's 8K source atlas was READ for the block comparison only (never written, never copied into the runtime). Tripo / Reallusion / Blender: NOT USED (Blender slot still reserved).
