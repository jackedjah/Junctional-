# M7 HALO scale/access focused visual check

One bounded actual-runtime NIGHT route produced nine views with zero console errors. The same route covered the mainland approach, engineered underside, connected road gradients, live ascent midpoint, full shell scale, arrival court, interior panorama, shell/promenade detail and phone-sized upper arrival.

Runtime facts from `report.json`:

- `WORLD` before ascent at ground `0 m`, pool `200`.
- `TRANSIT` midpoint at progress `0.503`, ground `121.124981 m`, pool `200`.
- `HALO` after docking at `240 m`, contained, guide interaction accepted, pool `200`.
- `WORLD` after the `20 s` down ride at ground `0 m`, pool `200`.
- Contract envelope: base radius `6.2 m`, shell radius `147.6 m`, diameter `295.2 m`, playable radius `144.8 m`, structural radius `153 m`, apex `387.6 m`.

Inspected images copied here: `03_ground_road_gradients.png`, `05_halo_exterior_scale.png`, `06_upper_arrival_court.png`, `07_halo_interior_panorama.png`, `09_phone_upper_arrival.png`. The phone-sized capture is visual framing evidence, not physical-phone acceptance.
