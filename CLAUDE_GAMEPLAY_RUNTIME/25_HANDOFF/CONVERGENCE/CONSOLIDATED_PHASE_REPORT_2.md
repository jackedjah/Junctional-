CONSOLIDATED PHASE REPORT 2 — master §20.5 form · 2026-09-18 22:50 UTC · gameplay / demo lane · answers the owner's C1 CHECKPOINT REVIEW (items 1–6)

WORK PACKAGE / CHECKPOINT:
  G37 RESOLVED BY REPAIR (item 1) · camera ownership preserved, report corrected (item 2) · botany kept at the prototype gate with the matched workload, far-LOD + placement DEFINITIONS, review packet (item 3) · P8 first entrance transaction (MAH GYM) + the two zero-stock shops DONE with readiness / cancellation / failure / repeat / duplicate evidence (item 4) · reporting corrected (item 5) · decisions printed (item 6, OWNER_DECISIONS_C2.md).
  Checkpoints (all on master, linear, nothing reset): 8962a11 (G37) → 5b761fa (botany workload / LOD / placement definitions / Packet C) → 3a1c6d5 (P8) → this report (documentation only). C1 preserved (ZIP + draft 6aad9c2b… untouched).

CURRENT JOB PRESERVED:
  The C1 candidate, the worktree, the live deployment (6aac25101f17ce5ed37166e4, https://mahdemo.fob.systems) and the rollback (6aab65567e997ae33c1da204) are untouched. P0 was not restarted; the unified master was not rewritten. No model change, purchase, install, protected-source edit or Blender launch. The three tree verdict, the phone experience and any contact-drift "exception" remain NOT approved — nothing below treats them as approved.

ACTUAL WORKTREE / BUILD / ASSETS:
  Repo C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS (git master) · packaged source commit 3a1c6d5 (clean) · build stamp 2026-09-18T22:42:21Z · 109 files · athlete dev_0.14 (ATHLETE_M_V9 GLB sha256 7d217d0f…) · protected sources unchanged (RAW_10_MODELS/Mah_Athlete_M.glb 7b023694…) · three.js 0.185.1 vendored · R170 still ABSENT (nothing invented from it; nothing blocked on it).
  New runtime files: lab/interiorScene.js, lab/assets/shops/shops_v1.json, lab/assets/botany/placement_v1.json (definition only), deploy/probe_interior.mjs, deploy/botany_placement_preview.mjs; changed: lab/RigAnimator.js, GlbCharacter.js, PresentationAdapter.js, play.js, play.html, gameHud.js, fieldScene.js, mahgicTree.js, assets/botany/tree_spec.js, presentation_defaults.json, play/PlayMode.js, play/rules1723/rules_17_23.dev.json (three ENTER doors), 00_CORE/dev_tuning.dev.json (three interior rooms), deploy/build_static_demo.mjs, probes probe_motion_e / probe_botany / probe_hud_pass3.

WHAT I DID:
  actual changed files and purpose
  · G37 — lab/RigAnimator.js `plantPass` (world-space support hold: the pointed tip end captured at the support event, 3D two-bone IK in the pelvis frame written AFTER the rate filter, minimal-swing continuity, engage 40 ms / release 80 ms blends outside the contact interval), lab/GlbCharacter.js `tipExtLocal` (measured at bind: 0.0296 bone units = 0.054 m), lab/PresentationAdapter.js (groundAt passed; the figure group keeps last frame's offset during animate), lab/play.js (motion trace records the tip ENDS and the anchors; the trace samples this frame's matrices), lab/presentation_defaults.json `gait.plant*` (rollback `plant:false` = the PASS 5 hold), deploy/probe_motion_e.mjs (E1 on the END; the joint kept beside it; the rollback re-measured), 16_TESTS/gameplay_g37_contact.test.mjs (8).
  · Botany — lab/mahgicTree.js (`tris_placed_by_part`, `shadow_casting_tris`, far-LOD build option), tree_spec.js `lod_far`, fieldScene `botanyVisible`, probe_botany C0 / M2 matched A B A B workload, placement_v1.json + deploy/botany_placement_preview.mjs (nothing placed), gameplay_mahgic_tree.test.mjs (30), botany/REVIEW_PACKET_C.png.
  · P8 — host: rooms GYM_INTERIOR / CLOTHING_SHOP / WEAPON_SHOP (interior, door, exit gate, ceiling, platforms, BROWSE counters), layout doors GYM_DOOR / CLOTHING_DOOR / WEAPON_DOOR (kind ENTER), PlayMode enterRoom (NOT_AT_DOOR, LAND_FIRST, TRANSIT_IN_PROGRESS, return_to → EXIT lands on the door facing away, ALREADY_IN_ROOM kept, NO_EXIT_TO / NOT_AT_EXIT for interiors, interior ceiling LOWERS the flight limit), interactablesFor (room-level counters), prompts carry `to` / `shop`, snapshot `room_interior` / `room_shop` / `return_to`, ROOM_ENTER audit with from / via. Client: explicit confirm sheet (owns Enter / Space / Escape and the buttons), curtain `#g-fade`, `enterTransaction` (fade → host → readiness → camera reset → fade; duplicate refused; scene failure → recover to the doorway; slow path stage line without percentages; 6 s cap stated), interiorScene halls (gym racks / cage / lifting platform; shop counters + finished NO-STOCK panels; own lights; room PMREM environment so metallic bodies keep reflections), interior walls feed the camera boom (`fieldScene.probeSolids`), threshold pads at the field doors, `hud.catalog` (registry empty state, CLOSE only), shops_v1.json registry + validator, probe_interior (18), gameplay_interior_entry (22); the HUD O3 contract accepts BROWSE; the layout contract counts the three doors.
  · Reporting — CONSOLIDATED_PHASE_REPORT_1.md ERRATA, P0_REGISTER_RECONCILIATION.md post-audit updates 2, RESOURCE_USE_RECORD.md phase-2 section, OWNER_DECISIONS_C2.md, CONTINUE_HERE.md.

WHAT I FOUND:
  confirmed causes with file:line
  · G37 — what the 42.77 cm WAS: state = the procedural separated (SPLIT) grounded walk at 3.58 m/s (and run 6.71 m/s), no clip (no mocap exists); contact vs hover = the tips HOVER (owner motion-master B, asserted by 16_TESTS/gameplay_rig_pose_fk.test.mjs "HOVER: no plant, no strike"; root lift 3.5 cm) — there was no ground contact at all; measured point = the TIP JOINT (bone origin) world position, not the pointed end; support surface / frame = none — the "support" was the animator's own HOLD, an anchor in the PELVIS-LOCAL sagittal frame that lab/RigAnimator.js:~330 moved back at 45 % of travel BY CONSTRUCTION (`support_drift 0.45`, "a hover thrust point, never a plant"); contact interval = frames with hold weight ≥ 0.9 (~140 ms at walk, ~30 ms at run); aggregation = per hold, horizontal XZ displacement first→last frame, median and max over L + R holds; producing build / test / evidence = commit 8c263e2, deploy/probe_motion_e.mjs, 25_HANDOFF/CONVERGENCE/motion_e/motion_e.json (11:46 UTC). Separation: the FUSED form is a beam glide with no support pulses (N/A by design and by master §11.1); the SEPARATED walk's number was genuine sliding OF THE SUPPORT POINT — a design that drifted the held point plus a solver that could not hold a world point even when asked (the PASS 5 "planted variant" 33 cm: pelvis-frame anchor, ~110 ms weight ramp, sagittal-only). Classification: genuine supported-phase sliding in the separated walk (the walk has support phases; it is not a glide) → REPAIRED, not waived. Result on the candidate (RENDERED/MEASURED, headless Chrome): walk 7 declared contacts median 0.24 / max 0.95 cm; run 11 contacts median 0.30 / max 1.93 cm (≤ 2 cm); no bind frames; separated start/stop transition max 27.97 rad/s under the logged threshold 62.4; walk identity unchanged (pace 3.578 / 3.59 m/s, start 383 / 456 ms, stop 288 / 290 ms, cadence 1.995 steps/s); legs 9/9. The old result is preserved: `gait.plant=false` re-measured the same way = walk 41.31 / run 21.87 cm, and the joint number is printed beside the end number (walk joint median 15.18 cm during the same holds — the joint rotates about the fixed end, which is why the joint was never the right point). The tips still do not touch the floor (the earlier owner rule stands); the held point sits where the swing's forward extreme leaves it (≈ 20–23 cm above the floor at walk) — an optional artistic knob, not a criterion. Existing approved motion / retargeting resources were NOT used: no captured locomotion clip exists (Spec F is Jah's capture) and the phase-1 documentation reads are not motion integration proof.
  · Botany cost (item 3): "+93 k vs 44–50 k" was two page loads with different LOD / culling states. Matched same-load A B A B: +14 draw calls, +100 095 renderer triangles = 50 216 placed (7 instances: 32 972 branches + 16 940 leaves + 304 gems) + 49 912 drawn again in the sun-shadow pass (branches + leaves cast, gems do not) — the accounting is exact to 33 triangles; mean frame interval 16.60 → 16.63 ms at the harness' 60 Hz vsync (render p50 ≈ 4 ms both ways: no measurable cost at 7 instances); phone-portrait emulated (no shadows on that tier): +2.5 draws / +18 647 triangles for the part of the cluster in the portrait frustum. Nine draws alone establish nothing — these numbers do; the distribution budget is NOT MEASURED (nothing beyond the prototype is placed).
  · P8 — no entrance / interior / fade existed (PASS 0); interactables were TRANSIT only; enterRoom spawned at room.spawn with no doorway memory; `ALREADY_IN_ROOM` blocked re-entry only of the same room; the boom ignored non-field rooms; a metallic body without an environment map rendered black inside a hall (fixed with a cached room PMREM); the exit MARKER sprite dominated the interior view (removed; the hall draws its own doorway).
  hypotheses and non-reproduced conditions separately
  · Rotation mid-transaction and OS backgrounding on a physical phone are NOT tested by the agent (the harness has no rotation event mid-run); the transaction is promise/timer-driven and logs HIDDEN_DURING_TRANSACTION, and its 6 s readiness cap prevents a stuck curtain — stated, not proven on a device.

RESEARCH BASIS:
  actual source observation → original MAHWORLD adaptation
  · V03 (third-party, no input trace): what is VISIBLE — after the character's in-place turn at 1.0–2.4 s the view re-aligns behind the new facing within ≈ 1 s, and the framing keeps the character ≈ 27–33 % of frame height (approximate, sampled). What is NOT inferable: whether that realignment is an automatic follow, a stick input or an edit. → No camera change: manual orbit persists until the explicit reset; there is no recenter timer, flight-pitch altitude control, speed zoom, lock-on reframing or strike shake in the build, and none is planned; the spring stays as is (a demonstrated alternative on the same route would be the only way to put a choice in front of Jah; none was made). The character is NOT resized: the build's 33.8 % desktop / 27.8 % portrait / 33.8 % landscape are outside the approximate 27–33 % band for two of three viewports and that is simply reported.
  · master §16.2–16.4 → the transaction shape (confirm → curtain → readiness → reveal; failure returns to the doorway; warm entry adds no card; slow entry shows a real stage line) · §17 → finished spaces with an intentionally empty catalog (no product / price / purchase) · §14.6 → placement mask + far LOD as definitions only until the verdict.

RESOURCE USE:
  input → tool/version/permission → output → runtime consumer → proof (RESOURCE_USE_RECORD.md phase-2 section): dev_0.14 rig → three.js 0.185.1 matrices / quaternions → world-space hold → RigAnimator → motion_e 12/12 (RUNTIME_INTEGRATED) · mocap / retarget resources → NOT USED (no clip; documentation is not integration) · tree family → InstancedMesh + shadow pass → matched workload + LOD/placement definitions (RUNTIME_INTEGRATED prototype / DEFINITION_ONLY) · interiors → three.js + PMREMGenerator (vendored) → halls + room environment → probe_interior 18/18 (RUNTIME_INTEGRATED) · nothing downloaded, installed or purchased.

EVIDENCE:
  inspected:            RigAnimator hold code, PASS 5 record + probe, rig GLB rest skeleton, PlayMode / RulesField / layout data, adapter update order.
  source-reported:      PASS 0–7 reports as reconciled in P0_REGISTER_RECONCILIATION.md (each with its producing commit); the bundle's own decode checks.
  recorded footage inspected:  SAMPLED coverage of the 13 bundle videos (MEDIA_REVIEW_LEDGER.md; 0.2–3 s spacing, 0.2–0.25 s over the cued intervals) — not exhaustive per-frame coverage.
  runtime rendered/measured (CURRENT-CANDIDATE REGRESSION on the packaged runtime 3a1c6d5, headless Chrome GPU): motion_e 12/12 (E1 walk 0.24 / 0.95 cm, run 0.30 / 1.93 cm; rollback 41.31 / 21.87 cm); walk identity 8/8; legs 9/9; botany 12/12 (matched +14 draws / +100 095 tris; sway 0.82 % of frame; boom 3.00 → 3.00 m inside the cluster); interior 18/18 (door prompt; NOT_AT_DOOR; cancel; enter 850 ms warm with no card; ceiling 3.2 m; boom 6.47 → 1.99 m against the wall; exit onto the door at (−86, 110) facing +x; duplicate → TRANSACTION_IN_PROGRESS; repeat identical mesh count 311; injected failure → SCENE_FAILED recovered to the doorway with the curtain cleared; slow path stage line "preparing the hall · 0.8 s"; clothing shop BROWSE → 0 items / NO STOCK YET / CLOSE only; exit onto (−4, 243.5) facing −z; zero console errors); HUD 22 / 22 / 17; tests 34 files / 1 192 checks (package stage 1).
  reusable prior evidence: PASS 1–4 flight / camera / fidelity / legs records on 4f7aadd as listed per row (P0 ledger) — not re-run on this candidate except where stated above.
  emulated device:      iPhone-portrait 390×844 @ DPR 2, touch emulated (shop transaction, botany M2, HUD portrait / landscape). Never a physical phone.
  physical phone:       NOT tested by agent
  Jah's actual verdict or PENDING:  PENDING — Packets A + B (phone) and Packet C (tree) on the C2 draft; decision (c) (Spec G table / patterns).

ACCEPTANCE (current candidate unless marked reusable; full rows in P0_REGISTER_RECONCILIATION.md + its post-audit sections):
  · G37 / EXT-15 contact → PASS (measured, declared contact defined; rollback preserved) · G38 → PASS (re-measured) · EXT-11 / U04 → PARTIAL (pace / cadence / identity re-measured; the look = Packet B) · G39 / EXT-12 → PASS (idle re-measured 20 s both forms)
  · G07 / G08 / G10 / G11 / G12 / EXT-05 camera → reusable prior evidence on 4f7aadd (Spec B / PASS 2 records); no camera change in this candidate; the "inside the band" statement withdrawn
  · G20 / EXT-07 → labels FIXED (L8b ×3 modes); phone readability BLOCKED_OWNER
  · U06–U10, U12, U14, U16 → PASS (measured) · U11 → BLOCKED_OWNER · U13 → PASS prototype + DEFINITION written · U15 → PARTIAL (matched workload for 7 instances; distribution NOT MEASURED)
  · G52 first entrance → PASS (confirm → fade → readiness → fade in → interior → EXIT to the same doorway; probe + host test) · G53 interrupted / failed / duplicate → PASS for cancel, duplicate, injected build failure, slow readiness, repeat; rotation / OS background NOT TESTED by the agent · EXT-20 interior → PASS (enclosure, purpose, circulation clear floor, MAH clearance 6.2 m / 4.6 m ceilings) · EXT-21 → PASS · EXT-22 warm → PASS (850 ms, no card) · EXT-23 slow → PASS (stage line, no percentage) · EXT-24 fail → PASS (recovers to the doorway) · EXT-25 race → PARTIAL (double enter and late-result ordering covered; rotation / background NOT TESTED) · EXT-26 / EXT-27 / EXT-28 / U18 → PASS (both shops enterable; catalogs empty; no fixture / price / purchase; empty state tested) · EXT-29 → PASS (registry with the four state lists; validator proven on a bad item) · U17 → PASS (gym + both shop functions on the first transaction pattern)
  · G56 / EXT-35 package → PASS (C2 pipeline complete) · G57 / EXT-36 → unauthenticated protection PASS (play 401 password page · asset 401); correct-password loading NOT RUN (no password held) · G58 → BLOCKED_OWNER · G54 → PARTIAL (same authority modules in the static host; interiors use the same ENTER_ROOM op) · EXT-30 / EXT-31 / EXT-32 → unchanged (PARTIAL / PARTIAL / NOT_TESTED)
  · G42 / G43 / G44 / G45 combat → unchanged (NOT_BUILT / FAIL_KNOWN on old footage / PARTIAL / NOT_TESTED) — PASS 7's independent parts are the next build step after the gate

OPEN RISKS / ART DEPENDENCIES:
  · Packet B look of the held-tip walk (Jah). · Packet C tree look (Jah). · Rotation / OS-background mid-transaction on a real iPhone (Jah observes; the agent cannot). · The market doors sit on the ground in front of the market steps (the GLB's own portal is a 2 m block at z 245–247.5): the threshold pad marks the entry; if Jah wants the door on the steps themselves, that is one registry number. · Disk: C: 6.7 GB free.

BOTANICAL FAMILY:
  prototype accepted? NO — PENDING (Packet C). distribution status: NONE (prototype only; placement_v1.json is a DEFINITION: 140 candidates in 38 clusters previewed, nothing placed). actual count/cost: 7 instances · 50 216 tris placed · 9 instanced draws (+6 shadow) · matched +100 095 renderer triangles / +14 draws · no measurable frame cost at vsync · far LOD defined at 47 % of the near triangles.

SHOPS / INTERIORS:
  actual enterable destinations: MAH GYM (GYM_INTERIOR via GYM_DOOR at (−86, 110)), MAH MARKET clothing (CLOTHING_SHOP via CLOTHING_DOOR at (−4, 243.5)), MAH MARKET weapons (WEAPON_SHOP via WEAPON_DOOR at (4, 243.5)); each with the confirm → curtain → readiness → hall → EXIT-to-doorway transaction. zero-stock/art status: both catalogs EMPTY by design (shops_v1.json ZERO_STOCK, purchase DISABLED_NO_ECONOMY; the BROWSE panel shows 0 items / NO STOCK YET / CLOSE); apparel and weapon art, fit and combat rules remain the owner's later decisions.

ZIP:
  C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS\CLAUDE_GAMEPLAY_RUNTIME\26_LOCAL_AUTHORITY\deploy\packages\CONSOLIDATED_C2.zip · 28 821 418 bytes · 109 entries · SHA-256 9d9e28837fc0ead91a12d8ee084b038f07e03b8c768ea801e53b54edd436d337 · packaged from commit 3a1c6d5 (clean; stamp 2026-09-18T22:42:21Z in CONSOLIDATED_C2.PACKAGE.json) · Section 2 pipeline: 34 test files / 1 192 checks, JS sweep 69 files, CSS sweep, stamps, backend boundaries, secret scan 109 files, ZIP integrity 109 = 109, fresh extraction (byte sizes match), smoke from the extraction 7/7, JS sweep from the extraction clean; extracted again with Windows bsdtar (109 files, hash re-verified) for the draft. The commits after 3a1c6d5 are this report and the package record only (no runtime file).

CANDIDATE / ROLLBACK:
  DRAFT https://6aadbeca4eab6fe0af3bd0df--mahworld-test-preview.netlify.app (netlify-cli draft of the extracted C2; gate check: play 401 password page · asset 401 — that is the unauthenticated-protection PASS; correct-password loading NOT RUN). Previous drafts C1 6aad9c2b…, B2 6aad2eeae… still up. Live https://mahdemo.fob.systems = 6aac25101f17ce5ed37166e4 UNCHANGED (verified 303 to the gate); rollback 6aab65567e997ae33c1da204. No production publish because tests pass; MAHFITT untouched.

NEXT:
  1. GATE (Jah, ≤ 3 steps each, in CONTINUE_HERE.md): Packet C verdict from botany/REVIEW_PACKET_C.png (or the draft); Packets A + B on the C2 draft incl. the held-tip walk; decision (c).
  2. Agent, independent: PASS 7's independent parts (tap/hold single dispatch, counter loop as PROPOSED, buffered input, measured hit-stop, tier data with `?` refused) — does not depend on the contact assumption (resolved) or the tree verdict.
  3. Agent, independent: P9 preparation — the extended integrated route script (spawn → HUD → Map → walk/turn/stop → + / Dash → obstacle → orbit / reset → hold ascent → exhaust / landing → split / walk / fuse → attacks → emotes → cluster → bridge/landmark → GYM ENTER → interior → EXIT → shops) as one probe with interruption cases, run on C2.
