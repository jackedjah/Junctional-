# CONVERGENCE PASS 0 — focused reconciliation and baseline (audit only, zero source/asset edits)

2026-09-17 · gameplay/demo integration owner (this Claude session) · repo `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS` (master, clean at audit time).
Reference pack: `C:\Users\jahsu\Downloads\MAHWORLD_CONVERGENCE_REFERENCES\` (extracted from the zip next to it). Authority order applied: Jah's latest
prompt → O1–O10 → unchanged V2 / Specs A–H → unchanged R170/project constraints.

## G00 — identifiers verified (no reset, no tag move, no rollback overwrite)
| pointer | verified value |
|---|---|
| checkpoint `owner_round_0917` | `dbaa132` = HEAD at audit start (working tree clean); older tags intact: `pass2_spec_b` d98f142, `pass1_spec_a` 4da1488, `s13_convergence` 74b3ec5 |
| demo deploy | `6aac25101f17ce5ed37166e4` on project `mahworld-test-preview` (968f80e5-889b-43a8-b2cd-5860c588546a) = https://mahdemo.fob.systems — the 17-44-45 recording shows this build's HUD (ATTACK/DASH left, LAND/GUARD/USE/MORE 2×2, 1–4 row) and the address bar `mahdemo.fob.systems`, so the footage is of this deploy (inspected, not proven by hash — the recording carries no build stamp) |
| S13 fallback | `6aab65567e997ae33c1da204` (previous production deploy; rollback preserved) |
| main MAHFITT site | untouched; its build source `Downloads/MAHFITT_CLAUDE_CODE_FINAL_PACK` (MAHFITT-BUILD) now exists on the laptop — NOT used, out of scope |
| R170 fileset | **NOT in the workspace** (searched Downloads, Documents, the repo; only references in briefs). Rules known from the pack (control grammar, harmless roam, macro world lock, Control Lab exposes real values) are applied; anything that only R170 defines is listed as UNVERIFIABLE where it comes up. |

## Actual ownership and source map (inspected)
- Entrypoint `R/lab/play.html` → `R/lab/play.js` (R = `CLAUDE_GAMEPLAY_RUNTIME/26_LOCAL_AUTHORITY`); static site `R/deploy/static_dist` built by `R/deploy/build_static_demo.mjs`; in-page authority `R/deploy/static/static_host.js` (fixed step, bounded catch-up); gate `R/deploy/static/edge/gate.js`.
- Scene owners: `R/lab/fieldScene.js` (field, floors, sky, sun + shadow box, PMREM env, landmarks + LOD + merge, collision probe), `R/lab/cityScene.js` (native town), `R/lab/assets/buildings/district_v1.json` (landmark registry: id, runtime GLB, target height, yaw, position, envelope, landing levels; `target_height_m_s11` = pre-owner-round value) → `R/deploy/build_district.mjs` → `R/play/rules1723/district_v1_colliders.json` (BOX columns + walkable deck slabs; 1 027 shapes at 0.67×).
- Camera: `R/lab/play.js` (`cam`, `CAMCFG`, follow block, boom block, `camDrag/camDragEnd`, `inputBasis` latch, `camFraming`) + `R/lab/cameraMath.js`; look zone `#tc-world` (left 42 % → right edge — O2 supersedes this), stick zone `#tc-stickzone` (bottom-left 30 % × 38 %).
- Input/HUD: `R/lab/touchControls.js` (zones, stick, buttons, gestures), `R/lab/gameHud.js` (HUD DOM incl. `#g-acts`, `#g-erty-t`, `#g-desk`, warn/toast, dock, MORE sheet), `R/lab/gameMenu.js` (Q menu / TAB emotes), keyboard in `play.js`.
- Movement/resource/flight state: host `R/play/PlayMode.js` (`flight` tick lines ~156–170: powered/forced/warning/intent/hold; `move` refused while `fl.forced` line 132; ENTER/ASCEND/DESCEND/HOVER/EXIT ops line 147–151), resources `03_RESOURCES/Resource.js`, tunables `00_CORE/dev_tuning.dev.json` (`flight`: climb 6.5 m/s, descent max 9, forced unpowered descent 3, warning fraction 0.25; `dash.profiles`), rules field `R/play/rules1723/*` (colliders `solidAt/groundHeight/blockedAt`, combat dash, lock).
- Animation owners: `R/lab/RigAnimator.js` (procedural composed pose: gait, flight, attack arcs, emote IK, donor upper-body layer), `R/lab/animData.js` (authored clips), `R/lab/assets/retarget/tripo_*_upper_v1.json` (donor clips, UPPER mask), `R/lab/MuscleLayer.js` (morph + tension), `R/lab/GlbCharacter.js` (rig binding, `setForm`, materials, runtime metallic look), `R/lab/PresentationAdapter.js` (root yaw smoothing = −host facing, gait/lean cues, animator LOD, effects). No `THREE.AnimationMixer` use anywhere in `R/lab` (procedural rig; clips are JSON keys).
- VFX: `R/lab/PresentationAdapter.js` (glow sprites, PROJECTILE/ZONE/IMPACT pools, particles), `R/lab/BeamFx.js` (beams / flight tips trail), `strikePulse` in play.js.
- Avatar source/runtime: source `MAHWORLD_CHARACTERS/RAW_10_MODELS/Mah_Athlete_M.glb` (sha256 7b023694… per the runtime manifest) and `WORKING_10_MODELS/` (Character lane) → runtime derivative `R/lab/assets/athlete_m_preview/dev_0.13/` (ATHLETE_M_V8, sha256 fd11f926…, `make_class_rig_v5.mjs --legs surgical`, 38 bones, 2 body primitives + `SPLIT_LEGS` mesh with the PACKED morph, muscles morphs, textures 2048 albedo + normal, material metalness 0 / roughness 0.5 in the export → runtime metallic response 0.72 / 0.34 in GlbCharacter).
- Landmarks: sources `MAHWORLD_CHARACTERS/INCOMING_BUILDINGS/MAHWORLD_BUILDING_EXPANSION` (Tripo exports) → runtime `R/lab/assets/buildings/_decimated/*_rt.glb` (70–80k tris) → registry above. Tripo donor pack `Downloads/MAHWORLD_TRIPO_DONOR_RECEIVED.zip` (purple armor donor GLB + reference frames + inspection).
- Map/route consumers: none yet (no Map screen exists; `district_v1.json` landing levels + colliders are the only route data). Entrance/interior system: none (no door/interior code; `USE` = contextual `INTERACT` prompts in the field only).
- Quality/tests: `R/lab/quality.js` tiers (LOW/MED/HIGH; shadows HIGH only), `R/lab/controlLab.js`; tests `CLAUDE_GAMEPLAY_RUNTIME/16_TESTS/*.test.mjs` (26 files, 1 011 checks green at dbaa132); probes `R/deploy/probe_*.mjs` (route, series, spec_b, extra, gestures, glitch hunt, public gate); packaging `R/deploy/package_pass.mjs`.
- Renderer/addons: vendored three.js **0.185.1** (`R/vendor/three/`: three.module.min.js, GLTFLoader, SkeletonUtils, BufferGeometryUtils; no Draco/Meshopt/KTX2 decoders); no post-processing; ACES tone mapping by day; PMREMGenerator used for the sky env.
- Toolchain on this laptop (inspected paths, not launched): Blender 5.2 (`C:\Program Files\Blender Foundation\Blender 5.2\blender.exe`, 113 MB) — **slot reserved by the Character owner; not launched by this session**; Reallusion iClone 8 (`C:\Program Files\Reallusion\iClone 8\Bin64\iClone.exe`) + AI Studio + Shared Plugins — no Character Creator / AccuRIG install found; no running Blender/iClone process at audit time. No Mixamo/ActorCore/Rokoko exports found in the workspace; no licensed audio library found. ffmpeg not installed (video frames are extracted with headless Chrome).

## Issue → test ledger (each defect: reproduction + required proof)
| ID | owner report / evidence | hypothesis (file:line) | reproduction | required proof |
|---|---|---|---|---|
| F01 flight | 17-44-45 @00:29/01:03/03:52/04:24 (`flight_state_contradiction.jpg`): meter 100/100 with "FLIGHT MAHGIC EMPTY — LANDING" and "FORCED LANDING IN PROGRESS", hovering for minutes | `PlayMode.js` ~164: a forced descent into a NON-walkable solid top is held there (`fl.hold = solid.id`, "no support advertised on a non-walkable top") so the landing predicate (altitude ≤ ground) never fires; `fl.forced` stays true → `move` refused (line 132 → the "FORCED LANDING IN PROGRESS" toast) and ENTER refused (line 147); the resource regenerates because `powered` is false. The recording shows the player over the gym/landmark roofs when the meter empties. | harness: fly over a landmark's solid roof, exhaust → expect the stuck hold; also on clear ground; also deliberate landing before exhaustion | trace (mode, forced reason, resource, ground/contact, hold id, altitude, vz, input owner, accept/reject reasons, transitions, warning source); a completed terminal landing, agreeing HUD, permitted takeoff again |
| F02 camera collapse | @00:03–00:16 (`01_camera_and_back.jpg`): repeated extreme back close-ups while flying along the gym façade, recovering in between | `play.js` boom block + `fieldScene.cameraDistance`: the probe hits the landmark's solid columns when the trailing camera enters the façade envelope; the boom is applied at once down to the 1.0–1.1 m floor (= the whole screen is his back) and recovers only when clear; `cam.dist` (preferred) is already separate from `cam.applied` | harness: fly alongside the gym façade at 1–3 m; hover over a deck edge | per-frame trace of preferred vs applied distance, hit shape, inside state, fade state; no inside frames; recovery restores the preferred distance; no extreme collapse loop |
| F03 back surface | @00:07–00:09/00:12 (`frame_0008.00.png`, `body_evidence_native_crops.jpg`): hard-edged dark, pale and background-coloured polygons at the shoulder/underarm/back at extreme close range | two overlapping hypotheses: (a) the owner-round close-space fade (`setMeOpacity`: transparent + depthWrite) — at opacity < 1 the two body primitives + split-legs mesh sort as separate transparent objects and show background/inner surfaces through each other; (b) design plates (dark chrome, metalness 0.72 now) — the S13-era matte look. (a) is new since the owner round and matches "background-coloured" | harness: same frozen pose at 0.9 m boom with fade on / fade off / neutral opaque / wireframe, and at 3 m | same-pose comparisons; fade restores cleanly; no missing-looking plates |
| F04 split legs | @03:36–04:00 (`08_separation_dense.jpg`): narrow shins with dense patterning, bright straight lines below the tips | the `SPLIT_LEGS` mesh is the S13 "surgical" derivative (outline-sampled outer half + convex inner D) — visibly not the fused anatomy's thighs/calves; the straight lines are the `BeamFx` flight/tips trails | harness stills front/side/back, slow split/fuse, VFX off/on | O6 faithful derivative proof (PASS 4) |
| F05 HUD/reach | @01:03 (`04_ui_and_separation.jpg`): ATTACK/DASH left above the stick, LAND/GUARD/USE/MORE right, 1–4 bottom, no Map/+/reset, "Horizonta" truncated, Q hint on touch | current `play.html` layout (owner round) — superseded by O3/O9 | rect + pointer traces | PASS 3 acceptance |
| F06 attacks | @02:22–02:27, 03:06–03:13: ~0.5 s excursions, small glows, straight trails | `RigAnimator` attack arcs (PUSH/PULL/HINGE/ROTATION) + adapter root drive; no hit-stop; effects = pooled sprites/points | harness per-move frames at 0.1 s | PASS 7 |
| F07 body life | @01:10–01:50: little pose change while hovering in the stuck forced landing | the hover pose during `fl.forced` is the hold state (F01), not normal idle; normal idle has breathing in `RigAnimator` (to be verified separately) | after F01: capture idle / hover / cruise / landing separately | PASS 5 |
| F08 architecture softness | @02:20–02:28, 03:06–03:13: smeared/streaked gym frontage at close range | decimated runtime GLBs (70k tris, baked textures) + envMapIntensity 0.35 + the 512-px recording; source vs runtime not yet compared | matched source/derivative/neutral-light captures at gameplay distance | PASS 4 / PASS 9 |
| F09 host/visual collider mismatch (found in PASS 1, not in the recording addendum) | the owner round shrank the landmarks to 0.67× in the manifest + `district_v1_colliders.json` (1 027 shapes) but the AUTHORITY still read `_runtime_mapping.field_colliders_district_v1` at the S11 size (2 356 shapes, gym envelope 71.5 × 72 m vs 47.9 × 48.2 m visible): invisible walls 12 m outside every landmark, mid-air holds/perches on invisible storeys, flight blocked where the picture shows open air | `deploy/sync_district_colliders.mjs` (textual splice, rest of the rules file byte-identical) + `16_TESTS/gameplay_district.test.mjs` 1b (mapping == JSON, landmark envelopes == manifest) | FIXED in PASS 1 |
| others from the brief | held ascent (O4), speed `+` (O3), dead inputs, class HUD (O9), Map/utilities (O3), landmark 0.7× once (O7 — **the owner round already applied 0.67× of the S11 heights; O7's "0.7 of the current verified size" must not be applied a second time**: G47 = verify, not re-scale), bridges/water, guidance, interiors, rendering softness | see the pass plan | | |

## Baseline preserved (rendered/measured, PASS 1–2 + owner round)
Spec A route (headless Chrome, unlocked pacing, this laptop): phone tier MED portrait p95 7.8 → 7.9 ms (PASS 1 build → owner round), desktop HIGH 9.9 → 11.7 ms (player sun shadow, recorded exception); files `25_HANDOFF/PASSES/perf/`. Spec B probes 20/20 ×3 on the owner-round build (desktop/portrait/landscape, touch emulated). These stay the comparison baseline; the O1 change (persistent manual orbit) supersedes the release-return checks A2–A4 (to be marked SUPERSEDED — O1 with replacements).

## Resource availability / use plan (G01 — nothing claimed used yet)
| resource | status now | planned bounded use |
|---|---|---|
| three.js 0.185.1 (vendored) | LOCALLY AVAILABLE; AnimationMixer unused; SkeletonUtils retarget helpers present | PASS 5: one real clip → `retargetClip` trial on the MAH rig (bone map + basis check) in the browser |
| Tripo exports | INPUT AVAILABLE: character source `RAW_10_MODELS/Mah_Athlete_M.glb`, landmark sources `INCOMING_BUILDINGS/…`, donor `MAHWORLD_TRIPO_DONOR_RECEIVED.zip`; no API/credit status known | PASS 4: source vs runtime comparison (matched light, neutral shading); no generation without approval |
| Reallusion | iClone 8 + AI Studio installed (export rights unknown); no CC/AccuRIG | PASS 5 candidate route for a licensed clip if one exists locally — none found; otherwise NOT USED (blocked: no source clip) |
| Blender 5.2 | installed; **slot reserved by the Character owner** — not launched | O6 separated derivative is done in node/JS on a derivative copy (gameplay lane); a Blender step would need the owner's "BLENDER RELEASED" |
| Mixamo / ActorCore / Rokoko / others | no local clips, no account action | request only the specific missing performance (PASS 6 capture card) |
| Poly Haven / Kenney / Sonniss | not fetched; disk is at 1.7–2.4 GB free | only a specific asset with recorded license if needed |

## Unresolved source/threshold questions (explicit)
- R170 fileset absent (see above). · Dropped-frame definition and bone angular-velocity threshold: still not defined by any source (PASS 5 will justify one prospectively). · Exercise table beyond the existing E/R/T/Y mapping: owner-only. · Password for the authenticated review check is not held by this session (NOT RUN stays honest).

Acceptance of PASS 0: map produced; baseline + rollback preserved; resource plan recorded; every listed defect linked to a reproduction + proof; unknowns explicit; **zero edits** made during the audit.
