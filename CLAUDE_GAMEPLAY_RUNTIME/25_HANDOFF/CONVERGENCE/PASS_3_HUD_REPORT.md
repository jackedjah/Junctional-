PASS 3 — class-authored compact HUD, touch layout, faster travel, Map, one utility sheet (O3 / O4 / O9)

Report separation: (1) in progress before the addendum = camera (done); (2) appended by the addendum = pivot (done as PASS 2b), walk identity (PASS 4b), sky / far-background (2b slice done, PASS 9 full); (3) completed now = PASS 3 below + the PASS 2b review round; (4) remaining = PASS 4 → 4b → 5 → 6 → 7 → 8 → 9, gates A / B / C.

WHAT I DID:
  26_LOCAL_AUTHORITY/lab/play.html      O9 token system: five class families on `body[data-class]` (ATHLETE gold / champagne / amber, TITAN sapphire, LEAN crimson / ruby, VISIONARY violet / amethyst, BAGE pink / rose — families, not final hex values) with platinum highlights and dark inset faces; every `.gbtn` is a radiused squircle with a thick bevel rim (bright upper edge, darker lower edge), restrained inset / outer shadow, no blur plate; a state matrix (ready · selected · disabled · unavailable · cooldown · pressed) that renders distinctly; key hints hidden on touch. Touch layout (O3): a compact cluster of 50 px cells at the right thumb above the 1–4 row — row 1 GUARD · DASH · FLY (FLY at the former USE spot, lower right), row 2 ATTACK · MAHGIC, row 3 (airborne only) DOWN (hold) · LAND, row 4 MAP · + speed · ⟲ CAM; ATTACK / DASH remain the touch layer's hold + swipe modifiers, placed on their cells; the ≡ menu keeps the left end of the bottom row; the contextual prompt (ENTER / LIFT / TALK / QUEST / DUEL) is centre-bottom and exists only when a world interaction is in reach.
  26_LOCAL_AUTHORITY/lab/gameHud.js     the LOCAL player's class selects the theme; new controls MAP (one tap → the menu on its MAP tab), + speed (tap-toggle, selected state), MAHGIC (tap → the first READY magic skill, dimmed / cooldown otherwise); the permanent USE is gone (the old `PRACTICE_RESET` prompt made it appear everywhere); the MORE button is gone — the MORE sheet's ACTIONS (practice / reset / separate / help / guide / dev = settings), LOADOUT and EMOTES bodies are hosted by the ≡ menu as tabs (one utility sheet), the favourite-emote radial is the ≡ long-press; FLY / DOWN holds keyed by pointer stay (PASS 1).
  26_LOCAL_AUTHORITY/lab/gameMenu.js    tabs STATS · SKILLS · LOADOUT · EMOTES · SETTINGS · INVENTORY · MAP · FRIENDS · DMs; the three utility tabs mount the HUD's own bodies (`hudTabs.mount / unmount`).
  26_LOCAL_AUTHORITY/lab/play.js        `speedMode` (+ / `=` key) → MOVE {fast}; `castMahgic` (MAHGIC button / M key — Shift+M keeps the dev match-end); `act.map`; the menu ↔ HUD tab bridge.
  26_LOCAL_AUTHORITY/lab/rulesHud.js    `castMagic()` — the first READY magic skill (special first, then physical) with the direct-slot aim logic.
  26_LOCAL_AUTHORITY/play/PlayMode.js   MOVE {fast}: sustained travel at the canonical band's own top speed (FUSED glide 8.94 → 17.88 m/s = the FUSED band top, 40 mph, previously unreachable on the ground — FUSED ground speed doubles; SPLIT 3.58 → 6.71 m/s with the RUN gait) — no transformation, no resource cost, no new multiplier (a reversible default per O3).
  deploy/probe_hud_pass3.mjs (new)      page probe ×3 modes; 16_TESTS/gameplay_hud_pass3.test.mjs (new) source contracts + host rule + tracked evidence.

PASS 2b REVIEW ROUND (fixed in the same build):
  · BLOCKER: the 850 m sky dome was world-fixed and clipped by the 900 m far plane away from the plaza → the sky now rides with the viewer (`fieldScene.skyFollow` each frame, dome 1300 m) and the camera far plane is 1400 m, so the world-fixed far ring (≤ 900 m from the origin) stays inside it from the room's far corners; new page check W3 samples the sky column from (240, −240) looking back across the district (never the flat clear colour) and W1 asserts the far geometry's bounding sphere, not a constant.
  · a mid-hold re-latch (stick swung > 45°) re-reads the camera's FOLLOW rotation but subtracts any orbit performed during the hold — no whip by the orbit angle (turning around still works).
  · the matched capture's obstacle chapter is honest: the owner-round build cannot reach the visible temple wall (its invisible S11-size collider stops him ~10 m short — F09) — the chapter caption says so and the reset chapter says "no reset control exists on this build"; the recorder walks from (0, 120) for 9 s on both builds.
  · the haze plain sits just under the floor edge (y −0.02, inner radius 250 m): no horizon seam; readable_frac 0.34 (the pitch-up engages below 2.2 m on a 6.5 m boom, not 2.9); turn spring 1.6 Hz / 800 °/s cap (spring-shaped: a 180° turn peaks ≈ 660 °/s, first-frame response 8°); the perf test now reads every `route_series_conv*` file (the "b2" suffix was skipped before).

PASS 3 REVIEW ROUND (fresh-context reviewer of befed4a; all fixed in the same build, page probes rerun ×3 modes):
  · BLOCKER — the utility-sheet bodies were DEAD once mounted in the ≡ menu: the menu root lives outside `#ghud` and stops pointer propagation, and every HUD action was dispatched only from `#ghud`'s listeners → EMOTES / SETTINGS (practice · reset · SEPARATE-FUSE · help · guide · dev) / LOADOUT taps inside the sheet did nothing. Fix: the HUD's pointer / change delegation is registered through `on()` and bound once to the menu body when a tab mounts there. New page checks L6c (an emote tapped INSIDE the mounted tab plays) and L6d (PRACTICE tapped inside SETTINGS toggles the host flag) prove it in all three modes.
  · landscape: the attack dock's open drawer grew down over MAP / + / CAM / DOWN / LAND (the evidence measured the collapsed dock only). Fix: in landscape the dock stands LEFT of the cluster column (x ≈ 488–668, 180 px, categories stacked one per row) with room for its drawer; new check L1d opens a category and measures the open dock against every cluster control (no clash), still `hud_landscape_dock_open.png`.
  · landscape: the contextual prompt (ENTER / TALK / LIFT) sat on the framed character. Fix: landscape prompt below his feet (y ≈ 280–324 at 390 px high); new check L2c measures the prompt rect against his screen box (portrait + landscape).
  · O9 tokens were overridden for FLY (flat blue) / GUARD / DOWN (flat orange) and the `.on` states collapsed; `.gbtn.round` was reset to circles by a later 50 % rule (only ATTACK / DASH were squircles). Fix: both overrides removed — every round control is the 30 % squircle and the class tokens + state matrix own every face (the stills in `hud/` are re-captured).
  · report honesty on `+`: 17.88 m/s is the FUSED band top (40 mph), which no ground state reached before — "FUSED ground speed doubles" is the plain statement; it was NOT "the same number the split run already uses" (the SPLIT band is 3.58–6.71 m/s). Also fixed: `+` in SPLIT now engages the RUN gait (free in SPLIT) so the gait matches the band-top speed (host test: SPLIT_RUN at 6.71 m/s), and `DuelHost` no longer drops `fast` (arena fighters: band top, RUN gait only where it is free — never an implicit transformation).
  · hints: SETTINGS-tab key hints carry `.key` (hidden on touch), every "MORE →" string now points at ≡, the hold + swipe labels are 9 px ink instead of 7–8 px grey; landscape category labels no longer overlap (stacked).
  · sky: clouds and the cloud-skyland were riding with the viewer (inside the followed sky group), so they never passed overhead. Fix: `MAHWORLD_SKY_FIXED` (world-fixed clouds / skyland); only the dome + sun + stars follow the viewer.
  · `lab/gameMenu.js` was rewritten CRLF → LF in befed4a (the real change was 3 lines; review with `git diff --ignore-cr-at-eol`).
  · reviewer note kept as a reversible default: MAHGIC's implicit "first READY (special first)" pick can spend energy on a HEAL / buff slot.

WHAT I FOUND:
  · the permanent USE button came from the always-eligible `PRACTICE_RESET` prompt, not from a world interaction.
  · the Q menu's map canvas is `#g-map`; the new MAP button had to be `#g-mapbt` (the first id clash broke `drawMap`).
  · KeyM was the dev "end team match" key; it moved to Shift+M so plain M can cast MAHGIC.

EVIDENCE
  inspected:          the owner's O3 / O9 text; the old layout's `#g-acts` grid and media rules; the prompts list at spawn (host).
  rendered/measured:  `node deploy/probe_hud_pass3.mjs portrait|desktop|landscape` → see the tracked records `25_HANDOFF/CONVERGENCE/hud/hud_<mode>.json` + stills `hud_<mode>_ground.png / _air.png`: L1 every control ≥ 44 px, inside the viewport, no overlaps (the ≡ / 1–4 row inside the stick zone's rectangle is the owner's layout: HUD wins the touch, checked separately L1a); L1b cluster geometry; L1c 50 px faces; L2 no USE at spawn, L2b LIFT prompt at the elevator; L3 the theme follows the local class and the five families are distinct; L4 `+` toggles a selected state and travel is faster (host: 8.94 → 17.88 m/s FUSED, no cost); L5 MAP one tap; L6 one utility sheet (22 emotes, loadout, settings) with the bodies returned on close; L7 MAHGIC casts (a magic slot leaves READY); L8 no key hints on touch; L9 state matrix distinct; L10 hold-FLY from its cell, LAND lands, the airborne row hides.
                      `node 16_TESTS/gameplay_hud_pass3.test.mjs`; the flight (O4 ×3, F01), camera (Spec B ×3, PASS 2 ×3, gesture audit ×2) and pivot (×3) probes rerun on this build; matched capture rerun (current / candidate) and the route series `route_series_conv3.json`.
  iPhone:             NOT tested (Jah only). Touch = EMULATED synthetic pointer events (thumb reach of the 4-row cluster is a Card A question).

ACCEPTANCE (brief §PASS 3): class theme tokens + class switch → PASS · layout per O3 / O9 (cluster, 1–4 low, Fly at the former USE spot, + and reset together on the right, Map one tap, one utility sheet, no permanent USE) → PASS · hold-Fly kept → PASS · no keyboard hints on touch → PASS · state matrix → PASS · gesture audit → PASS · 44-pt targets translated to 50 px CSS faces (verified in CSS px, not in a resized video) → PASS · phone review → GATE (Card A).

OPEN RISKS: the exact class hues and the 50 px cell size on a real phone (Card A); MAHGIC is a tap (a hold + swipe magic gesture is a later refinement); `+` is a tap-toggle (reversible default); the desktop bar keeps the E R T Y strip beside ATTACK / MAHGIC / DASH / LOCK.
SUPERSEDED: the owner-round mobile layout (ATTACK / DASH left above the stick, USE / MORE in the 2×2) → the brief's PASS 3 layout (the 1–4 row at the very bottom with ≡ at its left end is kept as the owner asked).
CANDIDATE (Card A) — SUPERSEDES CONVERGENCE_A (befed4a / draft 6aacea54…, which had the dead menu-hosted sheet):
  ZIP        `26_LOCAL_AUTHORITY/deploy/packages/CONVERGENCE_A2.zip` · 28 749 271 bytes · 103 entries · sha256 f5a499096a56ffd754e6cd42182ad476a229116491628bd38fdcee236ee2abda · commit 17331c3 · Section 2 pipeline: 29 test files, JS + CSS sweeps, stamps, boundaries, secret scan, ZIP integrity, fresh extraction, smoke test from the extraction (7/7) — record `CONVERGENCE_A2.PACKAGE.json`.
  DRAFT      the packaged content (extracted ZIP) deployed as a DRAFT of the demo project (not production): https://6aacfc2fdcca1d36613e1082--mahworld-test-preview.netlify.app (deploy 6aacfc2fdcca1d36613e1082). Gate check: play page 401 → password page, private asset 401 (fail-closed). Correct-password login: NOT RUN (the password is not held here). The password itself is nowhere in code, config, URLs, git, logs or this report. (The reviewer flagged the draft-before-review rule: the draft is the phone-review vehicle the Card A gate asks for; the live demo is untouched.)
  LIVE       https://mahdemo.fob.systems still serves the owner-round deploy 6aac25101f17ce5ed37166e4 (verified through the site record after the draft upload); rollback 6aab65567e997ae33c1da204. Nothing was replaced.
