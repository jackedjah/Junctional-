# OWNER INTERJECTION 2026-09-18 — PHASES D · E · F · G · H REPORT (§23 form) · Hinge + flight lean · the three Athlete attacks · stats / progression / catalog · regression + perf · gameplay / demo lane · 2026-09-19

CURRENT JOB PRESERVED:
  Same job, same worktree, linear history: beb9946 (C6 docs) → 715579d (phases D–G + the hotfix code) → the C7 docs commit. Nothing reset, no fork; lanes A–C of the enhancement pass, the interjection phases A–C, the interiors, camera, nav, hip connection and MAHGIC language are kept. The 2026-09-19 hotfix interjection (intactness gate + no generic ATTACK button) was ingested at the checkpoint after phase G and is reported in `INTERJECTION_HOTFIX_0919_REPORT.md`; the owner's full-job completion directive is applied (one candidate, one consolidated report: `CONSOLIDATED_PHASE_REPORT_6.md`).

BUILD / ASSET IDENTITY:
  Static build from the C7 commit (BUILD_INFO.json carries `commit` + `built`; the visible label is ≡ → SETTINGS about + the dev panel). Athlete rig ATHLETE_M_V11 = dev_0.16 (dev_0.15 + graded shoulder skin weights — the hotfix; NOT owner-approved; V10 / V9 selectable for rollback). Three.js 0.185.1 vendored. Host: in-page authority, 20 Hz, presentation delay 120 ms. Package + URL in CANDIDATE.

VIDEO REVIEW:
  NOT DONE against the owner clip — `ScreenRecording_09-18-2026 23-28-36_1.mp4`, IMG_1118, IMG_1117 and the interjection `.md` are still not on this machine (re-checked 2026-09-19). The page evidence is stills + probe records (sampled coverage, headless Chrome, touch EMULATED); the frozen-pose intactness sheets are labelled MANUAL LOOK.

HUD: unchanged from phase B except the hotfix (the generic ATTACK button deleted; quick slots 1–4 dispatch directly; island reflow; the ⚔ COMBAT tab gains the TRAINING ROOM card, ⚔ STATS the five-stat card, ⚔ SKILLS the PROGRESSION nodes + CATALOG STATUS). Probe on C7: portrait 17 / 17 · landscape 17 / 17 · desktop 5 / 5; combat after transitions 12 / 12.

LOWER BODY: the §6.1 gate stands (phase C: TOPOLOGICAL cause, native repair contract, runtime hip work stopped; V11 changes only shoulder weights, the hip connection of V10 is byte-identical inside V11). The split-flight leg trail now follows the ONE lean curve (phase D).

FLIGHT (phase D — what changed, measured on the page):
  · ONE centralized curve `presentation_defaults.flight.lean.curve_x_deg` = [[0, 6°], [1, 24°], [1.3, 40°]] over x = displayed horizontal speed / the form's cruise speed (the host publishes `flight.cruise_mps` per form: SPLIT 7.71, FUSED 20.56 = the canon band top × 1.15) — the animator's `flightLean(speed)` is the only writer; the old 8°–18° over speed / 8 m/s is gone.
  · BOOST = `run` (+ / fast) while powered = host `flight.boost_factor` 1.3 (PROVISIONAL, dev_tuning, adjustable) → x 1.3 → 40° = the Hinge forward-commitment silhouette: the pelvis leads (its share of the lean 0.45 → ≥ 0.53 at boost), the spine / chest follow, the arms trail back to the edge of the shoulder table, the legs trail BEHIND in line with the lean with a soft knee, the fused taper curves back; normal forward flight is the smaller related version (24°); hover 6°.
  · Camera never coupled: heading and pitch identical before and after the boost lean (probe D10).
  · Probe D (`probe_hinge_flight`): D7 judges every sample against the curve at its REAL displayed speed (≤ 3° off) and boost > cruise — the earlier nominal "40° at 9.0 m/s" expectation was the probe sampling before the boost speed was reached, not the lean.

ATTACKS (phases D + E — every exercise reads as the movement before VFX; hands measured in WORLD space):
  · HINGE (reauthored, D): stance → hips BACK with a neutral long spine inclined at the hips (fused PELVIS ≥ 0.45 rad, spine sum ≤ 0.22; split: femur 37° forward-down, shins ~vertical, the feet PLANTED through a planted-feet root offset — the foot end moves ≤ 0.08 m while the pelvis goes back ≥ 0.12 m) → held ≥ 90 ms → the hip DRIVE stands tall and the straight arms swing to chest height (never over the head) → MAHGIC hurled from the hip drive (PELVIS → HAND_R gather) → recovery; airborne = a pike, never fake floor support. The old Hinge Rise is gone as an attack.
  · SPECIAL #1 OVERHEAD EXTENSION RELEASE (`ATHLETE_OVERHEAD_RELEASE`, VERTICAL_PUSH): load = both hands 0.08 m ABOVE the head bone, 0.10 m behind the chest line, 0.40 m apart, elbows bent behind the head (the humerus at ~135° through the attack-scoped `overhead_release` shoulder allowance; the general table stays 100°) → release = the arms snap forward-down, elbows straight, hands 0.38 m ahead at chest + 0.08 m, wrists whipped → a granular ROD projectile leaves the fingertips (dustFx WAVE `rod`) → hit on the dummy (hp 1000 → 931.1).
  · SPECIAL #2 CHEST FLY CLAP RELEASE (`ATHLETE_CHEST_FLY_CLAP`, HORIZONTAL_ADDUCTION): load = arms HORIZONTAL at shoulder height, hands 1.25 m apart with the fixed 16° elbow bend, a gather on EACH hand → release = real horizontal adduction to the CLAP in front of the chest (hands meeting 0.35 m ahead; the elbow bend never changes) → a granular RING projectile → hit (931.1 → 854.6).
  · PHYSICAL MAHGIC EIGHT-STRIKE ALTERNATING CURL BARRAGE (`PM_CURL_BARRAGE`): out of range = refused HARMLESS / too far; in range (≤ 1.4 melee units) = a short APPROACH slide (2.3 m → 1.08 m, never a teleport, `lungeStep` through the movement controller) → 8 host-scheduled strikes at 0.11 s (each a real elbow flexion ≤ −0.8 rad to the shoulder, elbows pinned, arms alternating L R L R …) → the dummy loses health eight times (12.9 hp each) → COMBO_LOCK 1.0 s on the target (movement 0, availability COMBO_LOCKED, the world tag says COMBO) with the refusals HARMLESS_CONTEXT / TARGET_KO / TARGET_IMMUNE / TARGET_GUARD_IN_CONE / REAPPLY_COOLDOWN (6 s) and R1723_BARRAGE_BROKEN when the caster is staggered — no infinite stun. Performance during the barrage: sampler p95 46.6 ms, 0 stalls > 100 ms (desktop GPU Chrome).
  · Root cause fixed under the hotfix gate: until 2026-09-19 every SPECIAL_MAGIC cast played the generic CAST pose + the Tripo cast-a-spell donor clip, so #1 / #2 never showed their movement (one hand ahead, one behind). Now the pattern resolves by skill id and the donor stages only the generic cast.
  · Probe E (`probe_attacks_phase_e`): 9 / 9 on the C7 build (E1 / E2 arcs, E1b / E2b hits, E3 approach + 8 strikes, E3b alternation, E3c lock + tag, E4 perf, Z errors).

MAHGIC: the granular crystalline-dust language carries forward — ROD (overhead) and RING (chest fly) are distinct formations of the same dust (dustFx WAVE shapes), gold; the barrage keeps the body / contact GATHER, no detached damaging projectile for PHYSICAL MAHGIC. Nothing hides a pose: the intactness sheets carry VFX-off rows.

STATS / SKILLS (phase F + G):
  · `00_CORE/class_stats.dev.json` — the canonical five-stat family FORCE · POWER · ENGINE · CONTROL · RESILIENCE on 0–999; class baselines = null with `SOURCE_NOT_FOUND` (the MAHWORLD Fitness Class Archetypes source is not on this machine; nothing invented); `points_per_level` = OWNER_DECISION between 3 and 4 (one centralized provisional value, never hard-coded canon); allocation is PREVIEW ONLY (no host stat, no real points in the demo); Q07 competitive fairness UNRESOLVED.
  · ⚔ STATS card: the five bars, preview +/− with a reset, the "source not found" line; ⚔ SKILLS: FOUNDATION PROTOCOL current (levels 1–12), CLASS DEVELOPMENT locked from 13 (the ruleset band), PHYSICAL / MAHGIC / HYBRID / CLASS branches as FUTURE nodes (no invented requirement), the class future concepts from the ruleset; CATALOG STATUS: every class × mode × approved pattern cell BOUND / OWNER_DECISION / FUTURE straight from `attack_stacks.catalog_status` (140 cells; tiers OWNER_DECISION) — data + surface only, nothing enabled, no capacity change, no live rebalance.
  · Probe F portrait 4 / 4 + desktop 4 / 4 on the C7 build (the SKILLS tab reads the ruleset from a static copy the build now ships).

PERFORMANCE (phase H, `probe_perf_separated`, desktop GPU headless Chrome, normal speed, a straight walking leg; a phone is Jah's measurement): the five causes separated — INPUT DELAY host 31–53 ms / displayed 147–148 ms; ANIMATION STEPPING 0 zero-advance frames in every run (speed variation 0.21–0.44 across five runs on a loaded PC); FRAME STALLS p95 10–29 ms, 0 frames > 100 ms; CAMERA JITTER RMS 0.9–3.3 px per frame² (rises with the frame time); STATE DELAY 91–94 ms — CONSOLIDATED_PHASE_REPORT_6.md §8.

PACKAGE: CONSOLIDATED_C7.zip · 31 342 949 bytes · 116 entries · SHA-256 18172f155b98e6ccd5aebbdda1b27d7353d317b97fc3397ef64aef22eb97cb33 · from commit 4a17efd (clean) · pipeline 0–7e PASS · smoke 7 / 7.
CANDIDATE: DRAFT https://6aaeb66e5b7bbc1b49221b84--mahworld-test-preview.netlify.app (build label commit 4a17efd; gate 401 unauthenticated PASS; correct-password login NOT RUN).
LIVE: unchanged (https://mahdemo.fob.systems = 6aac25101f17ce5ed37166e4; no live promotion).

NEXT (≤ 3 owner actions; independent work continues):
  1. Jah (phone, on the C7 draft): PRACTICE on → tap slot 1 / swipe slot 2 / the category label → PHYS MAHGIC slot with the Curl Barrage → SPECIAL slots with the Overhead Release and the Chest Fly Clap; then hold FLY + `+` for the boost lean; then ⚔ (COMBAT / STATS / SKILLS). One line on each.
  2. Jah (one path): the four evidence files + the Fitness Class Archetypes stat source.
  3. Jah (decisions only when ready): points per level 3 vs 4; `PASS_7_COMBAT_DECISIONS_REQUEST.md` cells; the botany specimen.
