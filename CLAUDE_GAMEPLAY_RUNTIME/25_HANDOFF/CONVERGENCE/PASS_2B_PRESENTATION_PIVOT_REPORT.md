PASS 2b — presentation pivot slice (owner addendum): stable readable scale · connected movement (first slice) · independent viewpoint proof · sky / far-background depth (first slice) · matched capture

Report separation: (1) already in progress before the addendum = PASS 2 camera (finished, reviewed, committed); (2) appended by the addendum = this pass (pivot slice), PASS 4b walk identity, PASS 9 sky/far-background full treatment; (3) completed now = below; (4) remaining = PASS 3 HUD → 4 fidelity/legs → 4b walk identity → 5 motion → 6 library → 7 combat → 8 VFX → 9 town/world.

REFERENCE OBSERVATION (kept separate from inference):
  · `v12044gd0000dam5b47og65j5h5c8730.mp4` and `MAHWORLD_NEW_REFERENCE_STUDY`: NOT FOUND on this PC (Downloads / Desktop / Documents / Videos) — not watched, not read, nothing claimed about the clip.
  · What the addendum TEXT states was used as the direction: the reference character occupies roughly a quarter to a third of the image height in representative landscape frames (a composition reference, not a percentage, distance or FOV); calm connected movement (idle → turn → short travel → stop → settle); world-first exploration with layered depth.
ADAPTATION (inference, ours):
  · framing per orientation, fixed (no continuous zoom): landscape `frame_frac` 0.50 → 0.31 of the short side (he was 54 % of the image height in the owner-round build, now 34 %); portrait 0.50 → 0.55 of the width with the aim point lowered to his feet line (`look_height_frac` 0 for portrait) so the box clears the thumb-landing zone (23 % → 28 % of the height). Canonical character dimensions untouched; no second building-scale migration.
  · the displayed turn is a critically damped, rate-capped spring (1.6 Hz, ≤ 800 °/s so the spring — not the cap — shapes the turn; attacks / dash 5 Hz, ≤ 1800 °/s) instead of an exponential snap (was k = 16/s with a 900 °/s cap).
  · the movement frame latched at gesture start ignores manual camera drags during the hold (`latch_manual_drag` off; the old behaviour is one Control Lab switch away); a mid-hold re-latch (stick swung > 45°) re-reads the follow rotation but subtracts the orbit performed during the hold (review round).
  · three depth layers beyond the district (`lab/farWorld.js`): MID 230–330 m crystal walkway arcs + prism towers in the empty south / east / west sectors (lit, fogged naturally), FAR 620–860 m crystalline massifs + 5 world-scale spires (unlit silhouettes, colours mixed toward the horizon haze by distance — authored atmospheric perspective, fog OFF so the forms stay readable), a HAZE plain 250–890 m just under the floor edge (no seam); the sky dome 390 → 1300 m and it RIDES WITH THE VIEWER (skybox behaviour) while the camera far plane is 1400 m, so nothing clips from the room's far corners (review round: the first version's world-fixed 850 m dome clipped off-centre). 3 draw calls, static, no colliders, nothing copied, no near-field clutter.
ACTUAL CHANGE: files — `lab/play.js` (presets, latch rule, MOTION lab group, fieldDebug / cameraDebug), `lab/PresentationAdapter.js` (turn spring, `turnConfig`), `lab/fieldScene.js` (far world hook, dome radius), `lab/farWorld.js` (new), `deploy/probe_pivot.mjs` (new), `deploy/record_pivot.mjs` (new).
PROOF (runtime):
  · `deploy/probe_pivot.mjs` desktop 9/9, portrait 9/9, landscape 9/9 (after the review round: W1 on the far geometry's bounding sphere, W2 the sky rides with the viewer, W3 the sky column from the room's far corner is a real gradient) (`25_HANDOFF/CONVERGENCE/pivot/pivot_<mode>.json`): S1 idle box height 33.8 % (desktop 1200×750), 27.8 % (portrait 390×844), 33.8 % (landscape 844×390) — all inside the 22–36 % / 20–36 % bands; S1b anchored lower-middle; S2 walking straight 2.5 s: preferred boom Δ 0 %, applied Δ 0 %, box varies 13 % with the pose only; V1 a 92° orbit during a held W / stick: travel heading change 0.0° over 7.4 m; M1 a 180° turn-around takes 0.43–0.44 s (5 → 95 %), 0° overshoot, peak ≈ 660 °/s below the 800 °/s cap — spring-shaped (three modes); W1 far world = 3 meshes (5 040 far + 1 656 mid vertices), far ring 620–860 m; W2 dome / fog readable.
  · matched capture (desktop 1120×700, identical inputs; re-recorded after the review round — `capture_current_owner_round` vs `capture_candidate_pass3`): owner-round box 54.5 % idle / 46 % walk / 54 % turn → candidate 33.8 % / 30.4 % / 34.0 %; preferred boom 4.01 → 6.47 m constant through idle · turn · walk · stop · orbit · release · reset · flight; frame interval p50 16.7 / p95 16.8 ms in EVERY phase of both captures (no dropped frames this time; the earlier 33 ms hitches were machine load). Obstacle chapter: the owner-round build cannot reach the visible temple wall (its invisible S11-size collider — F09 — stops him first; its boom dipped to 2.3 m against that region), the candidate reaches the wall and sits at 0.50 m with the body hidden and a 1.0 rad pitch-up for the seconds it stays there, 0 inside frames on both; the reset chapter is a no-op on the owner round (no reset control existed) and the caption says so. Performance record: `route_series_conv2b2.json` (owner round vs PASS 2b: desktop p95 14.3 → 12.7, portrait 11.1 → 10.9) and `route_series_conv3.json` (owner round vs PASS 3: desktop 12.5 → 14.2 Δ +1.7 within the 1.8 tolerance, portrait 9.6 → 9.8) — no measurable regression within ±1.5–2 ms of run-to-run noise on this laptop; the first PASS 2b series (`*_noisy_DISCARDED`) carried two 2× outlier runs on both builds and is kept only for the record (README).
  · Spec B 22/22 ×3 and PASS 2 camera 8/8 · 13/13 · 13/13 rerun on the new framing (portrait: his box now clears the thumb-landing zone with the lowered aim point).

EVIDENCE
  inspected:          owner-round capture telemetry (`deploy/review/current_owner_round/telemetry.json`): idle box 54.5 % of the height, turn box 51–61 %, walk 45–55 %.
  rendered/measured:  see PROOF above; stills `pivot_<mode>_gameplay.png`, `pivot_<mode>_gameplay_south.png`, `pivot_<mode>_wide_manual_look.png` (labelled manual look toward the horizon).
                      matched capture `deploy/record_pivot.mjs`: current = owner_round_0917 build (`deploy/review/current_owner_round/`), candidate = this build (`deploy/review/candidate_pass2b/`): same scripted inputs, per-phase telemetry (box height fraction, preferred / applied boom, pitch bias, inside frames, frame interval p50 / p95 / p99), WebM + marks + stills.
                      videos: `26_LOCAL_AUTHORITY/deploy/review/current_owner_round/pivot_current_owner_round.webm` (105 s) and `.../candidate_pass2b/pivot_candidate_pass2b.webm` (72 s), chapters in `marks.json`; tracked: marks, chapter stills and telemetry summaries under `25_HANDOFF/CONVERGENCE/pivot/capture_*/` (the WebMs stay local, gitignored).
  iPhone:             NOT tested (Jah only). Touch = EMULATED.

CLOSE-UP INTEGRITY: reviewed separately (not by distance) — F03 fade / hide is proven; the surface-discontinuity question (pack F03 / G26 opaque + wireframe comparison) belongs to PASS 4 and is NOT closed here.

OPEN RISKS: the far layer's exact tone on a phone screen (Card C); the turn spring / latch rule feel (Card A); landscape 31 % may read small on a 390 px-high phone (the composition band is a proposal, reversible in one preset).
SUPERSEDED: none (the owner-round layout / framing values were working defaults, replaced by the pivot's).
CANDIDATE: not published; live demo unchanged.
NEXT: PASS 3 (class-authored HUD, O3 / O4 / O9). Card A gate after PASS 3 (camera + flight + HUD + pivot slice together).

REVIEW ROUND (fresh-context reviewer): BLOCKER dome clipping fixed (viewer-following sky, far plane 1400, W2 / W3 checks); relatch whip fixed; capture honesty fixed (obstacle / reset captions, re-recorded); haze seam fixed; perf test pattern fixed; readable_frac 0.34; turn spring 1.6 Hz / 800 °/s. Open by design: the far layer renders as pale crystalline cut-outs (tone continuity vs the lit mid layer is a Card C question).
