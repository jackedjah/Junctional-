PASS 4B — faithful separated-leg derivative (O6 / G32–G35) · PASS 4b — walk identity across leg states (owner addendum) · 2026-09-18

Report separation: (1) in progress before the addendum = PASS 4A fidelity (done, PASS_4_FIDELITY_REPORT.md); (2) appended by the addendum = WALK IDENTITY ACROSS LEG STATES (done below as PASS 4b, one stated difference), sky / far-background (PASS 9), presentation pivot (2b done); (3) completed now = the faithful separated legs (PASS 4B) + the walk-identity proof (PASS 4b); (4) remaining = PASS 5 motion foundation → 6 library → 7 combat → 8 VFX → 9 town / world, gates A / B / C.

WHAT I DID (PASS 4B — legs, node/JS only; Blender still reserved; no source asset touched):
  26_LOCAL_AUTHORITY/lab/assets/rig_legs_faithful.mjs (new)   the split legs are the fused lower body’s OWN triangles: every triangle below the crotch line is cut by the sagittal plane x = 0 (Sutherland–Hodgman, seam vertices shared per cut edge); the left half is the left leg’s lateral wall, the right half the right leg’s — the source vertices, normals and UVs unchanged, so every triangle still sits inside its own texture island. The missing MEDIAL wall is inferred from the species’ own anatomy: the lateral wall mirrored and compressed toward the seam plane (x’ = −0.31·x; inverse-transpose normals; the lateral wall’s UVs, i.e. the texture mirrors onto the inner face), welded along the seam → one closed manifold per leg. A shallow cone closes the top inside the pelvis (apex under the femoral head, the v4/v5 dome law). In the SEPARATED form each leg is pushed laterally by 0.31 × the thigh half-width (+0.5 mm) so the two medial walls meet at the midline without interpenetrating; PACKED = every lateral vertex back at its EXACT source position and every medial vertex folded onto the sagittal plane — the packed pair IS the fused silhouette.
  26_LOCAL_AUTHORITY/lab/assets/make_class_rig_v5.mjs            `--legs faithful` (+ `--leg-inner 0.31`, `--knee-blend 0.06`); the knee blends over ±0.06 H because the source tail’s own triangles are 2–3 cm long (measured: ±0.04 left 26 inverted triangles in the test swing, ±0.055 16 in the gather, ±0.06–0.07 none); manifest `split.method` = FAITHFUL … with the per-side counts; limitation text states the derivative identity.
  26_LOCAL_AUTHORITY/lab/assets/athlete_m_preview/dev_0.14/       ATHLETE_M_AM08_V6 = ATHLETE_M_V9 (GLB sha256 7d217d0f…, review-round build): same inputs as dev_0.13 (AM08 8K base + AM02B eye mask, k3, 2048², hip-x 0.045, col-min-arm 0, axilla unweld) with the faithful legs. Legs: L 2133 verts / 2148 tris (893 source triangles kept + 98 clipped), R 2389 / 2328 (983 + 98); 122 seam vertices per side; thigh half-width 0.0898, full lateral shift ±0.0283 source units (0.31 × 0.0898 + a 0.5 mm gap) RAMPED in below the crotch (0 at the pelvis ring, full 0.08 H lower — see the review round), top ring |x| 0.0898 = the pelvis shell half-width (asserted by the generator).
  26_LOCAL_AUTHORITY/lab/play.js · deploy/build_static_demo.mjs  ATHLETE_M_V9 is the default; V8 (dev_0.13, the surgical loft) stays registered and shipped for rollback (`?avatar=ATHLETE_M_V8`). Dev hooks for evidence: `legsDebug()` (which mesh is visible, morph influence, bone world positions, root transform), `vfx(on)` (beams / sprites / points hidden before the render), `restPose()`, `holdPose`, `pixelRatioFreeze()`, `meRoot()`.
  deploy/probe_legs.mjs (new) · 16_TESTS/gameplay_legs_faithful.test.mjs (new) · gameplay_leg_surface / gameplay_rig_v5_gates defaults → dev_0.14 (the winding gate now checks the geometric winding against the stored outward normals + a positive signed volume instead of the loft-only ring heuristic).

WHAT I FOUND (PASS 4B):
  · F04’s "narrow knees / shins with dense dark-gold patterning" had two causes in the v5 loft: the shins were narrowed to 0.5 of the fused half-width, and the loft sampled UVs from the nearest fused vertex across a 223-island atlas (the lower body alone has 223 UV islands, 117 of them 1–2 triangles) → a patchwork. With the source triangles the knee section is 39.7 mm (v5: 29.1) and the texture is the fused plates, mirrored on the inner face.
  · The "long bright straight lines below the tips" in F04 are the split-form propulsion beams (VFX), not geometry — the VFX-off captures show the pointed tips end where the legs end.
  · The fused geometry the legs derive from is the source: the 14 380 source vertices are byte-identical in the body mesh (max |Δpos| = 0, max |Δuv| = 0); the 303 extra body vertices are the S10 axilla patches + the crotch cap, none below the crotch line; the source GLB hash still matches the manifest.
  · Leg surface test (linear-blend skinning straight from the GLB): closed (0 boundary edges), manifold, gait swing 0.01 % edges out of band / 0 inverted, X-gather 1.97 % / 0 inverted, knee keeps 96 % of its section, 31 knee loops, PACKED 0 inverted — 8/8 (v5 was 6/8: 0.61 % / 5 inverted and 1.65 % / 81).
  · Page probe (`probe_legs.mjs`, 9/9, record `legs/legs_default.json` written 2026-09-18 11:46 UTC — every number below is copied from that file): D1 fused identity (fused lower visible, legs hidden) · D2 SEPARATE → SPLIT (legs visible, morph 0) · D2b the transformation traced per RENDERED frame in real time (111 frames, median dt 17 ms): the packed value only decreases, through the distinct intermediate values 0.54 · 0.32 · 0.29 · 0.25 · 0.21 · 0.10 · 0.05, the largest single-frame drop 0.46 (the authored 50 ms ENTER crack), the leg mesh shown on every non-packed frame · D3 independent hip / knee motion in the walk (the left knee’s horizontal offset from its hip across six mid-stride frames: 0.081 · 0.092 · 0.154 · 0.083 · 0.186 · 0.166 m) · D4 FUSE restores the fused identity · D5 G35: re-fused vs first-fused at the BIND pose, VFX off, his pixels only (an unlit magenta override render masks him exactly): mean |Δ| 0.04 / 1.94 / 0.86 per channel (front / side / back), 0.01 / 0.78 / 0.34 % of his pixels > 24 (the full-body view at 4.6 m is reported only: 13.0 / 15.6 % — its silhouette edge and the head’s animated emissive dominate its 55 k-pixel mask) · D6 repeated split / fuse leaves exactly one leg mesh with a constant vertex count · D7 G35 the FUSE swap itself — the SAME paused frame rendered with the packed legs and with the fused taper — differs by 6.8/255 per channel on his pixels (≤ 8): no pop at the swap (the frame-to-frame change at that moment, 85.7, is the authored SNAP beat itself: the legs straighten in 80 ms).

EVIDENCE (`25_HANDOFF/CONVERGENCE/legs/`, all from the 11:46 UTC run): `legs_default.json`; `split_views.jpg` (front / side / back, VFX off); `front_fused_split_wire.jpg`; `legs_crop_front_back_wire.jpg` (leg region at 2×); `hip_neutral.jpg` (the hip line under neutral shading — no ledge); `transition_strip.jpg` (deterministic 16 ms frames of SEPARATE, stills only); `refuse_strip.jpg` (deterministic frames of FUSE incl. the swap frame); `walk_and_crotch.jpg` (mid-stride frames VFX off + the low crotch view production / wireframe — G34); `pair_side.jpg` (fused → re-fused → split); `10_refusion_diff_front/side.png` (reference | difference map). Full set (stills for every view × production / VFX off / neutral / wireframe) in `26_LOCAL_AUTHORITY/deploy/probe_out/legs/default/`.

PASS 4B REVIEW ROUND (fresh-context reviewer of d5a996b; fixed in this build):
  · MAJOR — the separated form’s uniform lateral shift pushed each leg’s top ring 5 cm outside the pelvis shell: a visible hip ledge / flared "hem" and an exposed cone (the report had claimed the cone was hidden). Fix: the shift now RAMPS in below the crotch line (0 at the pelvis ring, full 0.08 H ≈ 15 cm lower); the medial D keeps its full depth (a flattened wall folds the front / back halves onto each other — measured: 30 non-manifold edges and a failed winding gate), so above the ramp the two D-walls overlap inside the pelvis (interpenetration, never shared geometry); the generator now asserts the top ring stays within the pelvis shell half-width and that the open arc is the crotch ring. Leg surface 8/8, gates 8/8, identity 10/10 again; `hip_neutral.jpg` shows the hip line.
  · MAJOR — the report quoted numbers from earlier probe runs than the committed records. Fix: every number above is from the tracked JSONs of one run (11:46 UTC) — stills and JSONs come from the same run.
  · MINOR — G35 was evidenced only at the end state and the pixel mask leaked background. Fix: the mask is an unlit magenta override render of him alone (exact colour test); a deterministic FUSE capture measures the swap frame itself (D7). While building D7 the swap WAS a pop (an 81/255 change: the packed legs were still 61 % gathered against a nearly straight tail when the old animator swapped at 60 % of the SNAP beat) — the animator now straightens the packed legs through the whole SNAP and swaps at 85 % (`lab/RigAnimator.js` REFUSE_SNAP / RECOVER); the swap-only difference is 6.8/255.
  · MINOR — D2b was under-sampled and raced the host’s stage clock under stepped virtual time. Fix: the metric is a per-rendered-frame real-time trace; the stepped stills remain stills.
  · MINOR — `gameplay_play_sample` bounded the first-tick displacement by the canonical FUSED band, so the ramp was no longer tested under the harmonized band. Fix: the harmonized band (a full-speed first tick would be 0.179 m; the 14 m/s² ramp gives 0.035 m).
  · MINOR — the builder coned every non-seam open edge without a guard. Fix: the arc must be the crotch ring (asserted); the manifest logs the gap / ramp / knee blend.
  · NOTE — the mirrored texture meets its own reflection along the front / back seam of each leg (a faint vertical line, inherent to the mirror construction) — stated in the manifest limitation. The arena: FUSED fighters no longer have the 2.5× ground-speed advantage over SPLIT fighters (both at the harmonized band) — a balance consequence of the owner’s identity rule, stated here. `play_sample` / `rules_17_23` / `BeamFx` were rewritten CRLF → LF by the patch tooling in d5a996b (1–2 real lines each; `git diff --ignore-cr-at-eol`); the tooling now preserves line endings.

ACCEPTANCE (brief §PASS 4 legs / O6): original-source provenance + unchanged fused geometry (G32) → PASS (test) · faithful separated shape / material correspondence (G33) → PASS on the harness (source triangles + mirrored UVs; test: 536 exact + 85 clipped source-triangle UVs of 621 sampled, 0 orphans), OWNER VISUAL ACCEPTANCE pending (Card B) · correct independent hip / knee motion + intact inner surfaces (G34) → PASS (closed manifold, D3, low crotch views), owner visual acceptance pending · seamless refusion (G35) → PASS (D4–D6 + the pixel diff), owner visual acceptance pending · derivative identity logged, never the original mesh id → PASS.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
PASS 4b — WALK IDENTITY ACROSS LEG STATES (addendum: "fused and separated must share the same locomotion identity … if needed, treat the separated-leg walk as the locomotion target and make the fused walk harmonize to it … log measured walk speed, step cadence, stride timing and transition timing for both; state whether they match or where they differ; if they differ, do not call the pass complete")

WHAT I CHANGED:
  00_CORE/dev_tuning.dev.json (`local_authority.play.movement.harmonize_fused_ground_to_split: true`) · play/PlayMode.js · DuelHost.js   the separated WALK is the locomotion target: the fused form’s GROUND travel now uses the SPLIT band (walk 3.58 m/s = 8 mph; `+` = run 6.71 m/s = 15 mph) instead of the canonical FUSED 8.94–17.88 m/s (20–40 mph) band; powered flight keeps the canonical FUSED band top (20.6 m/s with the air factor). One line rolls it back (`false` = the canonical bands). Acceleration / deceleration (14 / 40 m/s²) were already shared. The tests’ walk helpers and the dev `goTo` travel with `fast` (the band top) so their choreographies still reach their marks at the new pace.
  lab/RigAnimator.js `gaitState()` (read-only) · lab/play.js `walkTrace()` (one row per rendered frame: displayed + host position, host speed, gait phase / support, beam levels) · deploy/probe_walk_identity.mjs (new).

MEASURED (`probe_walk_identity.mjs`, the same idle → walk 4 s west on the open strip (60, −45) → release → settle, side camera, both forms; `25_HANDOFF/CONVERGENCE/walk_identity/walk_identity.json` written 2026-09-18 11:46 UTC with `trace_fused.json` / `trace_split.json` — the numbers are copied from that file; this run includes PASS 5’s snapshot interpolation):
  · walk speed (steady, displayed / host integrator): FUSED 3.574 / 3.576 m/s · SEPARATED 3.575 / 3.576 m/s → MATCH (ratio 1.000).
  · start transition (input → 95 % of steady, displayed): FUSED 414 ms · SEPARATED 405 ms → MATCH within 150 ms (the host ramp is identical: 14 m/s² → 0.26 s; the display sits 120 ms behind the host by design of the interpolation).
  · stop transition (release → still): FUSED 277 ms · SEPARATED 279 ms → MATCH.
  · overshoot: the host integrator peaks exactly at the steady speed in both forms (1.00); the displayed 300 ms-window peak-over-steady is 1.037 / 1.043 (before the interpolation buffer it was 1.13 / 1.16 — the snapshot ripple, identical in both forms).
  · step cadence / stride (SEPARATED): 1.99 steps/s, step period 507 ms, stride 1.81 m at 3.575 m/s (the owner’s distance-tied cadence: one full cycle per 3.6 m).
  · step cadence / stride (FUSED): NONE — the fused form is a GLIDE by the owner’s earlier rule ("RUN is a SPLIT gait — FUSED moves as a glide"): the gait phase stays frozen (advances = false), the propulsion beam carries the travel.
  → PACE, START, STOP and CALMNESS MATCH; the STEP RHYTHM exists only in the separated form. Per the addendum’s own rule this pass is NOT called complete: the difference is stated, and the decision is the owner’s — (a) accept the glide as the fused form’s walk (pace / timing shared, no step rhythm), or (b) give the fused glide the separated walk’s rhythm (a propulsion pulse at the 502 ms step period). Nothing was invented in either direction.
  · smoothness (identical in both forms, not an identity difference): with the host at a constant 3.58 m/s the DISPLAYED speed rippled ±10 % over 300 ms windows before PASS 5’s snapshot-interpolation buffer and ±4 % after it (see PASS_5_MOTION_E_REPORT.md).
  · stills: `walk_identity/walk_identity_fused.jpg`, `walk_identity_split.jpg` (idle / walk / stop, side view, same camera).

SUPERSEDED: the canonical FUSED ground band 8.94–17.88 m/s (PLAYER_MOVEMENT_DEFAULTS, and PASS 3’s "FUSED ground speed doubles with `+`") → the harmonized SPLIT band on the ground (walk 3.58 / `+` 6.71 m/s) for both forms; the canonical JSON itself is untouched and the runtime rule is a one-line rollback. PASS 3’s L4 page check still holds (`+` = 1.87× the walk).
OPEN RISKS: the owner’s visual acceptance of the legs (G33 / G34 / G35 — Card B) and of the harmonized pace (the district is ~300 m across: 84 s on foot, 45 s with `+`, 15 s in flight); the groin can still pinch at extreme hip angles (every auto-rig here); the faint seam line down the front / back of each leg (mirror construction); the residents (other classes) keep their v5 surgical legs — the faithful builder applies to any class GLB (`--legs faithful`) once Athlete_M is accepted (its guards will refuse a source whose lower body has stray boundaries).
RESOURCES: three.js 0.185.1 (GLB load / skinning / morph targets, vendored); node 22 (the generator, the GLB writer, the tests); headless Chrome (texture resample in the generator, the page probes, deterministic virtual time for the transition frames); the owner’s source `RAW_10_MODELS/Mah_Athlete_M.glb` (read only, hash-checked) and Astra’s AM08 / AM02B textures (read only, as in dev_0.13). Tripo / Reallusion / Blender: NOT USED (Blender slot still reserved).

CANDIDATE (Cards A + B in one build) — CONVERGENCE_B2 supersedes B (which had the hip ledge) and A2 for the phone review:
  ZIP        `26_LOCAL_AUTHORITY/deploy/packages/CONVERGENCE_B2.zip` · 28 785 929 bytes · 103 entries · sha256 6da0203901e38b578732ea9e49fee4f3f4bc7ec59078f8d2158c38203144e407 · commit 4f7aadd · Section 2 pipeline: 30 test files, JS + CSS sweeps, stamps, boundaries, secret scan, ZIP integrity, fresh extraction, smoke test from the extraction (7/7) — record `CONVERGENCE_B2.PACKAGE.json`.
  DRAFT      the packaged content (extracted ZIP) deployed as a DRAFT of the demo project (not production): https://6aad2eeae77aff15879b2553--mahworld-test-preview.netlify.app (deploy 6aad2eeae77aff15879b2553). Gate check: play page 401 → password page, private asset 401 (fail-closed). Correct-password login: NOT RUN (the password is not held here). The password itself is nowhere in code, config, URLs, git, logs or this report. (netlify-cli prints "some redirects have syntax errors" for the `/.netlify/images → 404` rule every deploy since gate v2 has carried; the gate check passes.)
  LIVE       https://mahdemo.fob.systems still serves the owner-round deploy 6aac25101f17ce5ed37166e4 (verified through the site record after the draft upload); rollback 6aab65567e997ae33c1da204. Nothing was replaced.
  ROLLBACKS inside the build: `?avatar=ATHLETE_M_V8` (the surgical-loft legs), `harmonize_fused_ground_to_split: false` (the canonical 20–40 mph fused glide), `presentation.interp_delay_s: 0` (the old position filter), `?quality=low` (render scale 1.0).
  REGRESSION on this build: every page probe green (pivot ×3, Spec B ×3, camera ×3, F01, O4 ×3, HUD ×3, gestures ×2, fidelity, legs, walk identity, motion E’s met items); F01 and Spec B portrait each failed once inside the long back-to-back chain (a settle-pulse timing and a reset-yaw sample under GPU contention) and passed when rerun alone — logged as harness flakiness, not a build defect.
