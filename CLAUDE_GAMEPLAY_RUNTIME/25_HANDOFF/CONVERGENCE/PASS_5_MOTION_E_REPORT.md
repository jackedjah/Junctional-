PASS 5 — SPEC E ANIMATION SYSTEM · measurement slice (rendered / measured on the real page) · 2026-09-18 night · STATUS: IN PROGRESS, NOT COMPLETE

Report separation: (1) in progress before the addendum = nothing of PASS 5 (PASS 4A / 4B / 4b are done and at the gate); (2) appended by the addendum = the connected-movement rules (idle → turn → travel → stop → settle, no snapping / bobbing / fidget, immediate response) fold into this pass; (3) completed now = the Spec E acceptance instrumentation and the first honest numbers, the tunables that make the two contact models comparable; (4) remaining = the planted-contact solve (owner decision first), the state-machine / competing-writer audit against Spec E’s explicit state list, PASS 6–9.

INVENTORY (PASS 0, unchanged): motion = procedural bone code (`lab/RigAnimator.js`, 38-joint derivative rig, identity-bind rest pose) + the Tripo donor upper-body clips (staging / hit reactions only, lower body always MAHWORLD) — no captured locomotion exists yet (Spec F is the owner’s capture); speed → the animator through the presentation adapter (`view.speed01 / speedMps`); attack motion lives in the animator’s pattern layer.

WHAT I ADDED (instrumentation with defaults unchanged, plus one presentation change — the snapshot interpolation, see SMOOTHNESS):
  26_LOCAL_AUTHORITY/lab/play.js            `motionTrace(start | stop, bones)` — one row per rendered frame: host speed, root yaw, support pulses L / R, the animator’s support-hold anchors, TIP_L / TIP_R / CHEST / PELVIS / HEAD world positions, the traced bones’ local rotations (identity = the bind pose); `presentationDefaults()` (the live defaults object, for A/B measurements).
  26_LOCAL_AUTHORITY/lab/RigAnimator.js     `gaitState()` now reports the support-hold anchors; the support-tip constraint’s three constants are tunables in `lab/presentation_defaults.json` → `gait.support_drift` (0.45 = the owner’s hover thrust, unchanged), `gait.support_hold_behind_m` (0.16), `gait.support_reach_frac` (0.86).
  deploy/probe_motion_e.mjs (new)           E1–E5 on the walk / run / idle / transform script in both forms; `25_HANDOFF/CONVERGENCE/motion_e/motion_e.json` + before / after strips.

MEASURED (Spec E acceptance, verbatim items):
  · "foot slide ≤ 2 cm per contact at walk and run (logged)" — UNMET, with the definition conflict stated: this species has no feet; by the owner’s motion pass D the separated tips HOVER over a propulsion beam and the animator’s support-tip constraint deliberately lets the held point drift back at 45 % of travel ("a hover thrust point, never a plant"). Logged (`motion_e/motion_e.json`, 11:46 UTC): WALK 3.58 m/s — 7 contacts (the animator’s own hold window, ~140 ms), median 42.8 cm, max 46.5 cm of tip drift per contact (over the whole support pulse ≥ 0.9, which includes the leg’s forward reach: median 50.8 cm); RUN 6.71 m/s — 11 contacts, median 18.3 cm, max 23.7 cm. FUSED — N/A (a glide on the beam: support L = R = 1, no contacts). A PLANTED variant was measured with the same code and the tunables at drift 1.0 / hold 0.5 m / reach 0.98: walk median 33.1 cm, run median 12.9 cm — i.e. the present constraint cannot plant even when asked to: it solves a sagittal two-bone chain in the pelvis frame while the pelvis rolls / yaws / leans and the hold weight ramps over ~110 ms, so the world point is not held. A real plant needs a world-space anchor solved in the actual PELVIS frame (Spec E "foot IK plants feet; pelvis adjusts height"). → OWNER DECISION before implementation: (a) keep the hover thrust (then the Spec E number does not apply to this species and the report says so), or (b) plant the tips (a new solve, and a change to the owner-authored hover style).
  · "no bind / T-pose frames" — MET on every script segment (11:46 UTC run): 0 bind frames of 2 385 (fused idle), the fused walk / stop / turn, SEPARATE, the separated walk / stop, 2 185 (separated idle) and FUSE (a bind frame = every traced bone within 0.02 rad of the identity rest).
  · "no bone angular-velocity spike above the logged threshold during transitions" — MET for locomotion transitions with the threshold LOGGED from each form’s own steady walk (p99 × 2.5): FUSED steady p99 1.76 rad/s → threshold 4.39, start / stop / quick-turn max 3.52 rad/s, 0 spikes; SEPARATED steady p99 22.15 rad/s → threshold 55.4, start / stop max 39.3, 0 spikes. Stated, not hidden: the separated steady p99 itself is high (≈ 1 270 °/s at the knee — the held-extreme thigh curve and the under-body knee flexion pass quickly); the SEPARATE transformation peaks at 21.1 rad/s (SHIN, the authored UNFUSE_BURST / STAR beat) — logged against the walk threshold, not enforced, because it is an authored burst. The threshold definition for "transition" is this pass’s own (no owner number exists).
  · "20 s idle capture shows breathing and weight shift" — MET in both forms: breathing = HEAD forward excursion 26.4 mm (fused) / 20.2 mm (separated) at a 4.7 s period (the defaults author 4–6 s); weight shift = HEAD lateral excursion 8.0 / 8.0 mm at the authored 9.0 / 9.2 s roll (the active idle rolls the pelvis / lumbar / chest over fixed hips — the pelvis does not translate, 0 mm; whether a visible hip translation is wanted is a style call, the roll is what the code authors).
  · "before / after frames saved" — `motion_e/e5_fused_idle_walk_stop_turn.jpg`, `e5_split_walk_planted_run_fuse.jpg` (+ the full stills in `26_LOCAL_AUTHORITY/deploy/probe_out/motion_e/`).

SMOOTHNESS (addendum: connected movement, no bobbing / jitter) — DONE: with the host at a constant 3.58 m/s the DISPLAYED speed rippled ±10 % over 300 ms windows in both forms (the 20 Hz host ticks polled every ~60 ms reaching the root through an exponential filter). `lab/PresentationAdapter.js` now renders every character root `presentation.interp_delay_s` (0.12 s, `lab/presentation_defaults.json`; 0 = the old filter) behind an estimated host clock (the host_t ↔ wall offset filtered over ~7 polls) by linear interpolation between the two bracketing snapshots; a teleport (> 6 m) or a snap resets the buffer; the newest sample is held if the clock runs past it. Measured on the walk-identity script (same file, 11:46 UTC): 300 ms-window peak-over-steady 1.13 / 1.16 → 1.037 / 1.043 (fused / separated); the 300 ms speed band during the steady walk 3.34–3.81 → min 3.34 / p50 3.57 / max 3.81 before, and after the filtered clock a per-frame p5–p95 of 1.9–6.3 m/s remains in the trace (the rAF timestamp vs the update clock inside one frame — a measurement artefact, not a displayed step). Start-to-95 % moved from ≈ 460 to ≈ 410 ms, stop from ≈ 480 to ≈ 280 ms (the buffer’s constant 120 ms replaces the filter’s tail). NPCs / other players / the arena opponent use the same path (host_t is passed for every entity).

STATE MACHINE (Spec E "one animation owner, explicit states"): the animator already owns IDLE · locomotion (speed-blended glide / gait) · quick turn · TAKEOFF / FLY / LAND · GUARD · ATTACKS (pattern layer) · HIT-REACT (donor) · KO / GET-UP · INTERACT · the transformation beats; JUMP does not exist as a state (the species flies); START / STOP are the gait blend-in / settle.

COMPETING-WRITER AUDIT (Spec E: "audit competing pose / root writers before adding new layers") — DONE, no change needed:
  · ONE pose owner: `lab/RigAnimator.js`. Every bone rotation is accumulated ADDITIVELY into one Euler map `E` per frame (`setRaw` adds; nothing replaces a layer's contribution), in a fixed order documented at the layer boundary (`/* 10a … ONE composition per frame */`): base locomotion / action (idle breath + gaze + variation + the 9 s balance roll → glide or gait → flight → attacks / guard / transformation) → the emote / dev override map → the Tripo donor UPPER-mask layer (weighted, lower body and root never accepted) → girdle rhythm → twist carriers → trunk distribution → joint limits (11: soft approach + hard clamp) → body clearance correctives (12: arm / hand clearance, the support-tip two-bone IK at lines 335–336, the clearance IK at 545) → inertialization on state changes → a per-frame rate filter (≥ 24 /s) → the bone quaternions. Counted writers per bone: PELVIS 23 `set` lines, SPINE 29, CHEST 32, HEAD 23, THIGH 15, SHIN 12, UPPERARM 28, FOREARM 25 — all inside that one function, all additive.
  · Root / position writers outside the animator: `PresentationAdapter` (the host position through the snapshot interpolation, the local drive offset and hover lift on the figure group, the root yaw with the turn spring) and `GlbCharacter.setForm` (the PACKED morph + leg / taper visibility). Neither writes a bone rotation. The host (`PlayMode`) is the single movement authority (R170); the presentation only displays.
  · Consequence for Spec E's additive layers: breathing, the weight-shift roll, lean into acceleration / turns and the look-at all ride on the base without replacing it — already the shape Spec E asks for; a planted-contact solve, if the owner chooses it, belongs in stage 12 (correctives) like the present hover hold.

ACCEPTANCE (brief §PASS 5): foot slide → UNMET / definition conflict reported, owner decision · no bind frames → PASS · no spikes above the logged threshold → PASS (threshold logged) · 20 s idle → PASS · before / after frames → PASS. PASS 5 is NOT complete: the planted-contact question (owner) and the competing-writer audit remain; the interpolation buffer is in. Missing real motion (Spec F capture) remains explicitly pending; nothing procedural is claimed as a capture.
RESOURCES: three.js 0.185.1 (vendored); headless Chrome; node 22. Tripo donor clips: present in the runtime as before (upper-body staging / hit reactions), not changed. Reallusion / Blender: NOT USED.
