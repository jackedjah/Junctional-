# MAHWORLD Astra takeover state

Updated 2026-09-24 (post-M6 quality continuation checkpoint 02; protected M6/production preserved). This is a compact live state record, not a replacement for `CONTINUE_HERE.md`, the B8 report, or the complete transfer master.

## Active post-M6 checkpoint 02 — world/gameplay presentation

- Checkpoint 01 is remotely verified at `80b96391c29e275cdac1c3b238b713b90c5e15d3` on `backup/mahworld-m6-20260924T190351Z`. The current source adds lower/longer Moon-keyed cloud carriers, connected subdued distant banks, layered far massifs/towers/arches and macro-ridge strata without replacing the actual Moon or route authority.
- Visible equipment sockets now use the source HAND→FINGERS palm span; authoritative equipment ownership, projectile/special behavior, damage, costs and exercise selection are unchanged. The crystal prompt/material pass remains near-only and preserves the existing break/drop/refill system.
- Bounded proof is current: 180-file clean static build; 11 NIGHT views; zero console/runtime errors; break + three drops/refills; complete L0 equipment; zero-error debug palm/midpoint grips; representative `PA_PUSH` accepted under its existing PHYSICAL_MAGIC profile. Existing focused checks remain 98/98.
- Exact visual remainder: after two focused presentation attempts, the bar shaft still intersects the rear neck/head silhouette during the inspected `PA_PUSH` load. The close bush prompt remains more prominent than ideal. Both are recorded rather than extended into an open-ended investigation.
- Next action is the single authorized release gate/package and one new protected unpublished draft. Preserve M6/M5C/production and do not claim physical-phone acceptance.

## Active post-M6 checkpoint 01 — foreground cohesion

- Preserved verified recovery base: public GitHub repository `jackedjah/Junctional-`, branch `backup/mahworld-m6-20260924T190351Z`, incoming tip `fa5576a0cb988eb226a880340aeeff8e0f9cdb98`. M6 deploy/package and production were not touched.
- Foreground source changes are limited to `lab/world/terrain.js` and `lab/world/gear.js`: restrained regional overlay; world-space satin road detail without changing junction ownership/collision; less clipped, denser crystal flora; softened geology beds; and a smaller `16 m` interaction hint. Resource authority remains unchanged.
- Focused local verification: source syntax/diff checks clean; static build/private scan clean at 180 files / 207.1 MB; one reused floor/junction view completed with zero unresolved/runtime errors and visibly restored road surface detail. No package/deploy/physical-phone claim.
- Next source milestone is the cloud/horizon shell, followed by one bounded representative movement/equipment/action check and the authorized release gates.

## Current release checkpoint — protected M6 candidate

- `ASTRA_M6_PRIVATE_20260924` is ready at immutable draft `6ab586ec9ad382fa1b83f1be`: `https://6ab586ec9ad382fa1b83f1be--mahworld-test-preview.netlify.app`. It is a `deploy-preview` with `published_at = null`, not production.
- Package SHA-256 `82d5433350d9785e665fe9b5b74b88e07ddbf2c40a1c9e36c3a1a4fea87ab02c`; 181 entries / 122,974,744 bytes. Existing package gates passed 56 programs / 1,518 checks and 7/7 exact fresh-extraction smoke.
- The exact persistent extraction passed the bounded M6 visual/action probe and ordinary-controller junction crossing: 0 console errors, 0 unresolved items, real bush break / three drops / three pickups, zero-error dumbbell and barbell grip reports, and `0.009 m` minimum junction distance.
- Gate is present: unauthenticated play and assets return `401`; wrong-password rejection works; served password string is absent. Authenticated hosted gameplay is not claimed because `MAHDEMO_PW` was unavailable.
- Production is still `6aac25101f17ce5ed37166e4`; M5C is still ready at `6ab454bf3a60c4bf88b1334a`. Next step is Jah's physical-phone review. No production promotion is authorized.

## Current recovery checkpoint — remote bytes verified, junction fixed locally

- Verified PUBLIC repository `https://github.com/jackedjah/Junctional-`; recovery branch `backup/mahworld-m6-20260924T190351Z`. Source commit `bdc43b60d56c22253aeddf2eac996a5394128407`; required-asset commit `a25277ebe6afc378af63ed19735a53f2c400327b`. No force push, default-branch merge, production deployment or visibility/access change occurred.
- Fresh remote readback verified 98/98 required assets and all 1,454,701,148 bytes by manifest SHA-256, with zero missing/mismatched files and no LFS pointers. Runtime derivatives/authoring files and registered originals/building inputs are recoverable; restricted footage, secrets, private machine state and separate job assets are intentionally excluded.
- `CW_GYM` / `RP_FOREST_W` cause: true-circle ribbon clipping met 20-sided inscribed frame/core junction discs, exposing the dark ground as circular/angular chord gaps. The bounded repair changes only their shared boundary to 128 segments (sub-3.1 mm maximum gap), preserving collision, paths, ground colors, trees, Moon/clouds, Dogkie, aura, resources, equipment and combat cadence.
- Fresh local static build passed its private-content scan. Matched NIGHT, oblique and 6.25 s ordinary-controller crossing proof is clean; crossing reached `0.188 m` from `(-40,40)` and logged zero unexpected console errors. Evidence is under `astra_m6_closeout/junction_isolation/` and `junction_repair_proof/`.
- Cloud execution is **NOT TESTED**. The next unfinished milestone is an explicitly authorized protected M6 preview; this run did not package or deploy it.

## Current checkpoint — M6 bounded closeout, editing only

- Same M1–M5C worktree and ownership boundaries remain active. M5C build `ASTRA_M5C_PRIVATE_20260923`, deploy `6ab454bf3a60c4bf88b1334a`, the earlier M5 comparison candidate, and production deploy `6aac25101f17ce5ed37166e4` are unchanged. No package, deploy, live publish, access change, Character/MAHFITT/S08/Blender edit, reset, clean, stash, stage, or commit occurred.
- World polish is integrated locally: one smoothly blended regional ground surface; blended road junction colors; denser ambient shard patches; 41 distributed crystal-resource patches with 64 compact shards each; elongated atmospheric cloud layers at `118/190/330 m`; the actual registered Moon derivative high at `0.28 rad`; Moon-direction night cloud highlights with smoother 40/42-edge carriers; richer distant ridge facets; and one efficient slow arcane-leakage mist language in place of blunt building bands. MAH GYM is blue.
- Imported-building scale, entrance alignment, collision, and interiors remain as accepted in M5C. Their materials gained bounded PBR/texture presentation improvements. HIGH/MED dumbbell and barbell rendering now uses existing L0 derivatives with exact palm/two-hand grip authority retained. Dogkie preserves original GLB albedo/material identity rather than receiving a synthetic body tint.
- The shared arcane-leakage field now includes seven selected existing tree-canopy anchors and two free accents, remaining one draw call and 321 faint/slow motes across 33 total anchors. No rectangular band or second particle system was added.
- Physical and physical-MAHGIC presentation now uses profile-specific QUICK/HEAVY/PRECISE/FLOW/BALANCED envelopes; PHYSICAL_MAGIC receives a small additional readability offset. Exercise poses and host phase/damage/cost/cooldown authority remain unchanged. This is presentation polish, not a combat redesign or S08 character-lane change.
- One bounded local visual session is saved at `astra_m6_closeout/visual_check/`: 11 frames, 0 runtime/console errors, actual `PA_PUSH` PHYSICAL_MAGIC sampling, resource break + three refill pickups, L0 dumbbell/barbell geometry, and zero-error palm/two-hand grip debug. It visually covered ground/junction, Moon/clouds, bushes/refill, source-color Dogkie, blue MAH GYM, selected canopy accents, rest-body integrity, and physical/physical-MAHGIC gear poses. No broad regression or soak ran.
- The previously unresolved `CW_GYM`/`RP_FOREST_W` circular/angular fragments are now closed by the cause-based junction-boundary repair above. The former `floor_recheck` frame is retained as baseline evidence rather than overwritten.
- Next action is owner review, followed only by a separately authorized protected M6 preview. No current M6 package, hosted acceptance, production or physical-phone acceptance is claimed.

## Current correction checkpoint — checkpoint 03 protected candidate closure

- Build `ASTRA_M5C_PRIVATE_20260923` is packaged from the preserved current worktree and deployed as protected draft `6ab454bf3a60c4bf88b1334a`: `https://6ab454bf3a60c4bf88b1334a--mahworld-test-preview.netlify.app`.
- Package gates: 56/56 programs, 1,518/1,518 checks, 104 syntax files, clean scans, 181 entries, and fresh-extraction smoke 7/7 twice. Archive SHA-256 is `9eb0816871a258a408a7b453e8d4982dc851708a48eccd90e2411494ac9d1571`.
- Password protection is live and verified on root/play/representative JS/build/Moon paths. Authenticated hosted loading is not claimed because `MAHDEMO_PW` is unavailable.
- Production is still `6aac25101f17ce5ed37166e4`; comparison candidate `6ab3abd739fc958c75339988` is still ready/unpublished. No promotion, alias, or gate change occurred.
- Durable release record: `astra_m5_corrections/private_candidate_release.json`, SHA-256 `d33bc948b51e2b4dd89bff61654c20b4ec3ed6ab95587b9351bcada460d9a850`. Physical-phone acceptance remains owner-pending.

## Prior correction checkpoint — checkpoint 02 integrated local closure

- Same worktree/ownership remains active at HEAD `3185a25f80a23131fbdb2062fca9a1a1ffb7684f`; immutable build `ASTRA_M5_PRIVATE_20260923`, deploy `6ab3abd739fc958c75339988`, production, improved trees, and JOB A/B boundaries are preserved.
- OP01–OP10 are fixed/verified locally; OP11 is optimized and measured locally/emulated but remains physical-device open; OP12 is fixed and verified across five family colors, both sky states, and desktop/phone emulation.
- Authority is synchronized at registry `825f6943b093d17df65522c1b1cb398b33bf9c2790bde2183ede3376aa757f5f`, with 792 district, 9,457 world, and 10,277 total mapping shapes.
- Canonical reports are desktop owner corrections `62/62` (`f586b35415ef0f1939e1dacf53042c3913a96b6d09af0661a88f34b6a91663cd`), phone-emulated owner corrections `62/62` (`9382801a31f9d5ee25cb673da18d43f51835bbb805e27dc96d5068987e9b52c2`), visual gates `20/20` per profile, interiors `26/26`, tree LOD `100/100`, and candidate route `20/20` (`55a3137e20e0f82d2e64b04a4b06a4eded7f5a1a21770bf23cebb3a75470270e`).
- The route preserves intended Titan Lake fish aggression and proves its normal counter: low flight at `11.46 m`, below the Phoenix threshold, followed by a complete landing outside the fish leash; zero defeat/respawn and no Titan-stage reset, heal, or placement shortcut occurred. The one earlier `PRACTICE_RESET` belongs to the explicit resource-drain fixture.
- Non-browser regression is `15/15` programs / `385/385` checks, with core `npm test` `125/125`. The 900-second soak, package, extraction, and protected draft deployment have not run.
- Building fidelity remains honestly bounded: traced reduced derivatives are not exact raw parity or recovered missing originals; textures are resampled, unsupported extensions removed, and interiors are authored functional spaces. Physical-phone acceptance remains owner-only.
- Durable checkpoint: `astra_m5_corrections/checkpoint_02_integrated_corrections.json`.

## Prior correction checkpoint — checkpoint 01

- Baseline/preservation is recorded at `astra_m5_corrections/baseline_checkpoint.json`; the immutable M5 comparison candidate is still build `ASTRA_M5_PRIVATE_20260923`, package SHA-256 `ebcad0ed…5995`, deploy `6ab3abd739fc958c75339988`. Production remains out of scope.
- Checkpoint 01 is `astra_m5_corrections/checkpoint_01_surface_collision_refill.json`. OP05/OP06 road topology, OP09 discoverable normal-play refill, and OP10 visible-monolith satellite collision are implemented with synchronized generated authority and focused headless tests. Their required runtime visual/touch gates are still open and are not mislabeled as owner acceptance.
- Current registry SHA-256 is `5512b124a8e19f5dd92247c3c6934a3979bb4f96c17667db8716490178419ab9`; mapping is 28 base + 1,247 district + 251 world = 1,526 shapes. Focused results: correction `9/9`, B8 equipment `25/25`, world JOB B `34/34`, M3 route `13/13`.
- Next: imported buildings/doors/interiors, actual Moon/cloud/light, far-tree identity/performance, then integrated captures, full regression, one representative soak, fresh package and a new protected non-production candidate. Physical-phone acceptance remains Jah's.

## Ownership and preservation

- Gameplay owner for the non-conflicting A/B continuation scope: this local Codex/Astra task.
- Repository/worktree: `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS`, branch `master`, HEAD `3185a25f80a23131fbdb2062fca9a1a1ffb7684f`; the working tree was clean at takeover.
- No fresh Claude release was available or required. The one Claude process aimed at this gameplay folder is an explicit read-only recovery check. Two other idle Claude processes have no child jobs and their recent task files resolve to the separate Character workspace.
- Character masters, S08 appearance work, and the Blender slot remain outside this scope. `BLENDER RELEASED` has not been established.
- No reset, clean, stash, clone, replacement world, new worktree, model/billing/auth change, purchase, external asset upload, deployment, or production publish was performed.

## Transfer ingestion

- Read completely: `MAHWORLD_ASTRA_TRANSFER/02_PASTE_IN_ASTRA.txt`, `03_MASTER_REVIEW/01_MASTER_CONTINUATION.txt`, and `04_GITHUB_REUSE_ADDON.md`.
- Read for the active milestone: the video review, source-asset audit/inventory, primary-resource index, state/provenance, and all 167 requirement-index entries.
- Live handoff read: `CONTINUE_HERE.md` and `B8_KILLER_DIRECTION_DELTA_REPORT.md`. No project `AGENTS.md` or gameplay `CLAUDE.md` exists under the repository.
- GitHub reuse add-on status: REUSED LOCALLY. The existing pinned Three.js `0.185.1` MIT vendor, its same-revision loaders/utilities, and the already-integrated meshoptimizer derivative workflow cover the authorized work. No new dependency was selected or downloaded.

## Verified live baseline

- B8 source lineage and docs HEAD are present. Reported maxima remain 2000 HP / 200 combat MAHGIC / 200 flight MAHGIC and must not be doubled again.
- C: free space at takeover: 49,021,251,584 bytes. Historical low-disk figures are stale.
- The original tree, Dogkie, dumbbell, barbell, fish, horse, and Phoenix GLBs all exist at the registry paths and match their recorded SHA-256 hashes.
- The tree fidelity defect was reproduced in neutral and gameplay views: the legacy 14,000-triangle L0 had torn silhouettes, black voids, collapsed hanging forms, UV discontinuities, and faceted trunk/crown surfaces caused by the old global quadric-collapse + representative-UV + recomputed-normal path.
- Tree L0/L1 are now source-normal/source-UV-preserving meshoptimizer derivatives: 14,792 / 7,392 triangles, SHA-256 `0bfb4e…5608b` / `2bbecd…723d8`. The immutable 1,849,522-triangle source remains unchanged and re-verified at `d2a7db…adbde0`.
- Dogkie raw and runtime L0 remain geometry-identical (18,627 triangles); matched neutral and gameplay day/night views confirm its derivative is not the source of the earlier tree defect.
- Imported Gold Temple source 1,866,763 triangles -> runtime 70,000. The runtime building is captured in day/night, but the original ornate-temple GLB is not present under the bounded Downloads search, so exact raw-vs-runtime temple parity is not claimed.

## Milestone and requirement reconciliation

M0 is complete for orientation/ownership. M1 source-faithful rendering foundation is complete for the owner-independent assets that are locally available; the missing raw ornate-temple source remains an explicit provenance gap. M2 gameplay motion, biomechanics, creature motion, true equipment grip, special ownership, and projectile lifecycle are locally complete and rendered/tested. M3 is complete locally: the real playable world has one continuous night-first quality route meeting the required floor/road/forest/pond/mountain/building/sky standard. M4 is complete locally: that quality/truth standard now spans the authorized mainland and all five sanctuary contracts without replacing the M3 route. M5 is complete through the authorized protected candidate: the ordinary-runtime route is 20/20, the final 900-second throttled emulated-phone soak completed without context loss/errors/rejections/early termination, the package is fully verified, and the unpublished gated Netlify draft is ready. Physical-phone acceptance remains owner-only.

| Requirement group | Current state | Evidence / next check |
|---|---|---|
| CONTINUITY-01..12 | PASS_CURRENT_WORKTREE | Same repository and queue preserved; takeover recorded; no device/deploy claim. |
| FOUNDATION-01..04 | PASS_CURRENT_BUILD | Temple causeway and forecourts re-probed at 24-bit depth: phone-emulated causeway/gym 0% hard flips; desktop temple causeway max 0.002%. Ground stack remains 0.012 < 0.03 < 0.045 with polygon offsets. |
| FOUNDATION-10..14 | PASS_LOCAL / DEVICE_OPEN | READY/lifecycle, static build, private-content scan, desktop evidence, and phone-sized emulation are current. Physical-phone acceptance remains owner-only. |
| ASSET_MATERIAL-01..12 | PASS_AVAILABLE_SOURCES | Tree, Dogkie, fish, horse, Phoenix, barbell, and dumbbell have raw/neutral/runtime checks and live day/night evidence. Temple gameplay is captured, but raw temple parity is not claimed. |
| CREATURES-01..22 | PASS_CURRENT_BUILD | Fish/horse/Phoenix L1/L2 tearing was reproduced and repaired. Live root/shader motion, materials, LOD switching, and exact tracked host IDs render cleanly. |
| QUALITY_RELEASE-04..11 | PASS_PROTECTED_M5C / DEVICE_OPEN | M5C package/extraction/deploy/protection gates are green; production and the prior M5 draft are unchanged. Physical-phone and authenticated-hosted gameplay acceptance remain open. |

## M5 integration details

- The authoritative candidate is `25_HANDOFF/CONVERGENCE/astra_m5/candidate_route_release_final_green/m5_candidate_route.json`: **20 passed / 0 failed**, 65 total route records, 54 exact required waypoints, all five sanctuaries reached within `1.2 m`, 35 screenshots, and `563,182 ms` total duration.
- It proves startup/restore gating, persistent minimal HUD, every menu body, FUSED/SPLIT transitions and exercises, dumbbell/barbell grips and projectile lifecycles, flight brake/hover/landing, manual camera persistence and obstruction handling, room transactions/teardown, resource recovery, ecology, DAY/NIGHT switching, and clean final state.
- Civic training repeat entry rebuilt the same bounded room topology and disposed every room-owned resource. Gold Temple, clothing shop, and Blue Tower also completed prompt/confirm entry and exit, with every exterior return placed `0.55 m` outward from its door.
- DAY and NIGHT switch measurements were p95/max `19.2/261.7 ms` and `34.8/43.4 ms`; shader programs remained `168→168`. The candidate ended FIELD/FUSED/grounded at NIGHT with FULL HUD and no transient gameplay residue.
- The route has 0 unexpected runtime errors, 0 page errors, and 0 console errors. Thirteen raw favicon/older-avatar-manifest 404 fallback lines are expected and explicitly filtered.
- `25_HANDOFF/CONVERGENCE/astra_m5/soak_phone_emulated_final/stability.json` records the final 900-second run at 4× CPU throttle, MED, `393×852`, 828 samples, READY completion, and no context loss/restore, authority errors/rejections, page errors, console errors, fatal condition, or early termination. Four benign Direct3D shader warnings remain recorded.
- The final soak's p50/p95/p99/max was `66.6/100/149.4/5179 ms`; the maximum is the startup gap. This is functional stress evidence only—not a 30 fps claim, physical-phone acceptance, physical GPU/memory/thermal proof, or a zero-leak assertion.

## M5 protected private candidate

- `ASTRA_M5_PRIVATE_20260923`: 175-entry, 65,008,929-byte archive; SHA-256 `ebcad0ed993899f66c7477a1b3f1cde57740b32047fd0c02b60cf71e85055995`. The package pipeline passed 51 test programs / 1,461 checks, all scan/syntax/integrity stages, and both built-in and second-extraction smoke tests `7/7`.
- Protected draft deploy `6ab3abd739fc958c75339988` is `ready`, context `deploy-preview`, `published_at = null`, with the edge gate present: `https://6ab3abd739fc958c75339988--mahworld-test-preview.netlify.app`.
- Unauthenticated proof is root `303 no-store`, play/password form `401 text/html no-store`, and representative JS / `BUILD_INFO.json` / GLB `401 application/json DEMO_LOGIN_REQUIRED no-store`. `MAHDEMO_PW` is unavailable, so an authorized hosted browser load is not claimed.
- Published production remained deploy `6aac25101f17ce5ed37166e4` at `2026-09-17T17:36:28.139Z` before and after. No production promotion, live replacement, alias, or gate removal occurred.
- Desktop and phone-sized targeting are each `25/25`; the mobile proof uses no direct-lock fallback. Durable metadata is `25_HANDOFF/CONVERGENCE/astra_m5/private_candidate_release.json`.

## M4 completion details

- The field remains one `121,975 m²` playable mainland. Five sanctuary contracts each resolve a real arrival, landmark/heart, fish-bearing water, bounded Dogkie territory, two horses, Phoenix anchor, resources, verticality, and NIGHT identity. Bage now has its ordered gardens and eight-record pack population.
- The 31-path road/trail system is one connected graph with three explicit collision-backed bridge links and six bridge-landing wayfinders. `QR_MOONLIT_TITAN_CIRCUIT` remains the unchanged default; `QR_FIVE_SANCTUARY_NIGHT_CIRCUIT` is a separate review circuit.
- Dogkie authority is a 54-record dormant pool, limited to 24 awake globally and 12 per sanctuary; combat aggression remains separately capped. Placement now resolves 121 trees and 160 path/plaza/bridge fixtures. Generated host authority is synchronized at 219 M4 world shapes / 1,494 total mapping shapes.
- Four previously false civic doors now execute real FIELD → bounded civic room → FIELD transactions. There are 14 registered buildings and 10 ENTER doors; the Exchange remains information-only with no currency/purchase schema.
- Region tinting is single-owner field tessellation rather than coplanar overlapping sheets. Map bounds come from the real walls. Low/mid gray-blue cloud bodies now occlude; high cirrus remains non-occluding. `NORTH_FALL` is explicitly scenic-only/unreachable.
- Registry SHA-256: `2b3a3f2f34b1c9d1ab59be3fc0726f5a1bec69e0ce577fd228ab03951c24ba6c`. Evidence is under `25_HANDOFF/JOB_B/evidence/astra_m4/after/`: 30 real-runtime captures plus reports, world READY, normal no-debug phone-sized navigation reached in one pass, 0 page errors, 0 console errors.
- Focused regression passes: M4 `12/12`, M3 `12/12`, interiors `24/24`, world JOB B `34/34`, creature combat `22/22`, rules `60/60`. Desktop-relative p50/p95 remained healthy: plaza `16.4/22.8 ms`, canal `20.5/32.0 ms`, forest `16.8/24.6 ms`. Static build is 174 files / 93,524,282 bytes; private-content scan clean.
- All evidence remains desktop/local or phone-sized emulation. Physical-phone acceptance remains pending Jah's actual device test.

## M3 completion details

- `QR_MOONLIT_TITAN_CIRCUIT` is the actual-world quality route: civic forecourt → Tower Causeway → `FOREST_NE` / `RB_NE_PATH` plus an attackable blue resource patch → fish-populated `TITAN_LAKE` → the raised `TITAN_MOON_PASS` → the named `MAH MATCH` door and `MATCH_HALL`. Its route metadata records all seven required standards: `FLOOR`, `ROAD`, `FOREST_RESOURCE`, `POND`, `MOUNTAIN`, `BUILDING`, and `SKY`.
- Moon Pass is real client/host ground: a `2.8 m` walkable top, monotonic south/north ramps, side lips, and four collision-backed Titan mountain monoliths. The route road is continuous and its body-width center corridor is clear. The generated collider registry is synchronized at 223 world shapes / 1,498 mapping shapes against the exact world-registry hash.
- First visits and route previews default to `NIGHT`; explicit URL and saved player preference still win. The READY gate prewarms both `DAY` and `NIGHT` without persisting or presenting the transient switch and restores the selected initial state. The ordinary world map draws the route on the existing road network.
- Evidence: `25_HANDOFF/JOB_B/evidence/astra_m3/m3_night_route_report.json` and eleven rendered PNGs. Acceptance is **11 passed / 0 failed**: fresh NIGHT state, both prewarm states, full world modules, forest/resource node, pond/fish, actual controller traversal over the pass, MAH MATCH approach and hall enter/exit, bounded DAY/NIGHT program count, and zero unexpected runtime/console errors.
- Final local regression is **49 / 49 test programs**, including core `125/125`, M2 motion `9/9`, B8 equipment `25/25`, targeting/Phoenix `12/12`, M3 route `12/12`, creature combat `22/22`, and world JOB B `34/34`. Static-host smoke is `7/7`; collider check and JavaScript syntax checks pass. The lab host's read-only Three.js mount now uses the preserved in-project vendor directory, removing the stale transfer-machine path. Existing local static output remains 174 files / 89.1 MB, 24 derivatives / 41.5 MB, private-content scan clean.
- This is desktop/local and phone-sized emulation evidence. Physical-phone acceptance is still owner-only. No commit, package, deployment, or publish was made.

## M2 completion details

- Split-leg idle/run/flight preserves separated transforms and measured hip→knee / knee→tip segment lengths with zero scale drift in the rendered acceptance samples. The existing one-authority flight brake remains intact.
- Athlete RDL, chest fly, cross-cable/Katana, overhead press, and the new barbell press/throw use explicit biomechanics rather than a generic donor cast. The press moves from rack through vertical extension and releases the same held barbell.
- Dumbbells are resolved at their owning palm sockets. The barbell is solved across both palm sockets, in front of the thighs at rest, with plates outside the body. Rendered palm/midpoint errors are zero at the sampled frames and the measured hand span is approximately `0.684 m`.
- Equipment identity is preserved through specials with `cast_id`, `projectile_id`, and exact `equipment_ids`. The lifecycle is `HELD`/`HOVERING` → `IN_SPECIAL` → `PROJECTILE` when applicable → `RETURNING` → the prior state. X-Wave uses the actual dumbbells, Plate Shot retains its owning bar, and the press throw releases the actual barbell. Dismiss is refused during an active special; once-only swept-hit, wall/miss, clamp-escape, and refund semantics are tested.
- Dogkie is a fused-limb surface glider with no paw or step cycle and attack-only lift. Phoenix maintains at least `10 m` terrain-relative clearance and uses bounded `WINDUP → DIVE → AERIAL_PASS → CLIMB`; the obsolete ground `SWEEP` path is gone.
- Evidence: `25_HANDOFF/JOB_A/evidence/astra_m2/m2_motion_report.json` and ten rendered PNGs. Acceptance is **10 passed / 0 failed**. Focused suites pass `25/25`, `9/9`, and `12/12`. Static build and private-content scan remain clean. This is desktop/local and phone-sized emulation evidence, not physical-phone acceptance.

## M1 completion details

- Barbell and dumbbell legacy runtime geometry visibly smeared source detail. They now use attribute-preserving meshoptimizer derivatives: barbell L0/L1 `40,005 / 11,995` tris, SHA-256 `d4a5b2…8a26 / 3de2a4…2e97`; dumbbell L0/L1 `39,992 / 12,000` tris, SHA-256 `e40a44…6f33 / 7bfc85…2fee`. Their base/normal textures remain capped at 1024².
- The west/east dumbbells were buried in the rendered bench cap. Registry placement is now `y=0.62`, matching the 0.50 m bench plus 0.12 m cap without changing the host collider.
- Fish/horse/Phoenix source and L0 were faithful, but the legacy skinned L1/L2 geometry tore into long shards. Replacements preserve normals, UVs, joints, weights, and topological borders at error 0.01: fish L1/L2 `6,498 / 5,150` tris (`c48e6a…ae83 / c3a954…6132`); horse L1/L2 share the safe 8,794-triangle fidelity plateau (`399f73…3411`); Phoenix L1/L2 share the safe 8,234-triangle plateau (`7e4840…115`). Lower requested ratios crossed the measured fidelity ceiling and were not promoted.
- Fish retains its exported inverse-bind relationship. Runtime no longer rebases or heuristically poses that partially weighted skeleton; host root motion plus the existing vertex-shader undulation remains authoritative.
- The evidence camera now requires the exact requested wildlife ID. It no longer silently substitutes a different fish when recording the summoned rise/breach.

## Next action

M5 owner corrections are delivered as protected build `ASTRA_M5C_PRIVATE_20260923`. Preserve the M1–M5 worktree, package, evidence, production deploy, and prior M5 comparison draft. Jah should open `https://6ab454bf3a60c4bf88b1334a--mahworld-test-preview.netlify.app` on the real phone and report acceptance. Do not promote this candidate to production without a separate explicit instruction. Physical-phone acceptance, authenticated hosted automation, exhaustive every-metre road coverage, an owner-approved band move set, and raw ornate-temple source parity remain open.

## M1 checkpoint evidence and tests

- Evidence root: `25_HANDOFF/JOB_B/evidence/astra_m1/` (neutral source/derivative views, rejected candidates, selected comparisons, final desktop/mobile gameplay day/night, final floor reports). Remaining-asset live captures are in `final_gameplay_remaining_desktop/` and `final_gameplay_remaining_mobile/`; each has 12 PNGs plus its exact-ID JSON report.
- Packaged static demo: 174 files, 89.1 MB; 24 derivatives / 41.5 MB; private-content scan clean. Six raw source hashes, 18 authoritative derivative hashes/byte sizes, and 15 shipped derivative hashes match. The three absent package files are the intentionally unshipped legacy tree/barbell/dumbbell L2s.
- Dense west-forest matched camera: legacy desktop 842,293 rendered tris / 16.268 ms mean; selected repair 885,965 tris / 17.697 ms mean. Final phone-sized emulation: 602,008 tris / 13.108 ms mean / 13.7 ms p95. A final no-geometry-change repeat on the loaded machine measured desktop 885,965 tris / 24.571 ms mean / 47.8 ms p95 and phone-sized 602,008 tris / 15.064 ms mean / 22.7 ms p95; both reports have zero runtime errors. These are local desktop/emulation measurements, not physical-phone acceptance.
- Floor final: desktop causeway 0.002% max, plaza edge 0.098%, gym forecourt 0.032%; phone-sized causeway 0%, plaza edge 0.07%, gym forecourt 0%.
- Final verification: five modified scripts pass `node --check`; seven manifests parse; `git diff --check` passes; gameplay core 125 passed, 0 failed.
- No package, draft deploy, or live publish was produced.

Usage/billing balance: UNKNOWN. No local tool exposes a spend ceiling; work proceeds by bounded milestones with testing/save reserve.
