# OWNER HOTFIX INTERJECTION 2026-09-19 — REPORT (§8 form) · character intactness hard gate + the generic ATTACK button deleted · gameplay / demo lane

CURRENT JOB PRESERVED:
  The running job (owner assignment 2026-09-18 lanes A–H with the ULTRA MASTER INTERJECTION folded in at C6) was not restarted, forked or rolled back. The hotfix was ingested at the checkpoint after phase G (catalog status) while phase D / E / F page probes were running; the queue order became: finish D–G code → apply the hotfix (A intactness gate, B delete the ATTACK button) → the full regression chain → the C7 candidate. The owner's full-job completion directive (received during the hotfix) is applied: this report and `CONSOLIDATED_PHASE_REPORT_6.md` are one candidate, not a stop.

INTACTNESS (§1–§4 — diagnosis, cause, repair, before / after; every number from a tool, no cause called from a screenshot):
  What the eye saw (owner practice-battle screenshots; reproduced on V10 = dev_0.15 in `intact_V10_before/`): at raised or crossed arms — the PUSH load, the ROTATION release, the double-biceps emote, the two new Specials — the deltoid / armpit broke into long thin white shards, the arm root looked torn from the pec and, at the rotation release, the background showed THROUGH the back under the raised arm.
  Diagnostic modes on FROZEN runtime poses (`deploy/probe_intactness.mjs`: 18 poses × A production + VFX / B VFX off / C neutral opaque / E wireframe × front / side / rear-¾ / rear + 1.9 m shoulder close-ups; `deploy/probe_mesh_stretch.mjs`: the mesh re-skinned on the CPU at the frozen pose — per-edge posed / bind length, overlap-pair separation; `deploy/mesh_components.mjs`: connected components; `deploy/mesh_seams.mjs`: coincident vertices across patches and their weights). Wireframe and neutral show the same defects as production → NOT material, NOT fade, NOT VFX occlusion, NOT culling of a healthy surface. The material is single-sided, so a see-through = the surface really is absent or turned.
  MEASURED CAUSES (two, both in the runtime derivative; the Tripo → AM08 → auto-rig chain, no Blender involved):
    1. TOPOLOGY — the "closed retained" body is a QUILT: the upper primitive is 1 485 connected patches (14 683 vertices), 3 940 positions where 2+ vertices of different patches coincide (unwelded seams), and 14 590 vertex pairs of different patches within 4 mm of each other (overlapping borders); the fused lower primitive is 223 patches. It is one surface to the eye only while every copy of a shared position moves identically.
    2. SKIN WEIGHTING — the auto-rig's geodesic distance weights drop UPPERARM 0.95 → 0.55 across ONE 8–11 mm edge at the deltoid / armpit boundary (and SCAP 0.34 ↔ SPINE2 0.40 at the lat), and assign SCAP_L to vertices just right of the spine and SCAP_R just left of it. At a raised or crossed arm those edges stretch 2.5–8.6× their bind length (rotation release: 359 edges > 2×, overhead 109, chest fly 115, double biceps 160, push 9) = the shards; where two overlapping copies carry different weights they separate into a gap (double biceps: overlap pairs opening up to 8 cm at the mid-back and the scapula) = the see-through.
    Checked and excluded: pose-writer conflict (one composition per frame; the Tripo cast donor was found blending over the two new Specials and is now gated off them — a motion defect, reported under ATTACKS), joint limits, twist carriers, bind transforms (no zero-influence vertex, no hard 0 / 1 boundary), VFX geometry (B rows identical).
  REPAIR (weights only, through the authorized derivative workflow `lab/assets/rig_shoulder_smooth.mjs`; positions, normals, UVs, indices, morphs, joints, inverse binds, materials, textures, the fused lower primitive and the split legs are byte-identical; no Blender, no other process touched) — three passes, each measured:
    · SEAM-WELDED gradient smoothing: coincident vertices are ONE node of the smoothing graph; Laplacian smoothing of the weight vectors inside a sphere around each glenohumeral centre (13 → 21 cm in metres) and a second sphere at the lower armpit / lat, 10 Jacobi iterations at λ 0.5, top-4 renormalised, every copy of a seam vertex written identically (4 694 seam groups). The first cut smoothed per patch and CRACKED the deltoid into plates (680 seam groups with differing weights) — caught by the same gate (`mesh_seams.mjs`), rebuilt; V9 / V10 / final V11 all read 0 differing seam groups.
    · OVERLAP WELD (whole upper body): vertex pairs of different patches within 4 mm at bind that face the same way (normal dot ≥ 0.5) are unified onto one mean weight vector (1 559 pairs, 233 groups, 1 131 vertices); pairs facing each other (an arm's inner skin against the torso, 710) are left alone — those separate legitimately.
    · MIDLINE SYMMETRY (the back): inside a 5 cm band around the spine the SCAP_L / SCAP_R (and CLAV) shares are made equal (262 vertices), fading out over the band — at the midline both blades pull equally.
    Result (`probe_mesh_stretch` on the same frozen poses, V11 = dev_0.16, sha256 a85d16c4…): edges > 2× — rotation 359 → 4 (max 2.3×), overhead 109 → 94 at the NEW 135° load (max 3.4×, never reachable on V10), chest fly 115 → 2, double biceps 160 → 39, push 9 → 28 (ARMTWIST carrier edges ≤ 2.7×); overlap pairs open > 12 mm: push 10 → 0, rotation 8 → 0, overhead 11 → 0, chest fly 5 → 0, double biceps 30 → 0 (max separation ≤ 2 mm everywhere); steepest weight change 55.8 → 36.8 per metre, UPPERARM jumps > 0.3: 70 → 0. Shipped as ATHLETE_M_V11 (dev_0.16, default; V10 / V9 selectable for rollback).
  WHAT THE EYE SEES NOW (V11 close-ups, `intact/`): the deltoid is one continuous surface at the push load (front / side, production and neutral), the rotation release no longer shows the background through the back, the chest fly and overhead loads keep the arm root closed from the side.
  RESIDUAL (honest, not hidden): at the DOUBLE-BICEPS emote (humerus ~92° abducted with the scapular rhythm at 0.53 rad) a hole still opens at the RIGHT rear shoulder / lat in the rear-¾ view (`emote_double_biceps_C_close_rear34.png` — the background shows). The overlap-pair separation there is 0, so it is not two copies pulling apart: a patch of the quilt is exposed whose covering patch (the rear deltoid) moves with the arm — a covered hole in the source topology. This is the same class as the hip (phase C) and is NOT repairable by weights: it goes to the Character / Blender lane as a bounded native repair (`SHOULDER_GIRDLE_NATIVE_REPAIR_CONTRACT.md`: weld the shoulder-girdle patches into one closed surface at the rear deltoid / lat / scapula, no redesign). Until then the emote is listed as NOT intact at rear-¾; every other pose of the matrix is judged per sheet in CONSOLIDATED_PHASE_REPORT_6.md (INTACTNESS TABLE). The owner's screenshots' practice-battle poses (push load / rotation release) are the ones repaired.
  NOT covered by anything: no bloom, no dense MAHGIC, no farther camera, no slower attack, no cover plate, no fake shell, no double-sided material, nothing hidden — VFX-off and wireframe rows are in every sheet.
  Two more real defects found by the same gate and repaired at their source:
    · the two owner-authorized Specials never played their authored exercise: `RigAnimator` forced the generic CAST support pose for every SPECIAL_MAGIC cast AND blended the Tripo cast-a-spell donor clip (a one-handed throw) over it — the OVERHEAD and CHEST FLY probes measured one hand ahead and one behind the chest. Now the OVERHEAD / CHESTFLY patterns resolve by skill id, the donor stages only the generic cast, and the poses were re-authored from measured bone → hand positions (shoulder_sign_probe): chest fly load = arms HORIZONTAL at shoulder height (hands 1.25 m apart), clap 0.35 m ahead at shoulder height; overhead load = the humerus at ~135° through a NAMED, attack-scoped shoulder allowance (`rig_profile.pose_overrides.overhead_release`, UPPERARM_x to −2.6 rad, both arms, the OVERHEAD frames only — the general 100° table is unchanged), elbows bent behind the head (hands 8 cm above the head bone), release to ~80° with the wrists whipped;
    · the phase-D flight boost (`run` in powered flight = 1.3× cruise) had shifted the old F01 host test's route onto a roof; the test route was made robust (no criterion changed).
  Physical-phone artistic acceptance remains Jah's.

ATTACK BUTTON (§5–§6 — exactly what was removed and how the slots dispatch):
  REMOVED: `#tc-attack` (`data-tc="attack"`, "ATTACK hold + swipe" / "CAST hold + swipe" caption), its CSS (island position, dock rollback position, halo, `#g-cell-attack`), the touch layer's `attack` modifier and its GESTURE world role, the page's `gesture` / `attackTap` callbacks, the old armed-move gesture dispatcher `performGesture`, and the HOLD-a-slot-to-arm two-step (`st.armLong`). Nothing replaced it: row C's two left cells are EMPTY (the probe's point there hits the world / camera-drag layer), the small category label sits under the quick-slot arc (tap = next category).
  SLOTS 1–4 ARE THE ATTACK CONTROLS: each `.gq` shows its move identity (authored short form), its category colour and its cooldown / unavailable state; TAP = `directAttack(slot, 'tap')`; a SWIPE from the slot (≥ 24 px, tracked per pointer in `gameHud`) = `directAttack(slot, 'swipe', dir)` checked against THAT move's arrow (a wrong direction is refused as WRONG SWIPE with the expected arrow, no cast). ONE dispatcher for every path — slot tap, slot swipe, E / R / T / Y, ⚔ COMBAT ARM + slot — `directAttack` → `rulesHud.castSlot` → RULES_SELECT + RULES_CAST on the in-page authority; the probe counts exactly one cast start per tap and zero dispatch while a menu is open. Desktop keeps V · E R T Y · M · B and the bottom bar.
  Reachable: GUARD (row D), DASH (corner, hold + swipe kept), FLY (row C, hold = ascend, release = hold altitude), ≡ (lower-right), ⚔ (row A) — measured faces 42 px / 36 px with 4 px hit halos, no overlaps (probe B2–B4).

REGRESSION (C7 build, headless Chrome, touch EMULATED): attacks E 9 / 9 · motion D 14 / 14 · stats F portrait 4 / 4 + desktop 4 / 4 · HUD portrait 17 / 17 · landscape 17 / 17 · desktop 5 / 5 · combat after menus / rotation / focus loss / flight / landing / split-fuse / Training Room 12 / 12 · perf separated: no stalls, no stepping in five runs (two threshold crossings on a loaded machine, see CONSOLIDATED_PHASE_REPORT_6.md §8) · node suite 42 files 1 319 / 0 · package pipeline 0–7e PASS · smoke 7 / 7 from the extraction.

BLOCKERS: none for the hotfix itself. Still owner-side: the four evidence files and the Fitness Class Archetypes stat source are not on this machine; iPhone acceptance is Jah's; the lower-body native repair (phase C contract) waits for the Blender slot.

CANDIDATE: ONE review URL — DRAFT https://6aaeb66e5b7bbc1b49221b84--mahworld-test-preview.netlify.app · build label BUILD_INFO commit 4a17efd (≡ → SETTINGS about / dev panel) · rig ATHLETE_M_V11 dev_0.16 · package CONSOLIDATED_C7.zip 31 342 949 bytes, SHA-256 18172f155b98e6ccd5aebbdda1b27d7353d317b97fc3397ef64aef22eb97cb33 · gate: play / BUILD_INFO / dustFx.js / the rig 401 unauthenticated (PASS), correct-password login NOT RUN · live untouched. Rollbacks on the same build: `?avatar=ATHLETE_M_V10`, `?hud=dock`, `?cam_follow=1`.
