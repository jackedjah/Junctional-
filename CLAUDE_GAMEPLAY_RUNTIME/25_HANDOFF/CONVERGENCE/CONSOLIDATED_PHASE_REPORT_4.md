CONSOLIDATED PHASE REPORT 4 — owner assignment 2026-09-18 "CONNECTED MOTION, GRANULAR MAHGIC, TRAINING ROOM AND TAP-TO-MOVE", §16 REPORT form · 2026-09-19 03:20 UTC · gameplay / demo lane · checkpoint after phases A → C (B = camera + tap-to-move, C = connected lower body + flight posture + hand glows). Lanes D → H continue after this checkpoint.

CURRENT WORK PRESERVED:
  Worktree C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS (git master), linear: cbc99b7 (C2 review code) → 7cd2855 (report 3) → 28853d6 (phase B) → fcf553a (phase C) → this report (docs). Nothing reset, no second demo, C1 / C2 / C3 ZIPs + drafts untouched, interiors and the C2 rig repair kept. Source / build / assets: 26_LOCAL_AUTHORITY/lab + play + 00_CORE; static root 109 files (42.5 MB); athlete default dev_0.15 (ATHLETE_M_V10, sha256 503272b0d0cb82f8d9b83668339787135471f0e5a3c616a851eed8aa93380fbe) with dev_0.14 (V9) shipped for rollback; NPC roster v4 unchanged; three.js 0.185.1 vendored; protected sources unchanged (RAW_10_MODELS read-only; Blender slot still RESERVED — not launched). No purchase, install, subscription, model change or credential action.

OWNER OVERRIDES APPLIED (superseded checks and their replacements are tabulated in ENHANCEMENT_RECONCILIATION_0918.md §A.2):
  camera heading independence replaces the 2026-09-17 rear-follow return (old spring = ?cam_follow=1 rollback only; probe_camera_pass2 F-rows and K1–K3 "FOLLOW stays FOLLOW" are superseded by probe_nav N1–N4 / K-rows now read as "the selected mode survives"); "tap on empty ground = nothing" replaced by ONE host-validated destination; "G37 ≤ 2 cm = gait acceptance" retained as a metric only; gym destination re-labelled TRAINING ROOM (lane E, pending); casting-anywhere, granular MAHGIC, HUD translucency, plaza / botany moves are recorded as lanes D–G (not yet built — nothing claimed).

CONFIRMED CAUSES (hypotheses kept separate):
  · "Separate shorts": CONFIRMED by inspection of the dev_0.14 GLB (deploy/hipseam_inspect.mjs → lower_body/hipseam_dev_0.14.json) — the pelvis shell between the split line (y 0.3938 bone units) and the femoral head (y 0.4513) carried 0 THIGH influence (hem: PELVIS 0.464 / TAIL1 0.408 / LUMBAR 0.126) and the split legs carried 0 PELVIS influence (top: THIGH 1.0); the shell's hem ring coincides with the legs' hip dome (rest gap median 0.9 mm), so the thigh swung 5.75 cm (10.5 cm real) below its own pivot under a rigid block. Cause = weight distribution + open-shell geometry. NOT transform ownership (both meshes share skin 0) and NOT shading alone. Hypothesis not needed: pose-dependent corrective shapes (the weight repair alone reached ≤ 2 cm).
  · "Yellow balls at the hands": CONFIRMED in-page (probe_out/hands) — the two hand-energy Sprites (class-gold soft glows, opacity 0 outside a magic cast) were forced to opacity 1 / colour white every frame by the ghost / KO material loop that traverses the figure; they were sprites, not crystals, geometry, helpers or particles.
  · Legs dangling in flight: CONFIRMED in RigAnimator — the flight layer and the airborne tuck both held THIGH +0.16 / SHIN +0.14–0.20 regardless of speed.
  · Hypotheses (not verified, not acted on): none open for §5 / §6 beyond Jah's artistic verdict on the crease.

CHANGES (real files, purpose):
  phase B (28853d6): play/Navigation.js NEW (bounded A* + string pull over the field colliders, straight-line fast path, bounded snap, mode-aware support / ceiling checks); play/PlayMode.js (NAV intent, navStep through the ordinary moveVec / band / accel / sliding, takeover rules, arrival settle, one re-plan then stop, nav / nav_end in the snapshot, PLACE dev fixture); Protocol.js (NAV whitelisted; dest_x / dest_z / dest_y intents, forbidden position fields kept); 00_CORE/dev_tuning.dev.json play.navigation (range_m 30 PROVISIONAL, centralized); lab/play.js (camera mode 'free' default, spawn rear view once, RESET one reframe, steering frame = live camera frame, occlusion-correct world pick, tap arbitration, refusal toasts, destination mark, Escape cancels, map tap, dev hooks); lab/PresentationAdapter.js NAV_MARKER; lab/gameMenu.js map range ring + tap → NAV; lab/touchControls.js doc; COMMAND_AUTHORITY_CONTRACT.md NAV section; deploy/probe_nav.mjs NEW; 16_TESTS/gameplay_navigation.test.mjs NEW.
  phase C (fcf553a): lab/assets/rig_hip_connect.mjs NEW (versioned derivative pass) → lab/assets/athlete_m_preview/dev_0.15/{Mah_Athlete_M_am08_v7.glb, manifest.json}; lab/GlbCharacter.js (split skin set bound with the form through a per-figure geometry container); lab/RigAnimator.js (THIGH at rest while the legs are hidden; split-flight leg trail + knee shape; tuck fades with speed); lab/PresentationAdapter.js (ghost loop scoped to body materials); lab/play.js + deploy/build_static_demo.mjs (V10 default, both versions shipped); deploy/hipseam_inspect.mjs NEW, deploy/probe_hip_connect.mjs NEW; 16_TESTS/gameplay_hip_connect.test.mjs NEW; gameplay_legs_faithful updated.

RESOURCE USE (input → operation → output → runtime proof):
  dev_0.14 GLB (own derivative, sha 7d217d0f…) → node rig_hip_connect.mjs (weights only) → dev_0.15 GLB (sha 503272b0…) + manifest lineage → GlbCharacter binds the split set with the form → probe_hip_connect 7/7, gameplay_hip_connect 12/12 (RUNTIME_INTEGRATED). three.js 0.185.1 (vendored) → Raycaster / SkinnedMesh.applyBoneTransform / BufferGeometry container → runtime. No mocap / Tripo / Reallusion / Blender / Quarks use (Quarks is not vendored; the custom sprite / points system is what lane D will extend). Headless Chrome (GPU) → probes. netlify-cli → draft only.

NAVIGATION (range, modes, gesture arbitration, cancellation):
  range 30 m from the PLAYER (play.navigation.range_m, PROVISIONAL inside the owner's 20–35 m band, adjustable; NOT a measured reference-game value), OUT_OF_RANGE beyond; modes WALK / LEVITATE (support-following, step ≤ 0.6 bone units, no deck climb) and AIR (horizontal at the held altitude, corridor checked against solids / ceilings; the marker shows the aerial waypoint with a plumb line); pick = nearest visible world mesh (actors, markers, effects, sky excluded), a facade / roof hit is TARGET_BLOCKED ELEVATED, sky = NO_PICK. Arbitration: no move on pointer-down; a gesture past the drag threshold is LOOK and its release never moves; one tap = one destination; actors → lock / talk, doors → their prompt, UI → itself; a tap while the stick / keys are held is refused DIRECT_INPUT_ACTIVE. Cancellation: stick / keys with a vector, DASH, ENTER / ASCEND / DESCEND / EXIT flight, TRANSFORM, EMOTE, ATTACK / RULES_CAST, ENTER_ROOM, TRANSIT, room change, Escape; a zero-input MOVE and a camera drag preserve the path; arrival decelerates into the point; one re-plan when stuck then a clear stop (BLOCKED). Evidence: probe_nav 21/21 (real CDP mouse + emulated touch: N5 click walked / arrived / marker removed; N6 down / drag never move; N7 / N8 / N8b refusals; N9 takeover; N10 drag keeps the path; N11 flight waypoint + ASCEND cancels; N12 map tap; N13 room + room change cancels; M1–M4 portrait / landscape incl. two-finger steering); gameplay_navigation 28/28.

LOWER BODY (shape preserved, deforming connection, remaining faults):
  Shape preserved: split-leg positions / indices byte-identical; the leg band 10–16 cm below the hem skins to identical positions (0 mm). Deforming connection: worst hem opening 1.24 cm through idle → start → walk → turn → reverse → stop → run → brake (V9 19.97 cm), 1.5 mm through flight lean / brake / landing (V9 6.4 cm), leg pushing into the shell ≤ 1.2 mm; the fused form is byte-identical in weights and skins to identical positions before SEPARATE and after FUSE (0 mm). Remaining faults (honest): the crotch / inner-thigh top stays with the pelvis by design (graded over ~11 cm) — artistic verdict for Jah on sheet_hip_closeup_ab.png; a sharper glute crease would need pose-dependent correctives (available, not authored because the numeric goal was met); the split flight posture is authored (leg trail 0.42 rad × speed × inclination, knee 0.34) — appearance verdict pending.

ATTACK MATRIX: NOT STARTED in this checkpoint (lane F). Inventory confirmed for planning: Athlete has PA_PUSH / PA_PULL / PA_HINGE / PA_ROTATION (PHYSICAL, BODY_STRIKE), PM_* (PHYSICAL_MAHGIC, BODY_STRIKE), ATHLETE_VECTOR / TRACK (SPECIAL_MAHGIC projectiles, pattern UNASSIGNED = Jah's decision), ATHLETE_SET (self buff); loadout capacity 4 / 2 / 2 with four direct shortcuts; the `?` tier cells stay owner decisions (PASS_7_COMBAT_DECISIONS_REQUEST.md).
TRAINING ROOM: NOT STARTED (lane E). Design recorded: reuse GYM_DOOR / GYM_INTERIOR with a TRAINING ROOM label, a per-room rules field instance with an interior collider layout so the practice dummy runs on the real eligible-target pipeline (the rules field is FIELD-only today).
WORLD + HUD: NOT STARTED (lane G). Measured baseline for the HUD: panels use `--hud: rgba(6,9,16,0.66)` + backdrop blur (the blur is the cost to remove first).

EVIDENCE:
  inspected:            dev_0.14 GLB skin data (seam inspector), RigAnimator layers, PresentationAdapter material loops, GLTFLoader attribute naming, three.js skinning API.
  rendered / measured:  probe_nav 21/21 · probe_hip_connect 7/7 (two Chrome sessions, same route) · probe_gait_sequence 7/7 on V10 · hands diagnostic (sprites opacity 0 / gold after the fix) · suite 36 files / 1 236 checks / 0 failures · package stages 1–7e PASS.
  emulated device:      iPhone-portrait 390×844 @ DPR 2 + landscape 844×390, CDP touch (M1–M4). Never a physical phone.
  physical phone:       NOT tested by agent.
  Jah's verdict:        PENDING on all of it (nav feel, camera heading, hip close-up sheet, flight posture). Nothing here is a visual PASS from counts.

PACKAGE:
  C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS\CLAUDE_GAMEPLAY_RUNTIME\26_LOCAL_AUTHORITY\deploy\packages\CONSOLIDATED_C4.zip · 28 956 501 bytes · 110 entries · SHA-256 91a3cf12bbd96aa7df9bf67074b606a1acd125682be78cd3af1854edc4dd7a7f · packaged from commit fcf553a (clean; stamp 2026-09-19T02:58:10Z) · fresh extraction 110 files byte-identical · smoke 7/7 from the extraction · JS sweep clean · record CONSOLIDATED_C4.PACKAGE.json.

CANDIDATE + ROLLBACK:
  DRAFT https://6aadfab9367fc0f2649b4f3d--mahworld-test-preview.netlify.app (netlify-cli draft of the fresh C4 extraction; BUILD_INFO commit fcf553a, athlete dev_0.15 label visible in BUILD_INFO / the HUD avatar note; gate: play 401 password page · asset 401 · BUILD_INFO 401 · the dev_0.15 GLB 401 — unauthenticated protection PASS; correct-password login NOT RUN, no password held). The candidate's login return path and assets are its own (drafts are self-contained; nothing points at production). No live promotion. Rollback = C3 (161da1a3…, draft 6aaddb1f…) / C2 / live 6aac25101f17ce5ed37166e4 unchanged.

NEXT (≤ 3 owner actions; remaining independent work):
  1. Jah (phone, ≤ 3 steps): open the C4 draft → walk with a tap on the ground ahead (then tap once while pushing the stick to feel the refusal) → drag to look, let go, walk sideways: the view should not swing back → ⟲ CAM once. Say "nav ok / camera ok" or what is wrong.
  2. Jah (artistic, one look): 25_HANDOFF/CONVERGENCE/lower_body/sheet_hip_closeup_ab.png — row 2 vs row 1: is the connected hip the direction you want (say "hip ok / sharper crease / less"), and does the trailing flight posture read right.
  3. Jah: the combat `?` cells (PASS_7_COMBAT_DECISIONS_REQUEST.md, printed in chat) — only when you want lane F to go past the approved patterns.
  Independent work continues without waiting: lane D (pull / push pair with the granular crystalline-dust language, airborne casting — the NO_ATTACK_WHILE_FLYING / AIRBORNE blanket locks in PlayMode.roomAttack / rulesCast / RulesField.cast are the first removal), then E, F, G, H.
