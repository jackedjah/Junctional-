# ENHANCEMENT RECONCILIATION — owner assignment 2026-09-18 "CONNECTED MOTION, GRANULAR MAHGIC, TRAINING ROOM AND TAP-TO-MOVE" · gameplay / demo lane

Phase A of the assignment (§14 A): reconcile the current candidate, the successful work and the changed owner requirements — no broad restart. This file is the record; the unified master is not rewritten. Written 2026-09-19 01:05 UTC at the checkpoint after CONSOLIDATED_C3.

## A.1 Actual current candidate (identified, not assumed)
- Worktree: `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS` (git master). HEAD 7cd2855 (report 3, docs) on cbc99b7 (code). Clean.
- Source / build: 26_LOCAL_AUTHORITY/lab + play + 00_CORE; static build `deploy/static_dist` (build_static_demo.mjs) — 108 files, athlete dev_0.14 (ATHLETE_M_V9), NPC roster v4 (nine residents), three.js 0.185.1 vendored, Quarks NOT vendored (custom VFX only: sprites / points / lines in lab/*).
- Candidate: CONSOLIDATED_C3.zip · sha256 161da1a3b6e729de569912573dc06488e57c95f5df9bc5c74a5f7dfe2e28b665 · draft 6aaddb1fcf22df3c0f64b7d8. C2 (9d9e2883…, draft 6aadbeca…) is a reference to verify, not a roll-back target. Live 6aac25101f17ce5ed37166e4 unchanged; rollback 6aab65567e997ae33c1da204.
- Jah's verdict on the correct review build: genuine improvement, especially the separated-leg shape (recorded as an improved verdict; NOT approval of every feature). The earlier wrong-link review is not evidence of a C2 regression.

## A.2 Owner overrides applied — superseded checks and their replacements
| Old requirement / check | Status | Replacement (this assignment) |
|---|---|---|
| Camera truth 2026-09-17: rear follow of displayed facing is the ONLY automatic motion; a drag orbits, ⟲ CAM glides back to rear follow (`cam.mode` follow / orbit / resetting; probe_camera_pass2 F-rows; K1–K3 "FOLLOW stays FOLLOW") | SUPERSEDED on heading | Camera POSITION follows travel through a stable target; camera HEADING is world-relative and independent of facing; drag chooses it, release keeps it; RESET = ONE smooth collision-safe reframe, then independent again (no continuous rear follow re-armed). Old follow mode kept only as a config rollback (`CAMCFG.heading_follow`), default OFF. Replacement checks: N-rows in probe_nav / probe_camera_pass3 (heading unchanged through cardinal / diagonal travel, turns, click navigation, casts; drag / reset do only their operation). |
| Steering frame latched to the camera's FOLLOW rotation (Spec B latch / re-latch) | RETAINED, re-based | The latch now reads the independent heading; automatic avatar rotation never feeds back (there is no automatic camera rotation left to feed back). Destination travel ignores the camera entirely (world-fixed target). |
| "Tap on empty ground = nothing" (touchControls doc, tapAt) | SUPERSEDED | Tap / click on valid empty world space within range submits ONE destination (NAV); NPC / target / door / UI taps keep their semantics. |
| Botany at the PROTOTYPE GATE with plaza specimen + south-west cluster (Packet C) | SUPERSEDED on placement, RETAINED on gate | The intended family moves to outer land beds (never central routes / water), darker sapphire-navy foliage, platinum trunks, clearer silhouettes; a revised specimen / perimeter composition is shown BEFORE propagation (still a Jah verdict). placement_v1 plaza-ring candidates are withdrawn. |
| GYM_INTERIOR = "MAH GYM training hall" with racks / cage as the gym destination | RETAINED as art, DEPRIORITISED | The entrance (GYM_DOOR) / Map entry / prompt / interior carry a consistent TRAINING ROOM label; gym gameplay deferred (no workout / program features claimed); finished hall art preserved. |
| Attack catalog: two showcase moves + PROPOSED defaults; `?` tiers refused as UNASSIGNED_TIER | RETAINED, EXTENDED | Every defined offensive category populated with complete, distinctive usable content from APPROVED patterns; equipped capacity unchanged (4/2/2 loadout vs four shortcuts reconciled in §F); `?` exercise / tier cells remain owner decisions. |
| Casting restricted to the practice field / battle ("LOCK-ON WORKS IN THE PRACTICE FIELD", FIELD_ONLY reasons) | SUPERSEDED for presentation | Unlocked / equipped attacks play anywhere (free roam, flight, both leg forms) with adapted support pose; OUTCOMES only for enrolled / practice targets; neutral NPCs, wildlife, buildings receive nothing. |
| HUD bars opaque for readability | SUPERSEDED | Translucent BACKGROUND panels (adjustable), compact HUD, optional hide / collapse of non-critical info; critical health / flight-depletion / landing warnings never hidden. |
| G37 ≤ 2 cm hovered pointed-end drift = gait acceptance | RETAINED as a metric, NOT as acceptance | Ground-support vs hovered-end metrics stay distinguished; low drift alone is not gait acceptance; the connected pelvis / glute / hip work (§C) is the appearance work. |
Everything else (protection, progression, class identities, asset lineage, backend boundaries, release rules, OD-29, H = separate / fuse, E/R/T/Y, TAB, Q, G, mobile button layout, no live promotion, password never in code / logs) remains in force.

## A.3 Responsibility map (names are responsibilities inside the existing architecture, not new module paths)
| Responsibility | Existing owner (file) | Change in this assignment |
|---|---|---|
| Semantic input | lab/touchControls.js (pointer roles STICK / CAMERA / GESTURE / DASH_SWIPE / PINCH / BUTTON, tap detection, cancel guards), lab/play.js keyboard + mouse + `tapAt` | `tapAt`: empty-world tap → destination pick (range from the player, occlusion-correct ray); map tap through the map's real transform; pointer-lock click never moves or attacks. |
| Movement / navigation | play/PlayMode.js `move` / `integrate` (accel / decel, bands, colliders `resolveMove`), rules1723/Colliders.js (`blockedAt`, `groundHeight`, `solidAt`, `ceilingAt`, `lineOfSight`) | NEW host action `NAV` (destination → range check → bounded route search over the same colliders → per-tick moveVec through the SAME integrate path; takeover / cancel rules; arrival settle). Provisional range 30 m centralized in dev_tuning `play.navigation`. |
| Pose composition | lab/RigAnimator.js (procedural gait, additive layers, support hold, inertialization) | §C: pelvis / hip cooperation with the hold; flight travel posture; corrective mapping if proven necessary (derivative region only). |
| Camera | lab/play.js `cam` (+ cameraMath.js) | §B: heading independence (mode `free` default), reset = one reframe, preferred vs obstruction distance already separate (PASS 2) — verify recovery. |
| Combat outcomes | play/rules1723/RulesField.js (combatants, lock, cast, practice, dummy), PlayMode `rulesCast` / `roomAttack` | §D / §F / §9: cast permitted contexts widened for presentation; outcomes unchanged and target-bounded; practice isolation for the Training Room. |
| Presentation | lab/PresentationAdapter.js, lab/play.js effects, lab/*Vfx* (custom sprites / points) | §D / §8: granular crystalline-dust language (pooled Points-based emitter with authored flow), pull / push pair first. |
| World registry | 00_CORE/dev_tuning.dev.json `play.rooms`, play/rules1723/rules_17_23.dev.json (layout, interactables, landmarks), lab/fieldScene.js / districts | §E: TRAINING_ROOM interior; §G: plaza cleanup, water / bridges, perimeter botany definitions. |
| Scene transitions | lab/play.js `enterTransaction` + `camRoomChange`, PlayMode `enterRoom` | §E reuses the proven C2 transaction unchanged (adds NAV cancel on room change). |

## A.4 Dependency order and current state (updated 2026-09-19 03:40 UTC)
- A reconcile — THIS FILE (done).
- B camera heading independence + gesture ownership + destination movement — DONE (commit 28853d6): mode `free` default, drag chooses / release keeps, RESET one reframe; NAV intent through the ordinary walker; probe_nav 21/21, gameplay_navigation 28/28.
- C connected hip / glute / thigh + coherent flight posture + §6 hand glows — DONE in the worktree (this checkpoint). CONFIRMED CAUSES (inspected on the dev_0.14 GLB with deploy/hipseam_inspect.mjs, record lower_body/hipseam_dev_0.14.json): (1) weight distribution — the pelvis shell between the split line (y 0.3938) and the femoral head (y 0.4513) carried 0 THIGH influence (PELVIS 0.46 / TAIL1 0.41 / LUMBAR 0.13 at the hem) and the split legs carried 0 PELVIS influence (THIGH 1.0 at their top), so the thigh swung from the split line 5.75 cm (bone units; 10.5 cm real) below its own pivot while the shell stood still = the "separate shorts"; (2) geometry — the shell is an open cylinder whose hem ring coincides with the legs' hip-dome surface (rest gap median 0.9 mm), so any relative motion opens a visible seam; it is NOT transform ownership (both meshes share skin 0) and NOT shading alone. REPAIR: dev_0.15 (`rig_hip_connect.mjs`, weights only): a split-form skin set on the shell (THIGH share up to 0.7 laterally, graded to 0 by +5 cm over the pivot, 0 at the crotch) bound only while the legs are shown, and a PELVIS share on the leg tops matched to the same junction field — one deforming junction; fused form byte-identical. MEASURED: hem opening max 1.3 cm walk / 1.5 mm flight (V9: 19.6 cm / 6.4 cm), leg band below the blend unchanged (0 mm), fused hem identical (0 mm). §6 CONFIRMED CAUSE of the yellow balls: the ghost / KO material loop forced the two hand-energy SpriteMaterials to opacity 1 / white every frame (they were sprites, not crystals, geometry, helpers or particles) — scoped to body materials. Flight posture: split thighs trail with speed × inclination, soft knee, tuck fades (rig limits 64/64).
  Remaining faults (honest): the crotch / inner-thigh region stays with the pelvis by design (a graded pelvis share over the top ~11 cm of the inner thigh) — an artistic judgement for Jah on the close-up sheet; no pose-dependent corrective shapes were needed to reach ≤ 2 cm, so none were authored (helper joints / correctives stay available if Jah's verdict asks for a sharper glute crease).
- D pull / push pair with the granular crystalline-dust language + casting anywhere — DONE in the worktree (this checkpoint). HOST: the AIRBORNE / NO_ATTACK_WHILE_FLYING blanket locks are removed (superseded checks: gameplay_play_sample "attack refused while flying" → "attack in flight accepted, outcomes rule-bound"; rules test "Athlete special pool of 3" → 4 with the CASCADE candidate not equipped); outcomes are HEIGHT-AWARE — a BODY_STRIKE reaches only bodies within 1.6 m of its own height (a strike from the air over a ground dummy is an honest MISS), a PROJECTILE carries its caster height (entity y) and hits only within 1.4 m of it; a strike that touches a neutral resident records a HARMLESS impact with cause NEUTRAL that the client never draws (no fake hit marker), walls keep cause WALL (a cosmetic dissolve). CATALOG: ATHLETE_CASCADE — ORIGINAL CANDIDATE (the owner's vertical-pull example: a descending granular curtain over the aimed target area), SPECIAL MAHGIC, AREA_DAMAGE at the aimed ground point (range 6 / radius 1 melee units), movement pattern PULL → VERTICAL_PULL binding, PROVISIONAL balance (copied from the existing aimed-area exemplar; owner decision), NOT default-equipped, capacity 4/2/2 unchanged (equippable in place of another special through the loadout). PRESENTATION (lab/dustFx.js, pooled Points, per-formation authored flow, phase-driven glints, quality-tier budgets; Athlete gold): GATHER on the striking hand → forearm for physical MAHGIC (grains live in the limb frame; contact burst; never a projectile), WAVE front at special projectiles (bounded trail ≤ 3 m; terminates where the authoritative entity terminates; grains gone ≤ 0.5 s after), CURTAIN over the aimed area from the pull's startup (the impending area is readable from the motion) breaking into dust at the floor, BURST at terminals (restrained 22-grain puff for PHYSICAL contacts, 24 for wall dissolves). The old smooth hand glow is a faint seat under the grains (0.18), the projectile kernel is tight (0.2). Flight: an active attack takes the arms while pelvis / legs keep the travel posture. MEASURED (probe_mahgic 9/9, gameplay_mahgic_anywhere 16/16): gather centroid ≤ 0.14 m from HAND_R through the startup; wave head 0.00 m from the entity, front ≤ 0.04 m ahead, filaments ≤ 3.0 m behind (bounded), gone 228 ms after the entity; curtain centroid ≤ 0.11 m from the aimed dummy, top 3.3 m; cast in flight at 8.8 m accepted, projectile at 8.88 m, still powered; eight casts p95 20.3 ms / p99 24.2 ms (idle p95 29.6 under the probe's sampling), 0 live effects after, pool ≤ 12. NOT DONE / honest: the wave reads as a granular front in motion but is subtle in stills at gameplay distance; only the Athlete pair is authored (lane F propagates); W2 (wall termination) ran on the host test path — the page probe skipped it for lack of a suitable wall at the spot; a physical phone has not seen it.
- E Training Room / dummy as a usable destination (reuses GYM_DOOR + C2 transaction; practice isolation).
- F remaining approved attack catalog + declared variants (owner `?` cells stay owner decisions).
- G HUD compactness, plaza cleanup, roads, water, perimeter botany presentation (independent lanes).
- H integrated routes, repeated-use performance, separate review candidate.
Bounded gates (do not freeze unrelated work): exercise / tier cells (PASS_7_COMBAT_DECISIONS_REQUEST.md), Vector Shot / BLOCK BREAK patterns, tree family verdict, phone verdicts, Blender slot (RESERVED — native corrective shapes would need the Character owner: exact input / region / output contract will be written when §C proves them necessary).

## A.5 Protected boundaries restated
No Blender launch, no protected-source edit (RAW_10_MODELS, ASTRA_VISUAL_CLEANUP read-only), no model change, no purchase / subscription / package upgrade / bulk download, no Tripo regeneration of the accepted body, no new weapons / merchandise / economy, no MAHFITT redeploy or domain change, no live promotion, no password in diagnostics. One lead (this session) owns shared runtime writes; the Character / native owner remains separate.
