# Owner addendum (2026-09-17, received during PASS 2) — integration into the convergence queue

## Recovery/M6 status note — 2026-09-24

- All existing master/addendum requirements remain recorded below; no row was discarded or reinterpreted by the emergency backup.
- Current M1–M6 source/context is remotely backed up on `backup/mahworld-m6-20260924T190351Z`; source commit `bdc43b6`, required-asset commit `a25277e`. Fresh remote readback matched all 98 required files / 1,454,701,148 bytes.
- The bounded M6 `CW_GYM` / `RP_FOREST_W` junction defect is fixed and locally proven. The separately authorized protected M6 preview is now ready as build `ASTRA_M6_PRIVATE_20260924`, deploy `6ab586ec9ad382fa1b83f1be`; production and M5C remain unchanged. Physical-phone acceptance remains owner-only.

The addendum extends the running convergence job; it does not replace the master brief, the pass order or the approved work.
Received while PASS 2 (camera) probes were running; the active step was finished first (PASS 2 probes / report / review).

## Reference availability (checked, not fabricated)
- `v12044gd0000dam5b47og65j5h5c8730.mp4` — NOT FOUND on this PC (Downloads / Desktop / Documents / Videos searched 2026-09-17 late evening). Not watched.
- `MAHWORLD_NEW_REFERENCE_STUDY` — NOT FOUND ("when available"). Not read.
- Consequence: every "reference observation" below is taken from the addendum TEXT only (quarter-to-third character height in landscape, calm connected movement, layered world depth). Nothing is claimed about the clip itself. When either file appears, the presentation-pivot slice is re-checked against it before PASS 5.

## Where each addendum item goes (dependency order preserved)

| # | Addendum item | Queue position | Why there | Gate |
|---|---|---|---|---|
| A | Presentation pivot — stable readable scale (framing presets, no continuous zoom, no character-dimension change, no second 0.7× migration) | **PASS 2b** (right after PASS 2, before PASS 3) | it is camera framing work on the PASS 2 camera; must precede the HUD layout pass (PASS 3) that measures screen space around him | Card A (phone) |
| A | Presentation pivot — independent viewpoint (rear follow default, persistent orbit + explicit reset, stable movement target, orbit never steers a held movement gesture, preferred vs collision distance) | **PASS 2 (done) + PASS 2b probe** | PASS 2 implements it; PASS 2b adds the "held stick + orbit" feedback-spin proof | Card A |
| A | Presentation pivot — connected movement (idle → turn → short travel → stop → settle; no snapping / bobbing / swivel / fidget; immediate response) | **PASS 2b (first slice) → PASS 5 (motion foundation)** | the slice tunes what exists (root turn smoothing, gait start/stop blend, idle life); the real motion foundation is PASS 5 | Card B |
| A | Presentation pivot — world depth and clearness (near / mid / far layers, platinum-crystal identity, no asset copying) | **PASS 2b (sky + far-silhouette slice) → PASS 9 (full town/world)** | a readable far background is needed for the pivot proof; water / bridges / town materials stay in PASS 9 | Card C |
| A | Presentation pivot — proof before propagation (matched current/candidate capture idle → turn → walk → stop → orbit/release → obstacle recovery → reset → brief flight/landing; screen-space bounds, camera-distance recovery, frame timing; close-up integrity separately) | **PASS 2b deliverable** | recorded with the existing WebM recorder (`deploy/record_*.mjs` family) + probes; "current" = owner_round_0917 build, "candidate" = the PASS 2b build | Card A/B |
| B | Walk identity across leg states (fused and separated share one locomotion identity; measured speed / cadence / stride / transition; side-by-side proof; not complete if they differ) | **PASS 4b** (after the separated-leg repair of PASS 4 / O6, before PASS 5) | depends on the separated-leg derivative existing first | Card B |
| C | Sky and far-background scale illusion (cinematic sky, far silhouettes, three depth layers, no fog/blur cover, no near-field clutter; render comparison near/mid/far; gameplay + wide view) | **PASS 2b (first slice) + PASS 9 (full)** | same as the world-depth item; PASS 9 owns water, bridges, materials, Map | Card C |
| D | General world-quality direction (smoothness, viewpoint orientation, scale/readability, separated-leg presentation, world presence) | carried across PASS 2b, 4b, 5, 9 | refinement of existing goals | — |

## Report separation (kept in every pass report from now on)
1. what was already in progress · 2. what the addendum appended · 3. what was completed now · 4. what remains in the larger roadmap.

## Status (2026-09-18)
- PASS 2b (pivot slice: framing, connected turn, latched movement frame, far-world depth, matched capture) — DONE, reviewed, fixed (sky follows the viewer, far plane 1400 m, relatch rule, honest capture captions).
- PASS 3 (HUD) — DONE on the harness; Card A gate next.
- PASS 4b walk identity — queued after PASS 4; PASS 9 full sky / far-background — queued.
- 2026-09-18 (later): PASS 3 review round fixed (dead menu-hosted sheet, landscape dock / prompt, token overrides, `+` honesty, world-fixed clouds); PASS 4A fidelity diagnosis DONE (render scale on phones was the loss stage; texture patches are source-side → character lane).
- 2026-09-18 (night): PASS 4B faithful separated legs DONE on the harness (dev_0.14 = ATHLETE_M_V9; owner visual acceptance = Card B); PASS 4b walk identity MEASURED — pace / start / stop match after harmonizing the fused ground band to the separated walk; the fused glide has no step rhythm (owner rule) → stated difference, pass NOT called complete, owner decision at Card B (`PASS_4B_LEGS_WALK_REPORT.md`).

## QUEUED 2026-09-18 15:10 — "QUEUED MASTER CONSOLIDATION" (owner)
- `C:\Users\jahsu\Downloads\MAHWORLD_UNIFIED_CONVERGENCE_MASTER.md` (119 KB, 1 132 lines): the consolidated brief for the NEXT phase (decisions, corrected research synthesis, resource methodology, P0–P9 roadmap, botanical requirement §14, acceptance register of 116 requirement rows, packaging rules, §20.5 report format).
- `C:\Users\jahsu\Downloads\MAHWORLD_MASTER_VIDEOS_AND_LINKS.zip` (396 MB) → extracted to `C:\Users\jahsu\Downloads\MAHWORLD_VIDEO_SOURCE_BUNDLE\MAHWORLD_VIDEO_SOURCE_BUNDLE\` (13 original videos in videos/{character_tripo, historical_gameplay, visual_references}, evidence/, research/, SOURCE_LINKS, VIDEO_MANIFEST, 00_START_HERE / 01_VIDEO_INDEX / 02_REVISIT_LINKS_AND_MEDIA / 04_SCOPE_AND_MISSING_MEDIA): open the videos themselves; revisit the three game-development links; record retrieval / viewing coverage incl. blocked or missing media.
- Rules restated by the owner: do not interrupt / restart / replace the running job; preserve the current task, worktree, evidence and roadmap; identify completed work to reuse; start the consolidated phase only at the agreed completed checkpoint or a gate that permits independent work; read the entire document first; do not ask Jah to reconstruct earlier prompts; historical reports are not a live-build audit; use the existing Demo workspace; keep the Character / native owner and protected sources intact; no replacement demo or parallel controller; §14 one rooted hip-height multi-level branching MAHGIC tree with leaves and restrained energy — one specimen + a small cluster for approval BEFORE it becomes the map's botanical family; the 116 rows are requirement records, reconcile each with actual evidence; resources through input → operation → output → runtime integration → proof; report in §20.5 format; no publishing over the live game because tests pass; no model changes, credits, tool installs or interrupting other processes.
- STATE at receipt: the CONVERGENCE brief (this queue) stands at the Cards A + B gate (draft `6aad2eeae…`, commit f6b322b, tag convergence_card_b2) with three owner decisions pending — a gate that permits independent work → the consolidated phase starts now, in dependency order, reusing PASS 0–6 work.
- STATUS 2026-09-18 20:20 UTC: consolidated phase 1 done — P0 register reconciliation (`P0_REGISTER_RECONCILIATION.md`), media ledger (`MEDIA_REVIEW_LEDGER.md`, all 13 videos opened frame by frame), link ledger (`SOURCE_LINK_LEDGER.md`), P7 botanical prototype (`botany/`, `gameplay_mahgic_tree.test.mjs`, `probe_botany.mjs`), G20 label fix; candidate `CONSOLIDATED_C1.zip` (sha256 642c082e…) draft `6aad9c2b987f7a96700b29fa`. Report: `CONSOLIDATED_PHASE_REPORT_1.md`. Gates open: Packet A+B (B2 → now C1 carries the same work plus the label fix), Packet C (tree verdict).
