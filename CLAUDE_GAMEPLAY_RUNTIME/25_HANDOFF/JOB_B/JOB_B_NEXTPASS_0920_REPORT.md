# MAHWORLD NEXT-PASS (owner directive 2026-09-20: five class sanctuaries · creature combat · flight brake · guide formation · crystal ecology + the ARCHITECTURE add-on + the DOGKIE colour / size add-on) · consolidated completion report · checkpoint B4

## Status statement
- **CURRENT JOB:** the owner NEXT-PASS DIRECTIVE, absorbed as three controlled passes after the JOB B redirect's safe checkpoint (B3, commit ad7e312 + docs 16096a6): **P1** creature combat (JOB A-owned combat authority consumed, JOB B-owned creature AI) · **P2** flight brake + guide formation + the Dogkie add-on · **P3 / P3b** the five class sanctuaries, roads, water, meadow, class architecture, populations. This report is the consolidated result. The "MODEL ESCALATION — continue from saved state" instruction was honoured: nothing restarted, forked or rolled back; the run continued from the P2 commit through P3 to this package.
- **WHAT WAS COMPLETED BEFORE THIS DIRECTIVE:** B1 (world), B2 (coast / beaches + JOB A phone controls), A2 (JOB A checkpoint), B3 (the OFFICIAL DEMO REDIRECT: regions / paths / ground detail / crown terraces / tree lock / sky + wind / sea ripple body / fish · horse · phoenix ecology / guide motion / map layer).
- **WHICH INTERJECTIONS WERE INTEGRATED (in precedence order, newest first):** the DOGKIE COLOUR + SIZE add-on (P2) · the ARCHITECTURAL COLOR / MOTION / VERTICALITY add-on (P3) · the NEXT-PASS DIRECTIVE §0–§32 (P1–P3b). Nothing from the older redirect was resurrected where the next-pass superseded it (the 2026-09-19 "Dogkies alert only in free roam" rule is superseded by §11 / §16: the Dogkie is the baseline enemy — the tests were re-pinned, not deleted).
- **A / B OWNERSHIP:** JOB A items (flight brake = the player animation + the flight controller's release deceleration; the crit rule and the WILD context inside the ONE rules field; the defeat / respawn state in the play authority; the PresentationAdapter crit flash; the HUD DEFEATED line) were implemented in JOB A's own files as surgical additions — no second controller, camera, dispatcher or combat system. JOB B items (creature AI / balance consumption, spawn systems, regions, roads, water, meadow, architecture, guide positioning) consume JOB A's authority through the existing cast / lock / damage path. **Conflicts found: none.** One JOB A HUD text correction was made on the way (the stale "hold ATTACK" tutorial / cue texts contradicted the owner's 2026-09-19 slot canon — corrected to the slot wording; no control added or removed).

## What changed, by directive section
**§2 FLIGHT BRAKE (JOB A, P2, commit a911928).** Full-speed joystick release in powered flight no longer snaps to idle: the host flight integrator decelerates at `T.flight.brake_decel_mps2` (55, `00_CORE/dev_tuning.dev.json`) instead of the ground `decel_mps2`, and publishes `flight.braking`; the animator (`lab/RigAnimator.js`) runs a brake ENVELOPE (rise 0.12 s → hold 0.08 s → settle 0.22 s, triggered above 70 % of the form's cruise speed on release, re-armed below 50 %) that sweeps the FUSED lower body forward as ONE unit (pelvis −0.34 rad, tail1 / tail2, split thighs) with a torso counter-angle (spine / chest / head) and a small arm sweep — all numbers in `lab/presentation_defaults.json` `flight.brake`; no altitude change (the vertical channel is untouched); low-speed releases do not trigger it. Diagnostic `flightBrake()` on the animator API. Proof `deploy/joba/probe_flight_brake_guide.mjs` 11 / 11 (evidence `25_HANDOFF/JOB_A/evidence/flight_brake_guide/`).
**§3 GUIDE FORMATION (JOB B entity, JOB A toggle untouched, P2).** `lab/world/guides.js` + registry `guide_motion.follow / orient / companion_brake`: Mrs. Mah sits on the LEFT SHOULDER (lateral 0.78 m ≈ 0.6–1.0 shoulder-widths, trail 0.38 m, up 1.42 m — shoulder / head level), a damped player-relative spring stiffened with speed (rate 7 × (1 + 0.03·v)), a short LEAD when the player runs; orientation = the player heading / travel direction (yaw rate 6, a small lag in turns), glances at the player only while settled; banking into turns; a COMPANION BRAKE (a small overshoot + settle when the player stops / brakes). The approved SIZE is unchanged (guarded in the registry note). Proof: the same probe (G1–G5: lateral / trail / heading match / turn smoothness / flight formation) 11 / 11.
**§4–§5 TREE-BASE GRAMMAR reused.** The red tree-base language (platinum structure + class-coloured crystal growth) is now the ROOT SKIRT of every landmark and class house (`lab/world/architecture.js` rootShard: a platinum root wedge + a family crystal shard leaning outward), the rim growth of the Bage basins, the monolith bases (terrain) and the crystal meadow.
**§6–§8 FIVE CLASS SANCTUARIES, ARCHITECTURE, HUB + SPOKES (JOB B, P3).** Registry `regions` (8): NEXUS (platinum / gold hub) · ATHLETE_SANCTUARY gold (the temple, the NE crown terrace) · TITAN_SANCTUARY blue (the tower, the gate, the lake, the east mountain range) · LEAN_SANCTUARY red (the NW crown terrace + north forests) · VISIONARY_SANCTUARY purple (the west forest + the new HIGHLAND_W terrace at 2.6 m) · BAGE_SANCTUARY pink (the market + twin basins) · LUMINOUS_COAST · MATCH_DISTRICT — each with ONE family, surface tags, a landmark, a meadow density and its monoliths; five class houses in `registry.architecture.class_houses`: LEAN_SPIRE_HOUSE (three platinum spires 24 / 18 / 14 m with crimson blades, a walkable 22 m TOP PLATFORM, a crown), VISIONARY_OVERLOOK (four 14 m supports, a walkable slab at 8.5 m over the highland, a SUSPENDED amethyst square-diamond core levitating 10 m above it — the one major animated feature), TITAN_GATE (two 12 m blue-crystal pillars flanking the tower causeway, platinum caps + square-diamond crowns, an oscillating energy beam between them), TITAN_LEDGE (a walkable platform at 12 m on struts around the mountain spire — a flight destination), BAGE_TERRACES (0.5 m platinum ring frames around both basins, rose rim growth). Landmarks (`registry.architecture.landmarks`): temple gold / tower blue / gym gold / market pink / match hall platinum-cyan, each with a root skirt, façade ENERGY SEAMS (fitted to the real façade through the building's own collider proxies), a hovering SQUARE-DIAMOND CROWN (platinum frame turned 45° + crystal core, slow spin + bob; the match hall a platinum ring), converging ENTRANCE DIAMONDS + a platinum door frame at the real door (gym, market, match hall), roof diamonds on the heroes. `landmark_looks` now carries the reflected class light (temple gold, tower Titan blue, gym champagne gold, market rose) on the derivatives. Hub + five spokes = the 20 m causeways; the outer loop = RP_RANGE_LOOP (blue), RP_EAST_LINK, RP_WEST_LINK, RP_COAST_WALK, RP_NORTH_TERRACE_W / RP_NORTH_BAGE / RP_NORTH_TERRACE_E (one family per leg).
**§9 ROADS.** `paths.causeway_w` 8 → **20 m** (regional 8, trail 3, spur 6); every causeway ends in a FORECOURT disc (r 12: platinum frame, family core, a thin seam ring — never a bright disc) and the road stops at the forecourt rim; an ENTRANCE SPUR runs from the forecourt to the real door, aligned with the door's outward vector (gym GYM_DOOR [1,0], market CLOTHING_DOOR [0,−1], match hall MATCH_DOOR [−1,0]); no path crosses a pond (verified programmatically, 0 crossings). Colliders: the walk lanes / exclusions follow the new width (`worldLayout.forestExclusions`).
**§10 CRYSTAL MEADOW.** `lab/world/meadow.js` (new): one InstancedMesh per 30 m CHUNK of a region rect (frustum-culled by its own sphere), 0.7 blades / m² × the region's meadow factor (0.45–0.7), 0.07–0.36 m multifaceted blades in the family tint with brightness variation, seeded per chunk, excluded from lanes / paths / forecourts / spurs / landmarks / water / doors / class houses (a per-chunk pre-filter keeps the build to one bbox test per shape per chunk), NEAR = full instances, MID (> 70 m) = half the instance count, FAR (> 130 m) = hidden (the region tint sheet carries the colour), the quality tier scales the reach (LOW ×0.55 / MED ×0.77 / HIGH ×1); a subtle shimmer by instance hash + a 2 cm sway in the vertex shader (no per-instance CPU work). Build: 93 chunks / 18 800 blades; visible ≈ 18–23 chunks (= draw calls) ≤ 150 k tris worst case.
**§11 DOGKIE POPULATIONS.** 49 seeded spawn points across FOREST_W / NW / NE / N_W / N_E, TITAN_RANGE, ATHLETE_TEMPLE, BAGE_MARKET, VISIONARY_HIGHLAND → per sanctuary VISIONARY 12 · LEAN 11 · TITAN 10 · ATHLETE 8 · BAGE 8; activation 70 m / dormant 110 m (AI sleep), aggro cap 3, leash 28 m, KO → dissolve → respawn 45 s + jitter.
**§12 WATER.** Four sanctuary PONDS (`water.rivers` with `pond: true`): TITAN_LAKE (137..161 × 90..118), VISIONARY_POOL (−104..−84 × 20..40), BAGE_BASIN_W / _E (−42..−18 / 18..42 × 250..272) — beach shores on all four sides (client `water.js` x-banks + host RAMP colliders on the x banks, bed BOX), the floor cut, the region tint sheets cut with WATER HOLES (cells over a pond skipped); fish bodies per sanctuary (CANAL_C Athlete, CANAL_W Lean, TITAN_LAKE, VISIONARY_POOL, BAGE_BASIN_W).
**§13 HORSES.** 10 spots (2 per sanctuary near the class house / landmark), pool max 10, collider-free spot search; 35 baseline hits (2 940 HP), damage multiplier 0, HIT → SNORT → FEINT → HOLD / WATCH.
**§14 PHOENIX.** One high patrol anchor per sanctuary (5: the crown spires, the highland, the east mountain, the crimson crown spire, the basins), pool max 5, 2× player, 18 baseline hits (1 512 HP), kills in 10, WINDUP → DIVE → CLIMB with the visible anticipation, leash + no-re-engage window.
**§15 FISH.** Glass cannon: 3 baseline hits (252 HP), kills in 4.5; PROVOKED by a low flyer inside 9 m: RISE → BREACH → HOVER / SWIRL (wind-up 1.1 s) → LAUNCH (its aura-covered body through the player = the field body strike) → RECOVER → RETURN; scenery breaches on their own timer.
**§16–§19 COMBAT TABLE / CRIT / AURA (P1, commit 79930d0).** `registry.ecology.species.*.combat` is the data table (hits_to_kill, hits_to_kill_player, aggression, aggro radius, leash, cooldown, speeds, attack motion, aura + attack aura, crit rule, respawn + jitter, active population, drop placeholder, pet source); `play/world/CreatureCombat.js` turns it into HP / damage at spawn from the STANDARD BASELINE (ATHLETE PA_PUSH: raw 100 × 100 % × (1 − 0.16) = 84 applied): Dogkie 5 hits → 420 HP, damage mult 1.323 (111 / hit → 9 hits kill a 1000 HP player); the CRITICAL rule lives in the authoritative damage batch of `RulesField.js` (`damage.critical` chance 0.03 / multiplier 1.5 — provisional config, player-sourced non-periodic hits, journaled `R1723_CRIT`, impact cause CRITICAL → a ×1.6 flash); the WILD context (`contextOf` → WILD for a player ↔ wild-creature pair in free roam; town NPCs / buildings still receive nothing); species aura identity (idle / alert / attack) from the data.
**§20 DEATH / RESPAWN.** HP 0 outside a match → DEFEATED (movement refused, the HUD line "DEFEATED — returning to the sanctuary in N s", journal PLAYER_DEFEATED) → 2.8 s → respawn at the last validated SAFE POINT (sampled every 4 s when grounded, out of water, no wild creature within 22 m, not hit for 8 s; else the room spawn) with HP restored, flight reset, journal PLAYER_RESPAWN; no penalties. Creature defeat = KO → a 2.4 s crystal dissolution (no gore) → staggered respawn.
**§21 PETS.** `ownership` metadata only (Dogkie free / earned path FUTURE, horse / fish / phoenix premium FUTURE, `payment_ui: false` everywhere); a kill never grants ownership (tested).
**§22 COLOUR BALANCE.** Platinum / chromium / graphite dominant; class colour only through crystal growth (skirts, blades, meadow), energy (seams, crowns, diamonds, path seams), the tinted forecourt / path cores and the reflected façade light; the Titan blue family (#2a5be0) is shared by the TITAN sanctuary and the Dogkie species (owner add-on: deep, saturated, never pale cyan).
**§24 MAP.** Region labels for the five sanctuaries (`registry.map.labels`: ATHLETE · GOLD, TITAN · BLUE, LEAN · CRIMSON, VISIONARY · VIOLET, BAGE · ROSE + the hub / coast / match district) on the existing data-driven map layer (JOB A surface untouched).
**§25 PHONE PERF.** Instancing (meadow, shards, trees), chunk / distance culling, tier-scaled reach, motion frozen beyond 220–240 m, shared timelines, ~24 architecture draw calls world-wide, ≤ 23 meadow calls near a sanctuary; AI sleep for every population. Measured below (desktop-relative only — a phone was never measured, emulation is not acceptance).
**§26 PLAYABLE LOOP.** Explore → the nearest Dogkies wake within 70 m (west forest ≈ 70 m from the plaza) → lock (tap) → slot 1–4 → the Dogkie reacts (ALERT → CHASE → BITE) → damage both ways → KO / dissolve or DEFEATED → respawn → the world continues. Proven on the host (`16_TESTS/gameplay_creature_combat.test.mjs`) and in the real runtime (`deploy/jobb/combat_proof.mjs`, evidence `evidence/combat_proof/`).
**ARCHITECTURE ADD-ON.** Building colour identity (skirts, seams, crowns, entrance frames, roof diamonds, reflected class light) · MAH motion language (crown spin + bob, travelling seam bands on a shared timeline, converging entrance diamonds, pulsing roof diamonds, the Titan gate beam oscillation, the suspended Visionary core) — hero = crown + entrance + seams, ordinary = calm (gym / match hall smaller, slower) · the SQUARE-DIAMOND symbol (every crown, every cap, the deck inlays, the map's diamond glyphs) · verticality (the LEAN 22 m platform, the VISIONARY 8.5 m slab + 18.5 m core, the TITAN 12 m ledge, the temple / tower crowns at 50–60 m — all flight destinations with walkable colliders) · ground integration (root skirts at every contact, rim growth, terrace slabs) · function-communicating animation (entrance diamonds converge on the real door; the beam marks the gate; the core levitates over the overlook).
**DOGKIE ADD-ON (P2).** Species look from the artifact (`DOGKIE_SPECIES.look`: body #2a5be0 deep Titan blue, aura #4f7dff), height 1.25 → 1.4 m (+12 %), one species everywhere (regional lighting only) — verified visually in the P2 evidence and again in the sanctuary shots.

## B5 addendum (same run, after the B4 candidate) — review pass + the two JOB A items that were still open
- **Adversarial review** (a 4-dimension / 28-agent verification workflow over the P3 diff): 23 verdicts, 21 confirmed, all fixed in commit 5f11888 — the 2.6 m HIGHLAND was enterable through its ramp-less sides (now walls + ramps N / S / E, the two paths climb the ramps), creatures on terraces fought at altitude 0 (every strike missed the 1.6 m vertical reach both ways — now they spawn and fight at their collider ground, and a walkable top never blocks line of sight), horses were drawn 2.6 m under the highland, pond corners were a floor hole over invisible ground (N / S shores now span the full width), the Bage basins had no fish school (every body has one), CW_MARKET ran through the temple (routed around it), root shards stood on the range loop and in front of the market doors (skirts re-roll off roads / forecourts / spurs / doors), the market's door frame straddled its two doors (one wide frame), the Titan gate pillars carried two colliders (one), regional paths and trails had trees on them (every tier excludes), the crown terraces' inner ramps overlapped the basin rings (clipped north of them), tint-sheet holes leaked over the shores (cell extent + waterline), DK04 / DK07 spawned in the Visionary pool (moved), two stale check texts (re-pinned to the data). Two claims were refuted (devPlay in-page route = no new capability; a probe 'hang' that does not reproduce).
- **SHIELD = body-silhouette SHEET (owner 2026-09-18 §15, commit a88e656):** the torus ring is gone; the guard is drawn on the character's own skinned meshes as an additive fresnel film (platinum for PHYSICAL, the class energy for MAHGIC), 3 cm along the skinned normal, morphs shared; durability through the real balance — `guard.sheet` (hits_baseline 2.5 × the blocked portion of the STANDARD BASELINE hit = 225 for an Athlete body; a Dogkie bite drains 119 → the 2nd bite breaks it; a baseline PA_PUSH the 3rd), at 0 the guard BREAKS (drops, `R1723_GUARD_BREAK`, re-guard refused `GUARD_BROKEN` for 1.2 s, the client shatters it outward in 0.35 s), regenerates only while the guard is down (6 s). `16_TESTS/gameplay_guard_sheet.test.mjs` 11 / 11; `deploy/joba/probe_guard_sheet.mjs` 6 / 6 in the static demo (evidence `../JOB_A/evidence/guard_sheet/`, 0 console errors).
- **Impact frames / tempo (owner §16, commit 8614aa0):** `deploy/joba/audit_attack_tempo.mjs` measured from the runtime's own data that the force-arc finish was held 1–3 frames (PA_PUSH 0.017 s) — the animator now HOLDS the arc to recovery p 0.45 (finish ≈ 20 % of the recovery: PA_PUSH 4 frames, PM 6–8, barrage 12) and the contact flash / grains fire with the pose peak (recovery p ≥ 0.22) instead of 0.09–0.26 s earlier at the host phase change; the host phases are untouched (controls never delayed). Table: `../JOB_A/evidence/attack_tempo/attack_tempo_ATHLETE.json`; the real-runtime phase E probe 9 / 9 after the change.
- **Candidate B5:** `26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B5.zip` · 62 314 087 bytes · 167 entries · sha256 `a7ba93f05af7848e34b8fb000cb0eb5527697deb8533ce84ea47921ef05d5ed8` · from commit **5f11888** (the only dirty path = this report) · pipeline 0–7e PASS (suite 45 files / 1 381 checks / 0 failing, fresh-extraction smoke 7 / 7). **DRAFT:** https://6aafa87364d396f38a2786a7--mahworld-test-preview.netlify.app — gate PASS (401 play / BUILD_INFO / architecture.js / PresentationAdapter.js; the images route 401), correct-password login **NOT RUN**; live published deploy still 6aac25101f17ce5ed37166e4 (verified after the draft). The B4 draft stays reachable for comparison.
- Still open for JOB A after B5: the movement-quality audit (pelvis participation, knees, no duplicate writers / clip restarts), the separated performance route on a quiet machine (this machine ran with 20+ foreign Chrome processes and a full disk), the JOB A output package; the RDL residuals.

## Acceptance tests (owner §28–§31) — what was proven, and how
**§28 FLIGHT** (probe `deploy/joba/probe_flight_brake_guide.mjs`, real runtime, 11 / 11):
- [x] full-speed release does not snap to idle — the host decelerates over ≈ 0.35 s (55 m/s² from ≈ 20 m/s), `flight.braking` true during it
- [x] a visible restrained braking action exists — the animator envelope (rise / hold / settle) measured on PELVIS / TAIL / SPINE
- [x] the fused lower body behaves as a unit — ONE envelope amount drives pelvis + tail1 + tail2 (and the split thighs) in code; the probe measured the envelope peak and its return (F3, F4), not each bone separately
- [x] physically intentional — torso counter-angle + arm sweep (authored), the release eases out over ≥ 0.2 s (F2 measured)
- [x] clean settle — the envelope returns to the flight pose (0.22 s), no snap
- [x] no unwanted altitude change — |Δalt| < 0.35 m through the brake (F5 measured; the FLY logic untouched)
- [x] low-speed releases do not trigger it — F6 measured a low-speed stop with the envelope idle (trigger > 70 % of cruise, re-armed below 50 %)
**§29 GUIDE** (same probe, G1–G5):
- [x] size preserved · [x] substantially closer to the shoulder (0.78 m lateral, was ≈ 2 m off the side) · [x] no longer far off to the side · [x] does not constantly look at the player (heading orientation, glances only when settled) · [x] matches the player heading / travel · [x] smooth turns (damped yaw rate 6) · [x] player / guide motion related (speed-stiffened spring, lead, companion brake) · [x] works running / stopping / turning / flight (measured in flight) · [~] does not block the camera — by construction (beside / slightly behind the camera's stable target at shoulder height, G5 measured the level), not separately measured against the boom
**§30 SANCTUARIES** (evidence `evidence/sanctuary_proof/` — 24 real-runtime shots + a phone-size EMULATED frame; `deploy/jobb/sanctuary_proof.mjs`):
- [x] five class region definitions exist (registry; test 21) · [x] colour identities distinct (gold / blue / red / purple / pink, one per region; test 21) · [x] architecture language differs by class (spire house / overlook / gate + ledge / basin rings / temple crown — shots 30–52) · [x] class-coloured tree-root / environment grammar exists (skirts, rim growth, monolith bases, meadow — shots 11, 32) · [x] the platinum structural tree rule remains intact (tree lock unchanged; shot 32 shows L0 trunks platinum, crystal at the extremities) · [x] major roads meaningfully wider (8 → 20 m; shot 00) · [x] roads no longer terminate into building sides (forecourt + door-aligned spur; shots 60–61) · [x] the crystal meadow has a performant implementation (instanced chunks, culled, tier-scaled; ≤ 23 draw calls) · [x] each sanctuary has water (lake / pool / two basins / the canal for Athlete + Lean) · [~] routes do not rely on huge empty gaps — the hub + spokes + outer loop are continuous with monoliths, meadow, shards, dust and populations along them; the east range and the coast walk still read sparse beyond the meadow (owner review).
**§31 CREATURE COMBAT** (`16_TESTS/gameplay_creature_combat.test.mjs` 21 / 21 on the host; `deploy/jobb/combat_proof.mjs` in the real runtime):
- [x] the Dogkie takes and deals damage · [x] TTK 5 baseline hits (4–6) · [x] kills a baseline player in 9 hits (8–10) · [x] fish combat exists · [x] breach / hover / swirl / body launch · [x] fish low-health / high-danger (3 hits / kills in 4.5) · [x] the horse reacts without becoming an enemy (SNORT / FEINT / HOLD, damage 0) · [x] the horse is extremely durable (35 hits) · [x] the phoenix engages a flyer · [x] 18 hits (15–20) · [x] kills in ≈ 10 · [x] crit 3 % default (config) · [x] monster combat consumes the established attack / damage authority (the same cast → damage batch; contexts) · [x] no second player combat system (one dispatcher, one field)

## What was preserved
Every B1–B3 and JOB A system; the base 28 plaza colliders and the district colliders; the tree seeds (FOREST_W trimmed to x −118 so the highland / pool fit — its trees re-seeded, 125 total unchanged); the match hall, guides, fixtures, celestial, sky, coast; the HUD islands, the slot canon, FLY / GUARD / SEPARATE, the camera truth; the attack catalogue; the Dogkie identity and pet metadata. Pet acquisition / economy: untouched (owner decision).

## Measured
- **Tests:** full suite via the package pipeline (stage 1): 44 files / 1 369 checks / 0 failing — two pins re-pinned to the accepted canon (`gameplay_world_jobb` 21 / 25 / 27 / 28 → the 8 regions / 12 monoliths / pond + house shapes / highland + basin ring; `gameplay_motion_phase_d` §7 → the P2 brake hoists `fxNow`), one test adjusted for the new world (`gameplay_creature_combat` teleports past the Titan-gate Dogkies for the horse leg — the ambush on the walk was real behaviour, not a defect). New / extended: `gameplay_creature_combat.test.mjs` 21 checks (data, baseline, balance, wild context, aggro cap, kill → dissolve → respawn, defeat → respawn, fish / horse / phoenix behaviour, town NPCs harmless); `gameplay_world_jobb.test.mjs` 29.
- **Real runtime (headless Chrome, GPU, the static demo):** `world_smoke.mjs` world READY, 0 console errors (13 page 'errors' = the known dev_0.x manifest 404 probes + favicon); `sanctuary_proof.mjs` OK: 24 shots + the phone frame, 0 page errors, 0 console errors, modules: architecture 5 landmarks + 5 houses → 172 root shards, 25 seams, 9 crowns, 12 entrance diamonds, 12 roof diamonds, 24 draw calls, 6 180 tris; meadow 93 chunks / 18 800 blades, 18–23 visible chunks; terrain 8 regions, 3 terraces, 5 forecourts, 4 spurs, 10 tint sheets (905 water-hole cells), 26 → 28 paths, 440 shards, 14 monoliths; `combat_proof.mjs` (P1) bites 111 / hit, kill in 5, dissolve, DEFEATED overlay, respawn at the safe point; `probe_flight_brake_guide.mjs` 11 / 11.
- **Frame time** (`deploy/jobb/frametime.mjs`, desktop headless GPU with vsync, one Chrome at a time, relative only — never a phone number): world ON plaza rest 18.7 / 25.4 ms (p50 / p95; B3 16.7–19.2 / 21.6–30.7), canal wading run 40.7 / 55.6 (B3 20.9–24.5 / 33.8–36.3), deep forest rest 27.0 / 37.8 (B3 16.7–17.8 / 20.1–37.2); world OFF 18.7 / 24.1 · 23.8 / 36.0 · 18.1 / 23.3. The canal spot now sits inside the ATHLETE / BAGE populations (9 active Dogkies + horses + fish + the temple / market architecture + meadow) and carries the largest delta (≈ +17 ms p50 on this desktop vs +4–8 in B3); the forest spot ≈ +9 (10 active Dogkies, the highland, the pool school, meadow). **Attribution** (`deploy/jobb/frametime_modules.mjs`, `?nomod=` one client module at a time, same spots): no single new client module accounts for more than the run-to-run noise (± 5 ms) — skipping meadow / architecture / terrain moves p50 by −1…+6 ms; the cost is the POPULATION density (skinned creatures animated per frame + their auras / tags) rather than the static decor. Levers if the phone tier needs them: creature animation LOD (skip the rig update beyond 24 m / every other frame), lower `active_population` per sanctuary on LOW, meadow density per tier (reach is already tier-scaled), fewer fish per body.
- **Budget:** smoke draw calls / triangles per place (final build): plaza_wide 600 calls / 1 034 k tris (B3: 377 / 654 k), canal_bridge_main 600 / 1 245 k (B3: 383 / 750 k), forest_w_inside 540 / 1 021 k (B3: 380 / 944 k) — the deltas are the sanctuary populations in view (skinned Dogkies / horses / fish figures), the meadow chunks (≤ 23 calls), the architecture (24) and the tint cells; the desktop HIGH tier draws shadows + full LOD; the sanctuary shots run 150–370 calls / 0.67–1.1 M tris on the desktop tier (HIGH: shadows + full LOD).

## Blockers / honest residuals
1. **Phone perf is unmeasured** (emulation only). The population near the temple / market is the heaviest new cost; the levers above are ready but untuned without a device.
2. **Rigs** (unchanged from B3): horse / phoenix / fish articulation is root + shader deformation; the Dogkie is the only skinned creature with a rig. Real limb / wing animation needs the asset lane (Blender slot reserved).
3. **Seams on the round temple / tower** hug the base through the collider proxies but stand free of the receding upper façade (the derivative tapers) — a fitted façade seam needs per-building anchor data (a small registry table per landmark) on owner review.
4. **"No dead space"** is partial: the outer loop is decorated (monoliths, meadow, shards, populations) but the east range and the coast walk are still open ground beyond the meadow reach.
5. **Class-house interiors / interactions:** none (the houses are exterior destinations with walkable platforms; enterable class houses are a future pass).
6. Pet acquisition / wild-encounter interaction: metadata only (owner decision).
7. The Visionary highland's 2.6 m step: reachable by the east ramp (9 m) only; the north / south lips are walls (by design — one ramp keeps the plateau reading as a highland).

## Files modified / added
JOB A lane: `play/PlayMode.js` (defeat / respawn state, brake deceleration, `flight.braking`, WILDLIFE_SUMMON dev op, wildlife snapshot), `play/rules1723/RulesField.js` (WILD context, critical rule, `hp_abs` / `wild` / `altitude` synthetics, `baselineHit` / `strikeVsBaseline` / `revivePlayer`), `play/rules1723/rules_17_23.dev.json` (damage.critical, baseline, wild_context), `00_CORE/dev_tuning.dev.json` (defeat, brake), `lab/RigAnimator.js` (brake envelope), `lab/presentation_defaults.json` (flight.brake), `lab/PresentationAdapter.js` (crit impact, braking), `lab/gameHud.js` (DEFEATED line, WILD ENCOUNTER state, the slot-canon texts), `lab/play.js` (`devPlay` incl. the in-page host route).
JOB B lane: `deploy/jobb/build_registry.mjs` → `lab/assets/world/world_registry_v1.json` (regions, paths, water, terraces, architecture, ecology combat, populations, looks, map); `play/rules1723/world_v1_colliders.json` + `rules_17_23.dev.json` mapping (213 world shapes: +4 ponds × 5, +20 class-house shapes, +8 terrace, 12 monoliths); `lab/world/worldLayout.js` (architectureColliders, pond x-shores, ring ground, exclusions); `play/world/CreatureCombat.js` (new), `play/world/WildlifeManager.js` (new), `play/world/DogkieManager.js` (wild spawn, aggro cap, KO / dissolve / respawn, ground); `lab/world/meadow.js` (new), `lab/world/architecture.js` (new), `lab/world/terrain.js` (forecourts, spurs, water holes), `lab/world/water.js` (pond banks), `lab/world/worldB.js` (module order, `?nomod`), `lab/world/wildlife.js` (host-driven presentation), `lab/world/creatures.js` (species look, dissolution), `lab/world/guides.js` (formation); `deploy/build_static_demo.mjs` (new host modules shipped). Tests / probes: `16_TESTS/gameplay_creature_combat.test.mjs` (new), `gameplay_world_jobb.test.mjs`, `gameplay_motion_phase_d.test.mjs`, `gameplay_hud_phase_b.test.mjs`; `deploy/jobb/combat_proof.mjs`, `sanctuary_proof.mjs`, `frametime_modules.mjs`, `deploy/joba/probe_flight_brake_guide.mjs`.

## Candidate
- **`26_LOCAL_AUTHORITY/deploy/packages/CONSOLIDATED_B4.zip`** · 62 307 537 bytes · 167 entries · sha256 `a7260934896f4534add4a2223a2181d65ca78cde53720bc3e3381ba0afb4fff5` · packaged from commit **8c57c00** (clean) · pipeline 0–7e PASS (suite 44 files / 1 369 checks / 0 failing, syntax / CSS sweeps, stamps, boundaries, secret scan, ZIP integrity, fresh-extraction smoke 7 / 7 (zero page / console errors)).
- **DRAFT (the one review URL, no live promotion):** https://6aaf9a9ed3d661699d465367--mahworld-test-preview.netlify.app — deployed from a fresh extraction of the zip; unauthenticated gate PASS (401 password page on play, 401 JSON on BUILD_INFO.json, architecture.js and meadow.js; the root 303s to the gate); correct-password login **NOT RUN**. Live https://mahdemo.fob.systems = 6aac25101f17ce5ed37166e4 untouched (verified after the draft: the site's published deploy is still 6aac25101f17ce5ed37166e4, published 2026-09-17, state ready).
- **Owner actions (≤ 3):** (1) phone, the draft: walk west into the amethyst forest — the first Dogkie fight (lock, slots 1–4, ~5 hits; let one kill you once to see DEFEATED → respawn); (2) fly north-east to the TITAN gate, through it to the tower, land on the mountain LEDGE, then west to the VISIONARY overlook and up onto the LEAN spire platform — say whether the five colours and the architecture read as five classes; (3) full-speed flight, release the stick: keep / change the brake; look at Mrs. Mah on the shoulder: keep / change the offset.
