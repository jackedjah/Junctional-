# OWNER INTERJECTION 2026-09-19 — MAHWORLD ENVIRONMENT + GUIDE SCALE — integration report (queued behind JOB B, absorbed at the safe checkpoint, same session / worktree)

Job preserved: JOB B was completed first (the world registry, forest, canal + bridges, fixtures, sun / moon, MAH MATCH, Dogkie habitat, guides, plaza cleanup — `JOB_B_WORLD_REPORT.md`), the interjection was ingested (preserve → queue → reconcile → apply) at the next safe checkpoint, nothing restarted, forked or rolled back; JOB A's systems untouched (§1 boundary). The full acceptance list is `JOB_B_WORLD_REPORT.md` §9; this is the interjection view.

## What changed for the interjection (commit 599fc13, candidate CONSOLIDATED_B1, draft https://6aaf261b588dbc75aeb59f8c--mahworld-test-preview.netlify.app)
1. MRS. MAH SCALE (§2) — a screen-presence target, measured, not a blind multiplier: the owner screenshot shows the companion at ≈ 0.66 of the player's head height on screen. The guide now measures 2.12–2.16 head-heights at rest in the real rear-follow camera (desktop 1280 × 800 and phone 393 × 852 @2×) = **3.2× the reference**, 3.2–3.4× while running (she rides beside the left shoulder, nearer the camera). Registry `scale.height_m` 0.75 (cap 1.3 — never a full humanoid), follow 1.5 m at 1.2 rad from behind (beside the shoulder), 1.3 m up, feed-forward on the player's velocity (rate 6.5, lead 0.22 s) so she runs WITH the player instead of trailing into the camera, camera clearance 1.6 m, floor 0.35 m above the feet, hover bob + banking, root-level only. Mr. Mah uses the same architecture and value. Evidence: `evidence/water/07_mrs_running.png`, `sheet_tune1.jpg`, `sheet_mobile.jpg`, the measurements in `water_proof*.json` (`guide_rest` / `guide_running`: guide_px, head_px, ratio, x_over_reference).
2. RESPONSIVE WATER (§4–§5) — `lab/world/waterRipples.js` (new) inside `water.js`: NEAR window GPU height field (256² half-float ping-pong render targets, 48 m window snapped to texels and following the player; each field remembers the origin it was written with and is re-sampled with its own shift so ripples persist while moving), the classic wave stencil at a fixed 60 Hz (≤ 3 steps a frame, c² 0.09, damping 0.992, edge fade), ≤ 8 gaussian impulses per step from a pooled queue (64, overflow counted, never unbounded), a 96² displaced grid with a normal from the height gradient + the drifting tileable normal map, the far single-quad surface discards its fragments under the window. Event sources: the player (wading splats scaled by speed while the feet are under the surface, a strong splat on a flight landing — `landed_count`), forest creatures in the water, `impulse(x, z, strength, radius)` for anything later (projectiles / attacks connect there — JOB A). IMPACT → EXPANSION → DAMPING → CALM is the timed series in `evidence/water/sheet_wade.jpg`; overlapping systems in `sheet_water2.jpg`; night `sheet_night.jpg`; phone size `sheet_mobile.jpg`. Tiers HIGH 256² / 48 m / 96², MED 192² / 40 m / 64², LOW 128² / 32 m / 48²; the field only simulates when the player is within a window of the water.
3. WADING (§5) — the canal is entered on foot: kerbs became 15 cm walkable lips, the bed is a walkable "wade floor" at −0.35 (host `Colliders.groundHeight` honours `water:true` ground below zero; `PlayMode.groundAt` follows), in / across / out on both banks and landing from flight verified on the headless host and in the browser (`gameplay_world_jobb` 5–6, `sheet_wade.jpg`).
4. FLOATING SURFACE OBJECTS (§6) — original MAHWORLD crystal float pads (hexagonal platinum discs with a crystal core, 11 along the canal away from the bridges): buoyant on an analytic ring model of the last 24 impulses + a slow breathing bob, tilting with the local slope (clamped), decorative (no collider), 2 instanced draw calls, no per-frame allocation.
5. ENVIRONMENT LESSONS (§7) — atmospheric depth: the daytime fog is now a cool platinum-blue haze 0xb9cddd from 75 m to 640 m (crisp foreground, softened middle, hazed silhouettes over the existing far massifs; nothing bleached, the night template unchanged); environment motion: forest crown sway (vertex shader, per-instance phase, base planted), water motion, pad bob, guide hover, fixture pulses; water + land: cut-out canal, trench walls, kerb lips, landing pads; lighting readability: the player keeps the key / rim / fixture light, the guide has a cyan rim.
6. MAHWORLD identity kept (§8): platinum / chromium / graphite / crystal / cyan, darker exposure, the registry-sized sun / moon (the legacy sky quads are hidden so there is one sun and one moon), the black MAH MATCH building, the dark-sapphire forest. Nothing from Kung Fu Panda or any third-party game was copied — the owner's clip was viewed as sampled frames in the session scratchpad only; no asset, texture, geometry, code or audio exists in the project.
7. PERFORMANCE (§9) — desktop headless GPU, vsync: canal with the ripple field running 18.6 / 20.7 ms (p50 / p95) vs 16.7 / 18.7 with the world layer off; plaza and deep forest at the vsync floor (`evidence/budget/frametime.json`). The world layer adds 18–29 draw calls at rest. A phone number is the owner's measurement.
8. NOT AN ISOLATED DEMO (§10): every capture is the real playable static build with the game camera (`deploy/jobb/water_proof.mjs`).

## Honest residuals
- Far water keeps a faint tiling rhythm at grazing angles (11 m tile) — a second frequency layer would need a custom far shader (deferred). No foam / contact darkening around the wading legs yet (the legs are simply under the surface).
- The separated perf route was contaminated by the owner's other sessions (17 foreign Chrome processes on the GPU) — rerun on a quiet machine before quoting it.
- Mrs. Mah at 0.75 m is a measured proposal (≈ 3.2× the reference); the owner's verdict decides (one number).
