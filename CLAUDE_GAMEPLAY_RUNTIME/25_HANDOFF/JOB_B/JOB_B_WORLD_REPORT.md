# MAHWORLD JOB B — MAP / WORLD / ENVIRONMENT / EXTERNAL GAMEPLAY / ASSET INTEGRATION — completion report (owner directive 2026-09-19, §26 output form) + the queued owner interjection "ENVIRONMENT + GUIDE SCALE" (2026-09-19) absorbed

Lane: gameplay session (JOB B first, per the owner's dependency override). JOB A's systems were consumed, never replaced (player controller, camera, combat dispatcher, attacks, phone HUD, click-to-move, rig / motion untouched — see §12 for the exact hooks left WAITING_FOR_JOB_A). The newest owner video (`4dbe2b3a…mov`) was viewed as sampled frames (the pond reference) — nothing from it or from any third-party game was copied into the project.

## 1. Worktree / commit / build
- Repo `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS` (linear, nothing reset). Code + data + evidence commit **599fc13** on top of a370fb1 (C7 docs). This report and the CONTINUE_HERE update are a documentation-only commit after it.
- Build: `node 26_LOCAL_AUTHORITY/deploy/build_static_demo.mjs` → `deploy/static_dist` (145 files, 69.9 MB; the 15 world derivatives = 23.0 MB). BUILD_INFO carries `world: "JOB B WORLD_REGISTRY_V1 …"` + the commit stamped by the packaging pipeline (visible in ≡ → SETTINGS about line / dev panel).
- Candidate: **CONSOLIDATED_B1** — see §10 (ZIP, SHA-256, fresh extraction, draft URL).

## 2. World registry (new) and its diff
`26_LOCAL_AUTHORITY/lab/assets/world/world_registry_v1.json` (generated by `deploy/jobb/build_registry.mjs`, version WORLD_REGISTRY_V1, 9 sources · 9 derivatives · 17 artifacts · 9 zones · 20 Dogkie spawn points). Before JOB B there was no world registry (only the district manifest + the botany prototype registry). The diff is therefore "everything below is new":
- `field` (walls, plaza radius 56 m, five routes), `sources` (provenance: canonical role name, original file name, path in `GLB_PHASE_AB`, sha256, bytes, triangles, skin / joints, generator, bounds), `derivatives` (per LOD: runtime path, tris, vertices, bytes, sha256, method), `material_families`, `landmark_looks` (platinum tints, never white), `zones` (FOREST_W / NW / NE / N_W / N_E with seeds, clearings, corridors, habitat; MATCH_DISTRICT; PLAZA_CLEAR; RIVER; MATCH_COURT), `water` (RIVER_N z 216–236, surface 0.02, wade floor −0.35, bed −1.2; BRIDGE_MAIN / W / E decks 0.35, kerb lips 0.15), `fixtures` (78 poles: routes + plaza ring + bridges, 3 hopping non-shadowing lights), `ground` (OPAQUE platinum, reflected-light prominence ≈ 0.15 through roughness / metalness / env — never opacity), `celestial` (sun 5.3° / moon 5.2° apparent at 330 m = ≈ 10× Earth-like), `artifacts` (17: projector beacon, gym barbell + 2 dumbbells, MATCH_HALL, MATCH_COURT_OUTDOOR, RIVER_N, 3 bridges, FOREST_TREE_FAMILY, STREET_LIGHT_FAMILY, GUIDE_MR / MRS, DOGKIE_SPECIES, CELESTIAL_SUN / MOON — each with collision policy, map policy, interaction metadata and state PLACED / WAITING_FOR_JOB_A / PROVISIONAL), `creatures.DOGKIE` (max 20, activation 70 m, dormant 110 m, leash 28 m, respawn 45 s, PROVISIONAL kit), `rooms_added.MATCH_HALL` (44 m hall, spawn, door, exit, arena r 12, two square spectator tiers as walkable platforms), `interactables_added` (MATCH_DOOR), `ai_encounters.spar`.
- Everything the host collides against is generated from this file: `deploy/jobb/build_world_colliders.mjs` (+ `--check`) → `play/rules1723/world_v1_colliders.json` (191 shapes in 11 zones, registry sha256 recorded) spliced into `rules_17_23.dev.json → field_colliders_district_v1` after the 1 247 district shapes (28 base + 1 247 district + 191 world = 1 466), the MATCH_DOOR interactable (6 interactables), the MATCH_HALL room + its 10 tier platforms and the gym barbell solid into `00_CORE/dev_tuning.dev.json`. `sync_district_colliders.mjs --check` still passes (it now preserves world shapes). The client derives the SAME shapes from the same registry (`lab/world/worldLayout.js`) for its camera boom and the Map — no stale colliders, one placement.

## 3. Source → derivative table (sources are SOURCE ART: read once, hashed, never overwritten; nothing in `GLB_PHASE_AB` was modified)
| Source (canonical) | original file | source sha256 (16) | source | Derivative | LOD | tris | bytes | derivative sha256 (16) |
|---|---|---|---|---|---|---|---|---|
| MAHWORLD_FOREST_TREE_PRIMARY_SOURCE | treevariaint(1).glb | d2a7db55096c6a53 | 76.5 MB · 1 849 522 tris | MAHWORLD_FOREST_TREE_PRIMARY | L0 | 14 000 | 1 191 128 | e37dfa9221917439 |
| | | | | | L1 | 5 000 | 871 276 | 17a836894dd07984 |
| MAHWORLD_GYM_BARBELL_SOURCE | barbell(2).glb | f51d65e08ba5ae5e | 78.7 MB · 1 926 191 | MAHWORLD_GYM_BARBELL | L0 | 39 999 | 2 684 220 | 1f44a5722032e849 |
| | | | | | L1 | 12 000 | 1 855 484 | 29066c05426cec0f |
| MAHWORLD_GYM_DUMBBELL_SOURCE | dumbell(2).glb | f9be985d869d3f91 | 76.5 MB · 1 926 482 | MAHWORLD_GYM_DUMBBELL | L0 | 39 999 | 2 307 820 | d530632feccdc16b |
| | | | | | L1 | 12 000 | 1 490 964 | ed65ad5c796b385f |
| MAH_GYM_PROJECTOR_BEACON_SOURCE | mahgymadvertsemet(1).glb | 4fe29962c1d55898 | 77.2 MB · 1 955 221 | MAH_GYM_PROJECTOR_BEACON | L0 | 30 000 | 2 311 440 | fd4844800cb22a94 |
| | | | | | L1 | 9 000 | 1 677 448 | 9c689fa9dc6c7abf |
| MAHWORLD_OVERSIZED_SUN_SOURCE | sun(2).glb | 7f5e402e5954db4b | 89.1 MB · 1 989 518 | MAHWORLD_OVERSIZED_SUN | L1 (texture source) | 2 243 | 760 632 | fc231fc1cfcb3154 |
| MAHWORLD_OVERSIZED_MOON_SOURCE | moon(2).glb | 6d5499f9654d00f3 | 93.8 MB · 1 985 082 | MAHWORLD_OVERSIZED_MOON | L1 (texture source) | 2 000 | 704 872 | 5a10b2a0a780b957 |
| DOGKIE_FOREST_CREATURE_SOURCE | dogkie(1).glb | 867ea26feb2335d5 | 14.6 MB · 18 627 · skinned 55 joints | DOGKIE_FOREST_CREATURE | L0 | 18 627 | 2 494 392 | fae377fbcc5fee9e |
| | | | | | L1 (skinned quadric) | 6 500 | 973 344 | 33c8ec29ba0d1f21 |
| | | | | | L2 (skinned quadric) | 2 600 | 752 808 | 8e6d52797442b85e |
| MR_MAH_GUIDE_SOURCE | mr(1).glb | d92e2d0c4b3699f1 | 89.5 MB · 18 492 | MR_MAH_GUIDE | L0 | 18 492 | 1 925 432 | 51a0fe2a983326b6 |
| MRS_MAH_GUIDE_SOURCE | mrs(1).glb | 9ca358074b9244df | 39.5 MB · 18 419 · skinned 78 joints | MRS_MAH_GUIDE | L0 | 18 419 | 2 138 996 | dde05e83f63948ad |

Pipeline (no Blender — the slot stays reserved): `deploy/jobb/derive_glb.py` — source → inspect → quadric collapse per LOD target (cascaded, UVs / normals rebuilt; skinned mode carries JOINTS / WEIGHTS through the surviving representative vertex and keeps the skin / node hierarchy byte-identical between LODs) → texture resample (2048 / 1024 JPEG for the props, 1024 / 512 for the creature LODs) → runtime GLB + `<CANON>.derivative.json` sidecar → registry → in-game proof (`asset_view.mjs` sheets, then the smoke captures). Decimation ratios were chosen per asset from visual checks (the 2 M-tri sun / moon shatter under quadric collapse, so their texture is applied to a faceted icosphere at runtime; the tree L2 shattered and is replaced by the runtime impostor). Full-size sources (9 × 15–94 MB = 730 MB) are NOT copied into the repo (disk 2 GB free) — the registry records path + sha256 as provenance. No claim that file size = performance: the budget numbers are in §8.

## 4. Placements (FIELD metres)
- FOREST beds (157 trees, seeded per zone, 4.2 m min gap, landmark / plaza / corridor exclusions): FOREST_W 49 (x −168..−72, z −45..48; clearings at (−120,0) r 14 and (−95,30) r 10; the gym-route corridor stays open) · FOREST_NW 30 · FOREST_NE 30 · FOREST_N_W 24 · FOREST_N_E 24. LOD: L0 < 26 m, L1 < 60 m, runtime cross-billboard impostors beyond (baked once from L0); dark-sapphire tints, crown sway, sapphire underbrush shards; trunk colliders CYLINDER r 0.55–0.74 h 4–5.4.
- CANAL RIVER_N z 216–236 across the mainland with three bridges (MAIN x −7..7 on the market route, W x −116..−108, E x 108..116): deck 0.35 with ramps, chrome rails + posts, cyan edge lines, landing pads; the floor carries a real cut-out over the water (a ShapeGeometry hole), the trench walls / bed are drawn; kerb lips 0.15 m. WADEABLE (interjection): the host ground inside the water is −0.35 (knee-deep), in and out over the lips on both banks, flight lands on it, bridges walk over it.
- MATCH district (east): MATCH_HALL at (122, 6) — 40 × 40 × 22 m black hall, 60 vertical cyan light strips, recessed 6 × 5 m doorway on the west face at (102, 6) with a cyan lintel, `MAH MATCH` plate, platinum plinth; interactable MATCH_DOOR (98, 6) r 3.5 → room MATCH_HALL (44 m, black walls + cyan strips, graphite floor, platinum arena r 12 with a cyan ring, two square spectator tiers 0.8 / 1.6 m that ARE the host platforms, six ceiling bars, EXIT doorway at (0, 19.6)). MATCH_COURT_OUTDOOR at (128, 50) r 16 — graphite disc, cyan rings, flat spectator inlays (7 / 14 cm, below the step rule), four pylons with trunk colliders.
- GYM: PROJECTOR_GYM_BEACON at (−80, 98) yaw 40°, 4.6 m, hologram emissive, ground pad, box collider; inside the TRAINING ROOM the barbell on the lifting platform (2.2 m, a walkable-platform-level room solid) and a dumbbell on each bench — interaction metadata TRAINING_STATION (WAITING_FOR_JOB_A, no workout economy).
- FIXTURES: 78 cyan / blue poles (7.2 m) along the five routes (14 m spacing, 3.6 m shoulder), a 16-pole plaza ring r 60 and the bridge approaches; additive ground pools; 3 real PointLights (no shadows) hop to the nearest poles around the player (day: only within 6 m; night: full).
- CELESTIAL: sun (faceted textured sphere, 30.6 m at 330 m = 5.3°, halo 14°) along the key-light direction (26, 34, 14); moon 5.2° at night (yaw 0.35 / elevation 0.085); both ride with the camera like the sky; the legacy sky quads are hidden.
- PLAZA CLEANUP (PLAZA_CLEAR): the 12 laser-plant clusters and the 5 mirror puddles moved out of the axis lanes / forecourts to the south perimeter beds (r ≈ 44–50 m; three puddles at the forecourt corners); landmarks, pathways, pylons, emblem, tree elevator and pods untouched; the 0.67× landmark scale untouched (no new multiplier).
- GUIDES: Mr / Mrs Mah follow beside the left shoulder (1.5 m, angle 1.2 rad, 1.3 m up, feed-forward on the player's velocity, camera clearance 1.6 m, floor 0.35 m above the feet), hover bob + banking, root-level only (no clips), no collision / damage / lock. 0.75 m tall (§11).
- DOGKIE habitat: 20 seeded spawn points (10 in FOREST_W, 4 NW, 4 NE, 1 N_W, 1 N_E), host manager `play/world/DogkieManager.js` (§6).

## 5. Lighting / material changes (art direction: platinum · chromium · graphite · diamond / crystal · cyan energy; plain white rejected)
- City palette (`lab/cityScene.js`): facades platinum 0xaab4bf r 0.38 / m 0.9 / env 0.55 (was 0xc9d0d8 rendering white under the sky), chrome 0xa4aeb9, recesses graphite 0x1d2229. Landmark GLBs receive registry tints multiplied onto their base colour (temple #c9d2dc, gym #9aa3ad, tower #b4bfca, market #cfcad8; env 0.45–0.55; faint district emissive) — never a flat repaint.
- Ground policy: day roughness 0.42 / metalness 0.9 / envMapIntensity 0.22 (night 0.3 / 0.9 / 0.55) applied to the plaza floor and the district ground when the registry arrives — opaque platinum with the reflected sky at ≈ 0.15 prominence (an env / roughness tuning, never opacity).
- Daytime fog (interjection §7 A): a cool platinum-blue haze 0xb9cddd from 75 m to 640 m (was pale 0xd6e4ee 110–720): crisp foreground, softened middle distance, far silhouettes fade — nothing bleached. Night template unchanged.
- Fixtures: cyan heads (emissive 0.35 day / 1.6 night), pools 0.05 / 0.45 alpha, three hopping lights reach 16 m; the character receives local light under the poles at night. No dozens of shadow lights (the sun stays the only shadow caster, HIGH tier only).
- Water (interjection §4): near-window GPU ripple field (256² half-float ping-pong, 48 m window snapped to texels that follows the player and re-samples the previous fields with their own origin shift, 60 Hz stencil, c² 0.09, damping 0.992, ≤ 8 gaussian impulses per step, queue 64), 96² displaced grid with a height-gradient normal in the fragment shader + the drifting tileable normal map (tile 11 m), the far surface discards under the window; sapphire trench; day 0x1f4f7a / night 0x10304f. Floating crystal pads (11 hexagonal platinum discs with crystal cores) ride an analytic ring model of the last 24 impulses (tilt with the local slope, bob) — decorative, no collider. Forest crown sway (vertex shader, per-instance phase, base planted). MED / LOW tiers: 192² / 40 m / 64² and 128² / 32 m / 48².

## 6. Spawn / AI / guide behaviour (host, through the existing rules field — no second combat or movement system)
- DOGKIE (`play/world/DogkieManager.js`): DORMANT (no combatant, not streamed) until a FIELD player is within 70 m of the home point; spawn safety (never within 12 m of a player, never on another active creature, never inside a collider); activation = `spawnSynthetic('NPC_DOGKIE_nn', ATHLETE profile at 0.45 hp, team NPC, respawn 45 s)` with an actor so the SAME `resolveMove` / colliders move it; dormant again beyond 110 m (combatant removed). States published in `p.creatures` (active only): IDLE · ROAM (seeded wander ≤ 10 m from home, hash of the host clock — replayable) · ALERT (free roam: faces a player inside 12 m and holds — harmless by rule, nothing damaged either way) · CHASE / ATTACK (PRACTICE only, inside 16 m or when hit: approaches at 3.4 m/s, lunges from 3.2 m every 4.5 s, bites inside 1.6 m every 1.6 s by casting its own equipped body strike through `rf.cast` — the real damage / guard / immunity rules; journaled DOGKIE_BITE / DOGKIE_LUNGE) · RETURN (leash 28 m) · KO (the field's own respawn) · HIT. PROVISIONAL: the pack is strong (three biters can KO a standing player in ~5 s in practice) — dev tuning for the owner, not canon.
- Client (`lab/world/creatures.js`): one L0 clone per published creature (SkeletonUtils), procedural quadruped over the classified 55-joint rig (breathing idle, diagonal trot scaled by speed, turn, lunge / bite over attack_t, recoil, KO sag + fade, respawn scale-in), 120 ms presentation lag, skinned LOD 24 / 60 m sharing the one skeleton, a small `DOGKIE` tag + hp bar only when hurt; the rules HUD skips creature ids (never a class body).
- AI-vs-AI spars (`play/world/SparScheduler.js`): every 70–140 s (p 0.6, per-NPC cooldown 240 s) two free fight-capable residents walk to the next spar spot ≥ 6 m from players, square up and alternate their own PHYSICAL moves through `rf.cast` (FREE context → whiffs, no damage / loot / progression), 12–20 s from arrival, ended by duration / KO / a player spar; residents in a spar are left alone by the wander (`n.aiSpar`, the only npcTick hook).
- Guides: setGuide('MR' | 'MRS') / setVisible / visible / current / options / exposeSkeleton — the tiny GUIDE toggle under Health is JOB A's (WAITING_FOR_JOB_A); exercised through the dev API for the proofs.
- Host snapshot additions: `p.creatures`, `p.ai_spar`; residents are listed only in the FIELD (they were drawn inside the halls before — fixed).

## 7. Entry / interior proofs
- MATCH HALL: walked from the plaza to (98, 6), ENTER accepted, room MATCH_HALL 44 m interior, spawn (0, 17) in the aisle, no practice dummy, practice OFF, `TEAM_MATCH START` forms a match inside (the official-match flows run there; match UI / FORFEIT contract = JOB A), tiers = 10 walkable boxes (visual == collider), EXIT lands on MATCH_DOOR (`gameplay_world_jobb.test.mjs` 8–13; browser `smoke/match_hall_interior_*`).
- TRAINING ROOM: unchanged entrance (GYM_DOOR), barbell + dumbbells placed (`smoke/gym_interior_props_1.png`), 8 solids (7 + the barbell), `gameplay_training_room` 12 / 12.
- Shops: entered through the bridge route (test updated to the canal world — the straight-line walker takes the bridge approach).

## 8. Water / bridge proof, navigation regressions, asset stats, performance
- Water (real follow camera, `25_HANDOFF/JOB_B/evidence/water/`): `sheet_wade.jpg` (bank rest → wade in → the timed series 0 / 350 / 800 / 1 500 / 2 600 / 4 200 ms: impact ring, expansion, damping, calm), `sheet_water2.jpg` (running wake, two hook impulses overlapping at three times, flight landing splash, float pads, Mrs. Mah), `sheet_night.jpg`, `sheet_mobile.jpg` (393 × 852 @2×). Host: `gameplay_world_jobb` 5–6 (kerb lips 0.15 walkable, wade floor −0.35, deck 0.35, rails block, flight at 2 m clears), the headless walk in / across / out on both banks.
- Navigation: `gameplay_navigation`, `gameplay_district` (31), `gameplay_interior_entry` (24), `gameplay_rules_17_23` (60), `gameplay_training_room` (12), the whole suite **43 files / 0 failed** (42 previous + `gameplay_world_jobb` 21). Click-to-move plans around trunks (the test uses the host navigator inside the forest).
- Asset stats (`world_budget.mjs`, desktop headless GPU, HIGH tier, day): the world layer adds 18–29 draw calls at rest (baseline 312–324 at these spots is the existing scene). Triangles: plaza +32 k (616 k vs 584 k), canal ≈ baseline (+ the 18 k near-water grid when inside the window), match approach +34 k, deep forest +510 k (885 k vs 375 k: L0 14 k × ≤ 11 near trees, L1 5 k × ≤ 37, creatures 6.5 k / 2.6 k LODs beyond 24 / 60 m; the HIGH tier doubles L0 in the sun shadow pass — MED / LOW have no shadows). Per-module costs and the inventory are in `evidence/budget/budget.json`.
- Frame time (`frametime.mjs`, vsync 60 Hz, same spots ON vs OFF): plaza 16.7 / 17.9 ms (p50 / p95) vs 16.6 / 18.9; canal wading with the ripple field running 18.6 / 20.7 vs 16.7 / 18.7; deep forest 16.6 / 17.9 vs 16.7 / 18.4 (`evidence/budget/frametime.json`). The separated route probe (`probe_perf_separated.mjs`) was run three times on this build while the owner's other sessions kept 17 Chrome processes on the GPU (CPU ≈ 65 %): p95 93–150 ms and one renderer death — NOT a clean measurement of this build; the C7 run (p95 29 ms) stays the reusable prior. The desktop harness is never iPhone proof.

## 9. Interjection acceptance (owner "ENVIRONMENT + GUIDE SCALE", §11)
- [x] Current work stayed intact (JOB B finished first; the delta absorbed at the safe checkpoint) · [x] JOB A ownership not violated (hooks only, §12).
- [x] Mrs. Mah reads at ≈ 3× the reference fairy: measured in the REAL rear-follow camera (`water_proof.mjs`): reference ratio fairy / head ≈ 0.66 (owner screenshot `references/interjection_0919/guide_scale_reference_owner_screenshot.jpg`); now 2.12–2.16 at rest (3.2× the reference) on desktop and phone-size frames, 3.2–3.4 while running (she rides beside the shoulder, closer to the camera); height 0.75 m (registry), cap 1.3 m, never a full humanoid. Mr. Mah shares the architecture (0.75 m for now).
- [x] Follow / hover from the actual gameplay camera (rest + running frames, feed-forward keeps her beside the player instead of trailing into the camera).
- [x] Water moves while calm (drifting normal map + ambient bob on the pads) · [x] contact creates localized disturbances (wading, running wake, landing) · [x] energy expands and dissipates (timed series) · [x] multiple disturbances coexist (two hook impulses + the wake) · [x] acceptable at phone size (`sheet_mobile.jpg`; the MED / LOW tiers are smaller fields — a phone run is the owner's) · [x] performance: +2 ms p50 at the canal on this desktop (§8) · [x] shore / contact: cut-out canal, trench walls, kerb lips, wading depth, landing pads · [x] atmospheric depth: haze fog + the existing far massifs; crown sway · [x] MAHWORLD identity kept (platinum / sapphire / cyan; no third-party look) · [x] no Kung Fu Panda asset copied (frames of the owner's clip were viewed in the scratchpad only; nothing stored in the repo) · [x] proof from the real runtime (all captures are the playable static build with the game camera).

## 10. Candidate — CONSOLIDATED_B1
- ZIP `C:\Users\jahsu\Downloads\MAHWORLD_CHARACTERS\CLAUDE_GAMEPLAY_RUNTIME\26_LOCAL_AUTHORITY\deploy\packages\CONSOLIDATED_B1.zip` · **51 884 608 bytes · 146 entries · SHA-256 c7fd0f71ad6142ea3595e3d698ab69cf0243f3e388f1ba14f32d8e98aea0e368** · packaged from commit **599fc13 (clean)** · record `CONSOLIDATED_B1.PACKAGE.json` · pipeline stages 0–7e all PASS (static build 145 files 69.9 MB · tests 43 files / 1 340 checks / 0 failing · JS sweep 84 files · CSS sweep · stamps · boundaries · secret scan 146 files · ZIP · integrity 146 = 146 · fresh extraction byte-match · smoke 7 / 7 from the extraction · JS sweep from the extraction).
- ONE review URL (netlify-cli DRAFT of a fresh extraction of that ZIP, no `--prod`): **https://6aaf261b588dbc75aeb59f8c--mahworld-test-preview.netlify.app** — build label = BUILD_INFO commit 599fc13 + the `world` line (≡ → SETTINGS about line / dev panel). Deploy id 6aaf261b588dbc75aeb59f8c, site 968f80e5-889b-43a8-b2cd-5860c588546a.
- Gate on the draft (unauthenticated protection PASS): play 401 · BUILD_INFO 401 · lab/world/waterRipples.js 401 · a world derivative GLB 401 · / 303 to the gate. Correct-password login NOT RUN.
- Live untouched: https://mahdemo.fob.systems still publishes deploy 6aac25101f17ce5ed37166e4 (verified through the site API after the draft). Previous candidates C7 (18172f15…, draft 6aaeb66e…) / C6 remain as rollbacks. The demo password is not in any file, URL, log or report.
- Documentation commit after the packaged commit (documentation only): this report, `INTERJECTION_ENV_GUIDE_0919_REPORT.md`, `CONVERGENCE/CONTINUE_HERE.md`.

## 11. Owner actions (≤ 3)
1. Phone, on the B1 draft: walk north over the main bridge to the market, step into the canal on the way (wade, watch the ripples and the pads), then west into the forest until the Dogkies wake (PRACTICE on to fight one), then east to the MAH MATCH door → ENTER → EXIT. One line each: keep / change.
2. One decision: Mrs. Mah at 0.75 m (≈ 3.2× the reference) — keep, or a number.
3. (when ready) the Dogkie PROVISIONAL kit numbers (bite 70 / lunge 110 are registry metadata; the live damage is the ruleset's body strike) and "BLENDER RELEASED" for the native contracts.

## 12. WAITING_FOR_JOB_A (hooks exist, nothing invented)
- GUIDE toggle under the Health bar → `worldLayer().guides().setVisible()` / selection in ≡ SETTINGS (`guides.options()`).
- Match boundary field + FORFEIT: the outdoor court and the hall publish their geometry (`registry.zones.MATCH_COURT`, `rooms_added.MATCH_HALL.arena`); the field visual should read `p.rules.match` (DuelHost BOUNDARY_CLAMP 12 m, OD-29 reconnect / NO_CONTEST kept); FORFEIT = the match UI / state contract.
- Projectile / attack water impulses → `worldLayer().modules().water.impulse(x, z, strength, radius)` (the player / creature / landing sources already run).
- Gym props' TRAINING_STATION interaction (the attack / exercise practice relationship).
- Creature presentation reads `p.creatures`; the rules HUD tag / lock for creatures stays the combat HUD's.

## 13. Unresolved / honest residuals
- The separated perf route could not be measured cleanly (foreign GPU load, §8); rerun when the machine is quiet. Renderer death once during a probe under that load.
- Far water still shows a faint tiling rhythm at grazing angles (11 m tile; a second frequency layer would need a custom shader — deferred). The wading player's lower legs are simply under the surface (no foam / contact darkening yet).
- Dogkie L1 / L2 read "glassier" than L0 beyond 24 / 60 m (quadric on a faceted crystal surface) — acceptable at distance; the owner's morphology / kit verdict is pending (PROVISIONAL).
- The 13 pre-existing 404s in the page log are the avatar slot probing manifests of unshipped rig versions + favicon — not JOB B.
- No live promotion; the physical-phone verdict (motion, feel, readability) is Jah's.
