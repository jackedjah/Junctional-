# MAHWORLD JOB A — CHARACTER GAMEPLAY / CONTROLS / MOVEMENT / COMBAT / PHONE — progress report (owner directive 2026-09-19, started after JOB B as ordered) · checkpoint B2

Priority kept: no budget on the V11 mesh's cosmetic holes (the current character is the motion / control / combat testbed); only generic control / motion work. Nothing restarted, forked or rolled back. Candidate carrying this work: CONSOLIDATED_B2 (`../JOB_B/OWNER_NOTE_0919_WATER_LAND_REPORT.md` for the ZIP / URL).

## Done in this checkpoint (commit 5eca38a)
1. PHONE FLIGHT — ONE FLY control (`lab/play.js` flyPress, `lab/gameHud.js`): press → after a 260 ms tap window the climb starts (ENTER / ASCEND); release after a hold → HOVER (hold the attained altitude); TAP airborne → DESCEND (the descent reaches the support and completes as the LANDING — no LAND button); TAP while descending → stop / hold; HOLD while descending → climb; TAP on the ground → lift into a hover. The permanent DOWN and LAND buttons are removed (the desktop Space / C / F holds are unchanged). The FLY face shows the state (`FLY hold ▲` · `▲ hold · tap ▼` · `▼ tap: hold`).
2. DASH → SEPARATE / FUSE — the permanent DASH corner button of the touch island is replaced by SEPARATE / FUSE (`tc-form`: tap = the H key's TRANSFORM through the host; the label shows the transition it will perform; B keeps the desktop dash; the dash gesture modifier code path stays unreachable on the phone).
3. CATEGORY STRIP — the small category label under the quick-slot arc is now `◂ PHYSICAL ▸`: real arrow buttons (previous / next) and a horizontal swipe on the strip (≥ 28 px, more horizontal than vertical) that steps the category; the strip owns its pointer (z-order above the slot hit halos, Chrome touch-target adjustment defeated by making the parts real buttons), never a camera drag, never an attack (`probe_phone_controls` 9–10: camera yaw unchanged, no cast).
4. MAH GUIDE TOGGLE — a tiny `✧ GUIDE` toggle under the Health bar (JOB A's hook into the JOB B guide layer): tap = show / hide Mrs. Mah (default), ≡ SETTINGS → `MAH GUIDE` picks Mr. / Mrs. (persisted locally); the label shows who is out.
5. Proof: `deploy/joba/probe_phone_controls.mjs` (portrait 393 × 852 @2×, EMULATED touch — never iPhone acceptance): 14 / 14 (elements present / absent, hold-climb, hold altitude, tap-descend, tap-stop, hold-climb-again, descend-to-landing, ground-tap hover, SEPARATE → FUSE → SEPARATE, arrows, swipe, guide on / off, zero console errors). Evidence `evidence/phone_controls/`. HUD tests re-pinned to the new canon (`gameplay_hud_phase_b` 18 / 18, `gameplay_hud_pass3` 22 / 22, `gameplay_direct_ui` 52 / 52, `gameplay_flight_f01` 29 / 29).

## Already true from C7 / earlier (kept, not redone)
Camera heading independent of facing; click-to-move 30 m PROVISIONAL (centralized `NAV_RANGE_M`); quick slots 1–4 are the attack controls (no generic ATTACK); three categories 4 / 2 / 2; the two Specials + the curl barrage; granular crystal-dust MAHGIC; ONE speed-driven flight lean curve; Training Room; FORCE · POWER · ENGINE · CONTROL · RESILIENCE card (points per level 3 | 4 = OWNER_DECISION); intactness gate.

## Next (JOB A, in order)
1. Future-character IMPORT CONTRACT (semantic adapter: root, pelvis, spine chain, chest, neck / head, clavicles, arms, hands / fingers, hips, knees, shins, pointed tips / propulsion anchors, fused tip, emitter anchors, morphs, face; per-character records) — a data contract + the adapter over RigAnimator's bone lookups.
2. Movement quality audit (pelvis participation, real knees, no duplicate writers / clip restarts, gait phase survival) and the RDL / Hinge femur-BACK ~45° check against the reauthored hinge — measured, not asserted.
3. SHIELD as a body-silhouette protective sheet (2–3 meaningful hits by attack strength through the real balance rules), impact-frame tempo check.
4. The separated performance route on a quiet machine; packaging + the JOB A output form.

## Checkpoint A2 (after B2) — commit below; the owner's JOB B REDIRECT package is queued right after this checkpoint (owner master redirection 2026-09-19)
6. FUTURE-CHARACTER IMPORT CONTRACT — `26_LOCAL_AUTHORITY/lab/importContract.js` (draft / validate / apply), `deploy/joba/validate_import_contract.mjs`, `lab/import_contract.TEMPLATE.json`, the CONFIRMED record for ATHLETE_M_V11 embedded in its manifest (32 roles, chains ordered, identity bind; `GlbCharacter` applies records on load and reports `capabilities.import_contract`), the adapter proven on a Tripo humanoid (23 roles drafted, the real hierarchy question and the oriented bind surfaced) and rejected for the quadruped. Contract document: `FUTURE_CHARACTER_IMPORT_CONTRACT.md`. The oriented-bind re-basing is specified, not built (needed by the first oriented character).
7. RDL / HINGE — the owner's ~45° femur decision applied (presentation_defaults.hinge femur 0.60 / torso 0.80 / arm hang 0.85 for the split RDL; the FUSED trunk hinge keeps its 2026-09-18 numbers 0.66 / 0.60; rig_profile hip table 105° → 90° trunk-to-thigh, THIGH_x −1.57); the planted-feet root offset now includes the pointed tip segment. Measured (`probe_hinge_flight.mjs`, `evidence/rdl/`): femur 48° forward-down in the world (band 38–52), shins 7° from vertical, neutral spine, hips back 0.40 m and down 0.13 m, release lockout / arms to chest height / airborne pike / the ONE flight lean curve all PASS (12 / 15). HONEST RESIDUALS (not hidden): the hands load at ~0.33 m ABOVE the knee (the arm hang does not reach the shins on this body — the trunk / arm relation needs re-authoring, deferred: no budget on the testbed's body); the foot end dips 9 cm at the deepest load (limit 8); the fused release's arm swing samples at −0.79 rad at the whip peak (criterion ≤ −0.85 — marginal, timing at the sampled frame). Tests: flight F01 29 / 29, attacks E 16 / 16, anim data 46 / 46.
8. Still open for JOB A (queued behind the JOB B redirect package): SHIELD body-silhouette sheet (2–3 hits by attack strength through the balance rules), impact-frame tempo audit, movement-quality audit (pelvis participation, knees, no duplicate writers / clip restarts), the separated performance route on a quiet machine, the JOB A output form / package.

## Between A2 and A3 — the JOB B OFFICIAL DEMO REDIRECT was absorbed (2026-09-19, report `../JOB_B/JOB_B_REDIRECT_0919_REPORT.md`)
JOB A systems were consumed, never rewritten: the HUD map surface received an additive registry-driven region / path / label layer (`lab/gameMenu.js` drawMap); the guide entity JOB A toggles gained its motion language on the JOB B side (`lab/world/guides.js`); nothing in the controller, camera, combat dispatcher, slots 1–4, phone HUD, click-to-move, FLY, SEPARATE / FUSE or the rig changed. The redirect also hands JOB A the ground SURFACE TAGS (`registry.ground_detail.surface_tags`, per region / path `surface`) for future footstep / movement audio. JOB A item 8 (SHIELD sheet, impact tempo, movement audit, separated perf route, the JOB A package) resumes next.

## Between A2 and A3 — the owner NEXT-PASS DIRECTIVE was absorbed (2026-09-20, report `../JOB_B/JOB_B_NEXTPASS_0920_REPORT.md`)
JOB A items implemented as surgical additions in JOB A's own files (no second controller / camera / dispatcher / combat system): the FLIGHT BRAKE (host release deceleration `flight.brake_decel_mps2` + the animator brake envelope in `presentation_defaults.flight.brake`, §28 proven 11 / 11), the CRITICAL rule (3 % / 1.5×, config) and the WILD context inside the ONE rules field, the DEFEAT → RESPAWN state in the play authority (safe points, `revivePlayer`), the crit impact flash, the HUD DEFEATED / WILD ENCOUNTER lines, and a correction of the stale "hold ATTACK" tutorial / cue texts to the slot canon (owner hotfix 2026-09-19 §5). JOB B consumed the attack / lock / damage authority for the creatures; the guide entity gained its shoulder formation on the JOB B side (§29 proven). Still open for JOB A (next): SHIELD body-silhouette sheet, impact-frame tempo audit, movement-quality audit, the separated perf route, the JOB A output package.

## A3 progress (2026-09-20, after B4) — SHIELD sheet + impact frames DONE, shipped in CONSOLIDATED_B5
- SHIELD (owner §15): a body-silhouette protective SHEET on the character's own skinned meshes (no ring), bounded durability through the real balance (`guard.sheet` data; ≈ 2–3 meaningful hits by strength; GUARD_BREAK → re-guard locked 1.2 s → regen out of guard) — commit a88e656, `16_TESTS/gameplay_guard_sheet.test.mjs` 11 / 11, `deploy/joba/probe_guard_sheet.mjs` 6 / 6, evidence `evidence/guard_sheet/`.
- IMPACT FRAMES (owner §16): measured audit `deploy/joba/audit_attack_tempo.mjs` (evidence `evidence/attack_tempo/`) → the force-arc finish held to recovery p 0.45 (was 1–3 frames), contact VFX on the pose peak; host timing untouched — commit 8614aa0; phase E probe 9 / 9.
- Still open: the movement-quality audit, the separated perf route on a quiet machine, the JOB A output package, the RDL residuals.
